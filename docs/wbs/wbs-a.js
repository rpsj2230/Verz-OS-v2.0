module.exports = [
{id:"M0",name:"Foundation and repository",wave:0,tasks:[
 {n:"Repository and tooling",s:["Private GitHub repo, branch protection, CODEOWNERS on the three core files","uv project, pyproject, locked dependencies, Python version pin","Ruff and mypy strict config, pre-commit hooks","Directory layout: gate, agents, connectors, knowledge, memory, console, ext, tests","Task runner: dev, test, migrate, seed, lint, invariants","Mutation harness as a module that takes a throwaway worktree by construction, so it cannot break a file another session is reading"]},
 {n:"Core type system",s:["Principal model: id, kind, employment, not_after","Scope model as jsonb predicate, evaluator, conjunction-only composition","Capability string grammar, registry table, validator","EntitlementSet type and ent_hash computation, stable and order-independent","TypedResult envelope: entity tag, id, nested arrays, Pydantic base","ToolDefinition: name, args schema, result contract, required capability, side effect, identity mode","Error taxonomy: denied, absent, unresolved, degraded, failed"]},
 {n:"Database foundation",s:["PostgreSQL 18 with pgvector, pg_trgm, fuzzystrmatch, unaccent","Alembic setup, naming convention, migration template","Non-superuser application role with NOBYPASSRLS","PgBouncer transaction-mode pool for the application","PgBouncer session-mode pool for the worker, for LISTEN and NOTIFY","Per-profile settings: shared_buffers, work_mem, autovacuum_work_mem set explicitly","Schema namespaces: auth, gate, agent, know, mem, obs, proj, er, ops"]},
 {n:"Compose and local development",s:["docker-compose.lite.yml","docker-compose.full.yml","Documented environment example with every variable explained","Seed command that builds the synthetic company","Reset command that drops and rebuilds"]},
 {n:"CI pipeline",s:["Lint, format, type check","Unit tests with a coverage floor","Migration forward and rollback against production-shaped data","Schema sweep: row-level security on every entity table","Schema sweep: no foreign key from any grant table to connector","Registry sweep: tool naming grammar","Dependency audit: licence allowlist, last-release age, archived check","Traceability check: nothing proven without a test id"]},
 {n:"Test fixtures",s:["Synthetic company: four departments, twelve users, one contractor with expiry, one partner, one dual-department user, one mid-transfer user","Entitlement spread including see-record-not-money and see-neither personas","Canary records with improbable strings in restricted fields on every entity type","Golden questions: twenty questions across three personas with known answers","Connector cassettes with recorded responses per source system","Adversarial corpus: injection attempts in documents and tool results","Factory helpers for principals, scopes and grants"]},
 {n:"Found by tracing every requirement to a task",s:["The dependency policy is proved by a recorded CI run that refuses a deliberately added copyleft dependency and an archived one, accepts the permissive licences on the allowlist for every image, and finds no component needing a commercial key in production"]}
]},
{id:"M1",name:"Identity, users, roles, permissions",wave:1,tasks:[
 {n:"Keycloak integration",s:["Realm setup script and exported realm config in the repo","OIDC login, token validation, JWKS caching","Claim mapping from subject to principal","SAML federation for a client identity provider","Group and role sync from the IdP","Session handling, refresh, logout propagation","Service-account authentication for API callers"]},
 {n:"Principal model",s:["principal table: id, kind, employment, not_after, disabled_at","principal_identity mapping channel identities to a principal","Disable and re-enable with cascade to sessions","Partner principal with zero standing entitlement","Break-glass session: time-boxed, separately audited, notifies"]},
 {n:"Role model",s:["Six roles as a compiled constant, not editable rows","role_grant with scope required for dept_admin and approver","Deputy flag, thirty-day maximum, depth-one enforcement","Super Admin floor of two, refuse to revoke the last","Constraint: a role may never be the subject of a capability grant"]},
 {n:"Entitlement system",s:["capability_grant: principal, capability, scope, granted_by, reason, not_after","Revocation as deletion of the grant, never a negative grant; additive-only invariant","capability_pack: named bundles with scope-bound assignment","Resolver as one SQL function called by both application and trigger","Valkey cache keyed on principal and grants_version, sixty-second TTL","grants_version bump on any write with immediate invalidation","policy_epoch increment on every entitlement mutation","Audit row on every grant, revocation and pack assignment"]},
 {n:"Teams",s:["team table within a department","Team membership, and scope predicates that may reference a team","Team as a grant subject alongside the individual"]},
 {n:"Staff sources",s:["Roster source interface producing directory assertions, read on a schedule and never at sign-in","Roster source and sign-in source are two axes: a spreadsheet can be the first and never the second","Per-source trust declaring what each may assert, existence only by default","Spreadsheet and Google Sheet adapters, existence only, because anybody with the link can edit one","Google Workspace Admin, Microsoft Entra and Lark adapters, permitted to assert department and role","LDAP and Active Directory adapter for an install with a directory behind it","Completeness declaration: an incomplete run may add and may never remove","Work address as the join between a roster entry and the sign-in identity, and what happens when one changes","Refuse role rules wired to a source not trusted to assert them, at configuration time not at run time","Warn when two sources both assert roles, because neither can see the other rows","Console page to choose, configure and test a staff source before it runs","Scheduled sync with a dry run showing what would be added and removed"]},
 {n:"Capabilities a screen needs can be granted",s:["Every capability a served route or console screen requires is in the capability registry an install is furnished with and can be granted from the console, and a sweep fails the build when one is not"]},
 {n:"Found by tracing every requirement to a task",s:["A person marked as external cannot be saved without an end date, and on that date every grant, session and channel binding they hold stops working","A service account or API key acts at its named owner's live reach narrowed by its own declared limit, holds no grant of its own, and stops working the moment its owner loses that reach or leaves","A department head is granted audit reads over the people placed in their department, the staff sync rewrites that set when someone joins or leaves, and no audit entry records a department","The approve permission alone decides who may approve, and the console flags anyone holding the Approver role without an approve permission, or an approve permission without the role, as a misconfiguration","Identity is proved on an install: a person signs in through the company identity provider and receives a product id, binds a chat identity only with a single-use code minted in the web session, an unbound chat identity gets nothing and is told how to bind, the same person has the same reach on web and chat, a session ends at its idle and absolute limits, a password-only sign-in is refused administration, an account bound to nobody is told once who can link it, a forgotten password is reset by email, and the chosen staff source asserts only that people exist and is re-read on its schedule, with the checks recorded","The staff source credential given at setup is kept in the vault for the scheduled sync and replaced from the console, and a sync whose credential is refused changes nobody and says so on the staff sources screen","Appointing one person as both Super Admin and Connector Admin shows a separation-of-duties warning that must be acknowledged with a reason, and the acknowledgement is recorded in the audit trail","Every permissions requirement in the requirements register is demonstrated on an install by a person, and each check is recorded against the requirement it proves","When the staff sync marks a person as a leaver, every agent they own is stopped and listed for transfer on the console until a new owner accepts it, which starts it again at no wider reach than before and records the change of owner in the audit ledger (owner decision 2026-09-21: stop rather than keep running)"]},
 {n:"Who can see a record",s:["A record's access view, showing who can see that record, opens only for someone who can already see it"]}
]},
{id:"M2",name:"Departments and scopes",wave:1,tasks:[
 {n:"Scope engine",s:["scope table: slug, predicate jsonb, department flag","Predicate evaluator compiling to a SQL where clause","Conjunction-only grammar validator so a scope can never widen","Scope composition for multi-scope principals","CI check: no agent slug or tool object collides with a scope slug"]},
 {n:"Department structure",s:["department table under company","Department admin assignment with scope not null","Department creation wizard with starter scopes","Cross-department behaviour: intersect, state the gap, offer request-access"]},
 {n:"Found by tracing every requirement to a task",s:["Department scoping is proved on an install: a department admin publishes an agent and adds a document at department reach, a member granted that department's knowledge is answered from its documents, a member of that department uses both on the web and in a chat channel, a member of another department can neither find nor reach either and is answered as if they did not exist, the department admin cannot grant, publish or widen beyond their department, a company-wide publication waits for a Super Admin, and the department halt stops only that department, with the checks recorded","Every departments requirement in the requirements register is demonstrated on an install by a person, and each check is recorded against the requirement it proves"]}
]},
{id:"M3",name:"The gate",wave:1,tasks:[
 {n:"Pipeline skeleton",s:["GateContext carrying trace id, principal, entitlements, hash, lane, channel, traffic class","Ordered middleware chain with explicit step boundaries","Recorder constructed at ingress before identify","Traffic class as a union with no default, so a new channel cannot compile without declaring it"]},
 {n:"Arrive and identify",s:["Channel ingress normalisation to one internal event shape","Dedupe by unique index on channel and external id","Channel identity to principal resolution","Unrecognised identity path with no entitlement and a prompt to bind"]},
 {n:"Entitle and intersect",s:["Resolve entitlements from cache or database","Compute the entitlement hash","Channel capability intersection","Assurance-level intersection"]},
 {n:"Screen",s:["Injection classifier over user input","Risk score written to the request row","Explicit no-block guarantee, tested","Autonomy tier escalation on a high score"]},
 {n:"Cache",s:["Key from question, entitlement hash, agent config hash, policy epoch, source epochs","Lookup and store paths","Hit surfaced to the user with its age","Never-cache list for volatile question shapes"]},
 {n:"Route and select agent",s:["Lane classifier from features computable without a model","Agent selection: a named agent the person may use when they address one, an agent they may not use answering as one that does not exist, otherwise channel binding, then rules, then cheap classifier","Routing decision written to the row, never inferred afterwards"]},
 {n:"Catalogue projection",s:["Project tools as agent grants intersected with entitlements","Static tool prefix with deferred loading so the provider cache is shared","Assert projection happens in our dispatcher, never delegated to a connector","Empty-catalogue error when a manifest declared required tools and none resolve"]},
 {n:"Loop and leash",s:["Agent loop invocation with the projected catalogue","Leash lookup for agent, target and scope","Shadow: simulate, record, return marked as simulated","Assisted: render artefact, suspend, await approval, resume","Autonomous: proceed, except a tool that declares a sensitive effect, which always waits for a person's approval of the exact prepared action","Three checks per call: capability, rung, mask"]},
 {n:"Redact, trace, answer",s:["Invoke the redaction walker","Emit post-redaction payload to the trace sink only","Compose the answer with citations and freshness","Attach the trace reference for explainability","The entitlement resolver, the entitlement hash and the redactor take no agent as input, and a test fails if any of them is given one, so no agent setting can widen what a run returns","When more than one leash rule applies to an action the strictest rung wins, so a narrower rule can never loosen a broader one","A request passes identify, entitle, screen, lane classification and agent selection, cache, route and catalogue projection, in that order, before any model is called, and a test fails if a model call happens earlier in the chain or a step runs out of order","A person can address by name any agent they are allowed to use, in the web application and by mention in a chat channel, and the request goes to that agent instead of the router's choice, while an agent they may not use answers as one that does not exist"]}
]},
{id:"M4",name:"Redaction and data protection",wave:1,tasks:[
 {n:"The walker",s:["Recursive depth-first walk over the typed result","Mask computation from the field policy","Delete keys outside the mask at any nesting depth","Drop and log objects lacking an entity tag, failing closed","Handle arrays, nested entities and mixed shapes","Opaque escape hatch requiring its own capability, flagging the trace and labelling the answer"]},
 {n:"Field policy",s:["field_policy: entity type, field, required capability, classification","Default-deny for unclassified fields","Simulate mode logging what would be withheld","Policy epoch bump on any field policy change","Count fields declared against the collection they count, and withheld when it is filtered"]},
 {n:"User-facing presentation",s:["Structural lock rendering, identical for every viewer","Never emit a count of hidden items","Permission-denied indistinguishable from nothing-found at record level","Request-access route where the owner sees the question and the asker learns nothing"]},
 {n:"Serialization safety",s:["Authorization-aware serializer as the only path to a channel","Structural prevention of returning an untyped shape from a tool","Property-based tests over nested shapes","Redaction counts and field names recorded, values never"]}
]},
{id:"M5",name:"Models, routing, fallback matrix",wave:1,tasks:[
 {n:"Provider layer",s:["LiteLLM SDK integration as a driver only, never as a proxy","Provider credentials in OpenBao","Per-provider client with timeout and retry overrides per lane","A separate Router instance per pool, since tag filtering is inoperative in SDK mode"]},
 {n:"Tier classification",s:["Deterministic classifier over tool count, context estimate, lane, explicit request, residency touch","routing_tier with a jsonb rule set","Test asserting no model call occurs in classification"]},
 {n:"The matrix",s:["routing_rung: tier, scope, position, role, deployment, attempts, timeout, concurrency","Role label derived by trigger rather than typed","Console editor for the matrix","One attempt row per try, so the executed chain reconstructs from a join"]},
 {n:"Fallback and breaker",s:["Closed trigger set for fallback (connection error, timeout, rate limit, server error, context exceeded), and a model's content refusal is recorded and answered once through abstention, never tried on another model","Explicit rejection of quality-based fallback","provider_health with live and probe ring buffers","Open on three consecutive live failures, ratio above half, or two probe failures with no recent live evidence","Cooldown with exponential backoff and jitter","Half-open single-request admission via claim and return","Background prober on a sixty-second interval","Alert on chain depth rather than final failure"]},
 {n:"Residency",s:["Residency constraint attached to scope","Chain skips non-compliant rungs rather than degrading silently","Provider residency registry with documented storage location","Refusal path when no compliant rung exists"]},
 {n:"Found by tracing every requirement to a task",s:["An install answers a question end to end through each online provider an administrator switched on (OpenAI, Anthropic, Moonshot and DeepSeek), with the check recorded","A change to the routing matrix is run against the golden questions and permission canaries through the gate before it takes traffic, and a regression holds the change with the failing cases shown","Model routing is proved on an install: a provider is made to fail and the chain falls back on the closed set only while the breaker opens and recovers, a residency-restricted request skips non-compliant models or is refused, a content refusal is not tried on another model, an agent's tier and the company's own keys are used, and every attempt is visible in the trace, with the checks recorded","Each provider registered on the install carries its processing region, retention and training terms and a link to the signed agreement, the console lists what categories of data each has been sent with counts, and that record exports as one document the company can show","Every models requirement in the requirements register is demonstrated on an install by a person, and each check is recorded against the requirement it proves"]},
 {n:"Online model providers",s:["OpenAI, Anthropic, Moonshot and DeepSeek are each a model provider an administrator switches on from the console with a key, and each answers on the install through the routing ladder","Another provider with an OpenAI-compatible interface is added from the console with only its address, key and model names, with no release","An agent uses a model level by default, and an administrator may pin a specific provider and model for it, tried first with the level's fallback behind it"]}
]},
{id:"M6",name:"Speed: fast lane, cache, streaming",wave:3,tasks:[
 {n:"Fast lane",s:["Fast-path rules as data rows, not code","Rule matcher over question shape and entity type","Database grants restricting the fast lane to projected tables only","Empty tool catalogue for the fast lane, enforced structurally","Zero-network test with egress blocked","Share measurement excluding machine traffic"]},
 {n:"Caching layers",s:["Answer cache keyed on entitlement hash and epochs","Plan cache storing tool ids only, filtered through projection at execution","Retrieval cache with scope in the key","Embedding cache keyed on content hash","Projection freshness cache","Explicit prohibition and test: no semantic answer caching"]},
 {n:"Streaming and perception",s:["Server-sent events for the answer lane","Named progress steps in user language at tool-input-start","Step labels scrubbed of scope-revealing detail","Citations streamed before prose","Reasoning never streamed to consumers","Cache hits shown as instant with the reason"]},
 {n:"Prompt efficiency",s:["Static tool prefix ordering for provider cache sharing","Prompt caching structure with the stable prefix first","Output-length instruction separate from effort setting","Per-lane effort pinning to avoid cache invalidation"]},
 {n:"Found by tracing every requirement to a task",s:["A department admin adds, tests and retires a fast-lane rule from the console for their own department, and the rule answers without a model call from the next request","Caching is proved on an install: two people with different reach ask the same question, a person moves department and a source changes, and no cached answer, plan or shared prompt prefix reaches anyone who may not see it or survives the source change, with the checks recorded"]}
]},
{id:"M7",name:"Knowledge, assets, document parsing",wave:4,tasks:[
 {n:"Asset ingestion",s:["Upload endpoint with size and type limits","Extract-from-link ingestion","Virus and content scanning before parse","Original storage in the object store with lifecycle policy","Ingestion queue with backpressure"]},
 {n:"Parsing",s:["Docling integration for layout-aware extraction","Table extraction preserved through chunking","Tika fallback for unsupported formats","OCR path for scanned documents","Parse failure handling with a visible error naming the cause","Memory-bounded parse worker with a hard container limit"]},
 {n:"Chunking and embedding",s:["Chunking strategy with overlap and size bounds","Chunk metadata carrying document, scope, owner and position","Local embedding via Qwen3 through the inference server","Batch embedding with a queue","Re-embedding on model change with a rebuild command"]},
 {n:"Knowledge model",s:["knowledge table: content, scope, owner, verified_by, verified_at, review_by, state","Company, department and personal visibility levels","Publishing flow: uploading never widens scope","Promotion to company scope requires the gated path","Supersession: a newer version marks the older superseded","Scheduled job opening re-verification tasks from review_by","Verification badge surfaced in the answer"]},
 {n:"Field-level classification within documents",s:["Column classification for tabular knowledge such as price lists","Sell price visible while cost and margin are restricted","Classification editor in the console"]},
 {n:"Kinds of knowledge, approved solutions and a knowledge base with no extra services",s:["Every knowledge item carries exactly one kind from a closed list (SOP, policy, FAQ, pricing note, service information, service package, brand guidelines, company rule, best practice, template, training material, approved solution), set when it is added, shown in the library and usable to narrow what an answer or an agent retrieves, and a pricing note never holds the price rows themselves","A solution becomes company knowledge only after a named person approves it, and the item records what it solved, who approved it and when, and is retrieved like any other verified item","A plain text, Markdown, PDF or Word document can be added from the console and answered from on an install with no worker, object store or embedding server, and it is found by text search within the uploader's scope"]},
 {n:"Found by tracing every requirement to a task",s:["A retrieved passage carries the department, visibility and owner its item was stored with, so a knowledge grant over one department reads that department's passages and never another's","Every knowledge collection, connected source and agent names a steward, set and changed in the console, and the steward is the person notified when anyone grants themselves access to that object","A price list is held as classified table rows, and a price list uploaded as a document is offered for conversion to those rows rather than indexed as text","The embedding width is an install setting read by the migration, a width above what the vector index supports is refused when it is read, and changing the width is refused while any chunk holds a vector","Marking a knowledge item public is allowed to a super admin, or to a department admin for their own department's items only, and each marking writes a ledger entry naming the person","The knowledge lifecycle is proved on an install: an administrator uploads a document holding a table and adds a link, sets its kind, owner and review date, a question is answered citing the passage with its freshness and verification badge, a newer version supersedes the older, and a due review date opens a re-verification task, with the checks recorded","Every knowledge requirement in the requirements register is demonstrated on an install by a person, and each check is recorded against the requirement it proves","Every stored vector records the model that produced it, changing the embedding model rebuilds every chunk in the background while answers keep using the previous vectors, and a search never compares vectors from two models","Every stored chunk records which parser produced it, and a citation to a passage recognised by OCR or read by the text fallback says so, including for the OCR pages of an otherwise layout-read document","An OCR confidence floor is applied per page, and a page under it is kept out of the index with a visible parse notice naming the page while the document's legible pages are indexed","The largest upload the install accepts is parsed by the layout service within its container limit, and the worker reports at start when the declared upload limit exceeds what the parse service can accept (needs the owner's choice of upload limit, container size or request format)"]}
]},
{id:"M8",name:"Provenance, abstention, escalation",wave:2,tasks:[
 {n:"Citations",s:["Row citations naming record and field with read time","Document citations deep-linking to the passage","Freshness and staleness state on every citation","Citation assembled from the tool trace, never from the model"]},
 {n:"Abstention",s:["Four states kept apart: nothing retrieved, retrieved but not answering and nothing connected are said to the asker, and not entitled reads to the asker as nothing retrieved and is shown only in an administrator's trace","Scope statement naming what was searched","Not-entitled path indistinguishable from nothing-found","Refusal when no citation exists, configurable per agent"]},
 {n:"Escalation",s:["Escalation as an authored step in a skill, not a model judgement","Handoff payload: who asked, what, what was tried, what is needed","Notice of a pending approval reaches the approver in their own channel, and the decision itself is taken only in the console, the staff web application or on a Lark card","Timeout and expiry behaviour on every escalation","Takeover signal fed to the circuit breaker"]},
 {n:"Found by tracing every requirement to a task",s:["Escalation and sensitive topics are proved on an install: an escalated question reaches the named person in their own channel with who asked, what, what was tried and what is needed and times out as configured, and a question about HR, harassment, whistleblowing, salary, medical or legal matters is intercepted and routed before any agent answers without its content being recorded, with the checks recorded"]}
]},
{id:"M9",name:"Chat and conversation",wave:1,tasks:[
 {n:"Conversation model",s:["conversation and message tables with scope and principal","Thread continuity across channels","Conversation search restricted to the asker","Export of a single conversation"]},
 {n:"Answer composition",s:["Answer assembly from typed results","Locked-field rendering","Follow-up handling with retained context","Correction handling that feeds the learning signal"]}
]},
{id:"M10",name:"Channels",wave:2,tasks:[
 {n:"Adapter contract",s:["Channel adapter interface with normalise, send, capabilities","Capability declaration per channel: ephemeral, cards, streaming, attachments","Per-channel sensitivity policy: which classes may be carried","Registration and health check per adapter","An adapter must render a payload label or refuse to send, so the opaque escape hatch cannot lose its warning"]},
 {n:"Lark",s:["Webhook receipt and signature verification","Message and mention handling","Interactive cards for approvals","Card patching within the rate limit, with a text fallback","Ephemeral messages for per-viewer bodies","Group and direct-message handling"]},
 {n:"Identity binding",s:["Outward-only binding: nonce minted in an authenticated web session","Binding expiry and single use","Unbound identity behaviour","Unbinding and rebinding on device change"]},
 {n:"Group-chat safety",s:["Room envelope computed at the floor of everyone present","Per-viewer body via the channel's ephemeral mechanism","Degradation ladder ending at a web link where the gate re-runs","Room membership change invalidating a pending render","Async delivery re-running entitlement at send time"]},
 {n:"Further channels",s:["Slack adapter","Microsoft Teams adapter","WhatsApp adapter with template and session-window handling","Telegram adapter","Web widget with session minting and abuse guard","Email adapter, inbound and outbound","REST API with key management"]},
 {n:"Channels an install can actually use",s:["Every channel adapter delivers its planned message through the vendor's API using a credential held outside configuration, and a delivery the vendor refused is recorded as refused and never as sent","A WhatsApp webhook is accepted only when its signature matches the app secret over the exact bytes received, and an unsigned or altered request is refused before it is read","Each channel is configured on the install as its own record holding its tenant identifiers, secret slot and on or off state, and switching one off stops both receiving and sending on it without touching any other channel"]},
 {n:"Found by tracing every requirement to a task",s:["An approval is decided only in the console, the staff web application or on a Lark card; a reply on WhatsApp, email or any other channel never decides one, and the approver is told where to decide it","An anonymous website widget visitor holds one grant over knowledge an administrator marked public and nothing else, a question about anything else is answered as nothing found, marking or unmarking an item public is audited with who did it, and a department admin can mark only their own department's knowledge public","Every shipped channel is proved on an install: on each chat channel, by email, through the REST API and in the web application a bound person asks and gets a permission-correct answer through that channel's own credential with progress streamed, a sender bound to nobody learns nothing about the company, a message whose signature does not verify is refused unread, a platform retry produces one answer, a waiting approval notice reaches the person it names, the conversation continues on a second channel, and an agent not enabled on a channel refuses there, with the checks recorded","Group chat and channel policy are proved on an install: in one group chat people with different reach see a shared message at the floor of everyone present and each receives their own body privately or as a link where the gate re-runs, a channel whose policy excludes a sensitivity class refuses to carry it, and an approval card is offered only where approvals are allowed, with the checks recorded","The website widget is proved on an install: an anonymous visitor on a website is answered only from knowledge marked public, a question about anything else returns nothing found, and repeated requests from one origin are capped and the session expires sooner than a signed-in one, with the checks recorded"]}
]},
{id:"M11",name:"Connectors, federation, projection",wave:2,tasks:[
 {n:"Connector framework",s:["Connector interface: capabilities, tools, health, credential binding","MCP transport adapter","REST and OpenAPI transport adapter driven by a spec plus field mapping","Database transport adapter restricted to allowlisted read-only views","Custom code transport adapter running sandboxed","Connector registration, enable, disable, upgrade","Manifest hash pinning so a silent redefinition fails closed on reconnect"]},
 {n:"Credentials",s:["Credential reference model, never a value in configuration","OpenBao storage with per-run leases","Scope-at-connect: one folder or one pipeline rather than everything","Read-only by default with write as a separate deliberate grant","Identity mode per tool: requester credentials by default, service credentials declared","Rotation without redeploy"]},
 {n:"Rate limiting",s:["Per-connector token bucket","Circuit breaker per connector","Retry policy with backoff and jitter","Metrics: requests per second and minute, concurrency, 429 rate, latency, error rate","Configured ceilings per source, matching documented limits"]},
 {n:"Projection",s:["proj.record table with source, entity type, identifiers and hot fields","Twelve-field cap per entity kind enforced by the manifest validator","Five-clause projectability test enforced at manifest review","PII deny-list blocking email, phone, address, identity numbers, bank and salary","Visibility predicate stored, never a resolved ACL","Change-signal subscription: webhook, CDC or updated-since cursor","No change signal means no projected fields, enforced","Backfill job with rate-limit awareness","Staleness surfaced in the answer"]},
 {n:"Federation",s:["Live fetch path with an eight-hundred-millisecond timeout","Parallel fan-out across independent sources with one critical path","Global and per-source call budgets","Thundering-herd protection on cache miss","An answer names a source that could not be fetched only to an asker who could already see that source, anyone else is told part of the answer is unavailable, and the full list goes to the operator log"]},
 {n:"First connectors",s:["Laravel MySQL via read-only views","Freshdesk","Lark Base","Lark Wiki","Xero","HubSpot","Google Drive or Microsoft 365"]},
 {n:"Connectors the owner named that the plan lacked",s:["Google Analytics is a connector that reads one property's traffic and conversion figures for a named date range, read-only, and an agent without that property bound gets nothing from it","Search Console is a connector that reads one verified site's queries, pages, clicks, impressions and indexing issues for a named date range, read-only","Cloudflare is a connector that reads the zones, DNS records and security events of the accounts it was given, and any DNS change goes through a prepared action a person approves","A domain and hosting connector reports each client domain's registrar, expiry date and hosting status, so an expiry question is answered from the source rather than from a spreadsheet","Slack is a connector that reads only the channels it was invited to, and a message from a channel the asker cannot see in Slack is never returned to them","Google Workspace is a connector whose included services (mail, calendar, documents) are chosen at connect, and mail and calendar are read with the asking person's own authorisation rather than a company-wide key","Every shipped connector can be connected, configured and switched off from the console, including the visibility rule and department it needs, so no source has to be connected at the server","An administrator adds a connector for a new API from the console by supplying its specification, field mapping and credential reference, and it becomes available to agents after review without a release"]},
 {n:"Found by tracing every requirement to a task",s:["Manifest review refuses a projected field that is not an id, join key, status value, timestamp, visibility predicate or a display label of at most 120 characters","A canary value placed in a federated field of a recorded source response is found in no table, memory row, projection row or log after the answer is delivered, apart from the payload store within its retention","Live reading is proved on an install: a question over a connected ticket source is answered live, a canary value from a ticket body is afterwards found in no table, memory, projection or log, deny-listed fields never appear in the projection, a question spanning many records is answered from projected headers, and document embeddings are computed with no outbound call, with the checks recorded","A change signal from a source advances that source's epoch, so a cached answer that read the source is never served after the source changed","A connector declares each write it can perform as a tool with a side-effect class, connecting it shows every such write switched off until an administrator turns one on with the change audited, and a write that is on runs only as a prepared action a person approved, at the requester's reach, reported done only after it is read back from the source","A connector that authorises by OAuth completes the consent flow from the console, keeps the refresh token by reference in the vault, and renews access before it expires without a person, and a refresh or consent the vendor refuses marks the source down on connector health in words","A scheduled check reads each connected source's schema every night, and a renamed or removed field a tool depends on is shown on connector health and makes that tool answer as degraded rather than silently return nothing","Live connector answers are proved on an install: each shipped connector is connected from the console with its key in the vault and scoped to the slice it was given, a question about a client's open tickets is answered from the live source citing the record, field and time read, and a call to a connector not bound to the agent is refused even though the asker could reach it, with the checks recorded","Connector safety is proved on an install: a key written in the console is never shown back in any screen, response or log, is leased for one run and revoked at its end, and is rotated without a redeploy; a source pushed past its rate ceiling is throttled and its circuit opens; and a changed tool manifest or renamed source field is shown on connector health rather than silently answered, with the checks recorded","A connector write is proved on an install: a write that was off is turned on by an administrator, the agent prepares a reply to a ticket, a person approves it, it runs once at the requester's reach and is reported done only after it is read back from the source, and a signed outbound event is delivered to a receiving system with a retry after a refused first attempt, with the checks recorded","A record the source has deleted is detected by the sync, withheld from answers from the next completed read and recorded with when it was noticed, and a record the source later returns is served again without reviving the retired row","The ticket desk connector can post a reply to a ticket as a prepared action, sent only after a person approves it and reported done only after it is read back from the ticket desk, as the first connector write of the first complete maintenance agent","Every connectors requirement in the requirements register is demonstrated on an install by a person, and each check is recorded against the requirement it proves"]},
 {n:"Every listed connector ready to use once connected, holding no copy of business data",s:["A connector keeps only a minimal index of a source (identifiers, display names and the few fields needed to find a record), never a copy of business values, and a test fails if the projection holds a field outside that minimal set","Every value an answer or an agent uses from a connected source is read live from the source at that moment, at the asker's reach","Google Drive, once connected from the console, is ready to use with no server step: a permitted person's question is answered from it on the install and an agent bound to it can use it","Lark, once connected from the console, is ready to use with no server step: a permitted person's question is answered from it on the install and an agent bound to it can use it","Slack, once connected from the console, is ready to use with no server step: a permitted person's question is answered from it on the install and an agent bound to it can use it","Freshdesk, once connected from the console, is ready to use with no server step: a permitted person's question is answered from it on the install and an agent bound to it can use it","Xero, once connected from the console, is ready to use with no server step: a permitted person's question is answered from it on the install and an agent bound to it can use it","The CRM, once connected from the console, is ready to use with no server step: a permitted person's question is answered from it on the install and an agent bound to it can use it","The maintenance portal, once connected from the console, is ready to use with no server step: a permitted person's question is answered from it on the install and an agent bound to it can use it","The domain and hosting connector, once connected from the console, is ready to use with no server step: a permitted person's question is answered from it on the install and an agent bound to it can use it","Cloudflare, once connected from the console, is ready to use with no server step: a permitted person's question is answered from it on the install and an agent bound to it can use it","Google Workspace, once connected from the console, is ready to use with no server step: a permitted person's question is answered from it on the install and an agent bound to it can use it","Google Analytics, once connected from the console, is ready to use with no server step: a permitted person's question is answered from it on the install and an agent bound to it can use it","Search Console, once connected from the console, is ready to use with no server step: a permitted person's question is answered from it on the install and an agent bound to it can use it"]}
]},
{id:"M12",name:"Tools, skills, capability registry",wave:3,tasks:[
 {n:"Tool registry",s:["tool_definition table with the closed naming grammar","Required capability per tool","Side-effect classification driving default leash","Result contract declaration: typed or opaque","Registration validation at startup","Registry sweep in CI"]},
 {n:"Skills",s:["SKILL.md folder format parser with frontmatter validation","Import from GitHub with a pinned commit","Import from URL","Import from uploaded archive with path-traversal validation","Imported state that is not executable until reviewed","Review queue with diff view","Version locking per agent","Progressive disclosure: names and descriptions first, body on demand","run_skill_script as the single execution tool, sandboxed and leashed","SOP import accepting messy Word and Confluence documents"]},
 {n:"Skills and tools the owner's architecture asks for",s:["An administrator exports an approved skill as a package another install can import, carrying its version and digest, and the import on the other side still lands unreviewed","An administrator edits a skill in the console and the edit is saved as a new version that waits for review, while every agent keeps the version it was pinned to until somebody moves it","A skill declares the knowledge it needs, and assigning it to an agent whose knowledge scope does not cover that need is refused with what is missing","A skill carries example tasks with the behaviour expected of it, and a new version cannot be approved until those examples have been rehearsed against it","An agent produces a document, a slide deck or a spreadsheet from an approved company template through one registered generation tool, and the file is stored as an artifact holding only what the caller may see","An agent reads a file a person attached to the conversation through a registered tool at that person's reach, and cannot read a file somebody else attached","An external AI service other than a chat model, such as image generation or speech to text, is registered as a tool with a required capability and a side-effect class, and is refused to any agent whose ceiling does not name it","Every tool that sends a message to a client, issues a quotation, changes DNS or hosting, changes financial records, deletes information, publishes content or changes a production system declares that effect, and no leash entry or promotion lets such a call run before a person approves the exact prepared action"]},
 {n:"Found by tracing every requirement to a task",s:["Each sensitive action the owner named (sending a client email, issuing a quotation, changing DNS, recording a financial change, deleting a record, publishing content, changing a production system) has a registered tool that performs it only after approval of the exact prepared action","Human approval of sensitive actions is proved on an install: for each named sensitive effect an agent prepares the action, the approver sees the real artefact at their own reach in the console and on a Lark card, the action runs once only after approval, an irreversible one needs two approvers neither of whom asked for it, a rejected one never runs, a reply by WhatsApp or email decides nothing, and no leash promotion lets such an action run unapproved, with the checks recorded","An administrator can switch off one tool from the console, after which every call to it from any agent, workflow or automation is refused naming the switch until it is switched back on, and a department admin can stop their own department the same way","An agent checks whether a website is working through a registered read-only HTTP tool that reports status, redirects, certificate expiry and response time for an address within its ceiling, and opens the read-only browser only when a rendered page must be inspected","An agent can run code in a sandbox with no network and no credentials over files and results the caller may see, the call is leashed and recorded, and its output is redacted like any other tool result","An administrator may approve a skill they imported themselves and the approval is recorded as a self-approval the audit view can list, while an import by anyone else needs a different reviewer","An install is furnished with a starter skill library holding an SEO audit, keyword research, website health check, ticket analysis, complaint analysis, monthly SEO report, proposal generation and website troubleshooting skill, each approved, assignable and rehearsed against its example tasks","The tool catalogue is proved on an install: one agent calls a typed API tool, a pinned third-party tool server, the sandboxed code tool, a document generation tool whose file is stored as an artifact with its provenance and retention class, and a file a person attached, each running as the requesting person, while a tool not attached to it, a tool switched off for the install and a tool whose manifest changed are each refused, with the checks recorded","The skill lifecycle is proved on an install: a skill is created in the console and another imported from a repository at a pinned commit, neither runs until reviewed against a diff, a self-approval of an own import is recorded as one, an edit becomes a new version that waits for review while agents keep their pinned version, approval waits for its example tasks to be rehearsed, and an exported skill lands unreviewed on a second install, with the checks recorded","Skill reuse is proved on an install: one approved skill is assigned to two agents and changes what both runs do, the agents load it by name first and in full only on demand, its script runs only through the sandboxed execution tool, an assignment whose knowledge or tool needs exceed the agent's scope is refused with what is missing, and removing it from one agent leaves it on the other with the detachment in history, with the checks recorded","A skill's approval digest covers the bytes of every script it carries, so a script changed after approval is refused before it runs","Skill frontmatter validation refuses a description that does not begin by saying when the skill should be used","A skill carries one or more categories an administrator sets, and the skills screen offers them as filter chips drawn only from skills the reader may see","A person creates a skill by describing it in conversation, the system drafts the skill with its instructions, needed knowledge, tools and example tasks, and the draft lands unreviewed and not executable, exactly like an import","Every tools requirement in the requirements register is demonstrated on an install by a person, and each check is recorded against the requirement it proves","A skill's digest covers the bytes of every script it carries, so a skill with scripts is admitted and changing one byte of a script requires a new review"]}
]},
{id:"M13",name:"Agents and templates",wave:3,tasks:[
 {n:"Agent model",s:["agent table: persona, scope, model tier, visibility level","Global, department and personal visibility","Audience separated from authority, asserted by test","Agent enable, disable and archive","Ownership transfer when a creator leaves"]},
 {n:"Template model",s:["Signed, versioned, immutable manifest","Manifest schema with identity, persona, skills, tools, connectors, scope, tier, leash, golden set, placeholders","Instance as a pinned reference plus an overlay","Per-path field ownership tracking who last set each field","Materialised effective document with a hash feeding the cache key","Five sealed paths enforced by a check constraint","Blank template so a hand-built agent uses one code path"]},
 {n:"Install",s:["Six-step wizard generated from the manifest JSON Schema","Connector binding step with readiness indicators","Placeholder collection: SOP, price list, escalation contact","Golden-set run as the installing principal","Golden-set run as a low-privilege fixture user","Incomplete state with an amber badge listing what is missing","Pin to Shadow when required connectors are absent"]},
 {n:"Versioning and upgrade",s:["Publishing a new version changes nothing observable","Upgrade badge on the instance","Three-column diff of conflicting paths only","Opt-in accept with per-path resolution","Decline forever without nagging"]},
 {n:"Template catalogue",s:["Business Analyst","Pre-Sales","Project Manager","UI Designer","UX Designer","HTML Developer","WordPress Developer","Laravel Developer","Shopify Developer","Content Uploader","Tester","Internal Helpdesk","Site Health Sentinel","Support Ticket Agent","Project Status Reporter","Capacity and Hours Analyst","Quote and Proposal Drafter","The receivables and renewal chaser is pinned to Shadow, the pin is reviewed at thirty days against measured confidence, and it never lapses because a date passed","Accountant Agent, Shadow-pinned","SEO Agent","SEM Agent, Shadow-pinned, human commits budget changes","SMM Agent","Knowledge Gap Curator"]},
 {n:"Client-authored templates",s:["Save any agent as a private template","Literal extraction and parameter hoisting","Leak report with accept or redact on every unclassified literal","Publish blocked until every item is dispositioned","Memory never exported under any setting","Visibility: private, organisation, catalogue"]},
 {n:"One agent runtime, and agents composed from separately configured parts",s:["Every way work reaches an agent (web, channel, automation, workflow step, delegated subtask) enters the same agent runtime, and a test fails if a second code path calls a model with tools","An agent run stops after a bounded number of model turns and tool calls, and the stop is recorded on the run with the bound that was reached","An administrator can halt one agent, and every run of that agent, including its automations and delegated subtasks, is refused until the halt is lifted","An agent answers only on the channels an administrator enabled for it, and a message reaching it from any other channel is refused before the agent runs, in the same words as an agent that does not exist","A question that reaches an agent through any channel is answered at the intersection of the asker's reach and that agent's ceiling, never at the wider of the two","An approved action runs once without the requester returning, at the requester's reach re-resolved at that moment, and a rejected, amended or lapsed one never runs","A suspended action with an irreversible effect runs only after two different people have approved the same prepared action, neither of them the person who asked for it nor the principal it runs as, and the database refuses it as well as the route","An agent run can call only the connectors bound to that agent, and a call to any other connector is refused by the gate even when the asking person could reach that source directly","A maintenance agent template ships in the catalogue declaring maintenance knowledge, a ticket desk and a maintenance records source, maintenance skills, a chat channel, the HTTP website check tool and the read-only browser tool, and it installs into an agent that answers"]},
 {n:"Found by tracing every requirement to a task",s:["An agent's connector list compiles into its ceiling as the tools those connectors provide, the gate resolves it through the one intersection used for every ceiling entry, and a test fails if connector access is decided anywhere else","A run that outlives a grant it started with is re-judged at its next tool call, so an automation or a streamed answer stops using a capability from the moment it lapses or is revoked","The harness is proved on an install: a question through each of the fast, answer and task lanes, a task killed mid-run and resumed, a halted agent, a switched-off tool, a run that hits its turn bound, a person addressing a named agent and an injected tool result are each exercised, and each trace shows the lane, the gate order, the projected catalogue, leased credentials never exposed and the refusal reason, with the checks recorded","Only a super admin can publish or retire an agent at company-wide visibility; a department admin or member who tries is refused, and a published company-wide agent appears for a person in every department","A department admin installs an agent from the catalogue on the install and it answers its golden set correctly, with the time from choosing the template to the first correct answer recorded","The maintenance agent template also declares a maintenance workflow, and installing it assigns that workflow so the installed agent runs it","The SEO agent template declares SEO knowledge, the analytics and search console connectors, SEO skills, the chat and web channels and the monthly SEO report workflow, and installs into an agent that answers","The catalogue has a sales, a finance, a support and a project management agent template, each declaring its knowledge, connectors, skills, channels and workflows and installing into an agent that answers","The agent lifecycle is proved on an install: a template with a missing connector installs pinned to Shadow with its badge, a custom agent is composed, rehearsed as a named person and published through the canaries and a second approver, an upgrade is offered and declined, a personal agent stays invisible to others, and an agent is saved as a private template, with the checks recorded","An install mints its template signing key once at setup into the vault, the application reads it and nothing can replace it while a signed template version exists, and the built-in templates are signed with it when the install is furnished","Accepting a new template version runs the same binding and completeness checks as installing it, so a tool the version declares is bound to a registered tool or reported missing, and a newly required connector or placeholder reopens the wizard before the version runs","Every agents requirement in the requirements register is demonstrated on an install by a person, and each check is recorded against the requirement it proves","A leash rung can carry an end date, the gate keeps the rung until that date and then reports it due for review rather than loosening it silently, and the Leash screen shows when each pinned rung ends","Every harness requirement in the requirements register is demonstrated on an install by a person, and each check is recorded against the requirement it proves","A template authored from an existing agent is stored as a draft that survives a restart, is shown for review with every value hoisted out of it, and becomes installable only after a person publishes it","An agent's audience can list named people and several departments, and adding one lets them find the agent without widening what any run reaches.","The manifest has a settable overview path, older signed templates still verify under their own schema version, and no model prompt includes the overview.","On an install with no vault the template catalogue says installing is unavailable and no signing key is stored anywhere else.","An agent's audience can name several departments and individual people beside its level, and each added person or department gains discovery only, never reach"]},
 {n:"Agents created by super admins and department admins",s:["A super admin creates an agent for the whole company from the console, from a template or from scratch, and in one flow sets its workflow, skills, tools, connectors, knowledge, channels, memory access and which actions need approval","A department admin creates an agent for their own department in the same flow, and can choose only the skills, tools, connectors and knowledge their department holds","On the install a super admin and a department admin each create an agent from their requirements and a workflow, assign its skills and tools, and the agent answers through its channel, with the checks recorded","A template agent installs with one click using sensible defaults, and the full setup wizard stays available for anyone who wants to change them"]}
]}
];
