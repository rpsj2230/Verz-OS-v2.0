module.exports = [
{id:"M39",name:"Agent workspace, the inside-an-agent surface",wave:3,tasks:[
 {n:"The agent as a container, not a config row",s:[
   {n:"Composition model",k:["An agent record composes persona, knowledge scope, connectors, skills, tools, workflows, channels, availability, leash, model policy, memory and automations","Every attachment stored as a reference with a pinned version, never an inline copy","Attachment add and remove written to the ledger with who, when and why","Divergence flag when an instance edits something its template supplies","Composition diff between an instance and its template, rendered side by side"]},
   {n:"Workspace shell",k:["Agent header: avatar, name, role line, owner, template lineage and version","Tab strip: Conversations, Automations, People, Knowledge, Memory, Artifacts, Settings","Right pane switches between Dashboard and Profile without losing tab state","Deep link to any tab so a console alert can point at the exact place","Keyboard navigation across tabs and back to the roster"]},
   {n:"Cost on the front page",k:["Spend, runs and messages as the agent's headline figures, not a buried report","Range selector across seven, thirty and ninety days and month to date","Cost attributed per caller so a single heavy user is visible","Projection to month end against the agent's own budget"]}
 ]},
 {n:"Capabilities tab, what the agent is assembled from",s:[
   {n:"Connectors on the agent",k:["Connector row with per-source icons and an overflow count","Attach and detach with the capability check performed at attach time","Each connector shows which fields it projects for this agent","A connector the agent asks for but the department has not granted shown as requested, not attached","Health inherited from the connector page so a degraded source is visible here"]},
   {n:"Skills on the agent",k:["Skill chips with version, source and review state","Attach only from the approved library, never directly from a repository","Unapproved skill offered with a route to the review queue rather than an attach button","Per-skill invocation count so unused attachments can be pruned","Skill description convention enforced: the description is the router prompt"]},
   {n:"Knowledge on the agent",k:["Knowledge scope expressed as a predicate, not a document list","Add by upload, by link extraction, or by widening the predicate","Preview of how many items the predicate currently matches","Coverage and staleness for exactly this agent's slice","Items the agent retrieved most and items it never touched"]},
   {n:"Channels on the agent",k:["Channel row with per-channel enable","Channel capability intersected with the agent ceiling before it is offered","Per-channel rendering profile chosen automatically","Group installation where the channel supports it"]}
 ]},
 {n:"Availability against ceiling, kept visually distinct",s:[
   {n:"Two different questions, two different blocks",k:["Availability block: which users and departments can find and invoke this agent","Ceiling block: the widest entitlement any run of this agent may reach","Explicit copy stating that availability is discovery and never authority","Worked preview: pick a person, see exactly what their run of this agent would return","Preview computed by the real gate, never by a separate estimation path"]},
   {n:"Leash on the agent",k:["Leash matrix per target and scope, read, draft and write on separate rungs","Promotion requires evidence: clean runs, agreement rate and a named approver","Demotion by circuit breaker shown with the metric that tripped it","Money and irreversibility boundaries pinned so a rung cannot rise without a second approver","History of every promotion and demotion with the evidence attached"]}
 ]},
 {n:"Memory made visible",s:[
   {n:"Memory viewer",k:["Memory rendered as readable text, never an opaque store","Split between curated memory and memory extracted from conversations","Change history with a diff per revision and the trigger that caused it","Direct edit and direct delete by the owner","Per-agent and per-person memory shown separately so neither hides the other"]},
   {n:"Learning on the agent",k:["Which tiers are active for this agent","Tier one changes listed with one-click undo, never an approval queue","Tier two rules shown with their shadow evidence and a promote control","Tier three items routed to the department queue with a link back here","A single control to freeze all learning for this agent during an incident"]}
 ]},
 {n:"Artifacts, what the agent produced",s:[
   {n:"Artifact store",k:["Every produced document, deck, report, export and image recorded as an artifact","Artifact carries producing run, agent version, caller, entitlement hash and timestamp","Stored in the object store with the retention class of its most sensitive input","Field-level redaction applied at production time, so an artifact never exceeds its caller","Re-download re-checks the requester entitlement rather than trusting the original link"]},
   {n:"Artifact surface",k:["Artifacts tab listing what this agent produced, newest first","Filter by type, by caller and by date","Provenance panel: which sources and which knowledge items fed it","Supersede and archive rather than delete","Artifact count and storage shown against the retention policy"]}
 ]},
 {n:"Automations owned by the agent",s:[
   {n:"Automation surface",k:["Automations listed under the agent that owns them, never in a global pile","Named as outcomes in the first person, not as trigger and action mechanics","Template gallery for common automations with a one-step install","Per-automation run history with the last result and the next scheduled time","Pause, resume and delete with the scheduler registry updated in the same transaction"]},
   {n:"Automation safety",k:["An automation runs on a named principal, never on the agent itself","Schedule changes are a tier three learning event and need a human","Failure after a threshold pauses the automation and notifies the owner","Every automation appears in the scheduled job registry with what it guards"]}
 ]},
 {n:"An agent assembled from console choices",s:["An agent is configured with which kinds of memory it may read and write (the conversation, the task, the project, the person and its own), and a run cannot read a kind of memory its configuration leaves out","An agent is assembled on the install from console choices alone, picking knowledge, connectors, skills, channels, tools and workflows that were each created separately, and it answers through its assigned channel with no code change or release, proved on the install for a maintenance agent and an SEO agent"]},
 {n:"Found by tracing every requirement to a task",s:["Changing an agent attachment is refused on the server unless the caller holds that attachment's editing role (owner for persona, knowledge, skills, memory and automations; department admin for connectors, channels, ceiling and leash; super admin for model policy), and the refusal names the role required","An agent's supervision pin is reviewed at thirty days against a confidence measured from its supervised runs, extends while that confidence is below 90 percent, ends only when a person confirms it at or above 90 percent, and never lapses because a date passed","The leash is proved on an install: the same agent action runs simulated and marked at Shadow, suspends at Assisted and proceeds at Autonomous only where no sensitive effect is declared, the strictest of overlapping rules wins, a department admin lowers a rung at once while raising one needs evidence and a named approver and two for a money boundary, a breaker demotes a rung showing the metric that tripped, and a supervision pin extends below its confidence bar, with the checks recorded","Artifacts are proved on an install: a report produced for one person holds only what that person may see, a re-download after their reach narrows is re-checked and refused, its retention follows its most sensitive input, and a new version supersedes rather than edits it, with the checks recorded","An artifact records the canonical client it was produced for, and an agent can retrieve the latest artifact of a given type for that client, re-checked against the asker's reach","The agent workspace lists the tools attached to an agent with add and remove, checks each against the agent's ceiling when it is attached, and a run's catalogue carries only attached tools","The maintenance agent runs end to end on the owner's install: a question in the chat channel is answered from uploaded maintenance knowledge and the ticket desk through its vault credential, its maintenance workflow runs on the worker, and one prepared action waits for and receives an approval on a chat card before it executes, with the checks recorded","Agent configuration is proved on an install: an agent is asked by a junior and a senior person, from an assigned and an unassigned channel, and is led toward an unassigned skill, tool, connector, workflow and memory kind and an action needing approval, and each is refused, narrowed or held for approval as its configuration says, with the checks recorded","The agent's Conversations section lists only threads the reader may see, filterable to threads the reader took part in, each showing participants, relative time, a preview and the run state including failed"]}
]},
{id:"M40",name:"Member self-service application",wave:4,tasks:[
 {n:"Sign-in and the member shell",s:[
   {n:"Authentication",k:["Single sign-on through Keycloak with the same identity the channels use","Channel identity and web identity resolved to one principal, never two","Session policy, idle timeout and device list","Password reset and recovery handled by the identity provider, not by us","Explicit test that a channel-only user can sign in and sees the same entitlement"]},
   {n:"Shell",k:["Member navigation distinct from the admin console and much shorter","No administrative surface reachable by URL guessing, enforced server side","Responsive down to a phone since approvals arrive on phones","Entitlement disclosure line rendered from the real entitlement set, never hand-written"]}
 ]},
 {n:"My workspace",s:[
   {n:"Landing",k:["Questions asked this month with corrections separated out","Agents this person can call, split between department-provided and self-built","Personal budget consumed against the personal cap","Count of things learned about this person, each reversible","Recent threads continued from any channel, so web and Lark are one history"]},
   {n:"My agents",k:["List with source, where each one runs and how often this person used it","Open an agent into the same workspace surface, bounded to what this person may see","Build a personal agent from a template within the person's own ceiling","Personal agent visible to nobody else unless explicitly shared","Request an agent the person cannot yet call, routed to the department admin"]}
 ]},
 {n:"My knowledge, skills and artifacts",s:[
   {n:"Knowledge I own",k:["Items this person uploaded with visibility scope, verification date and review due","Upload, replace and retire with the review cycle set at upload time","Usage count so an author can see whether anything reads what they wrote","Request promotion to company-wide, routed as a tier three decision","Never shows an item outside this person's entitlement, including their own department's restricted rows"]},
   {n:"Skills I wrote",k:["Personal skills with source, version and review state","Author in the studio, import from a repository or paste a URL","Submit to the department library for review","Personal skills usable only by this person's own agents until approved"]},
   {n:"My artifacts",k:["Everything produced for this person by any agent","Re-download re-checked against current entitlement","Share with a colleague only where that colleague's entitlement already covers the contents"]}
 ]},
 {n:"What the system knows about me",s:[
   {n:"Learning about me",k:["Plain-language list of every preference learned, with the evidence that produced it","One-click undo per item and an undo-all control","Weekly digest delivered in the person's own channel with the same controls","Opt out of personal learning entirely, with the cost stated honestly"]},
   {n:"My data",k:["What is stored about this person and where, in plain language","My conversation history with export","Deletion request routed to the retention policy rather than performed silently","Which agents have read my HR record, from the audit ledger"]}
 ]},
 {n:"Connections and channels",s:[
   {n:"Connected accounts",k:["Personal account connections with per-source consent","Explicit copy stating that connecting an account never widens what the person may see","Disconnect with immediate token revocation","Department-provided connections shown as inherited and not disconnectable here"]},
   {n:"Channels",k:["Which channels this person is reachable on and which is primary","Per-channel notification preferences","An approver's routing preference for envelope decisions chooses only between the console and a Lark card, never WhatsApp or email","Quiet hours honoured by automations and digests"]}
 ]},
 {n:"Approvals in the member surface",s:[
   {n:"Approval inbox",k:["Envelope approvals awaiting this person, with what will happen stated before the fact","Approve, reject and amend, each written to the ledger","Expiry on an unanswered approval with a stated default of doing nothing","Same approval reachable from Lark and from the web without double action","Mobile layout treated as the primary case for approvals"]}
 ]},
 {n:"Found by tracing every requirement to a task",s:["The mail relay configured at setup or on the notifications screen is also given to the identity provider, so a password reset email reaches the person on the install, and with no relay a forgotten-password link is not offered","A person can try an approved skill from its card, which opens a conversation with that skill pinned, run through the one agent runtime at the person's reach and metered like any other run","The web composer accepts an attached file and dictated speech, and dictation is transcribed through the registered speech tool at the person's reach before the text is sent","The member application is proved in the browser for a member who administers nothing: their agents, knowledge, skills, artifacts, memory, data, connected accounts and approvals, each showing only what that person may reach, with the checks recorded","The member application offers two starting points, start a task and talk to an agent, and a task started without an agent is routed by the gate to an agent the person may call, running at the person's reach","The member's recent tasks and threads each show their state, running, waiting for approval, done or failed, read from the job and suspension records, and a failed one says which step failed","A person can create a personal project grouping their threads and uploaded files, and a run inside it reads those files as project context at the person's reach on every turn, never storing them as memory","Opening an agent shows three starter prompts drawn from that agent's skills and the knowledge the person may reach, and never offers one the person's run could not answer"]}
]}
,
{id:"M41",name:"Client independence, built in rather than stripped out",wave:0,tasks:[
 {n:"Client independence, gated from day one",s:["Sweep refusing any client name, domain, work address, company id or storage path literal in src, migrations, console and compose","One settings module as the only place a client-specific value is read, so nothing reads the environment directly","Every client-specific value an environment variable with a declared default, a documented meaning and a refusal when it is missing","Branding as configuration: company name, logo, colours, product name, sender address","Identity provider as configuration: realm, issuer, client id, redirect URIs, brokered directory","Model and provider choice as configuration, including a local-only profile with no external provider","Storage, object store and vector store locations as configuration","The sweep runs in CI on every commit, so the final audit verifies a gate that has been green all along rather than cleaning a year of drift","Test: the application boots and answers a question configured as a fictitious company with no Verz value present"]},
 {n:"Install from empty, proven rather than described",s:["Migrations build every schema, table, index, constraint, trigger and row-level security policy from an empty database","Generic demo seed: a fictitious company, departments, people and records, deliberately not the Verz test fixture","No install step depends on a Verz database, dump, backup or export","First-run setup creating the first administrator, with no default password anywhere","CI job installing from empty into a scratch container and asking one question end to end","Uninstall and data-return procedure, because a client who leaves must be able to take their data and leave nothing behind","Starter pack loaded at install: the six roles, standard permission sets, the standard agents and sensible defaults, so a new client opens a furnished system rather than a blank one","Starter pack is separate from the demo seed, because a client wants the roles and not the fictitious records"]},
 {n:"The repository is the template",s:["The repository is the template and a client install is a release tag plus one environment file, so a fix reaches every client rather than one copy","No duplicated repository anywhere, because a copy diverges the first time a bug is fixed in one and not the other and nothing says so","Repository structure a reader can navigate: what is application, what is configured per client, what is never edited, where migrations, scripts and documentation live","Verz is one deployment: its values live in a per-install file and never in this repository"]},
 {n:"Found by tracing every requirement to a task",s:["On a scratch install a handover exports every store and the audit ledger in open formats with a manifest the company can check, then removes the identity realm, bucket, vault secrets, schedules, route, containers and configuration, revokes connector authorisations and the chat app, and issues a certificate naming the date the last backup expires"]}
]},
{id:"M42",name:"Client deployment template",wave:5,tasks:[
 {n:"Packaging and release",s:["Per-client variables in a separate private repository, one file per install, documented field by field","Secrets minted per install and never copied between clients, enforced by a check that refuses a shared value","One published image deployed by tag, never rebuilt per client","Release pinning per client, so one client can hold a version back without forking"]},
 {n:"Deployment documentation",s:["Server requirements and a recommended specification per profile, with the memory and connection arithmetic already measured","Operating system, runtime, package and port requirements","Step-by-step install from a clean server, written so it needs no reference to the Verz install","Domain, DNS, reverse proxy, TLS and firewall setup","Configuration guide: every value a new client must set, in one table, with where each comes from","Integration guide per connector, including what each one is trusted to read","Authentication and administrator setup, including the staff source choice","Backup, monitoring, logging and health-check configuration","Troubleshooting guide built from the failures actually hit during this build, symptom first","Update procedure and rollback procedure, both rehearsed rather than written"]},
 {n:"Deployment automation",s:["Install script, idempotent and safe to run twice","Environment template generator that refuses to emit a value it cannot explain","Database create, migrate and seed scripts","Health-check script a person runs after deploying and can read the output of","Backup and restore scripts, with the restore verified rather than assumed","Update script pinning a release tag, and a rollback script re-pinning the previous one","Deployment checklist: pre-deployment, server, application, database, environment, integrations, security, testing, go-live and post-deployment","A published release for every fix, with plain-English notes stating what changed, whether it changes the database, and how urgent it is","The console shows the running version and whether a newer release exists, because nothing else reminds a client IT team to update"]},
 {n:"Final audit and independent proof",s:["Audit source, configuration, compose, environment files and git history for any remaining client-specific value","Audit the database, logs, uploads, object store, vector store, embeddings and caches","Audit credentials, tokens, webhooks, third-party integrations and default administrator accounts","Verz is client number one: their live system is installed from the template by running the installer on their server, not migrated from the development environment","Verify on that install: it boots, sign-in works, the administrator works, core features answer and integrations configure, with nothing carried over from the development environment","Record the measured time from bare server to first answered question, because that number is what the template is for","A second install on a throwaway server as a fictitious client, run start to finish by somebody following only the documentation, because Verz succeeding proves the installer works for the person who wrote it","Every question asked during those two installs becomes a line in the documentation, since a question asked twice is a missing step"]}
,
 {n:"Installer and setup wizard",s:["install.sh: one command on a fresh server that checks the machine, installs what is missing, downloads the release, builds the database and starts everything","Every password, key and secret generated on the client server during install, never defaulted and never shipped in the template","A setup code printed once by the installer and required by the first wizard screen, so whoever loads the page first cannot claim the system","Setup wizard in the console: numbered steps with visible progress, step 3 of 10, and a back button on every screen","Wizard step: company name, logo upload and web address","Wizard step: the first administrator, who becomes the Super Admin","Wizard step: staff source, choosing between Google Workspace, Microsoft, Lark or a spreadsheet, signing in and pulling the list","Wizard step: AI provider, either their own key or a local-only profile with no external provider","Wizard step: optional data source connections, skippable and repeatable later from the console","Wizard step: review everything before any of it is written, because a wizard that commits each screen as it goes cannot be corrected","The wizard becomes unreachable the moment the first administrator exists, so the install path cannot be replayed by anybody who finds the address","A resumable install: closing the browser halfway continues where it stopped rather than starting the server again","Every step validates before it advances and says what is wrong in words the person can act on","A finishing screen that hands the person straight into the console already signed in, with nothing further to configure before the system answers a question","Installer output legible to a person: what it is doing, how far through it is, and what to do if a step fails","Wizard step: chat and email channels, each connected and sent a test message before the finish, skippable and repeatable later from the console","The installer is run start to finish on a fresh AWS instance and on a fresh VPS, each ending with the company's Brain answering a question, with the time taken recorded"]},
 {n:"Ready to use on arrival",s:["The administrative console is built by the release and served by the install, so an administrator has an address to open rather than a repository to build","The setup wizard writes what it collects, so a first install needs no hand-edited environment file for the values it asks for","A question can be asked and its answer read in the console, so an install proves itself end to end before any channel is connected","Console screens for skills: add a skill, see what it is trusted to reach, and assign it to an agent","Console screens for connectors: connect one, hold its credential in the vault, and see what it is trusted to read","A helper for a person who does not use a terminal: it asks for the server address and runs the one install command there","The first real release is published and fetched back on a clean machine, so the image and the archive are known to exist rather than known to be built","The rule for what belongs to the product and what belongs to one company is written into the repository's own instructions, with worked examples, and a commit that came from a staging problem has to say why its fix is generic"]},
 {n:"Found by tracing every requirement to a task",s:["The install health check reports whether the host exposes kernel virtualisation, and the browser isolation stated to the administrator is taken from that result rather than assumed","The release lifecycle is proved on an install: a CI-built, signed image that passed lint, types, tests, canaries and the licence check is pulled by digest with no inbound access, migrations run before traffic, in-flight work including a running workflow drains without loss, an unnamed image is refused, rollback to the previous release completes, and the copied-deployment check passes, with the checks recorded","The release image and archive carry the licence notice of every bundled third-party component and font, generated from the lockfiles, and a release missing one fails","The release archive carries the backup and restore scripts and the installer schedules the nightly backup, so an install made from a release backs itself up without a file copied from the repository","The owner's install runs the standard profile, and the System health screen shows the worker, the vault, the object store and the connection pooler each ready, with the provider key held in the vault and not in the environment file","The owner's install is pinned to a published release tag rather than the latest image, and the Version screen shows that tag and whether a newer release exists","Every other requirement in the requirements register is demonstrated on an install by a person, and each check is recorded against the requirement it proves","Probed from an address outside the allowlist, the install answers only on its public ports with valid TLS, every administrative console refuses sign-in, and a certificate within fourteen days of expiry alerts a named person"]}
]}
];
