# Tracker audit against the owner's install, 2026-09-17

Every task the tracker showed as done was checked against the owner's install, which runs commit f32c385. The standard is the owner's: a page in the console is not done; a task is done only when what it asks for exists and works on the install. Unit tests over fakes are not evidence.

## What the install is

- Runs only the app, a one-shot migration, PostgreSQL and a cache. No worker, secrets vault, object store, connection pooler or database backup.
- Holds one person, one company-wide scope, 76 grants, 80 audit entries and two routing rungs. No model has ever been called, no question answered, no agent, department, document, memory, connector or conversation exists.
- Sign-in carries a second factor (assurance strong). Learning, Memory and Records answer 404 for the first administrator.

## Result

| | Tasks |
|---|---|
| Done before the audit | 1213 of 1315 (92.2%) |
| Done after the audit | 167 of 1315 (12.7%) |
| Reopened | 1046 |

| Verdict | Tasks |
|---|---|
| Stays done: works on the install | 87 |
| Stays done: repository or CI item that really runs | 80 |
| Reopened: built and wired, never shown working on the install | 100 |
| Reopened: needs a component the install does not run | 212 |
| Reopened: nothing the install runs calls it | 604 |
| Reopened: fails on the install | 12 |
| Reopened: only part of what the task asks exists | 118 |

## By module

| Module | Done before | Done after | Reopened |
|---|---|---|---|
| M0 Foundation and repository | 38 | 26 | 12 |
| M1 Identity, users, roles, permissions | 40 | 9 | 31 |
| M2 Departments and scopes | 9 | 2 | 7 |
| M3 The gate | 37 | 4 | 33 |
| M4 Redaction and data protection | 19 | 2 | 17 |
| M5 Models, routing, fallback matrix | 23 | 2 | 21 |
| M6 Speed: fast lane, cache, streaming | 22 | 2 | 20 |
| M7 Knowledge, assets, document parsing | 25 | 0 | 25 |
| M8 Provenance, abstention, escalation | 13 | 0 | 13 |
| M9 Chat and conversation | 8 | 0 | 8 |
| M10 Channels | 27 | 0 | 27 |
| M11 Connectors, federation, projection | 39 | 0 | 39 |
| M12 Tools, skills, capability registry | 16 | 2 | 14 |
| M13 Agents and templates | 53 | 0 | 53 |
| M14 Entity resolution | 39 | 1 | 38 |
| M15 Knowledge retrieval and RAG | 15 | 1 | 14 |
| M16 Memory and learning | 27 | 0 | 27 |
| M17 Automation and the task lane | 24 | 0 | 24 |
| M18 Multi-agent execution | 16 | 0 | 16 |
| M19 Browser and computer use | 28 | 0 | 28 |
| M20 Verz Studio, the builder | 19 | 0 | 19 |
| M21 Cost, budgets and metering | 17 | 0 | 17 |
| M22 Admission control and capacity | 13 | 0 | 13 |
| M23 Rate limiting and abuse | 8 | 0 | 8 |
| M24 Audit, compliance, legal hold | 10 | 3 | 7 |
| M25 Retention, deletion, export, DSAR | 12 | 0 | 12 |
| M26 Joiner, mover, leaver | 16 | 1 | 15 |
| M27 Console, reports, observability | 94 | 13 | 81 |
| M28 Evaluation, canaries, regression | 11 | 2 | 9 |
| M29 Extension points | 13 | 0 | 13 |
| M30 Hosting, delivery and recovery | 33 | 10 | 23 |
| M31 Application scaffold and runtime | 38 | 22 | 16 |
| M32 Component deployment and wiring | 40 | 9 | 31 |
| M33 Role-specific surfaces | 40 | 2 | 38 |
| M34 Onboarding and adoption | 22 | 3 | 19 |
| M35 Localisation and accessibility | 17 | 1 | 16 |
| M36 Scaling triggers and performance | 19 | 1 | 18 |
| M37 Migration, launch and handover | 46 | 5 | 41 |
| M38 Continuous delivery and live status | 40 | 19 | 21 |
| M39 Agent workspace, the inside-an-agent surface | 71 | 0 | 71 |
| M40 Member self-service application | 52 | 0 | 52 |
| M41 Client independence, built in rather than stripped out | 21 | 13 | 8 |
| M42 Client deployment template | 43 | 12 | 31 |

## Every checked task


### M0 Foundation and repository

| Task | What it asks | Verdict | Why |
|---|---|---|---|
| M0.1.1 | Private GitHub repo, branch protection, CODEOWNERS on the three core files | reopen partial | CODEOWNERS exists, but the repository was measured public on 2026-09-14 and the owner said M0.1.1 stays open until it is private; branch protection on main is unverified and every change has been pushed straight to main. |
| M0.1.2 | uv project, pyproject, locked dependencies, Python version pin | keep repo | uv project with uv.lock, requires-python >=3.13,<3.14, and every build installs with uv sync --locked. |
| M0.1.3 | Ruff and mypy strict config, pre-commit hooks | reopen partial | Ruff and mypy strict are configured and enforced by CI and the pre-push hook, but there is no pre-commit hook: ops/hooks holds only commit-msg (id convention) and pre-push (lint, types). |
| M0.1.4 | Directory layout: gate, agents, connectors, knowledge, memory, console, ext, tests | keep repo | src/brain has gate, agents, connectors, knowledge, memory, console and ext packages and tests/ exists; they are the packages the served app imports. |
| M0.1.5 | Task runner: dev, test, migrate, seed, lint, invariants | reopen not wired | The task runner is a Makefile, and nothing uses it: no workflow or hook invokes a make target and make is not installed on the development machine. To prove it: A task runner that runs on the development machine (or CI calling the targets), used for dev/test/migrate/seed/lint/invariants.. |
| M0.1.6 | Mutation harness as a module that takes a throwaway worktree by construction, so it cannot break a file another session is reading | keep repo | brain.ops.mutation creates a throwaway git worktree with no directory parameter, CLAUDE.md requires it for every guard, and it is in active use (fixed today in e025867). |
| M0.2.1 | Principal model: id, kind, employment, not_after | keep install | The Principal model (id, kind, employment, not_after) is what first_administrator.py builds the appointment from, and the install holds that principal row (human, staff). |
| M0.2.2 | Scope model as jsonb predicate, evaluator, conjunction-only composition | keep install | Scopes are jsonb predicates in gate.scope (company, empty predicate), appointment ran assert_conjunctive over the 76 grants, and the Audit screen's 80 listed entries are each filtered through Scope.matches. |
| M0.2.3 | Capability string grammar, registry table, validator | keep install | The capability grammar and registry are live: gate.capability_registry holds 94 rows furnished at start and 76 grants were validated through Capability at appointment. |
| M0.2.4 | EntitlementSet type and ent_hash computation, stable and order-independent | keep install | EntitlementSet is resolved for every authenticated request and GateContext refuses unless ent_hash matches its recomputation; the install's audit entries were written with ent_hash. |
| M0.2.5 | TypedResult envelope: entity tag, id, nested arrays, Pydantic base | reopen unproven | TypedResult is produced only when a registered tool answers, and nothing on the install has ever asked a question or read a record (chat and question tables 0, /records/client 404). To prove it: Ask one question in the console that a registered row or knowledge tool answers and see the typed result come back.. |
| M0.2.6 | ToolDefinition: name, args schema, result contract, required capability, side effect, identity mode | keep install | Every start builds and freezes the tool registry from ToolDefinitions (name, args, result contract, capability, side effect, identity mode), and registration refuses a malformed one, so the install validates them on each boot. |
| M0.2.7 | Error taxonomy: denied, absent, unresolved, degraded, failed | keep install | The five outcomes are mapped by the installed exception handlers, and the install answers with the taxonomy's public sentence (Learning, Memory and Records answer 404 'I could not find that.'). |
| M0.3.1 | PostgreSQL 18 with pgvector, pg_trgm, fuzzystrmatch, unaccent | keep install | The install's database is PostgreSQL 18.6 on pgvector/pgvector:pg18 with vector, pg_trgm, fuzzystrmatch and unaccent installed. |
| M0.3.2 | Alembic setup, naming convention, migration template | keep install | The migrate job ran alembic upgrade head to 0083, env.py reads the URL from settings, the metadata naming convention shows in install object names, and migrations/script.py.mako is the template. |
| M0.3.3 | Non-superuser application role with NOBYPASSRLS | keep install | brain_app exists as a non-superuser NOBYPASSRLS login role and every application transaction switches to it; readiness checks that and the install reports ready. The connection itself logs in as the superuser brain. |
| M0.3.4 | PgBouncer transaction-mode pool for the application | reopen absent | The install runs no PgBouncer; the app connects straight to db:5432, although the repository's docker-compose.yml declares one. Needs: PgBouncer. |
| M0.3.6 | Per-profile settings: shared_buffers, work_mem, autovacuum_work_mem set explicitly | reopen partial | The install's db sets shared_buffers, work_mem and maintenance_work_mem but not autovacuum_work_mem, which only the repository compose sets; there is one set of values, not one per profile. |
| M0.3.7 | Schema namespaces: auth, gate, agent, know, mem, obs, proj, er, ops | keep install | All nine schemas (auth, gate, agent, know, mem, obs, proj, er, ops) are present on the install, and CI's schema_check asserts them from brain.db.SCHEMAS. |
| M0.4.1 | docker-compose.lite.yml | reopen not wired | docker-compose.lite.yml says lite is app, pooler, db and cache and is what runs in production, but the install runs a Coolify stored compose with no pooler and CI starts docker-compose.yml, so nothing runs this file. To prove it: Deploy an install from docker-compose.lite.yml (or make it the file Coolify deploys) and see it healthy.. |
| M0.4.3 | Documented environment example with every variable explained | keep repo | The .env.example documents each variable, and tests/unit/test_config.py (run by CI) fails when a setting is read but undocumented or documented but unread. |
| M0.4.4 | Seed command that builds the synthetic company | keep repo | python -m brain.seed loads the fictitious demo company, refuses a database holding real rows, and CI's install job runs it against an empty database. |
| M0.4.5 | Reset command that drops and rebuilds | reopen not wired | The only reset command is the Makefile's reset target (downgrade base, upgrade head, seed); make is not installed on the development machine and no workflow calls it. To prove it: A reset command that runs on the development machine and has been used to drop and rebuild a database.. |
| M0.5.1 | Lint, format, type check | keep repo | CI's static job runs ruff format --check, ruff check and mypy strict on every push and pull request. |
| M0.5.2 | Unit tests with a coverage floor | keep repo | CI runs the unit suite in four shards and the coverage job combines them and enforces fail_under = 80 from pyproject. |
| M0.5.3 | Migration forward and rollback against production-shaped data | reopen partial | CI runs upgrade, downgrade base and upgrade again, but over whatever the shard's own unit tests left in a scratch database; no production-shaped data or snapshot exists anywhere in the repository. |
| M0.5.4 | Schema sweep: row-level security on every entity table | keep repo | brain.ops.sweeps rls runs against migrated databases in CI's install and stack jobs, and every table on the install has RLS on. |
| M0.5.5 | Schema sweep: no foreign key from any grant table to connector | keep repo | brain.ops.sweeps grant_isolation runs in CI's sweeps, install and stack jobs. |
| M0.5.6 | Registry sweep: tool naming grammar | keep repo | The tool_registry and one_tool_grammar sweeps run in CI and the pre-push hook, and the running registry applies the same grammar at every start. |
| M0.5.7 | Dependency audit: licence allowlist, last-release age, archived check | reopen partial | Only the licence allowlist is applied; there is no release-age or archived check anywhere, and the sweep prints that those need network and are enforced in CI while CI runs this same function. |
| M0.5.8 | Traceability check: nothing proven without a test id | reopen partial | The sweep fails only for ids claimed in source docstrings; a leaf closed by a commit's Closes: line with no test id, which is what the tracker counts, is printed as a note and passes. |
| M0.6.1 | Synthetic company: four departments, twelve users, one contractor with expiry, one partner, one dual-department user, one mid-transfer user | keep repo | tests/fixtures/company.py builds four departments, active and expired contractors, a partner, a dual-department and a mid-transfer user, and the invariant suite in CI uses it. |
| M0.6.2 | Entitlement spread including see-record-not-money and see-neither personas | keep repo | The fixture carries the sees-record-not-money (u_weiling) and sees-neither (u_jason) personas with their grants, used by the invariant suite. |
| M0.6.3 | Canary records with improbable strings in restricted fields on every entity type | keep repo | CANARIES puts improbable tokens in restricted client, hr, ticket, invoice and agent fields and the invariant suite asserts none reaches a principal without the grant. |
| M0.6.4 | Golden questions: twenty questions across three personas with known answers | keep repo | GOLDEN holds twenty questions with expected outcomes across ten personas and the invariant suite runs them through the gate; the install itself has no golden run (Quality: Not on this install). |
| M0.6.5 | Connector cassettes with recorded responses per source system | reopen partial | Cassettes exist for Xero, Lark Base, Freshdesk, Laravel and HubSpot, but Google Drive has none (its tests are driven from documentation), and none was recorded from a live API. |
| M0.6.6 | Adversarial corpus: injection attempts in documents and tool results | keep repo | tests/fixtures/adversarial.py holds injection payloads by vector (documents and tool results) and the invariant suite runs them in CI. |
| M0.6.7 | Factory helpers for principals, scopes and grants | keep repo | The fixture module provides cap(), dept(), dept_in() and _staff() helpers for capabilities, scopes and principals, used across the suite. |

### M1 Identity, users, roles, permissions

| Task | What it asks | Verdict | Why |
|---|---|---|---|
| M1.1.1 | Realm setup script and exported realm config in the repo | reopen partial | The realm file is in the repo and the install's realm evidently came from it (amr mapper present), but the realm setup script half has never run: its own header still says THIS SCRIPT HAS NEVER BEEN RUN and the closing commit says it stays untested. To prove it: Run ops/keycloak/setup.sh against a throwaway Keycloak (or the install's) and read back the realm it wrote.. |
| M1.1.2 | OIDC login, token validation, JWKS caching | keep install | Every authenticated console request runs asking() -> authenticate() through keycloak_authority (JwksCache, validate_token); the install has signed in and served the administrator's console reads. |
| M1.1.3 | Claim mapping from subject to principal | keep install | StoredDirectory.principal_for_subject maps the token's issuer and subject to the bound principal on every authenticated request, and the install's one bound administrator has made console reads through it. |
| M1.1.4 | SAML federation for a client identity provider | reopen absent | SamlFederation is a config model with federation_gaps() and nothing in the application calls either; the realm has only a template SAML provider and the install has no client identity provider federated. Needs: a client SAML identity provider federated into the realm. To prove it: Federate a real (or test) SAML IdP into the install's realm and sign a person in through it.. |
| M1.1.5 | Group and role sync from the IdP | reopen not wired | Nothing writes auth.directory_role_grant: reconcile() is reached only from staff_sync.dry_run (a console trial), roles_held() has no caller, and the table is empty on the install. |
| M1.1.6 | Session handling, refresh, logout propagation | reopen unproven | Session recording runs on every request (3 sessions opened), but no session has ever been ended on the install, so logout propagation (ended session refusing its tokens) and refresh have not been exercised there. To prove it: End one of the administrator's sessions from Sessions (or sign out) and show a token from that session is then refused with 401 while a fresh sign-in works; leave the console open past token expiry to show a refresh.. |
| M1.1.7 | Service-account authentication for API callers | reopen not wired | The ServiceAccount model (owner delegation plus ceiling) is used only by channels/api_keys.py, which nothing the app imports; no route creates or binds a service principal, and the install has never authenticated a sid-less token. |
| M1.2.1 | principal table: id, kind, employment, not_after, disabled_at | keep install | auth.principal is on the install (migration 0002, head 0083) with RLS and its state trigger, and the appointment wrote the administrator's row into it. |
| M1.2.2 | principal_identity mapping channel identities to a principal | keep install | auth.principal_identity is on the install and holds the administrator's Keycloak binding, which StoredDirectory reads on every authenticated request. |
| M1.2.3 | Disable and re-enable with cascade to sessions | reopen not wired | The 0003 cascade trigger is on auth.principal, but no route or served code ever sets or clears disabled_at: identity.lifecycle.disable is called only from console/global_surfaces.py, which nothing imports. |
| M1.2.4 | Partner principal with zero standing entitlement | reopen not wired | standing_entitlement() returning NoStandingEntitlement for a partner is asked only by the elevation screen; the gate resolves through gate.entitlements_with_lapse, which never reads employment, and no grant route refuses a partner, so a partner with grants would hold them. |
| M1.2.5 | Break-glass session: time-boxed, separately audited, notifies | reopen partial | Elevation requests are served (time-boxed, audited by the 0062 trigger), but the served route tells the reader 'Nobody is sent a notice when somebody asks or is approved', so 'notifies' is not built; no elevation has ever been filed on the install. Needs: notification delivery (email relay or channel). To prove it: File an elevation as a second person, approve it as the administrator, see the ledger entry, see it lapse at its end, and see a notice delivered.. |
| M1.3.1 | Six roles as a compiled constant, not editable rows | keep install | Role is a compiled StrEnum with no table, and the appointment that ran on the install built a RoleGrant for Role.SUPER_ADMIN through it. |
| M1.3.2 | role_grant with scope required for dept_admin and approver | reopen partial | There is no role_grant table at f32c385, only the RoleGrant type; 399c71a says so and the served elevation route says 'this install does not record who holds a role'. |
| M1.3.3 | Deputy flag, thirty-day maximum, depth-one enforcement | reopen not wired | appoint_deputy is called only by console/scoped_authority.appoint, which no route calls, and there is no role table to hold a deputy. |
| M1.3.4 | Super Admin floor of two, refuse to revoke the last | reopen not wired | revoke_role and the floor are called only from console/global_surfaces.py (not imported) and elevation's notice list; with no stored role grants nothing on the install can count or refuse a Super Admin revocation. |
| M1.3.5 | Constraint: a role may never be the subject of a capability grant | keep install | gate.capability_grant has only principal_id (a foreign key to auth.principal) as its subject, so a role cannot be a grant subject in the install's schema, and the 76 appointment grants were written under it; the grant route also runs assert_not_a_role through PrincipalSubject. |
| M1.4.1 | capability_grant: principal, capability, scope, granted_by, reason, not_after | keep install | gate.capability_grant is on the install with RLS, policies and its two triggers, and holds the 76 grants written at appointment. |
| M1.4.2 | Revocation as deletion of the grant, never a negative grant; additive-only invariant | reopen unproven | The additive-only shape is on the install (no effect or deny column, no DELETE privilege), but revocation, POST /govern/grants/removal setting deleted_at, has never run there: all 76 audit grant entries are grants. To prove it: Grant a capability to a second person, remove it from People, and show the row gains deleted_at, a revoke ledger entry appears, and that person's next request no longer holds it.. |
| M1.4.3 | capability_pack: named bundles with scope-bound assignment | reopen partial | The pack half is on the install (gate.capability_pack holds the starter pack), but nothing in the application inserts gate.capability_pack_assignment, so a scope-bound pack assignment cannot be made there. To prove it: Assign the starter pack to a person over a scope from the console and see the expanded reach resolve.. |
| M1.4.4 | Resolver as one SQL function called by both application and trigger | keep install | Every authenticated request loads reach through gate.entitlements_with_lapse, which wraps gate.resolve_entitlements, and the grant audit trigger calls the same function; both ran for the 76 appointment grants and the administrator's console reads. |
| M1.4.5 | Valkey cache keyed on principal and grants_version, sixty-second TTL | reopen unproven | The Valkey entitlement cache is wired (VALKEY_URL set, readiness 200, resolve() reads and writes ent:<principal>:<grants_version> with a 60-second TTL on every console request), but nothing has shown a key landing: Valkey holds 0 keys and write failures are swallowed as misses. To prove it: While the administrator loads a console page, show Valkey holding ent:<principal>:<version> with a TTL of 60 seconds or less (or INFO keyspace_hits rising on a second load).. |
| M1.4.6 | grants_version bump on any write with immediate invalidation | reopen unproven | The bump trigger is on the install and gate.grants_version was written when the appointment inserted its grants, but 'immediate invalidation' has never been shown: no grant has changed while a cached reach existed. To prove it: Load a console page as a second person, change that person's grants, and show the very next request (inside 60 seconds) answers with the new reach and a new cache key.. |
| M1.4.7 | policy_epoch increment on every entitlement mutation | keep install | gate.bump_grants_version also merges gate.policy_epoch, and the install's policy_epoch row exists because the appointment's grant inserts fired it. |
| M1.4.8 | Audit row on every grant, revocation and pack assignment | reopen partial | Grant audit is shown working (76 trigger-written entries), but a revocation has never been audited on the install and a pack assignment cannot be audited at all, because nothing in the application inserts gate.capability_pack_assignment. To prove it: Remove a grant and show its revoke ledger entry; build a pack assignment writer and show its entry.. |
| M1.5.1 | team table within a department | reopen not wired | gate.team exists on the install, but nothing in the application inserts a team, so the table can never hold a row there; the Departments and teams screen shows 'There are no departments to show.' To prove it: Create a team inside a department from the console and see it listed.. |
| M1.5.2 | Team membership, and scope predicates that may reference a team | reopen broken | The membership route exists but needs an existing team, and nothing the install runs can create a department or a team, so a membership can never be written there; and team-referencing scope predicates (teams.membership_clause, team_scope) have no caller and the 0062 migration says no resolver reads a membership. |
| M1.5.3 | Team as a grant subject alongside the individual | reopen not wired | TeamSubject exists as a type, but gate.capability_grant has no column for a team and the grant route narrows every subject to a principal, so a team can never be a grant subject on the install. |
| M1.6.1 | Roster source interface producing directory assertions, read on a schedule and never at sign-in | reopen not wired | The StaffSource protocol exists, but nothing reads a roster on a schedule: the directory_sync runner has no run function (only a 'needs' sentence), and there is no worker on the install anyway. |
| M1.6.2 | Roster source and sign-in source are two axes: a spreadsheet can be the first and never the second | reopen unproven | The sign-in side is Keycloak-only and exercised, but the roster side has never run on the install (no INSTALL_STAFF_SOURCE saved), so 'a spreadsheet can be a roster and never a sign-in' has not been shown there. To prove it: Choose a spreadsheet staff source, read it, and show a person it lists is still refused sign-in until an administrator binds a Keycloak subject.. |
| M1.6.3 | Per-source trust declaring what each may assert, existence only by default | reopen not wired | trust_for and Asserts are consulted only inside dry_run and the setup trial; no process applies a roster, and the console trial gatherer (app.state.staff_trial_source) is never attached, so per-source trust decides nothing on the install. |
| M1.6.4 | Spreadsheet and Google Sheet adapters, existence only, because anybody with the link can edit one | reopen not wired | GoogleSheetSource has no caller anywhere in src; SpreadsheetSource is reached only by the setup wizard's trial, which is closed on this install, and nothing applies what either reads. |
| M1.6.5 | Google Workspace Admin, Microsoft Entra and Lark adapters, permitted to assert department and role | reopen absent | The Workspace, Entra and Lark adapters are reached only by the setup wizard's directory sign-in and trial, need a company-registered application and secret the install does not hold, and nothing applies the department or role they may assert. Needs: a registered directory application with credentials (Google Workspace Admin, Microsoft Entra or Lark) and a sync that applies assertions. To prove it: Register the Lark application, pull the staff list through the trial, and apply one department and role assertion.. |
| M1.6.6 | LDAP and Active Directory adapter for an install with a directory behind it | reopen not wired | LdapSource has no caller anywhere in src, and there is no directory behind the install. |
| M1.6.7 | Completeness declaration: an incomplete run may add and may never remove | reopen not wired | The add-but-never-remove rule is computed only inside staff_sync.dry_run, which stores nothing; no run applies a roster anywhere. |
| M1.6.8 | Work address as the join between a roster entry and the sign-in identity, and what happens when one changes | reopen not wired | brain.identity.staff_address is imported by nothing the application or migrate job loads. |
| M1.6.9 | Refuse role rules wired to a source not trusted to assert them, at configuration time not at run time | reopen not wired | assertions_from refuses untrusted role rules only when a dry run computes (run time, not configuration time), there is no configuration surface for role rules, and the console trial is never attached. |
| M1.6.10 | Warn when two sources both assert roles, because neither can see the other rows | reopen not wired | source_gaps' two-role-sources warning is called only from dry_run and from govern_surfaces.source_warnings, which nothing calls, and the install can select only one staff source (INSTALL_STAFF_SOURCE is a single setting), so the warning can never fire. |
| M1.6.11 | Console page to choose, configure and test a staff source before it runs | reopen broken | The Staff sources screen is routed and shows options, but it has no configure write, and its trial always answers 'This install cannot try your staff source yet' because app.state.staff_trial_source is never attached by the lifespan. To prove it: Choose and configure a source on the Staff sources screen and press Try; see the list it would add.. |
| M1.6.12 | Scheduled sync with a dry run showing what would be added and removed | reopen not wired | dry_run exists, but there is no scheduled sync: the directory_sync runner has no run, the console trial that would show the dry run is never attached, and the install runs no worker. |

### M2 Departments and scopes

| Task | What it asks | Verdict | Why |
|---|---|---|---|
| M2.1.1 | scope table: slug, predicate jsonb, department flag | keep install | gate.scope is on the install with RLS and policies and holds the furnished company-wide scope written at start. |
| M2.1.2 | Predicate evaluator compiling to a SQL where clause | reopen unproven | compile_where is wired into the record, document and resolution reads, but the only scope on the install is the empty company scope and no read of records or documents under a grant has ever run (no data grants, proj.record and know.* empty). To prove it: Grant read of one entity over a department scope to a second person and show /records returns only that department's rows.. |
| M2.1.3 | Conjunction-only grammar validator so a scope can never widen | reopen unproven | assert_conjunctive runs when the grant route builds a SubjectGrant, but no grant has gone through that route and nothing in the application can create a scope other than the furnished empty one, so the validator has only ever seen {} if anything. To prove it: Create a department scope and try to save or grant a widening predicate (empty IN, or a disjunction); see it refused.. |
| M2.1.4 | Scope composition for multi-scope principals | reopen not wired | core.department.compose has no caller anywhere in src; the only multi-department helper in use is membership_scope in document search, which has never run on the install. |
| M2.1.5 | CI check: no agent slug or tool object collides with a scope slug | keep repo | The slug_collisions sweep exists at f32c385 and the CI workflow runs it; note it compares slugs found in source text, not the live registry. |
| M2.2.1 | department table under company | reopen not wired | gate.department exists on the install, but nothing in the application inserts a department, so it can never hold a row there; the Departments and teams and Scopes screens say there are no departments. To prove it: Create a department from the console and see it listed with its scope.. |
| M2.2.2 | Department admin assignment with scope not null | reopen not wired | assign_department_admin has no caller; the served organisation routes appoint a department lead (gate.department_lead), which confers nothing, and no department can exist on the install to assign anybody to. |
| M2.2.3 | Department creation wizard with starter scopes | reopen not wired | create_department and starter_scopes have no caller, nothing inserts gate.department, and no console screen creates a department; 4c8ab47 claimed only the draft logic and said the screen was still open. |
| M2.2.4 | Cross-department behaviour: intersect, state the gap, offer request-access | reopen not wired | plan_cross_department (intersect, state the gap, offer request-access) has no caller anywhere in src, so no answer or screen on the install produces that behaviour. |

### M3 The gate

| Task | What it asks | Verdict | Why |
|---|---|---|---|
| M3.1.1 | GateContext carrying trace id, principal, entitlements, hash, lane, channel, traffic class | reopen not wired | GateContext is never constructed outside gate/context.py; the /answer route builds an Origin and console routes an Asking instead. |
| M3.1.2 | Ordered middleware chain with explicit step boundaries | reopen not wired | The ordered step chain (GateStep, Recorder) is never opened: open_trace has no caller, so no request runs the enforced order. |
| M3.1.3 | Recorder constructed at ingress before identify | reopen not wired | open_trace, the recorder built at ingress, has no caller anywhere in src. |
| M3.1.4 | Traffic class as a union with no default, so a new channel cannot compile without declaring it | keep repo | traffic_class_for uses assert_never over Channel, a compile-time property held by the type check CI runs. |
| M3.2.1 | Channel ingress normalisation to one internal event shape | reopen not wired | ChannelEvent normalisation lives in channel adapters, but no inbound channel route is mounted in brain.app and the install holds no channel credentials. |
| M3.2.2 | Dedupe by unique index on channel and external id | reopen partial | ChannelEvent.dedupe_key exists, but no migration creates a unique index on channel and external id, and no inbound event is ever stored. |
| M3.2.3 | Channel identity to principal resolution | reopen not wired | gate.ingress.resolve and bind are used only by channels/binding.py, which nothing imports; the console Keycloak lookup is a different path (M1.1.3). |
| M3.2.4 | Unrecognised identity path with no entitlement and a prompt to bind | reopen not wired | The Unrecognised reach and bind prompt come only from channel adapters with no mounted inbound route; an unbound console sign-in gets a 401 sentence instead. |
| M3.3.1 | Resolve entitlements from cache or database | keep install | asking calls resolve on every authenticated console request, and the administrator screens answer from the 76 grants resolved from the database; the cache half is tracked under M1.4.5. |
| M3.3.2 | Compute the entitlement hash | keep install | resolve computes ent_hash from the resolved set on every request, and asking carries it into every route. |
| M3.3.3 | Channel capability intersection | reopen unproven | admit intersects the channel ceiling on every request, but every request on the install came from the console channel, so withholding admin and approve on the API channel has never been shown. To prove it: Call /api/v1/me with a session-less service-account token for a bound principal and show admin and approve withheld.. |
| M3.3.4 | Assurance-level intersection | keep install | The assurance ceiling visibly worked on the install: a password-only session was refused admin screens (F1) and the OTP session now reads strong assurance and uses admin verbs. |
| M3.4.1 | Injection classifier over user input | reopen not wired | injection.assess is called only on automation tool-call step arguments; no question path scores user input. |
| M3.4.2 | Risk score written to the request row | reopen not wired | No table or migration has a risk score column and nothing writes one to a request row. |
| M3.4.3 | Explicit no-block guarantee, tested | reopen not wired | The no-block property holds only for a classifier that never screens user questions on the install; the screen step is not in the answer path. |
| M3.4.4 | Autonomy tier escalation on a high score | reopen absent | autonomy_ceiling tightens only on the automation tool-call and agent invocation paths, which need an automation runner and an agent the install does not have. Needs: automation runner and an installed agent. |
| M3.5.1 | Key from question, entitlement hash, agent config hash, policy epoch, source epochs | reopen not wired | key_for is reached only through an answer store nobody builds: the /answer route passes cached=None and no ValkeyAnswerStore is constructed. |
| M3.5.2 | Lookup and store paths | reopen not wired | The lookup and store paths are never called: the answer route passes cached=None and nothing stores an answer. |
| M3.5.3 | Hit surfaced to the user with its age | reopen not wired | serve_cached runs only when a cached answer is handed in, and the only route passes None, so no hit can reach a person. |
| M3.5.4 | Never-cache list for volatile question shapes | reopen not wired | The never-cache check sits inside key_for, which no running path reaches. |
| M3.6.1 | Lane classifier from features computable without a model | reopen not wired | classify_lane has no caller in src; only is_a_name_not_a_phrase is used by the fast lane. |
| M3.6.2 | Agent selection: channel binding, then rules, then cheap classifier | reopen not wired | select_agent has no caller in src and the install has no agent. |
| M3.6.3 | Routing decision written to the row, never inferred afterwards | reopen not wired | No lane decision or agent selection is ever produced, so none is written; request_telemetry has never received a row. |
| M3.7.1 | Project tools as agent grants intersected with entitlements | reopen not wired | catalogue.project is reached only from agent invocation, automation pieces, canaries and skills; the answer lane gives the model an empty catalogue and the install has no agent. |
| M3.7.2 | Static tool prefix with deferred loading so the provider cache is shared | reopen unproven | build_prefix is used by the model lane and the Prompts screen, but no model has ever been called on the install, so provider prefix caching has never happened. To prove it: Ask two questions that reach the model and show cached prefix tokens reported on the second.. |
| M3.7.3 | Assert projection happens in our dispatcher, never delegated to a connector | reopen not wired | ProjectedCatalogue dispatch exists only on agent and automation paths the install never runs. |
| M3.7.4 | Empty-catalogue error when a manifest declared required tools and none resolve | reopen not wired | EmptyCatalogueError is raised only by invoke and project on agent paths the install never runs. |
| M3.8.1 | Agent loop invocation with the projected catalogue | reopen not wired | invoke builds an Invocation for agent install and the reach view, but nothing runs an agent loop against a model with that catalogue. |
| M3.8.2 | Leash lookup for agent, target and scope | reopen not wired | Leash decide and govern are called only from builder/rehearse.py, which the app does not import, and the automation route passes NO_LEASH. |
| M3.8.3 | Shadow: simulate, record, return marked as simulated | reopen not wired | run_shadow has no caller on any running path. |
| M3.8.4 | Assisted: render artefact, suspend, await approval, resume | reopen not wired | suspend has no caller, so nothing produces a suspension; the Approvals screen says nothing is waiting. |
| M3.8.5 | Autonomous: proceed | reopen not wired | The autonomous route is decided only inside leash.govern, which no running path calls. |
| M3.8.6 | Three checks per call: capability, rung, mask | reopen not wired | The capability, rung and mask checks run only inside leash.decide and govern, which no running path calls. |
| M3.9.1 | Invoke the redaction walker | reopen partial | The answer lane serialises through the channel serialiser rather than the redaction walker with its trace, and its own docstring calls that a real gap; it has never run on the install. To prove it: Ask a question on /ask as a person holding a data grant and show the answer, its trace and its recorded row.. |
| M3.9.2 | Emit post-redaction payload to the trace sink only | reopen partial | The only trace sink is CountingTraceSink, which records that a trace happened and drops the payload, and no question has ever produced one. Needs: a trace store for post-redaction payloads. |
| M3.9.3 | Compose the answer with citations and freshness | reopen unproven | compose is on the /answer path, but no question has ever been answered on the install; Ask says nothing has been asked yet. To prove it: Ask a question on /ask as a person holding a data grant and show the answer, its trace and its recorded row.. |
| M3.9.4 | Attach the trace reference for explainability | reopen unproven | The trace reference is attached by compose on the /answer path, which has never run on the install. To prove it: Ask a question on /ask as a person holding a data grant and show the answer, its trace and its recorded row.. |

### M4 Redaction and data protection

| Task | What it asks | Verdict | Why |
|---|---|---|---|
| M4.1.1 | Recursive depth-first walk over the typed result | reopen unproven | The walker in core/redaction is on the records and answer paths, but it has never walked a real result on the install. To prove it: Classify a client field as restricted, grant a second person read of client without that field, and show /records/client returns the row with the field locked.. |
| M4.1.2 | Mask computation from the field policy | reopen unproven | Mask computation has never run over real rows on the install. To prove it: Classify a client field as restricted, grant a second person read of client without that field, and show /records/client returns the row with the field locked.. |
| M4.1.3 | Delete keys outside the mask at any nesting depth | reopen unproven | Deleting keys outside the mask at depth has never happened on real data on the install. To prove it: Classify a client field as restricted, grant a second person read of client without that field, and show /records/client returns the row with the field locked.. |
| M4.1.4 | Drop and log objects lacking an entity tag, failing closed | reopen unproven | Dropping untagged objects has never happened on the install. To prove it: Classify a client field as restricted, grant a second person read of client without that field, and show /records/client returns the row with the field locked.. |
| M4.1.5 | Handle arrays, nested entities and mixed shapes | reopen unproven | Arrays, nested entities and mixed shapes have never been redacted on the install. To prove it: Classify a client field as restricted, grant a second person read of client without that field, and show /records/client returns the row with the field locked.. |
| M4.1.6 | Opaque escape hatch requiring its own capability, flagging the trace and labelling the answer | reopen unproven | The opaque escape hatch has never been used on the install and no one holds its capability. To prove it: Grant the opaque capability, read an opaque result, and show the trace flag and the answer label.. |
| M4.2.1 | field_policy: entity type, field, required capability, classification | reopen not wired | gate.field_policy exists on the install but is empty, and classification is read from code constants rather than the table. |
| M4.2.2 | Default-deny for unclassified fields | reopen unproven | Default-deny for unclassified fields has never been applied to real data on the install. To prove it: Classify a client field as restricted, grant a second person read of client without that field, and show /records/client returns the row with the field locked.. |
| M4.2.3 | Simulate mode logging what would be withheld | reopen unproven | Simulate mode has never logged a would-be withholding on the install. To prove it: Run a simulate-mode policy change over real rows and show the log of what would be withheld.. |
| M4.2.4 | Policy epoch bump on any field policy change | reopen not wired | Nothing in the application writes gate.field_policy, so no field policy change can bump the epoch on the install. |
| M4.2.5 | Count fields declared against the collection they count, and withheld when it is filtered | reopen unproven | Count fields have never been declared or withheld over real data on the install. To prove it: Classify a client field as restricted, grant a second person read of client without that field, and show /records/client returns the row with the field locked.. |
| M4.3.1 | Structural lock rendering, identical for every viewer | reopen unproven | No locked field has ever been rendered on the install. To prove it: Classify a client field as restricted, grant a second person read of client without that field, and show /records/client returns the row with the field locked.. |
| M4.3.2 | Never emit a count of hidden items | reopen unproven | No hidden item has ever existed to not count on the install. To prove it: Classify a client field as restricted, grant a second person read of client without that field, and show /records/client returns the row with the field locked.. |
| M4.3.3 | Permission-denied indistinguishable from nothing-found at record level | keep install | The install answers a refused read with the same 404 sentence as a missing record: Records for client, Learning and Memory all answer I could not find that. to the administrator. |
| M4.3.4 | Request-access route where the owner sees the question and the asker learns nothing | reopen not wired | brain.core.access_route is not imported by the application, so no request-access route is served. |
| M4.4.1 | Authorization-aware serializer as the only path to a channel | reopen not wired | brain.core.serialize is not imported by the application, so it is not the path to any channel on the install. |
| M4.4.2 | Structural prevention of returning an untyped shape from a tool | reopen unproven | The tool registry is built and frozen at every start, but no tool has ever returned a result on the install. To prove it: Call a row tool through /records and show a typed result served.. |
| M4.4.3 | Property-based tests over nested shapes | keep repo | Property-based tests over nested shapes exist and run in the CI unit suite. |
| M4.4.4 | Redaction counts and field names recorded, values never | reopen partial | The answer lane records no redaction counts or field names (its docstring says the trace carries no redactions), and nothing has ever been redacted on the install. To prove it: Classify a client field as restricted, grant a second person read of client without that field, and show /records/client returns the row with the field locked.. |

### M5 Models, routing, fallback matrix

| Task | What it asks | Verdict | Why |
|---|---|---|---|
| M5.1.1 | LiteLLM SDK integration as a driver only, never as a proxy | reopen partial | LiteLLM is an optional extra never installed (c5913e9 still not installed); the running transport is httpx in brain.models.wire and no model has ever been called. To prove it: Reword leaf to the httpx driver, then press Check for anthropic on Models and health and see an ops.model_attempt row. |
| M5.1.2 | Provider credentials in OpenBao | reopen absent | Provider keys are meant to come from OpenBao; the install runs no vault and the key is a plain env var. Needs: OpenBao secrets vault. |
| M5.1.3 | Per-provider client with timeout and retry overrides per lane | reopen not wired | driver.ProviderClient per-lane overrides has no caller; calls.py says overrides have nowhere to be edited and requests use the rung timeout only. |
| M5.1.4 | A separate Router instance per pool, since tag filtering is inoperative in SDK mode | reopen not wired | PoolRouter is reached only via litellm_transport (LiteLLM not installed) and PoolDispatcher has no caller; the live executor selects rungs with RoutingChain.select. |
| M5.2.1 | Deterministic classifier over tool count, context estimate, lane, explicit request, residency touch | reopen absent | classify_tier runs only in model_lane after a document search returns passages; know.* is 0 and only the worker connector sync writes documents; only lane and context size are ever passed. Needs: worker connector sync (documents). |
| M5.2.2 | routing_tier with a jsonb rule set | reopen not wired | ops.routing_tier exists with 0 rows and nothing in src reads it. |
| M5.2.3 | Test asserting no model call occurs in classification | keep repo | Test exists and the sharded unit CI job runs it. |
| M5.3.1 | routing_rung: tier, scope, position, role, deployment, attempts, timeout, concurrency | keep install | ops.routing_rung has 2 real wizard-written rows with every named column, shown on the Routing screen. |
| M5.3.2 | Role label derived by trigger rather than typed | reopen partial | No trigger derives role: the only trigger on ops.routing_rung is the audit trigger and the wizard types role; bebeda6 says M5.3.2 is unbuilt. |
| M5.3.3 | Console editor for the matrix | reopen unproven | Routing screen shows the 2 rungs with an editor but no edit was ever saved (audit routing 2 = wizard inserts). To prove it: Edit one rung timeout on Routing, reload, see it kept and a third routing audit entry. |
| M5.3.4 | One attempt row per try, so the executed chain reconstructs from a join | reopen unproven | Attempt rows are written by the live executor but no model has ever been called. To prove it: Press Check for anthropic on Models and health and see an ops.model_attempt row. |
| M5.4.1 | Closed trigger set for fallback | reopen unproven | trigger_for is in the live executor but no call has ever been made or failed. To prove it: Provider check against a failing rung, attempt rows show stop or move-on per trigger. |
| M5.4.2 | Explicit rejection of quality-based fallback | reopen unproven | No content-based fallback path exists in the executor, but it has never run on the install. To prove it: Provider check that answers leaves exactly one attempt row. |
| M5.4.3 | provider_health with live and probe ring buffers | reopen partial | No provider_health table; health is replayed from 0 attempt rows and the probe ring is never fed. |
| M5.4.4 | Open on three consecutive live failures, ratio above half, or two probe failures with no recent live evidence | reopen partial | The probe-failure condition can never fire (no prober anywhere) and the live conditions have no attempts to replay. |
| M5.4.5 | Cooldown with exponential backoff and jitter | reopen partial | Jitter is zero when the breaker is replayed from attempts, and cooldown was never exercised. |
| M5.4.6 | Half-open single-request admission via claim and return | reopen not wired | Executor calls breaker.admits, not claim-and-return try_admit; try_admit is used only by the inference client, and no inference server exists. |
| M5.4.7 | Background prober on a sixty-second interval | reopen not wired | No prober runs anywhere; schedule_runner lists model_health_probes as still needing an endpoint. |
| M5.4.8 | Alert on chain depth rather than final failure | reopen not wired | No producer of ChainOutcome or depth alert outside models/health.py. |
| M5.5.1 | Residency constraint attached to scope | reopen not wired | No scope residency reaches routing; model_lane calls complete without residency so every call is UNCONSTRAINED. |
| M5.5.2 | Chain skips non-compliant rungs rather than degrading silently | reopen not wired | The residency skip only runs when a requirement is passed and no caller passes one. |
| M5.5.3 | Provider residency registry with documented storage location | reopen partial | REGION_STORAGE constant exists but there is no deployment registry; every rung is assembled GLOBAL. |
| M5.5.4 | Refusal path when no compliant rung exists | reopen not wired | Residency refusal can only fire with a residency requirement, which nothing passes. |

### M6 Speed: fast lane, cache, streaming

| Task | What it asks | Verdict | Why |
|---|---|---|---|
| M6.1.1 | Fast-path rules as data rows, not code | reopen not wired | gate.fast_path_rule has 0 rows and nothing on the install writes a rule (only demo.py seeds it); rules load only at restart. |
| M6.1.2 | Rule matcher over question shape and entity type | reopen not wired | The matcher runs on /answer but there are no rules and no way to create one, and no question was ever asked. |
| M6.1.3 | Database grants restricting the fast lane to projected tables only | reopen not wired | brain_fastlane exists but nothing assumes it; every transaction runs as brain_app. |
| M6.1.4 | Empty tool catalogue for the fast lane, enforced structurally | reopen not wired | The fast lane can never answer on the install (no rules, no rule writer, no records), so its empty catalogue is never exercised. |
| M6.1.5 | Zero-network test with egress blocked | keep repo | Zero-network invariant test exists and the CI invariants step runs it. |
| M6.1.6 | Share measurement excluding machine traffic | reopen not wired | Nothing emits a LaneObservation; ops/lane_share.py has no importer. |
| M6.2.1 | Answer cache keyed on entitlement hash and epochs | reopen not wired | /answer passes cached=None and nothing stores an answer; valkey holds 0 keys. |
| M6.2.2 | Plan cache storing tool ids only, filtered through projection at execution | reopen not wired | plan_cache has no caller and no planner exists. |
| M6.2.3 | Retrieval cache with scope in the key | reopen not wired | retrieval_cache has no caller. |
| M6.2.4 | Embedding cache keyed on content hash | reopen not wired | embedding_cache has no caller and the install has no embedding server. |
| M6.2.5 | Projection freshness cache | reopen not wired | freshness_cache has no caller. |
| M6.2.6 | Explicit prohibition and test: no semantic answer caching | keep repo | Prohibition constant and similarity checks exist and run in CI invariants and unit shards. |
| M6.3.1 | Server-sent events for the answer lane | reopen unproven | /answer returns text/event-stream and the Ask screen calls it, but nothing has ever been asked. To prove it: Ask one question on Ask and see the frames render. |
| M6.3.2 | Named progress steps in user language at tool-input-start | reopen partial | Progress steps are computed with the whole answer and written afterwards (frames_of iterates a finished tuple), so nothing arrives at tool-input-start; never asked either. |
| M6.3.3 | Step labels scrubbed of scope-revealing detail | reopen unproven | Closed step-label vocabulary is on the live answer path but no question was ever asked. To prove it: Ask one question on Ask and read the step labels. |
| M6.3.4 | Citations streamed before prose | reopen absent | Citations exist only when the lane answers, and nothing on the install can answer (no rules, 0 records, 0 documents, all brought only by the worker); frames are also not streamed incrementally. Needs: worker connector sync (records and documents). |
| M6.3.5 | Reasoning never streamed to consumers | reopen absent | The model lane never calls a model on the install without documents, so there is no reasoning to withhold. Needs: worker connector sync (documents). |
| M6.3.6 | Cache hits shown as instant with the reason | reopen not wired | /answer passes cached=None, so no cache hit can ever be shown. |
| M6.4.1 | Static tool prefix ordering for provider cache sharing | reopen not wired | The only live prefix is built with zero tools and no provider cache breakpoint is sent. |
| M6.4.2 | Prompt caching structure with the stable prefix first | reopen partial | Prefix-first layout exists but no cache_control is requested from the provider, and the model lane never reaches a model on the install. |
| M6.4.3 | Output-length instruction separate from effort setting | reopen partial | Output length is set, but the effort setting never reaches a request (DriverRequest carries no extra), and the model lane never calls a model here. |
| M6.4.4 | Per-lane effort pinning to avoid cache invalidation | reopen not wired | effort_for and as_extra have no caller outside effort.py. |

### M7 Knowledge, assets, document parsing

| Task | What it asks | Verdict | Why |
|---|---|---|---|
| M7.1.1 | Upload endpoint with size and type limits | reopen not wired | The upload door exists but no HTTP route calls it; c98bdf9 says 'there is no HTTP route'. Browser pass: 0 knowledge items, documents cannot be brought in yet. |
| M7.1.2 | Extract-from-link ingestion | reopen not wired | receive_link has no production caller and no route. |
| M7.1.3 | Virus and content scanning before parse | reopen partial | Only the ordering seam exists (Scanner Protocol); no scanner implementation anywhere and no caller (491d65f: no scanner is invoked). Needs: virus/content scanner and a caller. |
| M7.1.4 | Original storage in the object store with lifecycle policy | reopen absent | store_original has no caller and the install has no object store; Storage screen connects to nothing. Needs: object store. |
| M7.1.5 | Ingestion queue with backpressure | reopen not wired | admit_ingestion/queue_limits_for have no production caller; no ingestion job or route. |
| M7.2.1 | Docling integration for layout-aware extraction | reopen partial | Layout parser is a seam only; no Docling client or server, nothing calls it. Needs: Docling on an inference/parse server. |
| M7.2.2 | Table extraction preserved through chunking | reopen not wired | Tables only come from a layout parser that does not exist; the only chunk writer (worker connector sync) cuts plain text blocks. know.chunk 0. |
| M7.2.3 | Tika fallback for unsupported formats | reopen partial | No Tika client exists; parse_paths says 'Nothing calls anything in this module'. Needs: Tika fallback extractor. |
| M7.2.4 | OCR path for scanned documents | reopen partial | OcrEngine is a Protocol with no implementation; no OCR engine on the install. Needs: OCR engine. |
| M7.2.5 | Parse failure handling with a visible error naming the cause | reopen not wired | Cause vocabulary exists but no parser or route shows anyone a parse failure (491d65f: no parser calls it). |
| M7.2.6 | Memory-bounded parse worker with a hard container limit | reopen absent | The bound belongs to the parse-worker container overlay, which is not deployed. Needs: parse worker. |
| M7.3.1 | Chunking strategy with overlap and size bounds | reopen absent | Chunking runs only via chunk_store.ingest_document from the worker's connector sync; no worker, no connection, know.chunk 0. Needs: worker plus a connected source. |
| M7.3.2 | Chunk metadata carrying document, scope, owner and position | reopen absent | know.chunk has the metadata columns, but nothing on the install writes a chunk (0 rows); only the worker's sync does. Needs: worker plus a connected source. |
| M7.3.4 | Batch embedding with a queue | reopen absent | run_embed_job runs only in brain.ops.worker and there is no embedding server. Needs: worker and embedding server. |
| M7.3.5 | Re-embedding on model change with a rebuild command | reopen partial | The rebuild command plans but 'does not execute one'; ede4c9c said not claimed and 4c8ab47 closed it with no code. Needs: embedding server and an executing rebuild. |
| M7.4.1 | knowledge table: content, scope, owner, verified_by, verified_at, review_by, state | reopen absent | know.item exists (0040; content held in know.chunk.body) but has 0 rows and its only writer is the worker's connector sync. Needs: worker plus a connected source, or an upload route. |
| M7.4.2 | Company, department and personal visibility levels | reopen absent | Levels are CHECK constraints, but no item exists or can be written and there are no departments. Needs: a knowledge writer. |
| M7.4.3 | Publishing flow: uploading never widens scope | reopen not wired | admit_upload/default_for_upload have no production caller; no upload or publish route. |
| M7.4.4 | Promotion to company scope requires the gated path | reopen not wired | propose_promotion is called only by member_library.py (no importer); approval_sections has no caller. |
| M7.4.5 | Supersession: a newer version marks the older superseded | reopen not wired | supersede is called only from member_library.py, which nothing imports. |
| M7.4.6 | Scheduled job opening re-verification tasks from review_by | reopen absent | The re-verification runner runs only on the worker schedule; browser pass shows no job ever ran. Needs: worker/scheduler. |
| M7.4.7 | Verification badge surfaced in the answer | reopen not wired | provenance_for, the only caller of badge_for, has no caller and the answer path carries no badge; 34d02a1 said not claimed, 4c8ab47 closed it with no code. |
| M7.5.1 | Column classification for tabular knowledge such as price lists | reopen absent | Classifications are code constants shown on the Classification screen, but no tabular rows exist or can be produced (proj.record written only by the worker); Records 404. Needs: worker plus a tabular source. |
| M7.5.2 | Sell price visible while cost and margin are restricted | reopen not wired | No connector or job produces price_list rows, and no question has been asked. |
| M7.5.3 | Classification editor in the console | reopen partial | Screen is routed and readable, but edit is review-only: 'Nothing here writes anything, and there is nowhere for it to write'. Needs: stored classification with write and audit. |

### M8 Provenance, abstention, escalation

| Task | What it asks | Verdict | Why |
|---|---|---|---|
| M8.1.1 | Row citations naming record and field with read time | reopen absent | Row citations appear only on a fast-lane answer over real records; the install has no rules and 0 records. Needs: worker connector sync (records). |
| M8.1.2 | Document citations deep-linking to the passage | reopen not wired | provenance.DocumentCitation and Anchor have no caller on the answer path. |
| M8.1.3 | Freshness and staleness state on every citation | reopen not wired | Answer citations show only as-of fetched_at; the staleness state in provenance is not used on the answer path. |
| M8.1.4 | Citation assembled from the tool trace, never from the model | reopen absent | Citations from the redacted payload are in model_lane, but no model is ever asked without documents; assert_derived has no caller. Needs: worker connector sync (documents). |
| M8.2.1 | Four distinct states: nothing retrieved, retrieved but not answering, nothing connected, not entitled | reopen unproven | The abstention states are on the live /answer path but nothing was ever asked; two of the four need records or documents the install cannot produce. To prove it: Ask one question on Ask and see the nothing-connected or nothing-retrieved abstention. |
| M8.2.2 | Scope statement naming what was searched | reopen unproven | Scope statement is built from reach on the live path but no question was ever asked. To prove it: Ask one question on Ask and read the scope statement. |
| M8.2.3 | Not-entitled path indistinguishable from nothing-found | reopen absent | not_entitled arises only on a locked field of a real record, and the install has 0 records. Needs: worker connector sync (records). |
| M8.2.4 | Refusal when no citation exists, configurable per agent | reopen not wired | abstain_if_uncited and CitationPolicy have no caller, and there are no agents. |
| M8.3.1 | Escalation as an authored step in a skill, not a model judgement | reopen not wired | raise_escalation has no caller. |
| M8.3.2 | Handoff payload: who asked, what, what was tried, what is needed | reopen not wired | Handoff is never built outside tests. |
| M8.3.3 | Delivery into the approver's own channel | reopen not wired | Closing commit b271754 only added a test; nothing delivers an escalation and the install has no channel credentials. |
| M8.3.4 | Timeout and expiry behaviour on every escalation | reopen not wired | No escalation is ever raised, so no timeout or expiry runs. |
| M8.3.5 | Takeover signal fed to the circuit breaker | reopen not wired | AutonomyBreaker and TakeoverSignal have no caller, and by design they are not the provider breaker the leaf names. |

### M9 Chat and conversation

| Task | What it asks | Verdict | Why |
|---|---|---|---|
| M9.1.1 | conversation and message tables with scope and principal | reopen partial | chat.conversation has principal_id but no scope column, and nothing in src ever inserts a conversation or message (0 rows). |
| M9.1.2 | Thread continuity across channels | reopen not wired | No store queries chat.conversation and no turn is written; no channel credentials exist. |
| M9.1.3 | Conversation search restricted to the asker | reopen not wired | No route searches conversations; only the owner RLS policy exists over 0 rows. |
| M9.1.4 | Export of a single conversation | reopen not wired | Import and export says a person own conversations are not available; nothing writes a conversation to export. |
| M9.2.1 | Answer assembly from typed results | reopen absent | chat.turns.assemble has no caller; the live composition in the answer lane needs records or documents the install cannot produce. Needs: worker connector sync (records and documents). |
| M9.2.2 | Locked-field rendering | reopen absent | Locks render only on a real record with a restricted field; proj.record is 0 and Records answers 404 for client. Needs: worker connector sync (records). |
| M9.2.3 | Follow-up handling with retained context | reopen not wired | context_for has no caller and no turn is ever stored. |
| M9.2.4 | Correction handling that feeds the learning signal | reopen not wired | record_correction has no caller; My workspace says nothing records when you said an answer was wrong. |

### M10 Channels

| Task | What it asks | Verdict | Why |
|---|---|---|---|
| M10.1.1 | Channel adapter interface with normalise, send, capabilities | reopen not wired | normalise and send are never called; adapters are only instantiated for capabilities(). |
| M10.1.2 | Capability declaration per channel: ephemeral, cards, streaming, attachments | reopen not wired | Declarations are read only to list channels per agent (0 agents); no channel can carry anything on the install. |
| M10.1.3 | Per-channel sensitivity policy: which classes may be carried | reopen not wired | may_carry is reached only via offered_channels per agent (0 agents) and unused send paths. |
| M10.1.4 | Registration and health check per adapter | reopen not wired | registered() and healthy() have no production caller. |
| M10.1.5 | An adapter must render a payload label or refuse to send, so the opaque escape hatch cannot lose its warning | reopen not wired | Label-or-refuse sits in send paths nothing calls. |
| M10.2.1 | Webhook receipt and signature verification | reopen not wired | verified_handler is mounted on no route; only sign() is reused by the worker outbox. No Lark credentials. Needs: Lark credentials and inbound route. |
| M10.2.2 | Message and mention handling | reopen not wired | Lark handling lives in channels/lark.py and cards.py, which no route reaches, and the install has no Lark bot credentials. Needs: Lark bot credentials and inbound route. |
| M10.2.3 | Interactive cards for approvals | reopen not wired | Lark handling lives in channels/lark.py and cards.py, which no route reaches, and the install has no Lark bot credentials. Needs: Lark bot credentials and inbound route. |
| M10.2.4 | Card patching within the rate limit, with a text fallback | reopen not wired | Lark handling lives in channels/lark.py and cards.py, which no route reaches, and the install has no Lark bot credentials. Needs: Lark bot credentials and inbound route. |
| M10.2.5 | Ephemeral messages for per-viewer bodies | reopen not wired | Lark handling lives in channels/lark.py and cards.py, which no route reaches, and the install has no Lark bot credentials. Needs: Lark bot credentials and inbound route. |
| M10.2.6 | Group and direct-message handling | reopen not wired | Lark handling lives in channels/lark.py and cards.py, which no route reaches, and the install has no Lark bot credentials. Needs: Lark bot credentials and inbound route. |
| M10.3.1 | Outward-only binding: nonce minted in an authenticated web session | reopen not wired | binding.py is imported by nothing the app runs; no route mints a binding code. |
| M10.3.2 | Binding expiry and single use | reopen not wired | Expiry and single use live in binding.py with no route. |
| M10.3.3 | Unbound identity behaviour | reopen not wired | The unrecognised-sender prompt rule is used only by unreachable channel adapters. |
| M10.3.4 | Unbinding and rebinding on device change | reopen not wired | Unbinding lives in binding.py; member/connections.py has no importer. |
| M10.4.1 | Room envelope computed at the floor of everyone present | reopen not wired | channels/room.py is imported by nothing the install runs and no group-chat channel exists. |
| M10.4.2 | Per-viewer body via the channel's ephemeral mechanism | reopen not wired | channels/room.py is imported by nothing the install runs and no group-chat channel exists. |
| M10.4.3 | Degradation ladder ending at a web link where the gate re-runs | reopen not wired | channels/room.py is imported by nothing the install runs and no group-chat channel exists. |
| M10.4.4 | Room membership change invalidating a pending render | reopen not wired | channels/room.py is imported by nothing the install runs and no group-chat channel exists. |
| M10.4.5 | Async delivery re-running entitlement at send time | reopen not wired | channels/room.py is imported by nothing the install runs and no group-chat channel exists. |
| M10.5.1 | Slack adapter | reopen not wired | The Slack adapter is used only for its capabilities() declaration; no route or send path uses it and no credentials exist on the install. Needs: Slack credentials and a route. |
| M10.5.2 | Microsoft Teams adapter | reopen not wired | The Teams adapter is used only for its capabilities() declaration; no route or send path uses it and no credentials exist on the install. Needs: Teams credentials and a route. |
| M10.5.3 | WhatsApp adapter with template and session-window handling | reopen not wired | The WhatsApp adapter is used only for its capabilities() declaration; no route or send path uses it and no credentials exist on the install. Needs: WhatsApp credentials and a route. |
| M10.5.4 | Telegram adapter | reopen not wired | The Telegram adapter is used only for its capabilities() declaration; no route or send path uses it and no credentials exist on the install. Needs: Telegram credentials and a route. |
| M10.5.5 | Web widget with session minting and abuse guard | reopen not wired | Only normalise_origin is reused; widget session minting has no route and no widget is served. |
| M10.5.6 | Email adapter, inbound and outbound | reopen not wired | The Email adapter is used only for its capabilities() declaration; no inbound or send path uses it and no mail relay is configured. Needs: mail relay and a route. |
| M10.5.7 | REST API with key management | reopen partial | /api/v1 exists but channels/api_keys.py has no importer; no route issues, accepts or revokes a key (ad03af9). Needs: API key routes. |

### M11 Connectors, federation, projection

| Task | What it asks | Verdict | Why |
|---|---|---|---|
| M11.1.1 | Connector interface: capabilities, tools, health, credential binding | reopen absent | The contract is exercised only by a connected source read by the worker; none exists. Needs: vault, connector credentials, worker. |
| M11.1.2 | MCP transport adapter | reopen partial | No MCP client exists (transports.py: no client here and no socket); 4c8ab47 closed it with no code. Needs: MCP client. |
| M11.1.3 | REST and OpenAPI transport adapter driven by a spec plus field mapping | reopen absent | RestOperation is used only by the worker connector sync. Needs: vault, credentials, worker. |
| M11.1.4 | Database transport adapter restricted to allowlisted read-only views | reopen not wired | The DB transport is used only by laravel.py, which nothing imports. |
| M11.1.5 | Custom code transport adapter running sandboxed | reopen partial | THE_SANDBOX_IS_NOT_IN_THIS_MODULE; no sandbox runtime exists; closed by 4c8ab47 with no code. Needs: custom-code sandbox. |
| M11.1.6 | Connector registration, enable, disable, upgrade | reopen absent | Connect refuses without a vault and ConnectorRegistry (enable/disable/upgrade) is never instantiated. Needs: vault; upgrade not wired. |
| M11.1.7 | Manifest hash pinning so a silent redefinition fails closed on reconnect | reopen absent | Digest is pinned on ops.connector_connection but no connection exists; the reconnect check is unwired or worker-only. Needs: vault, connection, worker. |
| M11.2.1 | Credential reference model, never a value in configuration | reopen absent | The SecretRef path runs only after the vault check at connect; ops.credential_write is 0. Needs: secrets vault and connector credentials. |
| M11.2.2 | OpenBao storage with per-run leases | reopen absent | OpenBao leases need a vault; the install logs no secrets vault configured. Needs: OpenBao. |
| M11.2.3 | Scope-at-connect: one folder or one pipeline rather than everything | reopen absent | Scope settings are taken at connect, which cannot complete without a vault. Needs: secrets vault and connector credentials. |
| M11.2.4 | Read-only by default with write as a separate deliberate grant | reopen absent | No connection exists, so no read or write grant has applied to a connector call. Needs: secrets vault and connector credentials. |
| M11.2.5 | Identity mode per tool: requester credentials by default, service credentials declared | reopen absent | No connector runs, and the shipped tools all declare IdentityMode.SERVICE. Needs: secrets vault and connector credentials. |
| M11.2.6 | Rotation without redeploy | reopen absent | Rotation needs the vault, and there is no key replacement route. Needs: secrets vault and connector credentials. |
| M11.3.1 | Per-connector token bucket | reopen not wired | brain.ops.token_bucket has no production importer. |
| M11.3.2 | Circuit breaker per connector | reopen not wired | connector_breaker is used only by the inference client, not by connector calls. |
| M11.3.3 | Retry policy with backoff and jitter | reopen absent | retry_delay is used only by the worker sync and outbox. Needs: worker. |
| M11.3.4 | Metrics: requests per second and minute, concurrency, 429 rate, latency, error rate | reopen not wired | ConnectorMetrics and measure have no caller; the Rate limits screen cannot read throttling. |
| M11.3.5 | Configured ceilings per source, matching documented limits | reopen absent | Ceilings are listed on screen but applied only by the worker sync, which never ran. Needs: worker and a connected source. |
| M11.4.1 | proj.record table with source, entity type, identifiers and hot fields | reopen absent | proj.record exists (0008) but has 0 rows, only the worker writes it, and Records answers 404. Needs: worker and a connected source. |
| M11.4.2 | Twelve-field cap per entity kind enforced by the manifest validator | reopen absent | The manifest validator runs only at connect (blocked by the missing vault); the DB cap guards an empty table. Needs: vault, connector credentials, worker. |
| M11.4.3 | Five-clause projectability test enforced at manifest review | reopen absent | Manifest review happens only at connect, which cannot complete. Needs: vault, connector credentials, worker. |
| M11.4.4 | PII deny-list blocking email, phone, address, identity numbers, bank and salary | reopen absent | is_forbidden applies at connect and in the worker outbox; neither runs. Needs: vault, connector credentials, worker. |
| M11.4.5 | Visibility predicate stored, never a resolved ACL | reopen absent | The predicate lives in manifests built at connect; nothing has been projected. Needs: vault, connector credentials, worker. |
| M11.4.6 | Change-signal subscription: webhook, CDC or updated-since cursor | reopen not wired | connectors/change_signal.py has no production importer. |
| M11.4.7 | No change signal means no projected fields, enforced | reopen absent | Enforced during manifest validation at connect, which cannot complete. Needs: vault, connector credentials, worker. |
| M11.4.8 | Backfill job with rate-limit awareness | reopen not wired | connectors/backfill.py has no production importer. |
| M11.4.9 | Staleness surfaced in the answer | reopen not wired | Nothing in the answer path states staleness. |
| M11.5.1 | Live fetch path with an eight-hundred-millisecond timeout | reopen not wired | No request-path code performs a federated fetch; federation is used only by ops/scaling (no importer) and connector modules nothing reaches. |
| M11.5.2 | Parallel fan-out across independent sources with one critical path | reopen not wired | No request-path code performs a federated fetch; federation is used only by ops/scaling (no importer) and connector modules nothing reaches. |
| M11.5.3 | Global and per-source call budgets | reopen not wired | No request-path code performs a federated fetch; federation is used only by ops/scaling (no importer) and connector modules nothing reaches. |
| M11.5.4 | Thundering-herd protection on cache miss | reopen not wired | No request-path code performs a federated fetch; federation is used only by ops/scaling (no importer) and connector modules nothing reaches. |
| M11.5.5 | Graceful degradation naming what could not be fetched | reopen not wired | No request-path code performs a federated fetch; federation is used only by ops/scaling (no importer) and connector modules nothing reaches. |
| M11.6.1 | Laravel MySQL via read-only views | reopen not wired | connectors/laravel.py is imported by nothing and is not connectable. Needs: Laravel credentials. |
| M11.6.2 | Freshdesk | reopen not wired | connectors/freshdesk.py is imported by nothing and is not connectable; built against recordings. Needs: Freshdesk credentials. |
| M11.6.3 | Lark Base | reopen not wired | connectors/lark_base.py is imported by nothing and is not connectable. Needs: Lark credentials. |
| M11.6.4 | Lark Wiki | reopen not wired | connectors/lark_wiki.py is imported by nothing and is not connectable. Needs: Lark credentials. |
| M11.6.5 | Xero | reopen absent | Xero is connectable in the route but needs the vault, a key and the worker; built against recordings and never read. Needs: vault, Xero credentials, worker. |
| M11.6.6 | HubSpot | reopen absent | HubSpot is connectable in the route but needs the vault, a key and the worker; never read. Needs: vault, HubSpot credentials, worker. |
| M11.6.7 | Google Drive or Microsoft 365 | reopen not wired | connectors/google_drive.py is imported by nothing and is not connectable; no Microsoft 365 connector exists. Needs: Google Drive credentials. |

### M12 Tools, skills, capability registry

| Task | What it asks | Verdict | Why |
|---|---|---|---|
| M12.1.1 | tool_definition table with the closed naming grammar | reopen partial | The closed tool-name grammar exists (core/envelope.py TOOL_NAME_PATTERN) and runs at every startup when build_registry registers the three built-in tools, but there is no tool_definition table in any migration or on the install. The closing commit c372a12 says 'Not claimed: the registry table'. |
| M12.1.2 | Required capability per tool | reopen unproven | Each registered tool carries a required capability parsed at every startup by build_registry, but no tool has ever been called on the install (no question asked, Records answers 404), so the capability has never gated a real call. To prove it: Ask a question on Ask that a registered document or row tool answers, once as a reader holding its capability and once without, and see the answer and the refusal.. |
| M12.1.3 | Side-effect classification driving default leash | reopen not wired | registry.default_rung (side effect to default leash rung) has no production caller, and gate/leash.py deliberately has no default rung; rung_ceiling is reached only from browsing/envelope (no browser sandbox on the install) and scoped_authority.may_move_rung, which nothing calls. Registration does check effect against the capability verb, but nothing derives a leash from the side effect on the install. |
| M12.1.4 | Result contract declaration: typed or opaque | reopen unproven | The typed result contract is checked at registration for the three built-in tools on every start, but no tool result has ever been redacted on the install and no opaque tool is registered, so neither half has been shown working with real rows. To prove it: Answer one question through a registered tool on Ask and see a redacted, typed result; an opaque tool would need registering first.. |
| M12.1.5 | Registration validation at startup | keep install | brain.app lifespan calls build_registry, which registers the price-list row tool and two document tools through every registration rule and freezes, raising on failure; the install has started many times at f32c385 and reports /health/ready 200. |
| M12.1.6 | Registry sweep in CI | keep repo | sweep_tool_registry (now asking build_registry, asserting a non-zero count) and sweep_one_tool_grammar exist at f32c385 and the CI 'Schema and registry sweeps' job runs both. |
| M12.2.1 | SKILL.md folder format parser with frontmatter validation | reopen unproven | skill_from_markdown with frontmatter validation is reached from POST /api/v1/skills (skill_routes.add_skill -> skill_library.read_package), mounted in brain.app, and the first administrator holds admin:skill; but agent.skill has 0 rows, so no SKILL.md has ever been parsed on the install. To prove it: From the Skills screen, add a valid SKILL.md and see it listed as imported, then add one with a bad frontmatter field and see the refusal sentence.. |
| M12.2.2 | Import from GitHub with a pinned commit | reopen not wired | tools/fetch.fetch_skill_source and github_tarball_url exist but nothing outside tests calls them; the only skill import route (POST /skills) reads an uploaded or pasted package and always records SourceKind.UPLOAD. The console audit also lists skill_routes as upload and paste only. |
| M12.2.3 | Import from URL | reopen not wired | The URL fetch path (fetch_skill_source with SSRF checks) has no production caller; POST /skills accepts only uploaded or pasted bytes, so a skill cannot be imported from a URL on the install. |
| M12.2.4 | Import from uploaded archive with path-traversal validation | reopen unproven | POST /skills accepts a .zip and skill_library._from_archive applies safe_archive_members, member count, declared size, regular-file mode and encryption checks in memory (tools/extract.extract_zip itself has no caller); no skill has ever been added on the install. To prove it: Upload a .zip holding one SKILL.md on the Skills screen and see it added, then upload a zip with a ../x member and see the refusal sentence.. |
| M12.2.5 | Imported state that is not executable until reviewed | reopen not wired | POST /skills does write the imported, undecided state, but nothing the install runs ever executes or discloses a skill to an agent: body_of, offered_cards and run_skill have no production caller, so the gate that keeps an unreviewed skill from running is never reached. No agent can exist either (install_store.finish has no route). |
| M12.2.6 | Review queue with diff view | reopen unproven | The queue (tools/review.pending via skill_library.queue_entries) is served on GET /skills and decided on POST /skills/{digest}/review, but no skill exists and a decision needs a reviewer other than the importer, while only one principal is bound on the install. The diff is a list of changed field names, not a content diff. To prove it: Administrator A adds a skill, a second bound administrator B approves it from the queue; A then adds an edited version and B sees the changed fields listed.. |
| M12.2.7 | Version locking per agent | reopen not wired | A pin is written only by POST /skills/{digest}/assignments into the install overlay of an agent, which needs an agent with an install record; no route creates an agent or template instance on the install (install_store.finish has no caller), and resolve_pin is called only by the unregistered run_skill tool. |
| M12.2.8 | Progressive disclosure: names and descriptions first, body on demand | reopen not wired | offered_cards (names and descriptions) and body_of (body on demand) are only re-exported from tools/__init__.py; no answer lane, agent run or route offers skill cards to a model, and no model has ever been called on the install. |
| M12.2.9 | run_skill_script as the single execution tool, sandboxed and leashed | reopen not wired | SkillScriptTool is not registered in build_registry and no production module imports brain.tools.run_skill; the closing commit says no execution tool exists in production today, and six of ten sandbox properties are unenforced pending a container that does not exist. Needs: script sandbox container (not present on the install). |
| M12.2.10 | SOP import accepting messy Word and Confluence documents | reopen not wired | tools/sop_import (Word or Confluence document to a reviewable skill) has no production caller; only its injection patterns are imported by connectors/lark_wiki, a connector the install has no credentials or worker for. |

### M13 Agents and templates

| Task | What it asks | Verdict | Why |
|---|---|---|---|
| M13.1.1 | agent table: persona, scope, model tier, visibility level | reopen not wired | agent.agent exists on the install with persona, tier, visibility and scope columns (migration 0014, head 0083), but no route or startup step can write a row (install_store.finish has no caller) and the Agents screen shows no agents, so the table holds data nothing on the install can produce. |
| M13.1.2 | Global, department and personal visibility | reopen not wired | visible_agent_ids is applied by GET /agents, but no route or startup step creates an agent: the only writer of agent.agent is agents/install_store.StoredAgentInstalls.finish, which nothing calls (829d09f: No route calls finish), so the global, department and personal visibility rule only ever filters an empty set on the install. |
| M13.1.3 | Audience separated from authority, asserted by test | reopen not wired | AgentAudience and AgentAuthority are separate types with tests over the cross product, but no route or startup step creates an agent: the only writer of agent.agent is agents/install_store.StoredAgentInstalls.finish, which nothing calls (829d09f: No route calls finish); the separation has never been applied to a real agent on the install and tests alone cannot keep the leaf. |
| M13.1.4 | Agent enable, disable and archive | reopen not wired | agents/lifecycle enable, disable and archive have no route; their only importers are agents/install.py (disable on an incomplete install, itself unrouted) and identity/lifecycle.py, which no route calls. There is also no agent on the install to act on. |
| M13.1.5 | Ownership transfer when a creator leaves | reopen not wired | transfer_ownership and agents_needing_transfer are called only from identity/lifecycle.py (adopt, agents_for_adoption), which no route, startup step or directory sync calls; the closing commit itself says the caller does not exist. |
| M13.2.1 | Signed, versioned, immutable manifest | reopen not wired | agent.template_version is immutable on the install (brain_app holds INSERT and SELECT only, 2 policies), but nothing signs or stores a manifest there: no setting holds a template signing key, install_store.finish has no route, and the gallery shows the 23 built-ins unsigned. |
| M13.2.2 | Manifest schema with identity, persona, skills, tools, connectors, scope, tier, leash, golden set, placeholders | reopen not wired | TemplateManifest validates the 23 built-in manifests listed on Agent templates, but the parts the schema exists for (skills, connectors, leash, golden set, placeholders) are never used, because no template can be installed on the install. |
| M13.2.3 | Instance as a pinned reference plus an overlay | reopen not wired | agent.template_instance (pin columns plus overlay) is on the install, but nothing on the install creates a template instance (install_store.finish has no route, 829d09f), so no instance has ever been pinned or materialised there; the reads in prompt_routes and skill_routes join an always-empty table. |
| M13.2.4 | Per-path field ownership tracking who last set each field | reopen not wired | FieldOwner and set_field record who last set each overlay path, but nothing on the install creates a template instance (install_store.finish has no route, 829d09f), and the one overlay writer that is routed (prompt edit) needs an existing instance and ships switched off. |
| M13.2.5 | Materialised effective document with a hash feeding the cache key | reopen not wired | materialise and config_hash produce the effective document and its hash, but nothing on the install creates a template instance (install_store.finish has no route, 829d09f), and no question has been answered through an agent, so the hash has never fed a cache key on the install. |
| M13.2.6 | Five sealed paths enforced by a check constraint | reopen not wired | Migration 0016 puts sealed_paths_are_absent and overlay_paths_are_settable on agent.template_instance, but nothing on the install writes an instance, so the seal has never refused anything there. |
| M13.2.7 | Blank template so a hand-built agent uses one code path | reopen not wired | blank_template, blank_offer and begin_hand_built exist so a hand-built agent uses the template path, but no route or screen calls any of them and template.hand_built has no caller, so no agent can be hand-built on the install. |
| M13.3.1 | Six-step wizard generated from the manifest JSON Schema | reopen not wired | install.plan builds six steps from the manifest JSON Schema, but no route or console screen reaches agents/install.py: plan, connector_readiness, completeness and rehearse_golden_set have no production caller, and install.complete is called only by install_store.finish, which nothing calls (829d09f). The console has no install wizard route; the Agent templates page says it has no install control. |
| M13.3.2 | Connector binding step with readiness indicators | reopen not wired | connector_readiness and bind_tools exist, but no route or console screen reaches agents/install.py: plan, connector_readiness, completeness and rehearse_golden_set have no production caller, and install.complete is called only by install_store.finish, which nothing calls (829d09f); the install also has no connector connection to be ready. |
| M13.3.3 | Placeholder collection: SOP, price list, escalation contact | reopen not wired | install.provide collects placeholder answers into a draft, but no route or console screen reaches agents/install.py: plan, connector_readiness, completeness and rehearse_golden_set have no production caller, and install.complete is called only by install_store.finish, which nothing calls (829d09f); the placeholder answers have no store (8d27f96 and b6e6471 both say so). |
| M13.3.4 | Golden-set run as the installing principal | reopen not wired | rehearse_golden_set runs the golden questions as the installer through invoke, but no route or console screen reaches agents/install.py (install.complete is called only by install_store.finish, which nothing calls, 829d09f); the commit also says answers are not graded because no model was in the process, and no model has been called on the install. |
| M13.3.5 | Golden-set run as a low-privilege fixture user | reopen not wired | The low-privilege fixture rehearsal is part of rehearse_golden_set, which no route or console screen reaches agents/install.py (install.complete is called only by install_store.finish, which nothing calls, 829d09f); the install also has only one principal, so no low-privilege person exists to rehearse as. |
| M13.3.6 | Incomplete state with an amber badge listing what is missing | reopen not wired | completeness returns the amber InstallBadge and the missing list and disables the agent, but no route or console screen reaches agents/install.py (install.complete is called only by install_store.finish, which nothing calls, 829d09f); no agent page renders an install badge. |
| M13.3.7 | Pin to Shadow when required connectors are absent | reopen not wired | pinned_leash rewrites every rung to SHADOW while a declared connector is not serving, but it is only called inside the unrouted install flow (install_store.finish has no caller), so no agent on the install is ever pinned. |
| M13.4.1 | Publishing a new version changes nothing observable | reopen not wired | agents/upgrade.py (VersionShelf, review, accept, decline) has no route or console caller; its only importer is builder/drafts.py, which is itself unrouted, and nothing writes agent.upgrade_decline (b6e6471: no HTTP route and no console page calls any of this). There is no template version or instance on the install for a publish to leave unchanged. |
| M13.4.2 | Upgrade badge on the instance | reopen not wired | UpgradeBadge is computed by upgrade.review, but agents/upgrade.py (VersionShelf, review, accept, decline) has no route or console caller; its only importer is builder/drafts.py, which is itself unrouted, and nothing writes agent.upgrade_decline (b6e6471: no HTTP route and no console page calls any of this); no agent page shows an upgrade badge. |
| M13.4.3 | Three-column diff of conflicting paths only | reopen not wired | UpgradeReview and Conflict give the three-column diff of conflicting paths, but agents/upgrade.py has no route or console caller (its only importer, builder/drafts.py, is unrouted) and there is no instance on the install to upgrade; no screen renders it. |
| M13.4.4 | Opt-in accept with per-path resolution | reopen not wired | upgrade.accept applies per-path Resolution, but agents/upgrade.py has no route or console caller (its only importer, builder/drafts.py, is unrouted) and there is no instance on the install to upgrade. The closing commit also notes accept does not re-run install.completeness. |
| M13.4.5 | Decline forever without nagging | reopen not wired | agent.upgrade_decline is on the install with INSERT and SELECT only (no UPDATE or DELETE), which is the forever half, but nothing in src writes the table and agents/upgrade.py has no route or console caller (its only importer, builder/drafts.py, is unrouted) and there is no instance on the install to upgrade. |
| M13.5.1 | Business Analyst | reopen not wired | The business_analyst manifest is in brain.agents.catalogue.CATALOGUE and is listed unsigned on the Agent templates screen (GET /agent-templates), but no route installs a template (no signing key setting, install_store.finish has no caller), so it can never become an agent on the install. |
| M13.5.2 | Pre-Sales | reopen not wired | The pre_sales manifest is in brain.agents.catalogue.CATALOGUE and is listed unsigned on the Agent templates screen (GET /agent-templates), but no route installs a template (no signing key setting, install_store.finish has no caller), so it can never become an agent on the install. |
| M13.5.3 | Project Manager | reopen not wired | The project_manager manifest is in brain.agents.catalogue.CATALOGUE and is listed unsigned on the Agent templates screen (GET /agent-templates), but no route installs a template (no signing key setting, install_store.finish has no caller), so it can never become an agent on the install. |
| M13.5.4 | UI Designer | reopen not wired | The ui_designer manifest is in brain.agents.catalogue.CATALOGUE and is listed unsigned on the Agent templates screen (GET /agent-templates), but no route installs a template (no signing key setting, install_store.finish has no caller), so it can never become an agent on the install. |
| M13.5.5 | UX Designer | reopen not wired | The ux_designer manifest is in brain.agents.catalogue.CATALOGUE and is listed unsigned on the Agent templates screen (GET /agent-templates), but no route installs a template (no signing key setting, install_store.finish has no caller), so it can never become an agent on the install. |
| M13.5.6 | HTML Developer | reopen not wired | The html_developer manifest is in brain.agents.catalogue.CATALOGUE and is listed unsigned on the Agent templates screen (GET /agent-templates), but no route installs a template (no signing key setting, install_store.finish has no caller), so it can never become an agent on the install. |
| M13.5.7 | WordPress Developer | reopen not wired | The wordpress_developer manifest is in brain.agents.catalogue.CATALOGUE and is listed unsigned on the Agent templates screen (GET /agent-templates), but no route installs a template (no signing key setting, install_store.finish has no caller), so it can never become an agent on the install. |
| M13.5.8 | Laravel Developer | reopen not wired | The laravel_developer manifest is in brain.agents.catalogue.CATALOGUE and is listed unsigned on the Agent templates screen (GET /agent-templates), but no route installs a template (no signing key setting, install_store.finish has no caller), so it can never become an agent on the install. |
| M13.5.9 | Shopify Developer | reopen not wired | The shopify_developer manifest is in brain.agents.catalogue.CATALOGUE and is listed unsigned on the Agent templates screen (GET /agent-templates), but no route installs a template (no signing key setting, install_store.finish has no caller), so it can never become an agent on the install. |
| M13.5.10 | Content Uploader | reopen not wired | The content_uploader manifest is in brain.agents.catalogue.CATALOGUE and is listed unsigned on the Agent templates screen (GET /agent-templates), but no route installs a template (no signing key setting, install_store.finish has no caller), so it can never become an agent on the install. |
| M13.5.11 | Tester | reopen not wired | The tester manifest is in brain.agents.catalogue.CATALOGUE and is listed unsigned on the Agent templates screen (GET /agent-templates), but no route installs a template (no signing key setting, install_store.finish has no caller), so it can never become an agent on the install. |
| M13.5.12 | Internal Helpdesk | reopen not wired | The internal_helpdesk manifest is in brain.agents.catalogue.CATALOGUE and is listed unsigned on the Agent templates screen (GET /agent-templates), but no route installs a template (no signing key setting, install_store.finish has no caller), so it can never become an agent on the install. |
| M13.5.13 | Site Health Sentinel | reopen not wired | The site_health_sentinel manifest is in brain.agents.catalogue.CATALOGUE and is listed unsigned on the Agent templates screen (GET /agent-templates), but no route installs a template (no signing key setting, install_store.finish has no caller), so it can never become an agent on the install. |
| M13.5.14 | Support Ticket Agent | reopen not wired | The support_ticket_agent manifest is in brain.agents.catalogue.CATALOGUE and is listed unsigned on the Agent templates screen (GET /agent-templates), but no route installs a template (no signing key setting, install_store.finish has no caller), so it can never become an agent on the install. |
| M13.5.15 | Project Status Reporter | reopen not wired | The project_status_reporter manifest is in brain.agents.catalogue.CATALOGUE and is listed unsigned on the Agent templates screen (GET /agent-templates), but no route installs a template (no signing key setting, install_store.finish has no caller), so it can never become an agent on the install. |
| M13.5.16 | Capacity and Hours Analyst | reopen not wired | The capacity_and_hours_analyst manifest is in brain.agents.catalogue.CATALOGUE and is listed unsigned on the Agent templates screen (GET /agent-templates), but no route installs a template (no signing key setting, install_store.finish has no caller), so it can never become an agent on the install. |
| M13.5.17 | Quote and Proposal Drafter | reopen not wired | The quote_and_proposal_drafter manifest is in brain.agents.catalogue.CATALOGUE and is listed unsigned on the Agent templates screen (GET /agent-templates), but no route installs a template (no signing key setting, install_store.finish has no caller), so it can never become an agent on the install. |
| M13.5.18 | AR and Renewal Chaser, Shadow-pinned thirty days | reopen not wired | The ar_and_renewal_chaser manifest is in brain.agents.catalogue.CATALOGUE and is listed unsigned on the Agent templates screen (GET /agent-templates), but no route installs a template (no signing key setting, install_store.finish has no caller), so it can never become an agent on the install. The thirty days lives on supervision.ShadowPin (SHADOW_REVIEW_PERIOD), and supervision.pin and review have no production caller. |
| M13.5.19 | Accountant Agent, Shadow-pinned | reopen not wired | The accountant_agent manifest is in brain.agents.catalogue.CATALOGUE and is listed unsigned on the Agent templates screen (GET /agent-templates), but no route installs a template (no signing key setting, install_store.finish has no caller), so it can never become an agent on the install. Its Shadow pin is the SHADOW leash rung on the manifest, which only takes effect once installed. |
| M13.5.20 | SEO Agent | reopen not wired | The seo_agent manifest is in brain.agents.catalogue.CATALOGUE and is listed unsigned on the Agent templates screen (GET /agent-templates), but no route installs a template (no signing key setting, install_store.finish has no caller), so it can never become an agent on the install. |
| M13.5.21 | SEM Agent, Shadow-pinned, human commits budget changes | reopen not wired | The sem_agent manifest is in brain.agents.catalogue.CATALOGUE and is listed unsigned on the Agent templates screen (GET /agent-templates), but no route installs a template (no signing key setting, install_store.finish has no caller), so it can never become an agent on the install. Its human-commits constraint is SideEffect.DRAFT on the manifest, which only takes effect once installed. |
| M13.5.22 | SMM Agent | reopen not wired | The smm_agent manifest is in brain.agents.catalogue.CATALOGUE and is listed unsigned on the Agent templates screen (GET /agent-templates), but no route installs a template (no signing key setting, install_store.finish has no caller), so it can never become an agent on the install. |
| M13.5.23 | Knowledge Gap Curator | reopen not wired | The knowledge_gap_curator manifest is in brain.agents.catalogue.CATALOGUE and is listed unsigned on the Agent templates screen (GET /agent-templates), but no route installs a template (no signing key setting, install_store.finish has no caller), so it can never become an agent on the install. |
| M13.6.1 | Save any agent as a private template | reopen not wired | In code, author() turns an EffectiveAgent into a private AuthoredDraft, but brain.agents.authoring is imported by no production module and has no persistence (d631bf3: no HTTP route, and no persistence at all), and there is no agent on the install to save. |
| M13.6.2 | Literal extraction and parameter hoisting | reopen not wired | In code, scan() extracts literals and a HOIST decision replaces a value with a declared placeholder, but brain.agents.authoring is imported by no production module and has no persistence (d631bf3: no HTTP route, and no persistence at all), and there is no agent on the install to save. |
| M13.6.3 | Leak report with accept or redact on every unclassified literal | reopen not wired | In code, LeakReport offers every unclassified literal for accept or redact, but brain.agents.authoring is imported by no production module and has no persistence (d631bf3: no HTTP route, and no persistence at all), and there is no agent on the install to save. |
| M13.6.4 | Publish blocked until every item is dispositioned | reopen not wired | In code, publish_authored refuses while any item is undecided, but brain.agents.authoring is imported by no production module and has no persistence (d631bf3: no HTTP route, and no persistence at all), and there is no agent on the install to save. |
| M13.6.5 | Memory never exported under any setting | reopen not wired | In code, carried() names the manifest fields that travel so memory and placeholder answers never do, but brain.agents.authoring is imported by no production module and has no persistence (d631bf3: no HTTP route, and no persistence at all), and there is no agent on the install to save. |
| M13.6.6 | Visibility: private, organisation, catalogue | reopen not wired | In code, TemplateVisibility private, organisation and catalogue with set_visibility, but brain.agents.authoring is imported by no production module and has no persistence (d631bf3: no HTTP route, and no persistence at all), and there is no agent on the install to save. |

### M14 Entity resolution

| Task | What it asks | Verdict | Why |
|---|---|---|---|
| M14.1.1 | er.canonical: canonical entity with type and created provenance | reopen not wired | The schema is on the install via migration 0020, but nothing the install runs writes or reads it (no resolver, store or route); er.* is empty. |
| M14.1.2 | er.alias: every observed name form with source and first-seen | reopen not wired | The schema is on the install via migration 0020, but nothing the install runs writes or reads it (no resolver, store or route); er.* is empty. |
| M14.1.3 | er.identifier: hashed join keys by type with source | reopen not wired | The schema is on the install via migration 0020, but nothing the install runs writes or reads it (no resolver, store or route); er.* is empty. |
| M14.1.4 | er.link: source record to canonical mapping with confidence | reopen not wired | The schema is on the install via migration 0020, but nothing the install runs writes or reads it (no resolver, store or route); er.* is empty. |
| M14.1.5 | merged_into forwarding pointer so an issued id resolves forever | reopen not wired | The schema is on the install via migration 0020, but nothing the install runs writes or reads it (no resolver, store or route); er.* is empty. |
| M14.1.6 | Resolved alias view giving the current surviving entity | reopen not wired | The schema is on the install via migration 0020, but nothing the install runs writes or reads it (no resolver, store or route); er.* is empty. |
| M14.2.1 | Case folding and whitespace collapse | reopen not wired | Normalisation is Python in resolution/normalise.py, called only by the v1 carry tool and unreached resolution modules; nothing on the install normalises a record. |
| M14.2.2 | Punctuation stripping | reopen not wired | Normalisation is Python in resolution/normalise.py, called only by the v1 carry tool and unreached resolution modules; nothing on the install normalises a record. |
| M14.2.3 | Suffix stripping: Pte Ltd, Pte, Sdn Bhd, Inc, LLC, Limited, Ltd, Co | reopen not wired | Normalisation is Python in resolution/normalise.py, called only by the v1 carry tool and unreached resolution modules; nothing on the install normalises a record. |
| M14.2.4 | Accent folding via an immutable wrapper so it can be indexed | reopen not wired | er.immutable_unaccent and its index are installed by 0021, but no query uses them and er.alias is empty. |
| M14.2.5 | Minimum length guard: under four characters routes straight to review | reopen not wired | Normalisation is Python in resolution/normalise.py, called only by the v1 carry tool and unreached resolution modules; nothing on the install normalises a record. |
| M14.2.6 | Singapore UEN format validation | reopen not wired | Normalisation is Python in resolution/normalise.py, called only by the v1 carry tool and unreached resolution modules; nothing on the install normalises a record. |
| M14.3.1 | Stage one: hard identifiers including UEN, tax id, verified domain, verified phone | reopen not wired | The stage is in resolution/cascade.py, which its own constant says nothing in the running system calls; no match has run on the install. |
| M14.3.2 | Stage two: normalised name requiring corroboration from a second field | reopen not wired | The stage is in resolution/cascade.py, which its own constant says nothing in the running system calls; no match has run on the install. |
| M14.3.3 | Stage three: trigram similarity with an upper and lower threshold | reopen not wired | The stage is in resolution/cascade.py, which its own constant says nothing in the running system calls; no match has run on the install. |
| M14.3.4 | Stage four: human review for the band between thresholds | reopen not wired | The stage is in resolution/cascade.py, which its own constant says nothing in the running system calls; no match has run on the install. |
| M14.3.5 | Weight table exported from offline calibration | reopen partial | Weights are author-chosen constants no calibration produced, and no export is loaded anywhere. Needs: calibrated export and a loader. |
| M14.3.6 | Additive scoring evaluated in plain SQL for millisecond online cost | reopen not wired | The SQL score builder has no caller; eda7d05 says nothing has executed it against PostgreSQL. |
| M14.3.7 | Free-mail domain blocklist rather than low weighting | reopen not wired | The stage is in resolution/cascade.py, which its own constant says nothing in the running system calls; no match has run on the install. |
| M14.3.8 | Daitch-Mokotoff rather than Soundex, weighted low, for non-English names | reopen not wired | The stage is in resolution/cascade.py, which its own constant says nothing in the running system calls; no match has run on the install. |
| M14.4.1 | Splink offline job against a DuckDB export | reopen absent | The Splink job runs in the matcher image, which the install does not run; run once in a throwaway environment. Needs: matcher image. |
| M14.4.2 | Expectation-maximisation training of match and unmatch probabilities | reopen absent | EM training runs only offline and has never run over install data. Needs: matcher image and a real export. |
| M14.4.3 | Weight export to the online table | reopen not wired | There is no online weight table by design and nothing loads an export into the app. |
| M14.4.4 | Weekly schedule with a review of drift | reopen not wired | The resolution_calibration Runner has only a needs note and no run function; the scheduler does not run on the install. |
| M14.4.5 | No Python ML dependency in the request path, asserted | keep repo | An invariant test walks the import graph and asserts no ML library or offline module is reachable from the request path; CI Invariants step runs it on every push and PR. |
| M14.5.1 | Pre-image capture of every affected link, alias and identifier before any change | reopen not wired | Implemented in resolution/merge.py, which nothing imports; no merge has happened or can happen on the install. |
| M14.5.2 | Merge as a pointer move, never a source-record change | reopen not wired | Implemented in resolution/merge.py, which nothing imports; no merge has happened or can happen on the install. |
| M14.5.3 | Unmerge restoring from the pre-image | reopen not wired | Implemented in resolution/merge.py, which nothing imports; no merge has happened or can happen on the install. |
| M14.5.4 | Full audit of who merged, when, on what evidence | reopen not wired | Merge audit comes only from unwired merge.py and er.* has no audit trigger. |
| M14.5.5 | Cache, memory and projection invalidation on merge | reopen not wired | Implemented in resolution/merge.py, which nothing imports; no merge has happened or can happen on the install. |
| M14.5.6 | Money-boundary rule: never auto-merge entities carrying financial records | reopen not wired | The money-boundary refusal lives in unwired merge/cascade code. |
| M14.6.1 | Blocked values table for known junk and shared mailboxes | reopen partial | Blocked values are module constants with no table or migration (de784b3), and nothing calls them. Needs: blocked-values table and a caller. |
| M14.6.2 | Per-identifier caps on distinct values per entity | reopen not wired | Per-identifier caps are constants with no running caller. |
| M14.6.3 | Identifier priority order for collisions | reopen not wired | Identifier priority is a constant with no running caller. |
| M14.6.4 | Review queue UI with weighted evidence shown in plain language | reopen partial | review_queue returns data but there is no page and no route (de784b3), still none at f32c385. Needs: review queue route and console page. |
| M14.6.5 | Unresolved behaviour: state the ambiguity, never guess, offer the review link | reopen not wired | The unresolved notice has no caller in the answer path; no question has been asked. |
| M14.7.1 | The same cascade applied to people across HubSpot, Freshdesk and Lark | reopen not wired | PERSON_PROFILE names no source column and the cascade is not called; no people matched. |
| M14.7.2 | The same cascade applied to projects | reopen not wired | PROJECT_PROFILE names no source column and the cascade is not called; no projects matched. |
| M14.7.3 | Privacy: hashed join keys only, never full contact records | reopen not wired | er.identifier stores only key_hash, but the people/projects cascade is never called, so no join key has been hashed or stored. |

### M15 Knowledge retrieval and RAG

| Task | What it asks | Verdict | Why |
|---|---|---|---|
| M15.1.1 | Typed tool per entity with a parameterised query | reopen absent | Row tools are registered at start, but Records answers 404 for client (the admin holds no record grant, no data steward named) and proj.record is 0; rows only arrive by worker connector sync. Needs: worker connector sync (records) and a record grant. |
| M15.1.2 | Column projection compiled from capabilities before the query runs | reopen absent | Column projection has never run over a real row: no record grant exists and proj.record is 0. Needs: worker connector sync (records) and a record grant. |
| M15.1.3 | Assertion that no model ever writes SQL | keep repo | No-SQL assertion tests exist and CI unit shards run them. |
| M15.1.4 | Query plan test proving the scope predicate is inside the query | reopen partial | No EXPLAIN query-plan test exists; the test only checks the predicate is in the SQL text (6eb5008 says so). |
| M15.2.1 | Postgres full-text index with tsvector and weighting | reopen absent | Weighted tsvector and GIN index exist in the schema but know.chunk is 0 and only worker connector sync writes chunks. Needs: worker connector sync (documents). |
| M15.2.2 | pgvector column and index strategy | reopen absent | HNSW index exists but no embedding is ever written: no inference or embedding server and no documents. Needs: embedding server and worker. |
| M15.2.3 | Iterative scan enabled, or a per-scope partial index instead | reopen absent | Iterative scan is set only on the vector leg, which runs only when the install embeds questions; it does not. Needs: embedding server. |
| M15.2.4 | Test proving a narrow-scope user gets results rather than a near-empty set | reopen partial | The test runs over an in-memory Python corpus, not the SQL against pgvector, so the database behaviour is unproven. |
| M15.2.5 | Hybrid fusion by reciprocal rank | reopen absent | Hybrid fusion needs a vector leg (no embedding server) and documents (0). Needs: embedding server and worker. |
| M15.2.6 | Scope filter inside the query, never applied afterwards | reopen absent | The scoped search SQL has never run over real chunks; know.chunk is 0 and nothing on the install can add one. Needs: worker connector sync (documents). |
| M15.2.7 | Row-level security as the second wall | reopen absent | know.chunk RLS is on with 3 policies but there are 0 rows for it to wall off, and nothing on the install can add one. Needs: worker connector sync (documents). |
| M15.3.1 | Reranking evaluated and adopted only if it earns its latency | reopen partial | No reranker was ever evaluated; the record says NOT_MEASURABLE because no judged set exists. |
| M15.3.2 | Chunk-level and document-level result assembly | reopen absent | by_chunk and by_document run on the document tool path, which has no documents on the install. Needs: worker connector sync (documents). |
| M15.3.3 | Deduplication across planes | reopen not wired | assembly.deduplicate has no caller. |
| M15.3.4 | Retrieval logging for the learning signal | reopen not wired | No RetrievalEvent is ever emitted. |

### M16 Memory and learning

| Task | What it asks | Verdict | Why |
|---|---|---|---|
| M16.1.1 | Session memory in Valkey with thread lifetime | reopen not wired | formation.session_key and session_expiry are defined but nothing in src writes or reads a session memory in Valkey; the install's cache holds 0 keys. To prove it: A conversation turn on the install that writes a mem:session:<principal>:<thread> key with a 12h idle TTL, visible in Valkey.. |
| M16.1.2 | Persistent memory table with scope and capability tags | reopen not wired | mem.persistent exists on the install (0083, RLS on, 2 policies) but nothing on the install can write a row to it: StoredFormations, the only writer of a formed memory, has no caller, and the Memory screen that would show rows answers 404. To prove it: A stated memory (remember that ...) from a turn on the install landing in mem.persistent with its scope and capability tags, shown on the Memory screen.. |
| M16.1.3 | Adaptive memory table with confidence and decay | reopen not wired | mem.adaptive exists on the install with formed_confidence and formed_at, but nothing on the install can write an inferred memory (StoredFormations has no caller) and decay is only computed at a read that has no rows; the Memory screen answers 404. To prove it: An inferred memory (I prefer ...) from a turn on the install stored in mem.adaptive and shown with its decayed confidence.. |
| M16.1.4 | Capability set recorded at formation and re-checked at read | reopen not wired | Formation is never recorded on the install: StoredFormations.form/after_turn (the only writer) has no caller, the ask route does not call it, and mem.* is empty; the read-side may_recall only runs in console viewers over zero rows. To prove it: Wire StoredFormations.after_turn into the ask route, ask a question containing 'remember that ...', see a mem.persistent row carrying capability_tags, and read it back through /govern/memory.. |
| M16.1.5 | Never authoritative over the database, asserted by test | reopen not wired | The structural property and its unit test exist (Recollection has no value, row or citation), but no answer path on the install recalls memory at all, so the property governs nothing there; it is proven only over test objects. To prove it: Memory recalled into an answer on the install, with the answer still citing a database record rather than the memory.. |
| M16.2.1 | Question re-asked in different words within a window | reopen not wired | The vocabulary and re-ask detection (is_reask) exist as pure functions, but nothing on a running path observes a conversation, answer or approval and emits or stores a signal. To prove it: Two differently worded questions on one subject asked by one person on the install, and a stored REASKED signal naming the conversation and message.. |
| M16.2.2 | Answer copied | reopen not wired | The vocabulary and the COPIED signal exist as pure functions, but nothing on a running path observes a conversation, answer or approval and emits or stores a signal. To prove it: Copy an answer in the console on the install and see a stored COPIED signal.. |
| M16.2.3 | Contradicting follow-up | reopen not wired | The vocabulary and contradiction detection (is_contradiction) exist as pure functions, but nothing on a running path observes a conversation, answer or approval and emits or stores a signal. To prove it: A follow-up contradicting an answer on the install, and a stored CONTRADICTED signal.. |
| M16.2.4 | Escalation to a human | reopen not wired | The vocabulary and the ESCALATED signal exist as pure functions, but nothing on a running path observes a conversation, answer or approval and emits or stores a signal. To prove it: An answer escalated to a person on the install and a stored ESCALATED signal.. |
| M16.2.5 | Human takeover of an Assisted action | reopen not wired | The vocabulary and takeover detection (is_takeover) exist as pure functions, but nothing on a running path observes a conversation, answer or approval and emits or stores a signal. To prove it: A person taking over an Assisted agent action on the install and a stored TAKEN_OVER signal.. |
| M16.2.6 | Ticket reopened within a window | reopen not wired | The vocabulary and reopen detection (is_reopen) exist as pure functions, but nothing on a running path observes a conversation, answer or approval and emits or stores a signal. Needs: connector account (ticketing source) as well. To prove it: A ticket reopened within the window in a connected source and a stored REOPENED signal.. |
| M16.2.7 | Approval rejected with a reason | reopen not wired | The vocabulary and the REJECTED signal from an approval exist as pure functions, but nothing on a running path observes a conversation, answer or approval and emits or stores a signal. To prove it: An approval rejected with a reason on the install and a stored REJECTED signal.. |
| M16.2.8 | All signals logged from the first migration | reopen not wired | No table stores signals and no writer logs one: mem.learning holds learnings with an evidence array, not a signal log, and nothing emits an Observation, so nothing has been logged since the first migration. To prove it: A signal log on the install that gains a row when a question is re-asked.. |
| M16.3.1 | Tier zero: session, silent | reopen not wired | Tier zero is session memory and nothing writes session memory (see M16.1.1); the tier is a label with no path that applies it. To prove it: A session memory written and used within one conversation on the install.. |
| M16.3.2 | Tier one automatic: preferences, retrieval boosts and demotions, corroborated entity links, all negative signals | reopen not wired | The only tier-one application on a running path would be StoredFormations writing a PREFERENCE, and it has no caller; retrieval boosts, demotions, entity links and negative signals have no applier at all. To prove it: A preference learnt from a turn applied automatically and listed on the Learning screen with Undo.. |
| M16.3.3 | Tier two shadow-then-promote: fast-path rules and procedural shortcuts | reopen not wired | Nothing proposes a fast-path rule or procedural shortcut from learning, runs it in shadow or promotes it; the Learning screen says Promote is still a sentence rather than a control. To prove it: A shortcut proposed from repeated conversations, run in shadow, then promoted on the install.. |
| M16.3.4 | Tier two agreement threshold and promotion job | reopen not wired | may_promote is only called by reach_view to display promote_ready; there is no promotion job in controls, the schedule runner or the worker, and nothing records agreement. Needs: worker (and no promotion job exists to run there). To prove it: A promotion job promoting a tier-two proposal once enough separate conversations agree, seen on the install.. |
| M16.3.5 | Tier three gated: scope widening, company knowledge, leash increase, money-boundary merge, capability addition | reopen not wired | Tier three is a classification plus a Proposal with no approver field and no apply; the Learning screen draws no Decide control and review_queue has no caller. To prove it: A gated learning (for example a scope widening) queued, decided by a person and applied on the install.. |
| M16.3.6 | Blast-radius classifier deciding the tier automatically | reopen not wired | blast_radius is reached from running code only to classify automation schedule changes and library promotions, neither of which has happened on the install; no learning is ever proposed, so it has never decided the tier of a learning. To prove it: A learning proposed on the install whose tier is set by blast_radius and shown on the Learning screen.. |
| M16.4.1 | Time decay reducing confidence below the retrieval threshold | reopen not wired | Decay is applied by may_recall/confidence_now, which runs only in console viewers over memory rows, and nothing on the install can create a memory (StoredFormations uncalled) or recalls memory into an answer. To prove it: An inferred memory formed on the install that stops being recallable once its confidence decays below 0.35.. |
| M16.4.2 | Supersession on contradiction, marked rather than deleted | reopen not wired | Nothing detects a contradiction; the only writers of a Supersession are the console undo and edit of an existing memory, and no memory can exist on the install. To prove it: A contradicting statement in a later turn that writes a mem.correction supersession while both memories stay stored.. |
| M16.4.3 | Source truth wins immediately, memory demoted and flagged | reopen not wired | No code compares a memory against source records; a Demotion is only written by undoing a learning (digest.undo), never by a source disagreeing. To prove it: A memory contradicted by a connected record that is demoted to confidence 0 and flagged immediately.. |
| M16.4.4 | Entitlement expiry: memories stop matching when capabilities change | reopen not wired | may_recall re-checks the capabilities of the reader, but on the install it only runs in console viewers over zero memory rows and no answer path recalls memory; lifecycle.replayable_after has no caller. To prove it: A memory formed under a capability, the capability revoked, and the memory no longer listed or recalled for that person.. |
| M16.5.1 | Weekly digest of tier one and two learning with one-click undo | reopen not wired | weekly_digest is reached only through member_activity.digest_for_channel, which nothing calls; no sender or schedule delivers it and the install has no worker, email relay or channel. The undo exists on the Learning screen but has nothing to undo. Needs: worker, email relay or channel. To prove it: A weekly digest delivered to the person on the install with a working undo.. |
| M16.5.2 | Per-agent memory tab listing what was learned, the evidence, the confidence and a delete control | reopen not wired | The served Agent page has only Automations and Settings tabs, and no route serves a per-agent memory read; agent_memory is used only to filter the global Learning review. To prove it: Open an agent on the install and see a Memory tab listing what it learnt with evidence, confidence and a delete control.. |
| M16.5.3 | Department admin view of department memory | reopen partial | The closing commit narrowed the leaf to only the changes that alter who may see what, and even that narrowed view (review.department_memory) has no route, screen or caller. To prove it: A department administrator opening a department memory view on the install.. |
| M16.5.4 | Tier three review queue with a budget alarm when it grows | reopen not wired | review_queue and queue_alarm have no caller; the Learning screen draws tier three without a Decide control and no route computes an alarm. To prove it: Tier-three learnings queued on the install and an alarm shown when the visible queue passes 15 items.. |

### M17 Automation and the task lane

| Task | What it asks | Verdict | Why |
|---|---|---|---|
| M17.1.1 | Job table with state machine, attempts, and scheduling | reopen absent | The job model (brain.ops.jobs) and its queue rows exist only inside the worker and the Procrastinate driver schema, which install_queue creates from the worker CLI; the install runs neither. Needs: worker (python -m brain.ops.worker) and the Procrastinate queue schema it installs. |
| M17.1.2 | Procrastinate integration on Postgres | reopen absent | register_task/enqueue_job carry jobs into Procrastinate, but the driver schema is installed only by the worker CLI and no worker runs on the install. Needs: worker (python -m brain.ops.worker) and the Procrastinate queue schema it installs. |
| M17.1.3 | Worker pool with per-class concurrency | reopen absent | Per-class concurrency is the worker pool (brain.ops.worker slots and admission share); the install runs no worker process. Needs: worker (python -m brain.ops.worker) and the Procrastinate queue schema it installs. |
| M17.1.4 | Cancellation and timeout | reopen absent | Cancellation and timeout apply to jobs the worker runs; no job has ever run on the install because no worker exists. Needs: worker (python -m brain.ops.worker) and the Procrastinate queue schema it installs. |
| M17.1.5 | Dead-letter handling with an operator surface | reopen absent | Dead letters come from the worker queue, which is absent; the served Jobs screen lists schedule controls (pause, resume, run now), not dead letters, and redriving dead letters from the console is still the open leaf M27.13.3. Needs: worker (python -m brain.ops.worker) and the Procrastinate queue schema it installs. |
| M17.2.1 | LangGraph checkpointer on Postgres | reopen absent | The LangGraph saver schema is created only by install_checkpointer from the worker CLI, and graphs checkpoint inside the worker; neither runs on the install. Needs: worker (python -m brain.ops.worker) and the Procrastinate queue schema it installs. |
| M17.2.2 | Our own re-drive driver for crash recovery | reopen absent | The re-drive driver (queue verdicts plus side_effect_resume) acts on in-flight worker rows after a worker dies; there is no worker, and the commit records both controls as having no caller. Needs: worker (python -m brain.ops.worker) and the Procrastinate queue schema it installs. |
| M17.2.3 | Checkpoint at every side-effect boundary | reopen partial | The commit closed this on a narrowed reading (checkpoint means the durable ops.operation record, not a LangGraph checkpoint), and even that record has never been written on the install (ops.operation 0 rows). Needs: worker (python -m brain.ops.worker) and the Procrastinate queue schema it installs. To prove it: A side-effecting run on the install that leaves a checkpoint at each boundary before the effect.. |
| M17.2.4 | Resume after restart with state intact | reopen absent | Resume after restart is the worker re-drive over queue rows and checkpoints; with no worker, no queue schema and no checkpoint tables nothing can be resumed on the install. Needs: worker (python -m brain.ops.worker) and the Procrastinate queue schema it installs. |
| M17.2.5 | Entitlement re-check at every resume point | reopen absent | Re-resolution on resume lives in the guarded saver and the worker re-drive, both absent; the approval resume in the app would also re-check, but gate.suspension has 0 rows so no resume has ever happened. Needs: worker (python -m brain.ops.worker) and the Procrastinate queue schema it installs. To prove it: An approved suspension resumed on the install after the approver lost a grant, refused at resume.. |
| M17.3.1 | Idempotency key on every side-effecting operation | reopen unproven | The key is derived and issue_once is the only door on every side-effecting path (leash.run_real, channel sends, notification trial send, webhook delivery), but ops.operation has 0 rows: no keyed operation has ever been claimed on the install. To prove it: One side-effecting action on the install (an approved agent draft, or a notification trial send once a channel exists) that writes one ops.operation row with its derived key, and a repeat that is reported and not run again.. |
| M17.3.2 | Operation state machine: pending, sent, unknown, verifying, succeeded, failed | reopen unproven | ops.operation is on the install with the six-state check constraint and the transition trigger, but no operation has ever been claimed, won or settled there (0 rows), so the state machine has never moved a real record. To prove it: One side-effecting action on the install that takes an ops.operation row from pending to sent to succeeded, and a forced unknown that leaves only through verifying.. |
| M17.3.3 | Verification read-back per connector | reopen partial | The closing commit claims only the answer half (turning a reply into FOUND/ABSENT/INCONCLUSIVE); the query half that sends a read-back request is not built because no connector declares a write, and no connector is connected on the install. Needs: connector account. To prove it: A connector write on the install verified by a read-back request against the source.. |
| M17.3.4 | Connectors without read-back restricted to read-only, declared in the manifest | reopen absent | The restriction is a manifest-time derivation and every shipped connector declares only read tools, so it is vacuous; no connector connection or credential exists on the install for it to have restricted. Needs: connector account and credentials. |
| M17.3.5 | Kill-after-side-effect test proving exactly-once | reopen partial | The test kills no process: it proves at-most-once under a modelled crash at derived boundaries, and exactly-once is conditional on a source honouring the key, as the commit and the invariant file both state. To prove it: A test that kills a real worker process after a side effect and shows exactly one effect after recovery.. |
| M17.4.1 | Trigger types: schedule, connector event, webhook | reopen partial | Only the schedule trigger has a performer (brain.ops.automation_run, a worker control, and the worker is absent); connector-event and webhook triggers exist as TriggerKind values with no runner, and orchestration.unattended is imported by nothing but adoption.py. Needs: worker; connector-event and webhook triggers not built. To prove it: An automation fired on the install by each of a schedule, a connector event and a webhook.. |
| M17.4.2 | Named outcome templates rather than a blank canvas | reopen unproven | The gallery of four named outcome templates is mounted and drawn on the Automations tab of an agent, but it is addressed under an agent and the install has no agent, so nobody has seen it. To prove it: Install one agent, open its Automations tab and see the named outcome templates with the one-step install.. |
| M17.4.3 | Automation principal resolving the owner's live entitlements at each run | reopen absent | automation_run resolves the owner entitlements at the instant of each run, but it runs only as a worker control and the install has no worker and no automation. Needs: worker. |
| M17.4.4 | Suspension when the owner loses a required entitlement | reopen absent | The pause on owner_lost_reach is decided inside a run, which only the worker performs; no worker and no automation exist on the install. Needs: worker. |
| M17.4.5 | Timeout and expiry on every approval step | reopen not wired | The approval-step window check lives in orchestration.unattended, which nothing on a running path imports except for TriggerKind; the automations the install could run are admitted read only and have no approval steps. To prove it: An automation with an approval step on the install whose approval expires at its window.. |
| M17.4.6 | Run history with cost per run | reopen partial | agent.automation_run records outcome, reason and result but has no cost column, so run history carries no cost per run; and no run has happened because no worker runs. Needs: worker; cost per run not stored. To prove it: An automation run on the install whose history row shows what it cost.. |
| M17.5.1 | Postgres outbox with retry and backoff | reopen absent | ops.outbox_event and ops.outbox_delivery exist on the install, but retry and backoff are the dispatcher (outbox_dispatch), a worker control, and no worker runs. Needs: worker. |
| M17.5.2 | Signing and delivery guarantees | reopen absent | Signing and at-least-once delivery happen in the worker dispatcher, which is absent, and signing secrets live in the vault, which is also absent. Needs: worker and secrets vault. |
| M17.5.3 | Subscriber management in the console | reopen absent | The Webhooks screen and subscriber routes are mounted, but registering a subscriber keeps its signing secret in the vault and answers 409 with nothing written when the install runs no vault. Needs: secrets vault (OpenBao). |

### M18 Multi-agent execution

| Task | What it asks | Verdict | Why |
|---|---|---|---|
| M18.1.1 | Planner proposing subtasks and data dependencies | reopen not wired | Plan.read_proposal and graph_for parse a planner proposal of subtasks and data dependencies, but nothing asks a model for one; brain.orchestration is imported by no route, startup step or worker task (the only production import is adoption.py taking the TriggerKind enum from orchestration.unattended), no agent exists on the install, and no model has been called. |
| M18.1.2 | Subtask contract: objective, output schema, tool hints, scope predicate | reopen not wired | Contract.SubtaskContract declares objective, output schema, tool hints and scope predicate, but brain.orchestration is imported by no route, startup step or worker task (the only production import is adoption.py taking the TriggerKind enum from orchestration.unattended), no agent exists on the install, and no model has been called. |
| M18.1.3 | Schema validation on every subtask result, failure rather than merge input | reopen not wired | Contract validation turns a result that fails its schema into a failure rather than merge input, but brain.orchestration is imported by no route, startup step or worker task (the only production import is adoption.py taking the TriggerKind enum from orchestration.unattended), no agent exists on the install, and no model has been called. |
| M18.1.4 | Prose explicitly disallowed as a subtask contract | reopen not wired | The contract refuses prose as a subtask contract, but brain.orchestration is imported by no route, startup step or worker task (the only production import is adoption.py taking the TriggerKind enum from orchestration.unattended), no agent exists on the install, and no model has been called. |
| M18.2.1 | Dependency graph with cost and coupling weights | reopen not wired | Plan.DependencyGraph carries cost and coupling weights, but brain.orchestration is imported by no route, startup step or worker task (the only production import is adoption.py taking the TriggerKind enum from orchestration.unattended), no agent exists on the install, and no model has been called. |
| M18.2.2 | Six arithmetic conditions evaluated before any child starts | reopen not wired | Fanout.decide evaluates six arithmetic conditions (HOW_MANY_CONDITIONS = 6) before any child starts, but brain.orchestration is imported by no route, startup step or worker task (the only production import is adoption.py taking the TriggerKind enum from orchestration.unattended), no agent exists on the install, and no model has been called. |
| M18.2.3 | Collapse to a single loop as the default when any condition fails | reopen not wired | fanout.decide collapses to a single loop when any condition fails, but brain.orchestration is imported by no route, startup step or worker task (the only production import is adoption.py taking the TriggerKind enum), no agent exists on the install, and no model has been called. |
| M18.2.4 | Gate inputs and verdict recorded for audit | reopen not wired | GateInputs and GateRecord hold the inputs and verdict for audit, but nothing writes a GateRecord to the ledger or any table, and brain.orchestration is imported by no route, startup step or worker task (the only production import is adoption.py taking the TriggerKind enum), no agent exists on the install, and no model has been called. |
| M18.3.1 | Child grant computed in SQL as parent intersect agent intersect subtask | reopen not wired | Migration 0028 installs the SQL functions that compute parent intersect agent intersect subtask, but they run only inside the gate.delegation insert trigger and nothing in src inserts a delegation row (60087ad: Nothing writes a delegation row), so no child grant has ever been computed in SQL on the install. |
| M18.3.2 | Database trigger rejecting any non-subset delegation row | reopen not wired | The trigger is on the install (gate.delegation: RLS on, 1 policy, 1 trigger, INSERT only for brain_app), but nothing in src writes a delegation row, so it guards a table no path on the install reaches and has never refused anything there. |
| M18.3.3 | Orchestrator has no grant verb, asserted | reopen not wired | Delegation.grant_verbs_in checks by AST that the orchestration modules call nothing that writes a grant, and a test asserts it; the property is only ever checked by that test, because brain.orchestration is imported by no route, startup step or worker task (the only production import is adoption.py taking the TriggerKind enum), no agent exists on the install, and no model has been called. |
| M18.3.4 | Depth cap of two and per-run concurrency cap of eight | reopen not wired | MAX_DELEGATION_DEPTH = 2 and MAX_CHILDREN_PER_RUN = 8 are enforced by chain_refusals and concurrency_refusals, but brain.orchestration is imported by no route, startup step or worker task (the only production import is adoption.py taking the TriggerKind enum), no agent exists on the install, and no model has been called. |
| M18.3.5 | Global parallelism budget across all runs | reopen not wired | May_fan_out admits children against the global LONG_RUNNING_TASKS budget through ops.admission, but fan_out_request and may_fan_out have no caller; brain.orchestration is imported by no route, startup step or worker task (the only production import is adoption.py taking the TriggerKind enum), no agent exists on the install, and no model has been called. |
| M18.4.1 | Merge step writing the final answer from validated inputs | reopen not wired | Merge.merge writes the final answer from validated subtask inputs, but brain.orchestration is imported by no route, startup step or worker task (the only production import is adoption.py taking the TriggerKind enum), no agent exists on the install, and no model has been called. |
| M18.4.2 | Per-subtask cost and duration surfaced to the user | reopen not wired | SubtaskReport carries per-subtask cost and duration for the asker, but no route or screen shows one (Ask has never been used and nothing runs subtasks); brain.orchestration is imported by no route, startup step or worker task (the only production import is adoption.py taking the TriggerKind enum), no agent exists on the install, and no model has been called. |
| M18.4.3 | Partial-failure handling with a stated gap | reopen not wired | Gap and GapCause state what is missing when a subtask fails or is refused, but brain.orchestration is imported by no route, startup step or worker task (the only production import is adoption.py taking the TriggerKind enum), no agent exists on the install, and no model has been called. |

### M19 Browser and computer use

| Task | What it asks | Verdict | Why |
|---|---|---|---|
| M19.1.4 | Accessibility-tree snapshots rather than screenshots | reopen absent | Snapshots are proven only at the type level in observation.py; nothing on the install produces a snapshot because no runner, browser or CDP worker exists (M19.1.1 to M19.1.3 are still open). Needs: browser sandbox (gVisor runner, launcher, mitmproxy egress, Playwright/Chromium) and the worker side of the channel. |
| M19.2.1 | Planner that never reads page content, asserted by test | reopen absent | The planner cannot import observation, asserted by an import-graph test, but no browser run has ever been planned on the install, so the refusal has only been shown over test objects. Needs: browser sandbox (gVisor runner, launcher, mitmproxy egress, Playwright/Chromium) and the worker side of the channel. |
| M19.2.2 | Envelope compilation: plan intersected with capabilities and skill ceiling | reopen absent | Envelope compilation (plan intersected with capabilities and skill ceiling) is a pure function nothing on the install calls; no browser run, agent or skill exists there. Needs: browser sandbox (gVisor runner, launcher, mitmproxy egress, Playwright/Chromium) and the worker side of the channel. |
| M19.2.3 | Envelope storage and approval record | reopen absent | agent.browser_envelope is on the install (RLS on, 3 policies, brain_app INSERT/SELECT), but its only writer, browsing.envelope_store, is called by nothing the install runs, so no envelope or approval record has ever been stored. Needs: browser sandbox (gVisor runner, launcher, mitmproxy egress, Playwright/Chromium) and the worker side of the channel. |
| M19.2.4 | Target registry with declared surface maps | reopen absent | The target registry and surface maps are code in browsing/targets.py; no browser run on the install ever resolves a target against them. Needs: browser sandbox (gVisor runner, launcher, mitmproxy egress, Playwright/Chromium) and the worker side of the channel. |
| M19.3.1 | Enforcer with policy compiled at container start and immutable after | reopen absent | The policy is meant to compile at container start, and no browser container ever starts on the install. Needs: browser sandbox (gVisor runner, launcher, mitmproxy egress, Playwright/Chromium) and the worker side of the channel. |
| M19.3.2 | Reference validation against the snapshot just produced | reopen absent | Reference validation checks an action against the snapshot just produced, and nothing on the install produces snapshots or actions. Needs: browser sandbox (gVisor runner, launcher, mitmproxy egress, Playwright/Chromium) and the worker side of the channel. |
| M19.3.3 | Origin allowlist check per action | reopen absent | The origin allowlist check runs per browser action and in the egress proxy addon, neither of which runs on the install. Needs: browser sandbox (gVisor runner, launcher, mitmproxy egress, Playwright/Chromium) and the worker side of the channel. |
| M19.3.4 | Write verb and count checks against the envelope | reopen absent | Write verb and count checks apply to browser actions against a stored envelope, and no envelope or action exists on the install. Needs: browser sandbox (gVisor runner, launcher, mitmproxy egress, Playwright/Chromium) and the worker side of the channel. |
| M19.3.6 | Second independent check in the control plane on every returned result | reopen absent | The second check re-verifies results returned by the runner, and no runner returns anything on the install. Needs: browser sandbox (gVisor runner, launcher, mitmproxy egress, Playwright/Chromium) and the worker side of the channel. |
| M19.4.1 | Placeholder resolution at the tool-execution boundary | reopen absent | Placeholder resolution happens inside ops.secrets.borrow at the tool-execution boundary of a browser run; there is no browser run and no secrets vault on the install. Needs: browser sandbox (gVisor runner, launcher, mitmproxy egress, Playwright/Chromium) and the worker side of the channel; secrets vault. |
| M19.4.2 | Origin binding so a secret only substitutes on its own domain | reopen absent | Origin binding is checked when a placeholder resolves, which needs a stored credential in the vault and a browser action, neither present on the install. Needs: browser sandbox (gVisor runner, launcher, mitmproxy egress, Playwright/Chromium) and the worker side of the channel; secrets vault. |
| M19.4.3 | TOTP generation server-side from a stored seed | reopen absent | TOTP generation needs a seed held in the secrets vault and a browser sign-in to use it; the install has neither. Needs: browser sandbox (gVisor runner, launcher, mitmproxy egress, Playwright/Chromium) and the worker side of the channel; secrets vault. |
| M19.4.4 | Outbound scrubbing from message history | reopen absent | scrub_outbound is reached on the install only by log capture and answer screening, which scrub known process secrets, not by a browser message history, which does not exist there. Needs: browser sandbox (gVisor runner, launcher, mitmproxy egress, Playwright/Chromium) and the worker side of the channel; secrets vault. |
| M19.4.5 | Vision disabled so a credential cannot be read off a screenshot | reopen absent | Vision disabled is a property of the browser runner configuration, and no runner runs on the install. Needs: browser sandbox (gVisor runner, launcher, mitmproxy egress, Playwright/Chromium) and the worker side of the channel. |
| M19.5.1 | Rubric generated from the goal before seeing the trajectory | reopen absent | build_rubric takes a request and target before any trajectory, but no browser run has been graded on the install. Needs: browser sandbox (gVisor runner, launcher, mitmproxy egress, Playwright/Chromium) and the worker side of the channel. |
| M19.5.2 | Two-pass scoring: actions only, then with screenshots | reopen absent | verify_run scores actions and then masked pictures, and there are no runs, actions or pictures on the install to score. Needs: browser sandbox (gVisor runner, launcher, mitmproxy egress, Playwright/Chromium) and the worker side of the channel. |
| M19.5.3 | Agent's own success claim ignored | reopen absent | The success claim is carried and never scored in grading.py, shown only over test runs; nothing on the install has been graded. Needs: browser sandbox (gVisor runner, launcher, mitmproxy egress, Playwright/Chromium) and the worker side of the channel. |
| M19.5.4 | Tuned for low false-positive rate, accepting false negatives | reopen absent | Tuning for a low false-positive rate needs measured runs, and none exists on the install; the closing commit also lost its mutation table. Needs: browser sandbox (gVisor runner, launcher, mitmproxy egress, Playwright/Chromium) and the worker side of the channel. |
| M19.5.5 | Out-of-band read-back from the system of record where possible | reopen absent | read_back asks a registered tool through ReadBackPort as the asker, which needs a browser run and a connected system of record; the install has neither. Needs: browser sandbox (gVisor runner, launcher, mitmproxy egress, Playwright/Chromium) and the worker side of the channel; connector account. |
| M19.6.1 | Global browser concurrency limit | reopen absent | The global browser-session budget is admission.decide over a resource no process on the install ever requests, since no browser session can start. Needs: browser sandbox (gVisor runner, launcher, mitmproxy egress, Playwright/Chromium) and the worker side of the channel. |
| M19.6.2 | Per-domain concurrency limit | reopen absent | browsing.concurrency.may_open checks per-origin room, and nothing on the install opens a browser session. Needs: browser sandbox (gVisor runner, launcher, mitmproxy egress, Playwright/Chromium) and the worker side of the channel. |
| M19.6.3 | Per-agent limit | reopen absent | browsing.concurrency.may_open checks the per-agent axis, and the install has no agent and no browser session. Needs: browser sandbox (gVisor runner, launcher, mitmproxy egress, Playwright/Chromium) and the worker side of the channel. |
| M19.6.5 | Global kill switch | reopen absent | The browser consults the ops.halt running-signal, but no browser run exists on the install for the switch to stop. Needs: browser sandbox (gVisor runner, launcher, mitmproxy egress, Playwright/Chromium) and the worker side of the channel. |
| M19.6.7 | Skill loader refusing a browser skill where an API tool exists, unless exempted with a named approver and expiry | reopen not wired | browsing/skill_gate.py decides the refusal and the exemption, but nothing outside the browsing package imports it; the skill import route and skill store never consult it. To prove it: Import a browser skill on the install for a target that has an API tool, and see it refused unless an exemption names an approver and an expiry.. |
| M19.7.1 | Read-only capability shipped first | reopen absent | No browser capability, read-only or otherwise, is available to anything on the install; nothing that runs there was shipped. Needs: browser sandbox (gVisor runner, launcher, mitmproxy egress, Playwright/Chromium) and the worker side of the channel. |
| M19.7.2 | Write capability behind envelope approval | reopen absent | The write tool browser.act_on_surface raises the envelope as a leash suspension, but nothing on the install can start a browser run, and gate.suspension and agent.browser_envelope are empty. Needs: browser sandbox (gVisor runner, launcher, mitmproxy egress, Playwright/Chromium) and the worker side of the channel. |
| M19.7.3 | Assisted ceiling on writes unless a per-surface exception is signed | reopen absent | browsing.autonomy keeps writes at Assisted through rung_ceiling, and no browser write can occur on the install. Needs: browser sandbox (gVisor runner, launcher, mitmproxy egress, Playwright/Chromium) and the worker side of the channel. |

### M20 Verz Studio, the builder

| Task | What it asks | Verdict | Why |
|---|---|---|---|
| M20.1.1 | Seven sections: identity, persona, knowledge, skills, tools, leash, tests | reopen not wired | In code, compose.Section defines the seven sections (identity, persona, knowledge, skills, tools, leash, tests), but brain.builder has no router mounted in brain.app and no table (drafts live in memory), and the console builder components (ManifestForm, ProcedureCanvas, TraceGraph) are imported by no page or route in console/src/App.tsx; the only production use of brain.builder is member_activity importing publish.widened_capabilities. |
| M20.1.2 | Forms generated from the manifest JSON Schema via react-jsonschema-form | reopen not wired | In code, form.form_document and ManifestForm.tsx generate forms from the manifest JSON Schema with react-jsonschema-form, but brain.builder has no router mounted in brain.app and no table (drafts live in memory), and the console builder components (ManifestForm, ProcedureCanvas, TraceGraph) are imported by no page or route in console/src/App.tsx; the only production use of brain.builder is member_activity importing publish.widened_capabilities. |
| M20.1.3 | AI co-author proposing accept or reject diffs | reopen not wired | In code, coauthor.propose and accept offer accept or reject diffs from a model, but brain.builder has no router mounted in brain.app and no table (drafts live in memory), and the console builder components (ManifestForm, ProcedureCanvas, TraceGraph) are imported by no page or route in console/src/App.tsx; the only production use of brain.builder is member_activity importing publish.widened_capabilities. |
| M20.1.4 | Draft, save and version handling | reopen not wired | In code, drafts.save appends immutable revisions and publish_draft publishes the latest, but brain.builder has no router mounted in brain.app and no table (drafts live in memory), and the console builder components (ManifestForm, ProcedureCanvas, TraceGraph) are imported by no page or route in console/src/App.tsx; the only production use of brain.builder is member_activity importing publish.widened_capabilities. |
| M20.1.5 | Everything re-derived server-side, so an authored list is intent not authorisation | reopen not wired | In code, compose re-derives reach server-side so an authored list is intent and never authorisation, but brain.builder has no router mounted in brain.app and no table (drafts live in memory), and the console builder components (ManifestForm, ProcedureCanvas, TraceGraph) are imported by no page or route in console/src/App.tsx; the only production use of brain.builder is member_activity importing publish.widened_capabilities. |
| M20.2.1 | Read-only trace graph over completed runs using React Flow | reopen not wired | In code, TraceGraph.tsx draws a read-only graph of a CompletedRun, but no GET /runs/{run_id}/trace route exists (94c41a2 lists it as still owed) and no run has ever completed on the install; brain.builder has no router mounted in brain.app and no table, and the console builder components (ManifestForm, ProcedureCanvas, TraceGraph) are imported by no page or route in console/src/App.tsx; the only production use of brain.builder is member_activity importing publish.widened_capabilities. |
| M20.2.2 | Bounded procedure canvas outputting a SKILL.md | reopen not wired | In code, procedure.read_drawing and skill_markdown turn a bounded ProcedureCanvas drawing into a SKILL.md that passes skill_from_markdown; brain.builder has no router mounted in brain.app and no table, and the console builder components (ManifestForm, ProcedureCanvas, TraceGraph) are imported by no page or route in console/src/App.tsx; the only production use of brain.builder is member_activity importing publish.widened_capabilities. |
| M20.2.3 | Five node kinds with scope predicates as the only conditional grammar | reopen not wired | In code, compose.NodeKind has five node kinds with scope predicates as the only conditional; brain.builder has no router mounted in brain.app and no table, and the console builder components (ManifestForm, ProcedureCanvas, TraceGraph) are imported by no page or route in console/src/App.tsx; the only production use of brain.builder is member_activity importing publish.widened_capabilities. |
| M20.2.4 | No code node, enforced | reopen not wired | In code, NodeKind has no member for a code node, so none can be drawn; brain.builder has no router mounted in brain.app and no table, and the console builder components (ManifestForm, ProcedureCanvas, TraceGraph) are imported by no page or route in console/src/App.tsx; the only production use of brain.builder is member_activity importing publish.widened_capabilities. |
| M20.3.1 | Run against the real gate, leash and redactor | reopen not wired | In code, rehearse puts every take through brain.gate.leash.govern and the real redactor, but brain.builder has no router mounted in brain.app and no table, and the console builder components (ManifestForm, ProcedureCanvas, TraceGraph) are imported by no page or route in console/src/App.tsx; the only production use of brain.builder is member_activity importing publish.widened_capabilities; there is also no agent or automation on the install to rehearse. |
| M20.3.2 | Cassette replay so no real side effect fires | reopen not wired | In code, rehearse hands one recorded replay to the gate as both simulate and execute so nothing fires, but brain.builder has no router mounted in brain.app and no table, and the console builder components (ManifestForm, ProcedureCanvas, TraceGraph) are imported by no page or route in console/src/App.tsx; the only production use of brain.builder is member_activity importing publish.widened_capabilities; there is also no agent or automation on the install to rehearse. |
| M20.3.3 | Per-persona rehearsal showing what each user would see | reopen not wired | In code, publish.reach_refusals and shown pair a rehearsal with the persona whose reach it runs at, but brain.builder has no router mounted in brain.app and no table, and the console builder components (ManifestForm, ProcedureCanvas, TraceGraph) are imported by no page or route in console/src/App.tsx; the only production use of brain.builder is member_activity importing publish.widened_capabilities; there is also no agent or automation on the install to rehearse. |
| M20.3.4 | Measured cost per run from the rehearsal | reopen not wired | In code, rehearse.measured_cost prices the tool calls the gate let through with spend.estimate, but brain.builder has no router mounted in brain.app and no table, and the console builder components (ManifestForm, ProcedureCanvas, TraceGraph) are imported by no page or route in console/src/App.tsx; the only production use of brain.builder is member_activity importing publish.widened_capabilities; there is also no agent or automation on the install to rehearse. |
| M20.4.1 | System-authored permission canaries that can block | reopen not wired | In code, Check with CheckOrigin.SYSTEM permission canaries and blocking_failures, but brain.builder.publish has no route (no builder router is mounted in brain.app) and no store for a publish record, and there is no draft, agent or template on the install to publish; its one production use is member_activity calling widened_capabilities for the My workspace agent list. |
| M20.4.2 | Author-written tests advisory forever | reopen not wired | In code, author-written checks are advisory and never block (CheckOrigin.AUTHOR), but brain.builder.publish has no route (no builder router is mounted in brain.app) and no store for a publish record, and there is no draft, agent or template on the install to publish; its one production use is member_activity calling widened_capabilities for the My workspace agent list. |
| M20.4.3 | Grant-widening demotes to Shadow and requires a second approver | reopen not wired | In code, decide demotes a widening publish to SHADOW (rung_after) and approvers_needed adds a second approver, but brain.builder.publish has no route (no builder router is mounted in brain.app) and no store for a publish record, and there is no draft, agent or template on the install to publish; its one production use is member_activity calling widened_capabilities for the My workspace agent list. |
| M20.4.4 | Instruction edits publish without demotion | reopen not wired | In code, instruction_only lets an instruction-only edit publish without demotion, but brain.builder.publish has no route (no builder router is mounted in brain.app) and no store for a publish record, and there is no draft, agent or template on the install to publish; its one production use is member_activity calling widened_capabilities for the My workspace agent list. |
| M20.4.5 | Router collision check | reopen not wired | In code, binding_collisions checks router collisions without naming the other agent, but brain.builder.publish has no route (no builder router is mounted in brain.app) and no store for a publish record, and there is no draft, agent or template on the install to publish; its one production use is member_activity calling widened_capabilities for the My workspace agent list. |
| M20.4.6 | Publish record with who, when and the diff | reopen not wired | In code, record_publish builds a PublishRecord with who, when and the changed paths, but brain.builder.publish has no route (no builder router is mounted in brain.app) and no store for a publish record, and there is no draft, agent or template on the install to publish; its one production use is member_activity calling widened_capabilities for the My workspace agent list. |

### M21 Cost, budgets and metering

| Task | What it asks | Verdict | Why |
|---|---|---|---|
| M21.1.1 | Company budget with hard ceiling and alert thresholds | reopen not wired | The company level exists as BudgetRow/Allowance with alert fractions, but nothing on the install can write a budget row (budget_store.append has no caller) and nothing enforces one before a model call (spend.preflight has no caller). To prove it: Set a company budget on the install, then see a question refused at the hard ceiling and an alert at a threshold.. |
| M21.1.2 | Department budget as a share, with explicit transfer only | reopen not wired | Department shares and explicit transfers are decided in budgets.py, but no department exists on the install, no budget row can be written, and no transfer route calls it. To prove it: A department budget set as a share of the company budget and an explicit transfer recorded between two departments on the install.. |
| M21.1.3 | User daily and monthly allowance with a fair-share cap | reopen not wired | The user allowance with the fair-share cap is modelled and My workspace reads a user row through in_force, but nothing writes one and nothing checks a question against it before it is answered. To prove it: A user daily allowance set on the install that refuses a question once exhausted and shows on My workspace.. |
| M21.1.4 | Per-agent per-run and per-day ceilings | reopen not wired | Per-agent per-run and per-day ceilings exist as budget levels only; the install has no agent, no budget row and no pre-flight check on any run. To prove it: An agent run on the install refused at its per-run ceiling.. |
| M21.1.5 | All levels as versioned, audited rows | reopen partial | ops.budget_version is on the install with its four triggers (follows, refuses amendment, removal, truncation), but nothing writes a row (budget_store.append has no caller) and the rows are not audited into obs.audit_entry, which the closing commit states. To prove it: A budget version written from the console on the install that appears in the audit ledger.. |
| M21.2.1 | Pre-flight cost estimate from lane, tier, tool count and retrieval size | reopen not wired | spend.estimate is pure and is called only by the builder rehearsal and orchestration.plan, not by the ask route; no question has been estimated on the install. To prove it: A question asked on the install whose pre-flight estimate from lane, tier, tool count and retrieval size is recorded.. |
| M21.2.2 | Refusal or degradation before tokens are spent | reopen not wired | spend.preflight, which refuses or degrades before tokens are spent, has no caller on any request path. To prove it: A question on the install over an exhausted allowance refused or degraded before any model call, with ops.model_attempt still empty for it.. |
| M21.2.3 | Post-hoc accounting against principal, department, agent, model and lane | reopen not wired | Actuals are attributed in spend.Actual and stored by spend_store.record, which has no caller; model metering writes tokens onto request telemetry only, and no model has ever been called on the install. To prove it: A question answered on the install that writes an ops.spend_actual row attributed to principal, department, agent, model and lane.. |
| M21.2.4 | Estimator correction from actuals | reopen not wired | spend.correct and retune.post_launch_correction are reached only by the spend view over actuals, and no actual exists or can be written on the install, so the estimator has never been corrected. To prove it: Enough answered questions on the install to move the estimator correction, shown on the Spend screen.. |
| M21.2.5 | Degradation ladder: cheaper tier, no fan-out, queue, refuse | reopen not wired | The degradation ladder (cheaper tier, no fan-out, queue, refuse) is applied only inside spend.preflight, which no request path calls. To prove it: A question on the install near its allowance answered on a cheaper tier, then queued, then refused.. |
| M21.2.6 | Message naming which budget was hit and who can raise it | reopen not wired | The Refusal sentence naming the budget and the role that can raise it is produced only by preflight, which nothing calls; no person on the install has ever been told a budget was hit. To prove it: A question on the install refused over a budget, answered with the sentence naming the level, the period and the role that can raise it.. |
| M21.3.1 | Spend by department, user, agent, model and lane | reopen not wired | The Spend screen is routed, but its report reads ops.spend_daily, refreshed only by a worker control, over ops.spend_actual, which nothing writes; on the install it says the report has not been built yet. Needs: worker (spend_daily refresh). To prove it: The Spend screen on the install showing real spend broken down by department, user, agent, model and lane.. |
| M21.3.2 | Machine traffic excluded and labelled | reopen not wired | Actual.machine and the include_machine flag exist, but no actual is ever written, so nothing on the install has been excluded or labelled. To prove it: A machine-originated request on the install whose cost is excluded by default and labelled on the Spend screen.. |
| M21.3.3 | Cost per answer with the distribution, not only the mean | reopen not wired | The quantile logic withholds figures below a derived sample, but there are no actuals on the install, so no cost per answer or distribution can be shown. To prove it: At least ten answered questions on the install and the Spend screen showing the cost per answer median and ninetieth percentile.. |
| M21.3.4 | Most expensive agents and question shapes | reopen not wired | shape_report joins costs to trace-ledger shapes by trace id, but ops.spend_actual is written by nothing and obs.request_telemetry is empty on the install. To prove it: The Spend screen on the install listing the most expensive agents and question shapes from real answered questions.. |
| M21.3.5 | Budget consumption against pace | reopen not wired | Pace is computed over a budget row and its consumption, and the install has neither a budget row (nothing writes ops.budget_version) nor any spend. To prove it: A company or department budget on the install shown with its consumption against pace.. |
| M21.3.6 | Every refusal and degradation with its cause | reopen not wired | Refusals and degradations are produced only by preflight, which nothing calls, so there are none on the install to list with their causes. To prove it: A refused and a degraded question on the install listed on the Spend screen with their causes.. |

### M22 Admission control and capacity

| Task | What it asks | Verdict | Why |
|---|---|---|---|
| M22.1.1 | Global budgets: concurrent model calls, source calls per connector, browser sessions, long-running tasks, document jobs, embedding jobs, tokens per minute | reopen not wired | admission.decide is reached only from browser sessions (no sandbox) and upload ingestion (worker); seed_budgets has no caller, so no global budget applies to any request. |
| M22.1.2 | All budgets as configuration rows | reopen partial | Budget is an in-code dataclass, not a table: no configuration table holds budgets on the install and seed_budgets has no caller. |
| M22.1.3 | Priority behaviour under pressure per lane | reopen not wired | Priority under pressure lives in decide, which nothing on the install calls. |
| M22.1.4 | Queue with visible position and expected wait | reopen not wired | QueuePlacement is produced only by decide, which nothing on the install calls. |
| M22.1.5 | Shed-load policy naming what was deferred | reopen not wired | shed_plan has no caller. |
| M22.2.1 | Three classes: interactive, background, batch | reopen not wired | workload_class_for has no caller; no request is classed. |
| M22.2.2 | Separate connection pools per class | reopen not wired | The app has one engine with pool_size 5; pools_for has no caller. |
| M22.2.3 | Per-class resource budgets | reopen not wired | CLASS_CEILING is applied only inside decide, which nothing on the install calls. |
| M22.2.4 | Ingestion throttling so a bulk parse cannot slow the chat | reopen absent | Ingestion throttling is on the upload ingestion path, which only connector sync on the worker reaches. Needs: worker and parse worker. |
| M22.3.1 | Peak concurrency targets rather than daily volume | reopen not wired | seed_profiles has no caller. |
| M22.3.2 | Little's law sizing recorded per profile | reopen not wired | little_law_concurrency is called only by ops/scaling.py, which nothing imports. |
| M22.3.3 | Load test at peak concurrency | reopen partial | The test drives decide in memory; no HTTP, database or model is loaded and the latency conditions are left open (2e81cd3). |
| M22.3.4 | Documented first bottleneck at ten and one hundred times | reopen partial | Stated only as an unreferenced constant in admission.py; no doc of record or screen carries it. |

### M23 Rate limiting and abuse

| Task | What it asks | Verdict | Why |
|---|---|---|---|
| M23.1.1 | Per-principal sliding window in Valkey | reopen not wired | limit_store has no importer and request_limits no caller; valkey holds 0 keys and Rate limits says throttling cannot be read. |
| M23.1.2 | Per-channel limits | reopen not wired | request_limits (per-channel) has no caller. |
| M23.1.3 | Per-agent limits | reopen not wired | request_limits (per-agent) has no caller. |
| M23.1.4 | Widget session minting with an abuse guard | reopen not wired | No widget router is mounted in brain.app; mint_widget_session is reached only from channels/widget.py. |
| M23.1.5 | Backoff response with a retry hint | reopen not wired | QuotaExceeded is never raised on any route, so no backoff response is sent. |
| M23.2.1 | Anomalous volume detection per principal | reopen not wired | assess_volume has no caller. |
| M23.2.2 | Repeated permission-denied pattern alerting | reopen not wired | Nothing builds a DenialPattern or runs digest, and no channel sends alerts. |
| M23.2.3 | Automated traffic identification and exclusion from metrics | reopen absent | is_automated is read by the usage, spend and adoption views, but request telemetry is 0 and the install has no automated traffic source. Needs: automated traffic (worker automations or channels). |

### M24 Audit, compliance, legal hold

| Task | What it asks | Verdict | Why |
|---|---|---|---|
| M24.1.1 | Hash-chained append-only audit table | keep install | obs.audit_entry is on the install with RLS, INSERT and SELECT only for brain_app and three triggers, and holds 80 trigger-written chained entries. |
| M24.1.2 | Verification job proving the chain is unbroken | reopen not wired | brain.audit.verify is not imported by the application and no worker runs a verification job on the install. To prove it: Run the chain verification over the 80 entries on the install and show it reports the chain unbroken.. |
| M24.1.3 | Every grant, deny, leash change, merge, publish and break-glass recorded | reopen unproven | Only grant, routing, sign_in and setting entries exist on the install; no deny, leash change, merge, publish or break-glass has ever been recorded there. To prove it: Produce a denial, a leash change, a merge, a publish and an elevation, and show each ledger entry.. |
| M24.1.4 | Actor, timestamp, changed field names, reason code; never field values | keep install | The Audit screen lists the 80 real entries with actor, time and action, and the triggers write field names and reason codes rather than values. |
| M24.1.5 | Client-visible audit view with filters | keep install | The Audit screen is served, filters and pages over the 80 real entries for the administrator. |
| M24.1.6 | Export in a defensible format | reopen unproven | The Import and export page offers the audit trail export, but no export has ever been produced on the install. To prove it: Export the audit trail from Import and export and verify the file against the chain.. |
| M24.2.1 | Legal hold flag suspending deletion | reopen absent | No legal hold has been set, and deletion that a hold would suspend runs in retention sweeps on the worker, which the install does not run. Needs: worker running the retention sweep. To prove it: Place a hold, run the retention sweep, and show the held rows kept.. |
| M24.2.2 | Sensitive-topic interception for HR, grievance and whistleblowing | reopen unproven | Sensitive-topic interception sits on the question path, which has never run on the install. To prove it: Ask an HR grievance question and show it intercepted and routed as designed.. |
| M24.2.3 | Data processing record per connector | reopen absent | A data processing record per connector needs a connected source, and the install has none. Needs: a connected source (vault plus connector credentials). |
| M24.2.4 | PDPA breach assessment workflow with the correct clock | reopen unproven | No breach assessment has ever been opened on the install, so the workflow and its clock have not been shown. To prove it: Open a breach assessment on the install and show the deadline computed from discovery and the steps recorded.. |

### M25 Retention, deletion, export, DSAR

| Task | What it asks | Verdict | Why |
|---|---|---|---|
| M25.1.1 | Retention policy per data class | reopen absent | Retention policy per data class is declared in brain.ops.retention, but it is applied only by the retention sweep, a worker control that has never run on the install. Needs: worker. |
| M25.1.2 | Metadata ledger long retention | reopen absent | Long retention of the metadata ledger is enforced by the worker sweep, which never ran; nothing on the install has applied it. Needs: worker. |
| M25.1.3 | Payload store thirty-day expiry | reopen absent | The thirty-day payload expiry needs the object store and the worker sweep; the install has neither. Needs: object store and worker. |
| M25.1.4 | Trace and recording lifecycle | reopen absent | Trace and recording lifecycles need a trace store (Langfuse/OTel) and browser recordings, and the worker sweep; none exists on the install. Needs: trace store, browser recordings, worker. |
| M25.1.5 | Automated enforcement with a report | reopen absent | Automated enforcement with a report runs only on the worker schedule; the Retention screen shows no report and the sweep has never run. Needs: worker. |
| M25.2.1 | Subject access request assembling every store: rows, projections, memory, traces, recordings, backups | reopen not wired | The Import and export screen says the DSAR is not available because the part that reads each store for one person is not built. To prove it: A subject access request on the install that returns rows, projections, memory, traces, recordings and backups for one person.. |
| M25.2.2 | Deletion propagating to cache, index, memory and projection | reopen absent | Deletion propagation is carried out by the erasure worker, which does not run; ops.erasure_request is empty. Needs: erasure worker. |
| M25.2.3 | Backup deletion policy and its honest limits | reopen absent | The install has no database backup job, so there is no backup for a deletion policy to apply to. Needs: backups. |
| M25.2.4 | Deletion certificate with what was removed and when | reopen absent | A deletion certificate is produced when the erasure worker completes an erasure, and no erasure worker or erasure exists on the install. Needs: erasure worker. |
| M25.3.1 | Conversation export per user | reopen not wired | The Import and export screen lists the own conversations of a person as not available. To prove it: A person exporting their own conversations from the install.. |
| M25.3.2 | Bulk export for eDiscovery with an audit row and a required reason | reopen not wired | Legal and regulatory collection is shown as Not available yet because nothing reads the stores; ops.data_export is empty. To prove it: A bulk eDiscovery export on the install that requires a reason and writes an audit row.. |
| M25.3.3 | Knowledge export preserving scope metadata | reopen not wired | Knowledge export is shown as Not available yet on the install, and there is no knowledge to export. To prove it: A knowledge export on the install carrying each item scope metadata.. |

### M26 Joiner, mover, leaver

| Task | What it asks | Verdict | Why |
|---|---|---|---|
| M26.1.1 | Provisioning from the identity provider | reopen absent | Nothing provisions a principal from the identity provider: the directory sync is a worker control, the install reads no staff source, and its one person was bound by hand. Needs: worker and a staff directory source. |
| M26.1.2 | Role and starter capability pack assignment | reopen not wired | lifecycle.provision (role plus starter pack for a joiner) has no caller and gate.capability_pack_assignment is empty; only the first administrator was appointed, by a different path. To prove it: A second person joining on the install and receiving their role and starter pack.. |
| M26.1.3 | Department assignment with scope | reopen not wired | No department exists on the install and nothing creates one, so no joiner can be assigned to one with a scope. To prove it: A joiner assigned to a department on the install with the department scope on their grants.. |
| M26.1.4 | Welcome path with three department-scoped starter questions | reopen not wired | welcome_questions has no caller and no screen offers a welcome path. To prove it: A new person on the install shown three department-scoped starter questions on first sign-in.. |
| M26.2.1 | Entitlement re-resolution on the next request | reopen unproven | Entitlements are resolved on every authenticated request, but no grant has changed for a signed-in person on the install, so re-resolution after a move has never been shown. To prove it: Change a grant of a signed-in person on the install and see the next request reflect it.. |
| M26.2.2 | Personal agents following the person | reopen not wired | agents_following has no caller and the install has no agents. |
| M26.2.3 | Delegations re-evaluated and suspended rather than narrowed | reopen not wired | reevaluate_delegations has no caller and gate.delegation is empty. |
| M26.2.4 | Cached answers unreachable by construction | reopen unproven | The answer cache key carries the entitlement hash, but no answer has ever been cached on the install (Valkey holds 0 keys). To prove it: An answer cached for a person, their grants changed, and the same question not served from the old entry.. |
| M26.2.5 | Memories tagged with the old capability set no longer replayed | reopen not wired | replayable_after has no caller and no memory exists or can be formed on the install. |
| M26.2.6 | Knowledge access changing with no reindex | reopen absent | knowledge_reach_after has no caller and the install holds no knowledge; documents cannot be brought in yet. Needs: knowledge ingestion (parse worker). |
| M26.2.7 | Test covering every one of the above | reopen not wired | tests/unit/test_lifecycle.py covers the lifecycle functions over test objects, but every function it tests has no production caller, so the test proves nothing the install does. |
| M26.3.1 | Immediate disable with session termination | reopen not wired | disable_principal in console/global_surfaces.py has no caller and no screen disables a person; only individual sessions can be ended. To prove it: Disable a person on the install and see every session of theirs end and further tokens refused.. |
| M26.3.2 | Owned agents flagged for adoption | reopen not wired | agents_for_adoption has no caller and the install has no agents. |
| M26.3.3 | Automations suspended | reopen not wired | automations_to_stop has no caller, and the automation pause on a lost owner runs only in the absent worker. Needs: worker. |
| M26.3.4 | Personal knowledge and memory disposition policy | reopen not wired | departure_disposition has no caller; no leaver flow applies a knowledge or memory disposition. |
| M26.3.5 | Audit record retained regardless | keep install | obs.audit_entry holds 80 real rows on the install and brain_app has only INSERT and SELECT on it (no UPDATE or DELETE), so an audit record survives whatever happens to the person. |

### M27 Console, reports, observability

| Task | What it asks | Verdict | Why |
|---|---|---|---|
| M27.1.1 | Trace id minted at ingress before identify | keep install | The app's trace middleware mints a trace id before any route identifies the caller; the 80 audit entries carry trace ids. |
| M27.1.2 | Metadata ledger with no content, long retention | keep install | obs.request_telemetry exists on the install (partitioned, RLS, INSERT/SELECT for brain_app) with names and counts only. |
| M27.1.3 | Payload store with a separate role and an audit row before every read | reopen absent | No payload store exists: trace_sink drops the payload and read_payload_with_role has no production caller. Needs: trace payload store (unbuilt; no Langfuse/OTel). |
| M27.1.4 | Client-side masking before any span leaves the process | reopen absent | No span leaves the process: span_for has no production caller and no collector is configured. Needs: OTel/Langfuse collector. |
| M27.1.5 | Per-request fields: principal, agent version, policy epoch, entitlement hash, lane, model, provider, time to first token, tokens in and out, tool count and latency, connector, cache hit, redaction count, fallback count, retry count, status | reopen partial | Five named fields are declared unfillable and no request row was ever written on the install. |
| M27.1.6 | Traffic class declared at ingress with no default | reopen not wired | open_request and gate.context.open_trace have no production caller, so nothing declares a traffic class at ingress. |
| M27.2.1 | Company overview, the landing screen with honest counting | reopen not wired | Overview shows only the identity chip; operate.overview has no route. |
| M27.2.2 | Live runs | reopen absent | Live runs reads ops.control_run, written only by the worker; nothing ever ran. Needs: worker. |
| M27.2.3 | Model matrix and provider health | reopen unproven | Two rungs shown but no request ever finished; health is replayed from 0 model attempts and the probe loop has no scheduler. To prove it: Run the provider check or ask a model question and see measured rung health.. |
| M27.2.4 | Connector health | reopen absent | No connected source; connecting needs the vault, reading needs the worker. Needs: vault, worker, connector credentials. |
| M27.2.5 | Knowledge coverage and staleness | reopen not wired | Registry key knowledge_coverage has no route or menu entry. |
| M27.2.6 | Queue and automations | reopen not wired | Registry key queue has no route; automations run only on the worker. |
| M27.2.7 | Incidents: what is degraded now and what it blocks | reopen not wired | Registry key incidents has no route or menu entry. |
| M27.2.8 | The stop button, and everything currently stopped | reopen not wired | Registry key halt has no route; press_stop has no caller. |
| M27.3.1 | People and grants | reopen unproven | People shows one person and his grants, but no grant or removal was ever written from it. To prove it: Bind the second user; grant and remove a capability from People.. |
| M27.3.2 | Scopes and departments | reopen not wired | Scopes shows only 'company' and Departments none; nothing on the install can create a department or department scope (no insert into gate.department in src; gate.scope only 'company'; M27.11.1 open). |
| M27.3.3 | Roles: the six, what each is for, and who holds it | reopen partial | Roles does not show who holds each role (holders_are_not_recorded_yet). |
| M27.3.4 | Capability catalogue: everything that can be granted at all | keep install | Capabilities lists the furnished 94-row catalogue. |
| M27.3.5 | Staff sources: where the staff list is linked from and what each may assert | reopen partial | Staff sources: no staff list is read, other sources 'Still to set', trial never attached. |
| M27.3.6 | Access review: each department admin recertifies what their people hold | reopen unproven | Access review shows only the admin's own grants, which he may not certify; no lead can exist. To prove it: Grant a second person, then keep and remove it from Access review.. |
| M27.3.7 | Approvals queue: what is waiting on a human | reopen not wired | Nothing produces a suspension (put_suspension no caller) and nobody holds approve:action. |
| M27.3.8 | Sessions, with a control to end one, because revoking a grant does not close a session | reopen unproven | Sessions lists three open sessions; none was ever ended. To prove it: End a session from Sessions.. |
| M27.3.9 | Exports: what left the building and who took it | reopen absent | No export ever taken; DSAR, knowledge and legal exports 'Not available yet'. Needs: export readers beyond the audit trail. |
| M27.3.10 | Retention and erasure, with the deletion queue | reopen absent | No retention report; sweep and erasure run only on the worker. Needs: worker. |
| M27.3.11 | Access friction: where people repeatedly hit a boundary, by shape and never by name | reopen not wired | Registry key access_friction has no route or menu entry. |
| M27.3.12 | Agents and leash state with promotion and demotion history | reopen not wired | No agents, and no route creates one (install_store.finish has no route). |
| M27.3.13 | Skills and templates with the review queue | reopen unproven | No skill imported or reviewed; templates listed but none installable. To prove it: Import a skill, second person approves, assign to an agent.. |
| M27.3.14 | Knowledge library across every department, with per-item visibility scope | reopen not wired | Library holds 0 items and documents cannot be brought in. |
| M27.3.15 | Learning review across every department, tiers separated and tier one undoable | reopen broken | Learning answers 404 to the first administrator, who holds neither read:learning nor the content plane and cannot grant them. |
| M27.3.16 | Artifacts produced, with provenance and retention | reopen absent | No artifacts; keeping one needs the object store and no agent produces one. Needs: object store. |
| M27.3.17 | Memory viewer with change history | reopen broken | Memory answers 404 for the admin's own reference; nobody holds read:memory at content plane. |
| M27.3.18 | Audit | keep install | Audit lists the 80 real entries with filters and a cursor. |
| M27.4.1 | Questions and gaps | reopen unproven | Nothing recorded: no question has ever been asked. To prove it: Ask questions on Ask and see them on Questions and gaps.. |
| M27.4.2 | Usage and tokens by person, department, model and agent | reopen unproven | Usage all zero: no model ever called; department and agent axes cannot be filled. To prove it: Ask model-answered questions and see tokens by person and model.. |
| M27.4.3 | Budgets and spend limits, and what happens at the ceiling | reopen not wired | Spend 'has not been built yet'; spend_store.record and budget_store.append have no caller. |
| M27.4.4 | Quality and canaries | reopen absent | No canary run; golden questions not on the install; canaries run on the worker. Needs: worker, evaluation corpus. |
| M27.5.1 | Console reads are tool calls with required capabilities | keep install | Every registry screen route asks console.reads.permitted over capability and plane; the admin screen loads (200s, 404 on content plane) exercise it. |
| M27.5.2 | Console rows entity-tagged so the redactor applies to reports | reopen not wired | reads.tagged has no production caller, so report rows are not entity-tagged for the redactor. |
| M27.5.3 | Report audience intersected with the caller's scope, never unioned | reopen not wired | reads.audience is used only by agent capability views, never by a report route. |
| M27.5.4 | Three planes of visibility: existence, configuration, content | keep install | Planes enforced on the install: admin holds existence/configuration and is refused the content-plane Learning and Memory. |
| M27.5.5 | Super Admin self-grant writes a loud audit event and notifies the steward | reopen not wired | notices_for and self_grant_notices have no production caller; nothing sends notices. |
| M27.5.6 | Navigation computed from grants and never from a role | reopen partial | The company console menu is a constant in Shell.tsx, so the admin is offered screens his grants cannot open (Learning, Memory). |
| M27.5.7 | No count of the screens, rows or filter options withheld from a caller | keep install | No screen or menu answer carries a withheld count; lists return no totals. |
| M27.5.8 | Filter option lists intersected with the caller reach, because a dropdown is a listing | reopen unproven | Filters offer only drawn values, but no reader on the install has rows withheld, so it was never exercised. To prove it: A narrower second person sees filter options limited to their rows.. |
| M27.5.9 | Every screen narrowable by department and by person, one filter shape for all of them | reopen not wired | No route takes a department or person narrowing (only the audit actor); no department exists. |
| M27.5.10 | Screens whose subject is the installation are not offered at department scope | reopen not wired | Department console exists but nothing on the install can create a department or department scope (no insert into gate.department in src; gate.scope only company; M27.11.1 open). |
| M27.6.1 | This install: release, migration level, profile and the features it turns on | reopen partial | Shows commit, migration 0083 and profile, but names no release (image :latest) and every feature is off. |
| M27.6.2 | Backup, last verified restore and the recovery drill | reopen absent | Backup and recovery reads nothing. Needs: object store, backups. |
| M27.6.3 | Rate limits and what is currently being throttled | reopen not wired | Throttling cannot be read: app.state.throttle_source is never set. |
| M27.6.4 | Capacity: connections, memory and pool sizes against what is deployed | reopen partial | Capacity is arithmetic over declared values; deployed memory is not read (0 MiB). |
| M27.7.1 | Every console read module has a screen in the browser or a written reason it has none, listed on every run, because a read nobody can open is a screen nobody has | reopen partial | Listing runs in the CI traceability sweep, but several reads (queue, incidents, halt, knowledge_coverage, access_friction, budget, exports) have neither screen nor written reason. |
| M27.7.3 | People: who is here, what each holds, and the reach that follows from it | reopen unproven | People shows one person; no grant made from it and no second person. To prove it: Bind the second user and grant them from People.. |
| M27.7.4 | Departments and teams, and who belongs to each | reopen not wired | No departments; nothing on the install can create a department or department scope (no insert into gate.department in src; gate.scope only company; M27.11.1 open); closing commit says a department or team cannot be created. |
| M27.7.5 | Roles: the six, what each is for, and who holds it | reopen partial | Roles shows the six but not who holds each. |
| M27.7.6 | The capability catalogue: everything that can be granted at all | keep install | Capabilities lists the furnished 94-row catalogue. |
| M27.7.7 | Granting and removing a capability, at the scope the grantor holds and never wider | reopen unproven | Grant and removal writes never used on the install (all 76 grants from appointment). To prove it: Grant a second person one capability and remove it; see rows and audit entries.. |
| M27.7.8 | Elevation requests: who asked for more, what they were given, and when it lapses | reopen unproven | No elevation requests; deciding needs a second person; nobody is notified. To prove it: Second person requests, admin approves, grant lapses.. |
| M27.7.9 | Access review: each department lead recertifies what their people hold | reopen unproven | Access review shows only the admin own grants; no lead can exist. To prove it: Grant a second person, keep and remove it from Access review.. |
| M27.7.10 | Sessions, with a control to end one, because revoking a grant does not close a session | reopen unproven | Three open sessions, none ever ended from the screen. To prove it: End a session from Sessions.. |
| M27.7.12 | Subscribers and notifications: who is told what, and how to stop it | reopen absent | No subscribers; every notice says nothing sends it yet. Needs: worker, vault, email relay. |
| M27.7.13 | The audit ledger, readable by an auditor and narrowable without naming what is withheld | reopen unproven | Audit shows 80 entries to the admin but no auditor exists and nothing is withheld from him. To prove it: Second person with read:audit and some kinds filters Audit.. |
| M27.7.14 | Usage and tokens by person, department, model and agent | reopen unproven | Usage all zero; no model call ever made; department/agent axes cannot be filled. To prove it: Ask model-answered questions and see tokens by person and model.. |
| M27.7.15 | Spend, budgets and what happens at the ceiling | reopen not wired | Spend has not been built; nothing writes spend or budgets. |
| M27.7.16 | Service levels: what was promised, what was measured, and where the gap is | reopen unproven | Service levels unmeasured, 0 requests. To prove it: Ask questions and see measured levels.. |
| M27.7.17 | Adoption: who is using this, for what, and who has stopped | reopen unproven | Adoption: nothing to show. To prove it: Two people use it over a period and Adoption shows them.. |
| M27.7.18 | Questions and gaps: the questions no connected source could answer, by department and the source that would have, never counting one answered with nothing found | reopen partial | Wording narrowed in closing commit 5c1a5a6 (was: what people asked that the system could not answer); screen shows nothing recorded. |
| M27.7.20 | Knowledge library across every department, with per-item visibility scope | reopen not wired | Library holds 0 items; documents cannot be brought in. |
| M27.7.21 | Learning review across every department, tiers separated and tier one undoable | reopen broken | Learning answers 404 to the first administrator; nobody can be granted its read. |
| M27.7.22 | Memory viewer with change history | reopen broken | Memory answers 404 for the admin own reference. |
| M27.7.23 | Artifacts produced, with provenance and retention | reopen absent | No artifacts; keeping one needs the object store. Needs: object store. |
| M27.7.24 | Exports, retention and erasure, with the deletion queue | reopen absent | No retention report; erasure runs on the worker; most exports not available yet. Needs: worker, store readers. |
| M27.7.25 | This install: release, migration level, profile and whether a newer release exists | reopen absent | Cannot say which release runs (image :latest, none published); newer-release check off; 328c2fe admitted the release could not be read. Needs: published release tag, BRAIN_RELEASE_CHECK. |
| M27.7.26 | Backup, last verified restore and the recovery drill | reopen absent | Backup and recovery reads nothing. Needs: object store, backup job. |
| M27.7.27 | Rate limits and capacity: what is throttled now, and connections, memory and pools against what is deployed | reopen partial | Throttling cannot be read (never wired) and capacity reads no deployed memory; 328c2fe admitted live throttling unread. |
| M27.7.28 | My own things: what a person has asked, kept and been given, for a reader who administers nothing | reopen unproven | My workspace all zero; no non-administrator reader exists. To prove it: Second person with the member grant asks a question and sees it there.. |
| M27.7.29 | The same screens at department scope, proved by a department-scoped person opening them and seeing less, with no screen whose subject is the installation offered at all | reopen not wired | Proved only by a unit test with in-memory rows; nothing on the install can create a department or department scope (no insert into gate.department in src; gate.scope only company; M27.11.1 open). |
| M27.8.1 | The audit docs/admin-console.md asks for: every thing an administrator would need to manage, read out of the schema, the modules, the routes and the settings, compared screen by screen against what the console serves, and each gap built or recorded with its reason | keep repo | Console audit generated from code and checked by console/tests/console-audit.test.ts in CI. |
| M27.8.2 | Navigation grouped by what an administrator is trying to do rather than by the module that serves it | keep install | Served menu grouped Operate, Govern, Report, Use, Install, seen in the browser pass. |
| M27.8.3 | Loading, empty, unreachable and failed are four different sentences on every screen, never one blank panel | keep install | Screens show distinct empty sentences and failure sentences with references on the install. |
| M27.8.4 | A destructive action is confirmed, and the confirmation says what will happen and to what | reopen unproven | Confirmations exist but no destructive action was ever taken on the install. To prove it: End a session or remove a grant and see the confirmation.. |
| M27.8.5 | Validation before the write, and an error that says what to do about it | reopen unproven | Validation exists but no form was ever submitted on the install. To prove it: Submit a blank form and see field problems.. |
| M27.8.6 | Long lists page, search, filter and sort, with bulk actions wherever they are safe | reopen unproven | No list exceeds a page and bulk ending sessions was never used. To prove it: Search/sort People and Audit; end two sessions in one step.. |
| M27.8.7 | A credential is written once and never read back, proved by a test that no response and no log line carries one | reopen absent | Credential writes need the vault; ops.credential_write 0. Needs: secrets vault. |
| M27.8.9 | Prompts and system instructions, managed from the console | reopen not wired | Prompt editing switched off and there is no agent to edit, and nothing creates one. |
| M27.8.11 | Notifications and email, managed from the console | reopen absent | Every notice: nothing sends it yet; relay password needs the vault. Needs: email relay, vault. |
| M27.8.12 | Webhooks, managed from the console | reopen absent | Signing secrets need the vault and delivery needs the worker; no subscribers. Needs: vault, worker. |
| M27.8.13 | Scheduled jobs and background work, seen and controlled from the console | reopen absent | Every job has not run on this install; controls switched off; jobs run on the worker. Needs: worker. |
| M27.8.14 | Logs and errors, readable from the console without a shell on the server | reopen unproven | Logs loads; 12 application_log rows exist but the browser pass did not confirm they show; Errors empty. To prove it: Open Logs and see the 12 rows incl. the credential refused warning, filtered by level.. |
| M27.9.2 | A sign-in without a second factor is told in words which of its own verbs it cannot use here and how to sign in again, and no refusal about data changes wording | reopen unproven | The admin session is strong, so the weak-sign-in banner was never shown on the install. To prove it: Sign in without the second factor and see the banner.. |
| M27.9.3 | Approvals are stored on a running install, and each decision leaves one ledger entry that survives a restart | reopen not wired | Nothing produces a suspension (put_suspension no caller) and nobody holds approve:action, so no approval can be stored. |
| M27.9.4 | The first administrator can open the Routing and Classification screens, and the audit export carries what the exporter may read with no count of the rest | reopen unproven | Routing and Classification open, but the audit export was never taken (ops.data_export 0). To prove it: Take the audit trail export and check its contents.. |
| M27.9.6 | Every failure a console request can meet answers with a sentence the screen shows: no plain-text 500, no message-less 422, and a switched-off feature names its switch | keep install | Failures answer sentences on the install (404 sentences; switched-off controls name Install, Features); no plain 500 seen. |
| M27.9.7 | The application role holds the privileges every console route's tables need, proved by an invariant over the routes rather than found on a server | keep install | Migration 0069 is on the install (ops.control_run SELECT) and every screen read answers 200; invariant runs in CI. |
| M27.9.9 | The setup wizard names a data steward, who is granted each connected source's declared capabilities, so data access begins with a named, audited person and never with the administrator by default | reopen absent | No steward named on the install (grants still 76; wizard ran before this commit) and no source can be connected without the vault. Needs: secrets vault / connected source. |

### M28 Evaluation, canaries, regression

| Task | What it asks | Verdict | Why |
|---|---|---|---|
| M28.1.1 | promptfoo driven through our gate, never against a bare model | keep repo | Owner accepted pytest instead of promptfoo (needs-rupash 36); the golden run through answer_lane runs in CI invariants. |
| M28.1.2 | Golden corpus of real questions per persona | reopen partial | The corpus is 20 fixture questions about a synthetic company, not real questions; Quality says golden questions are not on this install. |
| M28.1.3 | Scoring and threshold configuration | keep repo | evaluation.score and thresholds are used by the CI invariant golden run. |
| M28.1.4 | CI integration blocking merge on regression | reopen partial | Committed quality baseline is 0.0, so a quality regression cannot fail CI; only the permission half blocks, and whether CI is a required merge check was not verified. |
| M28.2.1 | Same question as three synthetic personas, answers diffed | reopen absent | The canary comparison runs only from the worker schedule (canary_run); Quality shows no canary run. Needs: worker. |
| M28.2.2 | Canary string absence assertion across trace and payload stores | reopen not wired | scan_stores is called only by tests; the production run skips the store scan and there is no trace or payload store. |
| M28.2.3 | Catalogue projection assertion per persona | reopen absent | Projection findings run only in the worker canary run. Needs: worker. |
| M28.2.4 | Scheduled production canary run with alerting | reopen absent | Scheduled canary run needs the worker and an alert channel; no run exists. Needs: worker and alert channel. |
| M28.3.1 | Department lead flags a bad answer from the console | reopen not wired | No console route or screen flags an answer; flag_answer is reached only from a channel correction adapter. |
| M28.3.2 | One click converts it into a permanent regression test with mocked tools | reopen not wired | feedback.capture has no caller. |
| M28.3.3 | Two-tier quality: automated evaluation on every request, human annotation on a daily sample | reopen not wired | sampling.draw and the automated and annotated readings have no caller. |

### M29 Extension points

| Task | What it asks | Verdict | Why |
|---|---|---|---|
| M29.1.1 | Channel adapter interface | reopen not wired | The channel_adapter point names brain.channels.adapter:ChannelAdapter at the SIDECAR tier, but plugins have no loader (feature_routes.py:110 plugins_have_no_loader, ops/features.py:41), ops.plugin_registry is imported by nothing, and ops.plugin_install is empty, so nothing on the install supplies or runs an extension through this point; the in-repo adapters are not plugins and the install has no channel credentials either. |
| M29.1.2 | Connector and tool provider interface | reopen not wired | The connector point names brain.connectors.contract:ConnectorFetch at the SIDECAR tier, but plugins have no loader (feature_routes.py:110 plugins_have_no_loader, ops/features.py:41), ops.plugin_registry is imported by nothing, and ops.plugin_install is empty, so nothing on the install supplies or runs an extension through this point; the install has no connector connection either. |
| M29.1.3 | Skill interface | reopen not wired | The skill point names brain.tools.skills:Skill at the SANDBOX tier, but plugins have no loader (feature_routes.py:110 plugins_have_no_loader, ops/features.py:41), ops.plugin_registry is imported by nothing, and ops.plugin_install is empty, so nothing on the install supplies or runs an extension through this point; skills themselves have no run-time effect and the script sandbox does not exist. |
| M29.1.4 | Template interface | reopen not wired | The template point names brain.agents.template:TemplateManifest in process, but plugins have no loader (feature_routes.py:110 plugins_have_no_loader, ops/features.py:41), ops.plugin_registry is imported by nothing, and ops.plugin_install is empty, so nothing on the install supplies or runs an extension through this point; no template can be installed on the install either. |
| M29.1.5 | Model provider interface | reopen not wired | The model_provider point names brain.models.driver:ModelDriver at the SIDECAR tier; the in-repo drivers are assembled at startup but no model has ever been called, and plugins have no loader (feature_routes.py:110 plugins_have_no_loader, ops/features.py:41), ops.plugin_registry is imported by nothing, and ops.plugin_install is empty, so nothing on the install supplies or runs an extension through this point. |
| M29.1.7 | Field classifier interface | reopen not wired | The field_classifier point names brain.ops.pii:Recogniser at the SANDBOX tier, but plugins have no loader (feature_routes.py:110 plugins_have_no_loader, ops/features.py:41), ops.plugin_registry is imported by nothing, and ops.plugin_install is empty, so nothing on the install supplies or runs an extension through this point. |
| M29.1.12 | Notification sink interface | reopen not wired | The notification_sink point names brain.ops.digest_delivery:DigestSender at the SIDECAR tier, but plugins have no loader (feature_routes.py:110 plugins_have_no_loader, ops/features.py:41), ops.plugin_registry is imported by nothing, and ops.plugin_install is empty, so nothing on the install supplies or runs an extension through this point; the Notifications screen also says nothing sends any notice yet. |
| M29.2.1 | Manifest schema: identity, version, required capabilities, config schema, core compatibility range | reopen not wired | PluginManifest declares identity, version, required capabilities, config schema and core compatibility range, but no manifest has ever been read on the install: plugins have no loader and no route (feature_routes.py:110 plugins_have_no_loader), ops.plugin_registry is imported by no production module, and ops.plugin_install and ops.plugin_version are empty on the install. |
| M29.2.2 | Registry tables | reopen not wired | ops.plugin_version and ops.plugin_install are on the install (RLS on, 2 and 3 policies, INSERT/SELECT and INSERT/SELECT/UPDATE, 1 trigger on installs), but their only writer ops/plugin_registry.py is imported by nothing, so no path on the install can put a row in them. |
| M29.2.3 | Install, enable, disable, upgrade, rollback | reopen not wired | plugins/lifecycle has install, enable, disable, upgrade and rollback transitions, but plugins have no loader and no route (feature_routes.py:110 plugins_have_no_loader), ops.plugin_registry is imported by no production module, and ops.plugin_install and ops.plugin_version are empty on the install; the Features screen offers no plugin switch. |
| M29.2.4 | Drain on upgrade so in-flight work finishes on the old version | reopen not wired | lifecycle.Drain, still_running and version_for keep in-flight work on the old version, but nothing runs a plugin, so there is never in-flight plugin work to drain: plugins have no loader and no route (feature_routes.py:110 plugins_have_no_loader), ops.plugin_registry is imported by no production module, and ops.plugin_install and ops.plugin_version are empty on the install. |
| M29.2.5 | Isolation tier per point: in-process, sidecar, sandbox | reopen not wired | Each ExtensionPoint declares IN_PROCESS, SIDECAR or SANDBOX, but no loader honours a tier and no sidecar or sandbox runtime exists on the install: plugins have no loader and no route (feature_routes.py:110 plugins_have_no_loader), ops.plugin_registry is imported by no production module, and ops.plugin_install and ops.plugin_version are empty on the install. Needs: plugin sidecar and sandbox runtimes (not present on the install). |
| M29.2.6 | Core contract test: a plugin cannot see an unredacted result it did not produce, and cannot widen entitlements | reopen not wired | contract.assert_cannot_see_unredacted and plan_run are exercised only by tests/unit/test_plugins_contract.py over declared manifests; no plugin is ever run against the contract because plugins have no loader and no route (feature_routes.py:110 plugins_have_no_loader), ops.plugin_registry is imported by no production module, and ops.plugin_install and ops.plugin_version are empty on the install. |

### M30 Hosting, delivery and recovery

| Task | What it asks | Verdict | Why |
|---|---|---|---|
| M30.1.1 | Coolify deployment of the compose stack | keep install | The install is a Coolify service running a compose stack (app, migrate, db, cache) that serves /health/ready 200 at f32c385. It is a reduced stack (no pgbouncer, unlike docker-compose.yml) set up by hand rather than from docs/install/coolify.md, which the closing commit delivered. |
| M30.1.2 | Traefik routing and automatic TLS via Coolify | keep install | The install answers over HTTPS with a Let's Encrypt certificate issued through Coolify's Traefik, and /build, /openapi.json and /api/v1/me all answer on that address. |
| M30.1.4 | Per-install variables file in a separate private repo | reopen absent | No per-install variables repository exists for the install: its values are typed into the Coolify panel. brain.deployment.variables (variables_repository_gaps, template_for) has no caller outside tests. Needs: private per-install variables repository holding this install's file. |
| M30.1.5 | Secrets minted per install, never copied between clients | reopen not wired | The mint step lives in install.sh, which never ran on the install (its secrets were set by hand in Coolify), and the shared-value check shared_secrets has no caller outside tests; with one install nothing has ever compared two. Needs: installer mint step and a variables repository to check. |
| M30.2.1 | GitHub Actions: lint, types, tests, migrations, canaries | keep repo | ci.yml runs ruff format/check, strict mypy, the invariant suite, sharded unit tests (including the permission canaries test), migrations forward/back/forward and the schema sweeps on every push, and Deploy builds only on a green CI run, so f32c385 passed it. |
| M30.2.2 | Container image build and signing | keep repo | deploy.yml builds the image with docker/build-push-action, signs it with keyless cosign and verifies the signature in the same run; the install runs an image this workflow produced. Nothing on the install verifies the signature before pulling, which the leaf does not ask for. |
| M30.2.3 | Registry publication | keep repo | deploy.yml pushes ghcr.io/rpsj2230/verz-brain-v2.0 tagged with the short SHA and :latest, and the install pulls that image. |
| M30.2.4 | Pull-based deployment so the server needs no inbound access | keep install | The install host runs brain-autodeploy.timer, which pulls :latest and redeploys, and it has deployed f32c385; deploy.yml calls no panel API and only reads the live /api/status.json from outside. |
| M30.2.5 | Per-client release pinning | reopen absent | The install runs APP_IMAGE :latest and no release tag has ever been published, so nothing is pinned; Version and updates says this install pins no release. Needs: a published release tag the install is pinned to. To prove it: Publish a release tag, set APP_IMAGE to it on the install, and see Version and updates name that release.. |
| M30.2.6 | Migration backward compatibility for one release | keep repo | ci.yml runs python -m brain.deployment.compatibility on every push, which reads each migration's operations and fails on a change the previous release cannot run against. |
| M30.2.7 | Rollback by re-pinning the previous tag | reopen absent | No release tag exists and no rollback has been rehearsed on the install; it runs :latest, so there is no previous tag to re-pin, and ops/update/rollback.sh has never run anywhere. Needs: published release tags and a rehearsed rollback. To prove it: On the install pinned to tag N, run ops/update/rollback.sh to tag N-1 and read /health/ready and the running commit.. |
| M30.2.8 | Install checklist covering platform project files, temporary CLI state, git remotes and scheduled jobs | keep repo | docs/install/checklist.md has a table of the four links a copied install carries (platform project files, temporary CLI state, git remotes, scheduled jobs), and brain.ops.install_docs holds it and the timer list against ops/**/*.timer in CI. |
| M30.3.1 | pgBackRest with continuous WAL archiving, daily incremental, weekly full | reopen partial | pgBackRest and WAL archiving were never built: the closing commit shipped a nightly pg_dump copy (ops/backup/brain-backup), and operations.md says the WAL, incremental and full schedule is written down and nothing runs it. The install has no backup job at all. Needs: pgBackRest with WAL archiving (not built); any backup job on the install. |
| M30.3.2 | restic for object store and configuration | reopen partial | restic is not in the repository at all (only named in docstrings), and b6a3abb, which closed this leaf, copies the database, not the object store or configuration. The install has no object store and no backup job. Needs: restic snapshots of object store and configuration (not built). |
| M30.3.3 | Off-host destination, mandatory | reopen partial | brain-backup uploads to the object store bucket it discovers on the same host, and recovery.destination_gaps (the off-host rule) has no caller outside tests. The install has no backup job and no object store. Needs: an off-host backup destination and a check that runs. |
| M30.3.4 | Encryption at rest with a client-held key | reopen partial | Nothing encrypts a backup: brain-backup writes a plain pg_dump, and the client-held key rule exists only in recovery.destination_gaps, which nothing calls. The install takes no backups. Needs: backup encryption with a client-held key. |
| M30.3.5 | Retention: thirty daily, twelve weekly, twelve monthly | reopen partial | The leaf asks for thirty daily, twelve weekly and twelve monthly; the owner decided 30 daily and weekly copies to day 35 (daily=30, weekly=5, monthly=0), and 73fbc87 says nothing prunes backups yet. The install has no backups to retain. Needs: a pruner applying the ladder, and backups on the install. |
| M30.3.6 | Weekly automated restore into a scratch container | reopen partial | The restore rehearsal is not automated: restore-drill.md says no timer schedules a drill, most steps are by hand, and nobody has followed it on a server. The install has no backups to restore. Needs: a scheduled weekly restore drill. |
| M30.3.7 | Smoke query and permission canary against the restored copy | reopen partial | brain-restore-drill checks row counts and the migration head; restore-drill.md says the schema check, the row-level security sweep and the permission canary are still done by hand. No drill has ever run on the install. Needs: automated permission canary against the restored copy. |
| M30.3.8 | Measured RTO recorded from the drill | reopen absent | No drill has ever run, so no recovery time has been measured or recorded; operations.md says so, and Backup and recovery has no control that runs a rehearsal. Needs: backups and a verified restore drill. To prove it: Take a backup on the install, run the restore drill, and see Backup and recovery show a last verified restore with its measured recovery time.. |
| M30.3.10 | Alert on backup age exceeding the profile RPO | reopen not wired | recovery.alerts has no caller outside its module and tests, so no alert is ever raised, and Backup and recovery cannot read where copies are kept. The install takes no backups. Needs: an alert path reading backup age, plus backups and an object store. |
| M30.3.11 | Louder alert on a failed verification | reopen not wired | The louder verification alert is the same uncalled recovery.alerts, and nothing produces a verification to fail. Needs: an alert path, and restore verifications to judge. |
| M30.4.1 | Failure-mode matrix covering every component | keep repo | reliability.MATRIX is the failure-mode register and matrix_gaps refuses a compose component with no row, held in CI. |
| M30.4.2 | Operation classification: safe to retry, unsafe, retry after verification, needs a human | reopen not wired | OPERATION_CLASSES and retry_class_for are read by nothing outside reliability.py and tests. |
| M30.4.4 | Malformed responses and 429 injection | reopen not wired | brain.ops.crash classifies malformed and 429 answers but 046612a says the module is imported by nothing; no worker or connector runs on the install. |
| M30.4.5 | Disk-full behaviour | reopen not wired | Disk-full handling exists only in brain.deployment.database commands the install never runs, and 24d8556 says the serving path is still a generic failure. |
| M30.4.6 | Policy change during an active task | reopen not wired | Tested as a state over the unwired brain.ops.crash; no task has ever run on the install. |
| M30.4.7 | User disabled during an active task | reopen not wired | Tested over the unwired brain.ops.crash; no task has ever run on the install. |
| M30.4.8 | Kill after every side-effect boundary with exactly-once verification | reopen not wired | redrive and verify_once have no production caller and the install runs no worker or side effect. Needs: worker with re-drive wired. |
| M30.5.1 | SLO definitions per lane and per stage | keep repo | LANE_OBJECTIVES and STAGE_OBJECTIVES declare the SLOs per lane and stage; a declaration, served by the Service levels screen. |
| M30.5.2 | Measurement wired to the ledger | reopen unproven | TelemetryRecorder is wired to the answer lane but no question has ever been asked, so obs.request_telemetry has 0 rows. To prove it: Ask one question and see a telemetry row with duration_ms.. |
| M30.5.3 | Dashboard against target | reopen unproven | Service levels loads but shows every lane unmeasured with 0 requests. To prove it: Ask questions, then see attainment against each lane objective.. |
| M30.5.4 | RPO and RTO per profile written into the client agreement | reopen absent | No client agreement exists and the service level statement refuses without a measured drill; the install has no backups or drill. Needs: backups and a verified restore drill. |

### M31 Application scaffold and runtime

| Task | What it asks | Verdict | Why |
|---|---|---|---|
| M31.1.1.1 | App factory with settings injection | keep install | brain.serve runs brain.app:app built by create_app(settings), which the install serves. |
| M31.1.1.2 | Lifespan startup: database pool, Valkey, OpenBao client, model registry | reopen absent | The lifespan attaches the database, Valkey and the model service, but the install names no vault, so no OpenBao client exists there; 6e79bad had said this leaf was not built. Needs: Secrets vault (OpenBao). |
| M31.1.1.3 | Lifespan shutdown with graceful drain | keep install | The lifespan finally block drains and closes clients, and the install recorded 12 shutting down rows. |
| M31.1.1.4 | Health endpoint separating liveness from readiness | keep install | /health/live and /health/ready are separate; the install answers /health/ready 200 and the container healthcheck uses it. |
| M31.1.1.5 | Readiness gated on database, cache and secret store all reachable | reopen partial | Readiness checks database, migrations, tools, cache and row security, but no secret-store check exists in the code, and the install has no vault; 6e79bad said this was deliberately not claimed. Needs: Secrets vault (OpenBao). |
| M31.1.2.1 | Uvicorn worker configuration per profile | keep install | detect_profile sizes uvicorn from the container cgroup, and the install runs 4 workers under its 1 GB limit. |
| M31.1.2.2 | Graceful shutdown honouring in-flight requests | reopen unproven | uvicorn gets an 8 second graceful timeout under the Docker 10 second stop, but no stop on the install has been shown to let an in-flight request finish, and needs-rupash item 28 records recreates dropping in-flight requests. To prove it: Start a slow request, trigger a redeploy, and see the request complete with a 200.. |
| M31.1.2.3 | Signal handling for container stop | keep install | Container stops reach uvicorn as SIGTERM and run the lifespan shutdown, shown by 12 shutting down rows on the install. |
| M31.1.2.4 | Request timeout middleware distinct from the model timeout | keep install | TimeoutMiddleware wraps every request with its own deadline separate from model timeouts, and every install request passes through it. |
| M31.1.3.1 | Correlation id assignment before anything else | keep install | The trace middleware mints or accepts a validated x-trace-id for every request before identification, and install audit entries carry trace ids. |
| M31.1.3.2 | Structured logging with the trace id on every line | keep install | structlog binds trace_id per request and log capture keeps it; the install credential refused line was logged inside a request. |
| M31.1.3.3 | Exception handler mapping the error taxonomy to responses | keep install | The installed handlers map the taxonomy to ErrorBody; the install answers 404 I could not find that and 401 with a sentence. |
| M31.1.3.4 | CORS policy for the console and widget only | reopen partial | No widget route is mounted in the served app, and the install sets no CORS origins, so the widget half cannot exist; only same-origin console use holds. |
| M31.1.3.5 | Security headers | keep install | Every response gets nosniff, frame DENY and no-referrer from middleware the install runs; no CSP or HSTS is set by the app. |
| M31.1.4.1 | Versioned route prefix and deprecation policy | reopen partial | Routes are under /api/v1, but no deprecation policy exists anywhere in src or docs. |
| M31.1.4.2 | OpenAPI generation with auth documented | keep install | The install serves a narrowed /openapi.json (200) that documents bearer authentication. |
| M31.1.4.3 | Error response schema shared across every endpoint | keep install | ErrorBody is returned by every handler and the failure-body middleware, and the install 401 and 404 answers use it. |
| M31.1.4.4 | Pagination convention applied uniformly | reopen partial | The shared Page cursor model is used by 8 of 36 routers; audit routes define their own next_cursor page, so the convention is not uniform. |
| M31.2.1.1 | Declarative base with naming convention | keep install | brain.db metadata carries the naming convention and the install objects follow it (pk_request_telemetry). |
| M31.2.1.2 | Session management per request and per job | reopen absent | Per-request sessions run on the install, but per-job sessions belong to the worker, which the install does not run. Needs: Worker. |
| M31.2.1.3 | Async engine configuration against PgBouncer transaction mode | reopen absent | The engine is configured for PgBouncer transaction mode, but the install has no PgBouncer. Needs: PgBouncer. |
| M31.2.1.4 | Separate session-mode engine for the worker | reopen absent | The session-mode worker engine serves only the worker, which the install does not run. Needs: Worker. |
| M31.2.1.5 | Model mixins: timestamps, soft delete, audit fields | keep install | Timestamp and soft-delete mixins are on tables holding rows on the install (principal, grant, session, setting, routing rung). |
| M31.2.2.1 | Alembic environment reading settings, not hardcoded URLs | keep install | migrations/env.py takes the URL from configuration, and the install migrate job ran to 0083. |
| M31.2.2.2 | Autogenerate review rule: never merge an unreviewed autogenerated migration | keep repo | brain.ops.migration_policy refuses generated markers and runs in the CI sweeps job. |
| M31.2.2.3 | Backward-compatibility test for one release | reopen partial | CI runs brain.deployment.compatibility, a static reading of upgrade against downgrade that reports unreadable statements; nothing runs the previous release against the new schema. |
| M31.2.2.4 | Data migrations separated from schema migrations | keep repo | migration_policy refuses a migration mixing schema and data, run in CI. |
| M31.2.2.5 | Migration run order at deploy, before traffic | keep install | Migrations run in the migrate job and in the app lifespan before it serves, and the install is at 0083. |
| M31.3.1.1 | Pydantic settings with per-environment overlays | keep install | pydantic Settings with cumulative per-environment requirements, applied by brain.serve on every install start. |
| M31.3.1.2 | Every variable documented in the example file | keep repo | test_config.py in CI keeps .env.example and the settings read in step. |
| M31.3.1.3 | Fail-fast on a missing required variable at startup | keep install | assert_valid runs before uvicorn binds on every install start and refuses missing production settings. |
| M31.3.1.4 | Config as data in Postgres for anything an operator tunes | keep install | Operator values live in ops.setting (7 rows written by the wizard, read at start) and ops.routing_rung (2 rows, editable on Routing). |
| M31.3.2.1 | Deployment and unseal procedure documented for the client | reopen absent | ops/openbao/UNSEAL.md exists, but no install runs OpenBao, so the procedure has never been carried out. Needs: Secrets vault (OpenBao). |
| M31.3.2.2 | Policy per role: application, worker, browser runner | reopen absent | Policies exist as HCL files but no vault on the install loads them. Needs: Secrets vault (OpenBao). |
| M31.3.2.3 | Dynamic lease issuance for connector credentials | reopen absent | Lease issuance needs OpenBao and connector credentials, neither on the install. Needs: Secrets vault (OpenBao). |
| M31.3.2.4 | Lease revocation on run end | reopen absent | Lease revocation needs a vault and a run, neither on the install. Needs: Secrets vault (OpenBao). |
| M31.3.2.5 | Rotation without redeploy | reopen absent | Rotation without redeploy needs a vault on the install. Needs: Secrets vault (OpenBao). |
| M31.3.2.6 | Audit device enabled and shipped to the ledger | reopen absent | The audit device needs a vault on the install. Needs: Secrets vault (OpenBao). |

### M32 Component deployment and wiring

| Task | What it asks | Verdict | Why |
|---|---|---|---|
| M32.1.1.2 | Resource limits per service so it cannot starve the application | reopen absent | The install runs no Langfuse services. Needs: Langfuse overlay. |
| M32.1.1.3 | Retention configuration on traces and observations | reopen absent | Trace retention applies to Langfuse, which the install does not run. Needs: Langfuse overlay. |
| M32.1.1.4 | Profile flag so the lite install runs the ledger only | keep install | config.check runs trace_config_conflicts at every start with the default lite profile, the install runs no trace ledger, and Capacity shows profile lite. |
| M32.1.2.1 | Client-side masking before any span leaves the process | reopen absent | The span mask in brain.ops.tracing has no production importer and no Langfuse runs. Needs: Langfuse overlay. |
| M32.1.2.2 | Assertion test that no canary string reaches the payload store | reopen not wired | The canary test asserts over tracing.mask, which nothing in production calls; no payload store exists. Needs: Langfuse overlay. |
| M32.1.2.3 | Environment vocabulary fixed at first ingest and asserted against the database enum | reopen absent | Environment vocabulary is asserted at first Langfuse ingest, which never happens on the install. Needs: Langfuse overlay. |
| M32.1.2.4 | Separate Keycloak role for payload access | reopen absent | may_read_payloads has no caller and there is no payload store to guard. Needs: Langfuse overlay. |
| M32.1.2.5 | Audit row written before every payload read | reopen absent | read_payload has no caller and no payload store exists. Needs: Langfuse overlay. |
| M32.2.1.1 | Presidio analyzer configured with the recognisers we need | reopen absent | No compose file declares a Presidio service and nothing calls the scrubber. Needs: Presidio analyzer service. |
| M32.2.1.2 | GLiNER for entities the default recognisers miss | reopen absent | pii.py says the GLiNER leg has no weights behind it on any install. Needs: GLiNER model serving. |
| M32.2.1.3 | Singapore recognisers: NRIC, FIN, UEN, local phone formats | reopen not wired | The Singapore recognisers exist but scrub is reached only from brain.gate.screening, which nothing imports. |
| M32.2.1.4 | Chinese, Malay and Tamil name handling | reopen not wired | Name handling lives in the same unwired scrubber. |
| M32.2.2.1 | Used only for scrubbing free text before third-party egress | reopen not wired | No egress path scrubs text; the scrubber has no request-path caller and no model has been called. |
| M32.2.2.2 | Explicitly never an authorisation boundary, asserted by test | reopen not wired | The property holds only vacuously because PII detection is not wired into any request path. |
| M32.2.2.3 | Detection results logged as telemetry, never as a block | reopen not wired | No detection is ever produced or logged on the install; the scrubber is unwired. |
| M32.3.1.1 | Deployment and volume configuration | reopen absent | The install runs no SeaweedFS. Needs: SeaweedFS object store. |
| M32.3.1.2 | Bucket layout: assets, recordings, exports, backups | reopen absent | No buckets exist; Storage says every bucket is not counted. Needs: object store. |
| M32.3.1.3 | Lifecycle policy per bucket | reopen absent | No object store, so no lifecycle policy applies. Needs: object store. |
| M32.3.1.4 | S3-compatible credentials issued from OpenBao | reopen absent | No vault and no object store on the install. Needs: OpenBao vault and object store. |
| M32.3.2.1 | Client-provided S3 or R2 as a configuration option | reopen absent | No S3_* or object store settings on the install; Storage connects to nothing. Needs: client S3 or R2 store. |
| M32.3.2.2 | Storage backend behind the extension interface so either works | reopen absent | Neither backend works on the install; Storage points at a default address that does not exist. Needs: object store. |
| M32.4.1.1 | Procrastinate on Postgres for the job queue | reopen absent | Procrastinate runs only in the worker, which the install does not run. Needs: worker. |
| M32.4.1.2 | LangGraph checkpointer configuration | reopen absent | The checkpointer is used only by the worker and bf2f5b8 says no graph uses the saver yet. Needs: worker. |
| M32.4.1.3 | Our re-drive driver for crash recovery | reopen not wired | queue.py says queue_redrive is still not wired, and no worker runs. Needs: worker. |
| M32.4.2.1 | Hatchet evaluated and documented as the swap | keep repo | The Hatchet swap is recorded in queue.py SWAP_CANDIDATES with its trigger. |
| M32.4.2.2 | Adapter interface so the swap changes one file | keep repo | Procrastinate is imported only in brain/ops/queue.py, held by a test so the swap changes one file. |
| M32.4.2.3 | Decision recorded with the trigger that would cause it | keep repo | The decision and its trigger and measured_by are recorded in queue.py. |
| M32.5.1.1 | React and Vite project with typed API client generated from OpenAPI | keep install | The image builds the Vite console against a client typed from the exported OpenAPI document, and the install serves it; the browser pass loaded every menu screen. |
| M32.5.1.2 | Routing and layout shell | keep install | The routed shell and menu work on the install; the browser pass navigated every group. |
| M32.5.1.3 | Auth integration with Keycloak | keep install | The administrator signs in through Keycloak with strong assurance; 3 sessions and a sign_in audit entry exist. |
| M32.5.1.4 | Theme with light and dark | reopen unproven | Light and dark tokens exist, but nothing on the install has shown both themes. To prove it: Switch the browser between light and dark and see the console follow.. |
| M32.5.2.1 | TanStack Table for every grid, with server-side pagination and filters | reopen partial | TanStack Table is used only by components/DataTable.tsx; about thirty pages (Audit, Sessions, Questions, Jobs and others) draw hand-written tables. |
| M32.5.2.2 | react-jsonschema-form for all generated forms | reopen unproven | SchemaForm backs Routing, People, Classification and Records, but no form write from them has been made on the install and Records answers 404. To prove it: Edit a routing rung and add a grant through the forms and see both saved.. |
| M32.5.2.3 | React Flow for the trace graph and procedure canvas | reopen not wired | TraceGraph and ProcedureCanvas are imported by no page, so React Flow is not in any served screen. |
| M32.5.2.4 | Shared chip, badge and status primitives | keep install | Chip, Badge and Status primitives render on the served console, including the Overview identity chip. |
| M32.6.1.1 | Deployment as an optional sandboxed container | reopen absent | The install runs no Activepieces container. Needs: Activepieces automation overlay. |
| M32.6.1.2 | Egress allowlist and no database credentials | reopen absent | No Activepieces runs, so no egress allowlist applies. Needs: Activepieces automation overlay. |
| M32.6.1.4 | Enabled per client by configuration, not a fork | reopen absent | The install has not enabled the automation canvas. Needs: Activepieces automation overlay. |
| M32.6.2.1 | Documented rule: deterministic automations only, never agent control flow | keep repo | The deterministic-only rule is written in brain.ops.automation and enforced by assert_deterministic. |
| M32.6.2.2 | Test proving a flow cannot reach data outside the caller's entitlements | reopen absent | The test covers flow_reach over in-memory grants; no flow, automation or agent can run on the install. Needs: Activepieces automation overlay and an agent. |

### M33 Role-specific surfaces

| Task | What it asks | Verdict | Why |
|---|---|---|---|
| M33.1.1.1 | All agents, skills, knowledge and connectors, filterable by department and user | reopen not wired | estate/visible_estate have no route; the separate screens are all empty. |
| M33.1.1.2 | All activity with the same filters | reopen not wired | Department filter for activity has no route (b77ec34: no route calls either surface). |
| M33.1.1.3 | Company budget and consumption | reopen not wired | company_consumption has no caller; nothing writes spend or budgets. |
| M33.1.1.4 | Global kill switch | reopen not wired | press_stop has no caller; halt screen not routed. |
| M33.1.2.1 | Publish and retire global agents | reopen not wired | Publish/retire has no route and no agent exists. |
| M33.1.2.2 | Approve department publication requests | reopen not wired | approve_publication has no caller. |
| M33.1.2.3 | Confirm role nominations | reopen not wired | confirm (role nominations) has no caller. |
| M33.1.2.4 | Disable a principal | reopen not wired | disable_principal has no caller; People offers no disable. |
| M33.1.2.5 | Self-grant path that writes a loud audit event and notifies the steward | reopen not wired | Self-grant notices have no caller; nothing sends notices. |
| M33.2.1.1 | Everything within their scope, same shape as the Super Admin view | reopen not wired | department_surface has no caller and no department or department scope can be created on the install. |
| M33.2.1.2 | Department activity | reopen not wired | department_activity has no caller; no department or department scope can be created on the install. |
| M33.2.1.3 | Department budget against pace | reopen not wired | department_pace has no caller; nothing writes spend or budgets. |
| M33.2.1.4 | Department knowledge coverage and gaps | reopen not wired | department_coverage has no caller; no department or knowledge exists. |
| M33.2.2.1 | Approve department publications | reopen not wired | Department publication approval has no route. |
| M33.2.2.2 | Grant within their scope only | reopen not wired | write_grant is wired but no department or department scope can be created on the install, so no department admin can exist to be held to scope. |
| M33.2.2.3 | Adopt orphaned agents when someone leaves | reopen not wired | adoptable/may_adopt have no caller; no agent exists. |
| M33.2.2.4 | Lower a leash, never raise beyond the ceiling | reopen not wired | may_move_rung has no caller. |
| M33.2.2.5 | Appoint a deputy with a maximum thirty-day expiry | reopen not wired | Deputy appointment has no route. |
| M33.3.1.1 | Their own agents | reopen not wired | My workspace lists callable agents but none exists and nothing creates one. |
| M33.3.1.2 | Their own knowledge and uploads | reopen not wired | No knowledge or upload path; documents cannot be brought in. |
| M33.3.1.3 | Their own conversation history and export | reopen not wired | export_own_history has no route; own conversations export not available. |
| M33.3.1.4 | Their own memory with a delete control | reopen not wired | delete_own_memory has no route and no memory is formed. |
| M33.3.1.5 | Their own usage against allowance | reopen not wired | Allowances drawn from spend and budgets nothing writes. |
| M33.4.1.1 | Full audit trail including Super Admin activity | reopen unproven | Audit shows admin activity, but no auditor exists. To prove it: Second person with read:audit sees the administrator entries.. |
| M33.4.1.2 | Grant and leash history | reopen not wired | Grant history served, but leash history cannot exist (no agent can be created); no auditor. |
| M33.4.1.3 | Redaction and refusal statistics | reopen not wired | refusal_statistics and redaction_statistics have no caller. |
| M33.4.1.4 | Explicit absence of any content access, asserted by test | keep repo | No role implies a capability; entitlement resolves from grants only; asserted by tests CI runs. |
| M33.5.1.1 | Install and configure connectors | reopen absent | Connecting needs the vault, reading needs the worker; none connected. Needs: vault, worker, connector credentials. |
| M33.5.1.2 | Bind and rotate credential references | reopen not wired | ConnectorRegistry.rebind has no production caller; no vault either. |
| M33.5.1.3 | Set permission-sync capability per connector | reopen partial | Permission sync is only a manifest declaration shown on Connectors; no control sets it; no connector exists. |
| M33.5.1.4 | No agent or knowledge access by virtue of the role | keep repo | No role implies a capability; entitlement comes only from grants, held by tests CI runs. |
| M33.6.1.1 | Pending approvals within their own entitlement | reopen not wired | Nothing produces a suspension and nobody holds approve:action. |
| M33.6.1.2 | The artefact rendered, not the tool call | reopen not wired | No approval card can exist: nothing produces a suspension. |
| M33.6.1.3 | Approve, reject with a reason, or take over | reopen partial | Take-over and amend not offered (c6cbbcc) and nothing produces a suspension. |
| M33.6.1.4 | Timeout visibility and expiry behaviour | reopen not wired | expiring_within has no caller; no suspension exists. |
| M33.7.1.1 | Zero standing entitlement | reopen not wired | Break glass unreachable on the install: no partner principal exists and the partner rule is not on the request path. |
| M33.7.1.2 | Time-boxed elevation with a stated reason | reopen not wired | Break glass unreachable on the install: open_break_glass has no production caller. |
| M33.7.1.3 | Separate immutable audit chain | reopen not wired | Break glass unreachable on the install: no break-glass session is ever opened. |
| M33.7.1.4 | Client notification on every elevation | reopen not wired | Break glass unreachable on the install: no session is opened and nothing sends notices. |
| M33.7.1.5 | Automatic expiry and revocation | reopen not wired | Break glass unreachable on the install: no session is opened so nothing expires or is revoked. |

### M34 Onboarding and adoption

| Task | What it asks | Verdict | Why |
|---|---|---|---|
| M34.1.1.1 | First connector chosen so a real question is answerable before any document is uploaded | reopen not wired | The choice is `brain.adoption.first_connector`, which nothing outside tests calls; the setup wizard never offers a first connector, and the install has no connector connection or credentials. |
| M34.1.1.2 | Three department-scoped starter questions generated from what actually indexed | reopen not wired | `starter_questions` is called only by `brain.ops.guide_docs`, a docs checker nothing in the app imports, so no starter question is generated from what indexed; the install has indexed nothing (know.* 0). |
| M34.1.1.3 | Answers verifiable against a system the client already trusts | reopen not wired | Only the `checkable` flag inside the uncalled `first_connector`/`starter_questions` expresses this; no answer has ever been given on the install and no trusted source system is connected. |
| M34.1.2.1 | Per-scope statement: record counts and last sync | reopen not wired | `adoption.coverage_report` has no caller; the served Knowledge screen's coverage card lists department names only and says freshness is not measured, with no record counts or last sync per scope. |
| M34.1.2.2 | Explicit not-connected state per department | reopen not wired | The per-department NOT_CONNECTED coverage state lives in the uncalled `coverage_report`; the Questions screen only says nothing connected install-wide, and the install has no departments. |
| M34.1.2.3 | Shopping list for the champion rather than a progress bar | reopen not wired | `adoption.shopping_list` has no caller anywhere in src/, and no console route or screen renders a champion's shopping list. |
| M34.2.1.1 | Each department lead verifies five documents in week one | reopen not wired | `adoption.verification_targets` (WEEK_ONE_TARGET=5) has no caller; nothing asks a lead to verify documents, and the install has no departments, leads or knowledge items. |
| M34.2.1.2 | Verification badge visible in answers | reopen not wired | `gate.provenance.provenance_for`/`badge_for` are called by nothing outside that module and tests, so no answer path attaches a badge; the install has never produced an answer or held a knowledge item. |
| M34.2.1.3 | Re-verification nag on the review date | reopen absent | The nag is a control run by the worker's schedule, and the install runs no worker; the closing commit also says nothing sends a nag and `put_item` has no caller, so no knowledge item exists to lapse. Needs: worker (schedule runner) and an outbox sender. |
| M34.2.2.1 | Correction path in every channel | reopen not wired | The closing commit states nothing on a live request path calls `brain.channels.correction` and there is no channel webhook route; grep confirms no importer, and the install has no channel credentials. |
| M34.2.2.2 | Wrong-sources feedback distinct from wrong-answer, so retrieval and reasoning bugs separate | reopen not wired | `FlagReason.WRONG_SOURCES`, `plane_of` and `by_plane` exist but `flag_answer` is reached only from the uncalled channel correction parser, and no console screen or route files a flag or shows the planes. |
| M34.2.2.3 | Unanswered question log as the connector roadmap | reopen unproven | Wired: every finished request goes through `GapRecorder` (app.py request_recorders_for) and the routed Questions screen reads it via /report/questions; but no question has ever been asked on the install, so the gap log has never held a row. To prove it: Ask a question the install has no connected source for, then open Questions and gaps and see the gap counted under its missing source.. |
| M34.3.1.1 | What it can and cannot see, in plain language | reopen not wired | `adoption.visibility_summary` is called only by tests; no staff page, console screen or served document states what the system can and cannot see. |
| M34.3.1.2 | The privacy posture stated openly | reopen not wired | `adoption.privacy_posture` and `posture_gaps` are called only by tests, and no page served by the install or document in docs/guides states the privacy posture. |
| M34.3.1.3 | How to ask well, with examples from their own department | reopen partial | docs/guides/asking-well.md exists and CI checks it, but its per-department examples are the demo company's, it is a repository file the install does not serve, and the closing commit says nothing shows staff their own department's examples. |
| M34.3.2.1 | Runbook per console screen | reopen partial | 36 runbooks exist for the registry's keys, but the served console routes about 20 more screens with no runbook (jobs, errors, logs, webhooks, notifications, storage, import-export, prompts, features, spend, adoption, departments, elevation, subscribers, sign-in-links, classification, records, routing), and every runbook still says the screen cannot be opened. |
| M34.3.2.2 | Grant and scope cookbook with worked examples | keep repo | docs/guides/grants-and-scopes.md exists at f32c385 and `tests/unit/test_guide_docs.py`, run by the CI unit shards, resolves every worked example through the product's resolver and row plane. |
| M34.3.2.3 | Incident playbook per failure mode | reopen not wired | The playbook is the Python table `ops.reliability.MATRIX`, exposed only through `brain.launch.playbooks`/`assemble`, which nothing runs; no document or served page renders it for an administrator. |
| M34.3.3.1 | Install checklist including the four hidden production links | keep repo | docs/install/checklist.md exists with the four links a copied install carries, held by `install_docs` in the CI unit shards against the WBS sentences and ops timers. |
| M34.3.3.2 | Per-client variables reference | keep repo | docs/install/configuration.md exists and is held by `install_docs` (CI unit shards) against .env.example, the install script's mint step and every profile's compose files. |
| M34.3.3.3 | Upgrade and rollback procedure | reopen absent | The procedure pins and rolls back by release tag and refuses `latest`, but the install runs `:latest` redeployed by an autodeploy timer and no release tag has ever been used, so the procedure cannot be followed there and has never been. Needs: a published release tag (install runs :latest via brain-autodeploy.timer). |
| M34.3.3.4 | Restore drill procedure | reopen absent | docs/install/restore-drill.md exists and is CI-checked, but the page itself says no script, timer or console control performs a drill and none of its commands has run on a server, and the install has no database backup to drill from. Needs: database backups (none on the install). |

### M35 Localisation and accessibility

| Task | What it asks | Verdict | Why |
|---|---|---|---|
| M35.1.1.1 | English and Simplified Chinese interface strings | reopen not wired | The Simplified Chinese catalogue lives in `brain.locale.MESSAGES`, but the served console contains no Chinese string and no locale switch, and the server renders only English (`data_steward_routes` uses a fixed CONSOLE_LANGUAGE); `enabled_locales`/`default_locale` have no caller. |
| M35.1.1.2 | Locale-aware dates, numbers and currency | reopen not wired | `format_date`, `format_timestamp`, `format_number`, `format_money`, `currency` and `time_zone` have no caller in src/; console screens format dates with the browser default locale and never read INSTALL_LOCALES, INSTALL_CURRENCY or INSTALL_TIME_ZONE. |
| M35.1.1.3 | Right-to-left readiness deferred with a stated trigger | reopen not wired | The deferral and trigger are written (`locale.RIGHT_TO_LEFT`), but its own reason relies on an RTL locale being refused by name, and `enabled_locales`, `locale_facts` and `triggered` have no caller, so nothing on the install would refuse or report the trigger. |
| M35.1.2.1 | CJK tokenisation for lexical search so Chinese queries do not silently retrieve worse | reopen absent | The query side is wired (document search tool registered at startup runs `lexical_legs`), but the `know.chunk.cjk` index is filled only by `chunk_store.ingest_document`, called only from the worker connector sync; the install runs no worker and holds no chunks. Needs: worker (connector sync ingest) and a connected source. |
| M35.1.2.2 | Mixed-language document handling | reopen absent | Mixed-language handling (per-script detection and per-script lexical legs) only matters over ingested documents, and ingest runs only in the worker connector sync, which the install does not run; no document has ever been ingested. Needs: worker (connector sync ingest). |
| M35.1.2.3 | Language detection on ingest recorded as metadata | reopen absent | The `know.chunk.language` column exists (migration 0024 applied, head 0083) but is written only by `ingest_document` via the worker connector sync, which the install does not run. Needs: worker (connector sync ingest). |
| M35.2.1.1 | Screen-reader-safe streaming: no naive aria-live on a token stream | reopen unproven | The served Ask screen keeps the answer out of any live region and puts only the progress steps in role=status, but no question has ever been asked on the install, so no stream has been rendered there. To prove it: Ask a question on /ask with a screen reader running and hear the answer not read token by token.. |
| M35.2.1.2 | Announce on completion, not on every token | reopen unproven | Ask.tsx moves focus to the skip control on completion (FOCUS_IS_RETURNED_AND_NEVER_TAKEN) as the one announcement, but no answer has ever finished on the install. To prove it: Ask a question on /ask by keyboard and hear Skip to answer announced once when the answer finishes.. |
| M35.2.1.3 | Skip-to-answer control | reopen unproven | The Skip to answer button exists on the served Ask screen and appears only once an answer or refusal has arrived, which has never happened on the install. To prove it: Ask any question on /ask, press Skip to answer, and see focus land on the Answer region.. |
| M35.2.2.1 | Keyboard navigation across the console | reopen unproven | Native controls, a skip link and the check-boundaries rules (run by CI) are in the served bundle, but keyboard reach is proved only by a jsdom test; nothing records the console being navigated by keyboard on the install. To prove it: On the install, tab from the skip link through the shell navigation into a list screen and a form, reaching every control in order without a mouse.. |
| M35.2.2.2 | Visible focus states | reopen unproven | A focus-visible outline rule exists and CI refuses outline none, but the ring has only been asserted in jsdom, not seen on the served console. To prove it: Tab through the served console in both themes and see a visible focus ring on every control.. |
| M35.2.2.3 | Contrast checked in both themes | keep repo | The check measures the real stylesheet: tests/unit/test_locale.py:777 asserts contrast_gaps over theme_palettes of console/src/theme/tokens.css is empty for light and dark, run by the CI unit shards; the install serves a bundle built from that file. |
| M35.2.2.4 | Form labels and error association | reopen unproven | Served forms tie errors to inputs (FirstRun, SchemaForm, DataStewardCard use aria-invalid and aria-describedby; wizard errors carry a field), but nothing on the install shows a field error was ever rendered, and the wizard is now unreachable there. To prove it: Submit an invalid value in a console form on the install and see the message announced with its field (aria-describedby).. |
| M35.3.1.1 | Console usable on a phone for approvals at minimum | reopen not wired | The Approvals screen and decide route are mounted, but nothing in src/ calls put_suspension, so no approval can ever be pending on the install to decide from a phone. |
| M35.3.1.2 | Approval cards readable without horizontal scroll | reopen not wired | The approval card is laid out for a phone, but no production code raises a suspension (put_suspension has no caller), so no card can render on the install. |
| M35.3.2.1 | Installable shell | reopen unproven | The bundle index.html links /manifest.webmanifest with start_url and scope at the root, and the console fallback serves unhashed bundle files at the root, but nobody has installed the console from the install origin. To prove it: Open the install on a phone or in Chrome and see the install prompt and a valid manifest in DevTools.. |
| M35.3.2.2 | Push for approvals, deferred with a stated trigger | reopen not wired | The deferral (locale.PUSH_FOR_APPROVALS, 4-hour trigger) is written, but triggered has no caller and nothing measures approval wait, so the trigger can never fire; no approval has ever been raised on the install. |

### M36 Scaling triggers and performance

| Task | What it asks | Verdict | Why |
|---|---|---|---|
| M36.1.1.1 | Monthly partitions of the metadata ledger, created and detached by the worker in plain SQL | reopen absent | Partitions are created and detached by the worker, which the install does not run. Needs: worker. |
| M36.1.1.2 | Partition by month with automatic creation | reopen absent | Automatic monthly creation runs only in the worker. Needs: worker. |
| M36.1.1.3 | Retention detach and archive rather than delete | reopen absent | Detach runs only in the worker, and 0728f1d says dropping after settle days is not built. Needs: worker. |
| M36.1.1.4 | Trigger: ledger row count or query latency threshold | reopen partial | The written trigger says partitioning is due at creation, not at a row count or latency threshold, and partitioning_is_due has no production caller. |
| M36.1.2.2 | Console reads routed to the replica | reopen absent | The install runs no read replica. Needs: read replica. |
| M36.1.2.3 | Replication lag monitoring with a staleness banner | reopen absent | No replica, so no lag to monitor or banner to show. Needs: read replica. |
| M36.1.2.4 | Trigger: console queries affecting answer latency | reopen not wired | replica_is_due has no production caller, so the trigger is never evaluated. |
| M36.1.3.1 | Views for the heaviest report screens | reopen partial | One view (ops.spend_daily) exists for one screen, built WITH NO DATA and never refreshed; Spend says the report has not been built. |
| M36.1.3.2 | Refresh schedule and staleness display | reopen absent | The refresh runs only on the worker schedule. Needs: worker. |
| M36.1.3.3 | Trigger: a report exceeding two seconds | reopen partial | report_is_due uses ANSWER_P95_MS of 8000 ms, not the two seconds the leaf states, and nothing evaluates it. |
| M36.1.4.3 | Trigger: RAM pressure or a resilience requirement | reopen not wired | host_split_is_due has no production caller and the resilience half is not anchored. |
| M36.2.1.1 | Dependency graph over independent source calls | reopen absent | FanOutPlan is used by connectors, and the install has no connector credentials. Needs: connector accounts. |
| M36.2.1.2 | Fan-out with one critical path rather than a sum | reopen absent | The critical-path fan-out only runs on connector calls, which the install never makes. Needs: connector accounts. |
| M36.2.1.3 | Per-source and global budgets still enforced under parallelism | reopen partial | scaling.py says there is no global budget to read, and its fan-out checks have no production caller. |
| M36.2.2.1 | Measure the serial cost of the three screens | keep repo | The serial cost of the three screens was measured and recorded in brain.gate.screening; the screens themselves have no request-path caller. |
| M36.2.3.1 | Identical in-flight requests share one execution | reopen not wired | may_share and share_role have no caller outside scaling.py, so no in-flight answers are shared. |
| M36.2.3.2 | Entitlement equality required before sharing, never similarity | reopen not wired | The entitlement-equality rule sits in the same uncalled may_share. |
| M36.2.4.1 | Peak concurrency test rather than daily volume | reopen partial | The test drives the admission controller, not the deployed stack, as 8cd397c says. |
| M36.2.4.2 | Documented first bottleneck at ten and one hundred times | reopen partial | The first bottleneck is arithmetic over vendor ceilings (Xero, Lark), not a load test result. |

### M37 Migration, launch and handover

| Task | What it asks | Verdict | Why |
|---|---|---|---|
| M37.1.1.1 | Catalogue what the current Company Brain holds: knowledge, prompts, tool definitions, conversation history | reopen not wired | brain.migration.inventory is a register only tests call, and no catalogue of the v1 Company Brain has been recorded anywhere. |
| M37.1.1.2 | Decide per item: migrate, rebuild or retire | reopen not wired | Dispositions exist only as types in brain.migration.inventory called by tests; no per-item decision for the v1 system has been made or stored. |
| M37.1.1.3 | Map old permissions onto the new capability model, explicitly rather than by assumption | reopen not wired | The permission mapping register has no caller outside tests, and no v1 permission has been mapped onto capabilities on the install. |
| M37.1.2.1 | Knowledge and asset export from the old system | reopen not wired | brain.migration.carry defines Export types but nothing exports from the v1 system; only tests call it. |
| M37.1.2.2 | Re-parse and re-embed rather than copying vectors, since embeddings are model-specific | reopen not wired | reparse_plan is called only by tests, and re-parse and re-embed would need the worker and an embedding server the install does not run. |
| M37.1.2.3 | Entity registry seeded from existing client records | reopen not wired | EntitySeed is only a type used by tests; no entity was seeded (er.* 0 rows on the install). |
| M37.1.2.4 | Historical conversations archived, not imported, unless there is a stated reason | reopen not wired | The Archive rule exists only in brain.migration.carry called by tests; no v1 conversation has been archived. |
| M37.1.3.1 | Both systems answering the same questions for an agreed period | reopen not wired | brain.migration.parallel is called only by tests; nothing runs the v1 and new systems side by side, and no question has ever been asked on the install. |
| M37.1.3.2 | Answer comparison log with disagreements surfaced | reopen not wired | The comparison log is an in-memory type with no store, route or screen; only tests use it. |
| M37.1.3.3 | Cutover criteria agreed in writing before the period starts | reopen not wired | Period refuses late criteria but nothing stores or shows an agreed criterion; only tests call it. |
| M37.1.4.1 | Old system read-only, then archived | reopen not wired | brain.migration.decommission records read-only then archived only in tests; the v1 system has not been made read-only or archived through it. |
| M37.1.4.2 | Credential revocation on every connector the old system held | reopen not wired | The revocation register has no production caller; no v1 connector credential revocation is recorded. |
| M37.1.4.3 | Scheduled jobs disabled and verified stopped | reopen not wired | The verified-stopped window is only a type used by tests; no v1 scheduled job has been recorded as disabled and watched. |
| M37.1.4.4 | Final backup retained per the retention policy | reopen absent | The final backup rule derives retention from BACKUP_RETENTION_DAYS but only tests call it, and the install has no backup machinery at all. Needs: backups. |
| M37.2.2.2 | Convert the description convention, since theirs are written as router prompts beginning "Use the X skill to" | reopen not wired | proposed_description in brain.migration.skills is not called by the Skills import path or anything else outside tests. |
| M37.2.2.3 | Re-review every imported skill through the normal queue rather than trusting it because it worked before | reopen unproven | The Skills screen import stores a skill unreviewed and the importer cannot decide it, but no skill has been imported on the install and review needs a second person, who does not exist there. To prove it: Import an AnyGen skill on the Skills screen and have a second administrator approve it through the review queue.. |
| M37.2.2.4 | Diff behaviour on a real task before and after import, not just that the file parses | reopen not wired | The before-and-after task check exists only in brain.migration.skills called by tests, and no skill runner exists to run a real task. |
| M37.2.2.5 | Retire any skill that has not been invoked in ninety days rather than carrying it over | reopen not wired | The ninety-day retirement rule has no production caller and there are no AnyGen invocation records to apply it to. |
| M37.2.3.1 | Rebuild each agent from a template rather than importing its configuration, because their model has no ceiling and no leash to carry across | reopen not wired | brain.migration.rebuild is called only by tests; no AnyGen agent has been rebuilt (agent.agent 0). |
| M37.2.3.2 | Set an explicit ceiling per agent, which AnyGen never had | reopen not wired | The explicit-ceiling rule lives in the uncalled rebuild module; no rebuilt agent exists on the install. |
| M37.2.3.3 | Start every rebuilt agent at Shadow on writes regardless of how it behaved there | reopen not wired | Shadow-on-writes for rebuilt agents is enforced only inside the uncalled rebuild module; no agent exists on the install. |
| M37.2.3.4 | Map their Availability onto our availability, and their absent authority onto our entitlement, separately - conflating the two is the mistake their model makes | reopen not wired | audience_for and authority_for are called only by tests; no availability mapping has been made for any agent. |
| M37.2.4.1 | Their learning is one toggle over an opaque store; ours is four tiers with a review queue, and there is no honest mapping between them | keep repo | This is a design decision, recorded in brain.migration.learning and its commit, and realised by the absence of any learning importer at f32c385. |
| M37.2.4.2 | Read the memory files as evidence and re-derive tier one preferences rather than importing beliefs wholesale | reopen not wired | Re-derivation from memory files exists only in brain.migration.learning called by tests; no tier-one preference was derived (mem.* 0). |
| M37.2.4.3 | Discard anything that would widen a scope, since it was never approved by anyone under our rules | reopen not wired | The scope-widening refusal is only reached from tests; no AnyGen memory has been read on the install. |
| M37.2.4.4 | State plainly in the handover that the new system starts without their accumulated learning, and why that is the right trade | reopen not wired | The handover note is rendered only by the uncalled learning module, and no handover document containing it exists or is served. |
| M37.3.2.4 | Adoption measured as real questions from real people, machine traffic excluded | reopen unproven | QuestionRecorder is installed on every finished request and the Adoption screen reads it, but no question has ever been asked, and the screen shows there is nothing to show. To prove it: Have a real person ask questions on the install and see them counted on Adoption, with an evaluation run excluded.. |
| M37.4.1.2 | Invariant suite green, evidenced | keep repo | ci.yml runs the invariant suite to a JUnit report, invariant_evidence fails on skips, the report is uploaded named for the commit, and deploy.yml runs only after a successful CI. |
| M37.4.2.2 | Service level statement with the RPO and RTO from the chosen profile | reopen absent | brain.launch.service_level refuses without a verified restore and nothing calls it; the install has no backups and Backup and recovery says nothing has looked at the copies. Needs: backups and a verified restore drill. |
| M37.4.2.4 | Subprocessor list covering every model provider used | reopen not wired | brain.launch.subprocessors has no caller, so no subprocessor list is rendered or served for the install. |
| M37.4.3.1 | Runbook per console screen | reopen partial | Same runbooks as M34.3.2.1: 36 registry runbooks, about 20 served screens without one, and every runbook says the screen cannot be opened. |
| M37.4.3.2 | Incident playbook per failure mode | reopen not wired | Same playbook as M34.3.2.3: reliability.MATRIX is exposed only through brain.launch.assemble, which nothing runs. |
| M37.4.3.3 | Named client owner for knowledge, grants and connectors | reopen not wired | brain.launch.Owner and owner_gaps have no caller and nothing stores a named client owner. |
| M37.4.3.4 | Scheduled job registry handed over with what each one guards | keep install | The registry with what each control guards is shown on the served Jobs screen with real rows and handed over in docs/install/operations.md via handover_lines. |
| M37.4.3.5 | Thirty-day post-launch review booked at handover | reopen not wired | review_gaps in brain.launch has no caller; nothing books or records a thirty-day review. |
| M37.5.1.1 | Every scheduled control listed with what it guards | keep install | CONTROLS lists every scheduled control with its guards sentence and the Jobs screen shows each one on the install. |
| M37.5.1.2 | CI assertion that each control appears in the scheduler | reopen partial | The CI invariant asserts registry and source agree, but it pins six known orphans and three wired-but-unscheduled controls rather than asserting each control is in the scheduler. |
| M37.5.1.3 | Alert when a scheduled job has not run within its window | reopen not wired | controls.overdue has no production caller and nothing sends an alert; the Jobs screen only displays that a run is owed, and the install has no worker, channel or email relay. |
| M37.5.1.4 | Explicit test that no safety mechanism lacks a caller | reopen partial | The explicit test exists and CI runs it, but it tolerates six named orphans (backup_exposure, denial_digest, resolution_calibration, queue_redrive, side_effect_resume, model_health_probes) instead of failing on them. |
| M37.5.2.1 | Alert routing and severity definitions | reopen not wired | alerting.route_for is reached only through controls.runbook_for, which has no caller; no alert is ever routed on the install. |
| M37.5.2.2 | Runbook per alert | reopen not wired | controls.runbook_for has no caller, so no runbook per alert is produced or published. |
| M37.5.2.3 | Escalation path to us where the client cannot resolve it | reopen not wired | The escalation ladder exists only in alerting types reached from the uncalled runbook_for; no escalation contact or route is live. |
| M37.5.3.1 | Measured spend against the projection after thirty days | reopen not wired | variance is called only from spend_view.cost_review, which nothing calls; the Spend screen says the report has not been built and the install has no spend actuals. |
| M37.5.3.2 | Estimator correction from actuals | reopen not wired | post_launch_correction is reached only from the uncalled cost_review, spend_correction is wired but not scheduled, and there are no actuals. |
| M37.5.3.3 | Budget thresholds retuned with real distribution rather than assumptions | reopen not wired | retune_alert_fraction and retune_daily_burst have no caller and the commit says the correction is applied to no default; no real distribution exists. |
| M37.6.1.3 | Decide whether the administrative consoles stay on public subdomains or move behind an SSH tunnel, and write down which and why | keep repo | The decision (public addresses with a second factor and an IP allowlist, and why) is written in docs/install/network.md and held by tests/unit/test_install_consoles.py in CI. |

### M38 Continuous delivery and live status

| Task | What it asks | Verdict | Why |
|---|---|---|---|
| M38.1.1.1 | Branch per track named for its module, so twelve concurrent tracks do not collide | reopen not wired | check_branch_name has no caller (not in any hook or workflow), origin branches do not follow the M<id>/name form, and changes have been pushed straight to main. |
| M38.1.1.2 | Commit message convention carrying the task id, for example M3.2.1 | keep repo | ops/hooks/commit-msg runs brain.ops.conventions on every commit (core.hooksPath ops/hooks), and brain.status reads the ids. |
| M38.1.1.3 | Pre-push hook running lint, types and the invariant suite locally | keep repo | ops/hooks/pre-push runs format, lint, mypy, invariants and sweeps, installed through core.hooksPath. |
| M38.1.1.4 | Pull request template listing the task ids the branch closes | keep repo | The pull request template asks for Closes: leaf ids. |
| M38.1.1.5 | CODEOWNERS forcing review on the gate, the redactor and the catalogue projection | reopen partial | CODEOWNERS names the gate, redactor and projection, but nothing forces a review: branch protection is unverified, the repository was public, and changes go straight to main. |
| M38.1.2.1 | Actions workflow: lint, type check, unit tests, invariant suite, schema sweeps | keep repo | ci.yml runs lint, types, unit shards, invariants and the schema sweeps on every push. |
| M38.1.2.2 | Migration forward and rollback against a production-shaped snapshot | reopen partial | The migration round trip runs over test-left data, not a production-shaped snapshot. |
| M38.1.2.3 | Container image build with the commit SHA as the tag | keep repo | deploy.yml builds the image tagged with the short commit SHA and latest, and the install image reports commit f32c385. |
| M38.1.2.4 | Image signing | keep repo | deploy.yml signs the digest keylessly with cosign and verifies it; the install autodeploy never verifies it. |
| M38.1.2.5 | Push to the registry | keep repo | deploy.yml pushes to ghcr.io/rpsj2230/verz-brain-v2.0, which the install pulls. |
| M38.1.3.1 | Coolify deployment triggered on every push to main | keep install | Every push that passes CI publishes latest and the host brain-autodeploy timer redeploys it; the install runs f32c385, the head of main. The trigger is the host timer rather than Coolify on push. |
| M38.1.3.2 | Migrations applied before the new image takes traffic | keep install | The app lifespan runs migrations before serving, and the install is at head 0083. |
| M38.1.3.3 | Health gate: readiness must pass before the old container stops | reopen broken | The install deploy (brain-deploy) force-recreates the only app container and then polls health, so the old container stops before the new one is ready; item 28 measured each recreate as briefly unreachable. |
| M38.1.3.4 | Automatic rollback if readiness fails within the window | reopen not wired | Rollback lives in ops/watch-and-deploy.sh, which the install does not run; brain-deploy on a failed health check only prints logs and exits 1. |
| M38.1.3.5 | Deployment record written to the ledger with the SHA and the task ids in that release | reopen not wired | Deployment records are written only by ops/watch-and-deploy.sh to a host file; the install autodeploy writes a heartbeat only and the ledger has no deployment entries. |
| M38.1.4.1 | Local compose for development | keep repo | docker-compose.yml is the local stack and the CI stack job starts it; no developer machine here has Docker. |
| M38.1.4.2 | Staging on the same server, separate compose project and database | reopen absent | There is no staging install; this install is the only one. Needs: Staging install. |
| M38.1.4.3 | Production, deployed on every push once staging has passed the same commit | reopen absent | Production deploys on every CI-green push, but no staging exists to pass the commit first. Needs: Staging install. |
| M38.1.4.4 | Seed data in staging, never production data | reopen absent | No staging install exists to hold seed data. Needs: Staging install. |
| M38.2.1.1 | Tag a release at the end of each wave | reopen absent | The tagging tool exists but no wave or release tag has ever been cut; the install runs latest. Needs: Release tag. |
| M38.2.1.6 | Wave report generated automatically from the closed task ids | reopen not wired | wave_report is reached only by the python -m brain.release CLI (never run, no tags) and the unsent digest. |
| M38.2.2.1 | W0: nothing user-facing, but the pipeline itself deploys and is provably working | keep install | The pipeline deploys the install automatically and it serves the commit it built (f32c385) with /build 200. |
| M38.2.2.2 | W1: a person asks in the console and gets a permission-correct answer from seeded data | reopen partial | 5fa4718 says wave one is proven only as a composition and is live only on an install with BRAIN_TOOL_SOURCE=demo and the demo loaded; the install has neither, Records answers 404 and nothing has ever been asked. To prove it: On an install reading the demo, a signed-in person asks one question in the console and gets the permission-correct answer.. |
| M38.2.2.3 | W2: the same question answered in Lark, against connector cassettes | reopen absent | Answering in Lark needs a Lark channel credential and a model call; the install has neither and the milestone exists only as an e2e test over fakes. Needs: Lark channel credentials. |
| M38.3.1.1 | CI step parsing every commit message for task ids | keep repo | The Deploy workflow runs python -m brain.status, which parses subjects and Closes: trailers for leaf ids. |
| M38.3.1.2 | Status file written to the repository on each merge to main | reopen partial | docs/status.json is gitignored and never written to the repository; it is computed inside the Deploy build and baked into the image. |
| M38.3.1.3 | Task marked done only when its branch merges and CI is green, never by hand | keep install | The served tracker ticks leaves only from /api/status.json, computed from commits in the Deploy build that runs only after CI succeeds. |
| M38.3.1.4 | Percentage, per-wave and per-module progress computed from that file | keep install | status.json carries overall, per-wave and per-module progress, shown on /build and /build/tracker (200 on the install). |
| M38.3.2.1 | Status page served by the application itself at a fixed path | keep install | The app serves the status page at /build, 200 on the install. |
| M38.3.2.2 | Reads the status file baked into the image at build time | keep install | /build reads docs/status.json generated in the Deploy build and copied into the image. |
| M38.3.2.3 | Shows overall percentage, current wave, tasks closed today, and what is next | keep install | /build renders percentage, current wave, closed today and next up from status.json. |
| M38.3.2.4 | Shows the deployed commit SHA and when it shipped | keep install | /build shows the commit from status.json and shipped time from the RELEASE.json baked into the image. |
| M38.3.2.5 | Visible to the client so progress needs no meeting | keep install | /build is public on the install address with no sign-in. |
| M38.3.3.1 | Automated message to a Lark channel each evening | reopen not wired | The evening digest has no sender: deliver_digest is referenced only by name in notices.py, no scheduled control or worker job calls it, and the install has no worker and no Lark channel credentials. Needs: Worker, Lark channel credentials. |
| M38.3.3.2 | Tasks closed, tasks opened, anything overdue | reopen not wired | The digest content is composed in brain.ops.digest but never sent. The evening digest has no sender: deliver_digest is referenced only by name in notices.py, no scheduled control or worker job calls it, and the install has no worker and no Lark channel credentials. |
| M38.3.3.3 | Wave burn-down against the target date | reopen not wired | The burn-down is part of the digest, which nothing sends. |
| M38.3.3.4 | Sent by the system itself once W2 has shipped the Lark channel | reopen not wired | The evening digest has no sender: deliver_digest is referenced only by name in notices.py, no scheduled control or worker job calls it, and the install has no worker and no Lark channel credentials. Needs: Lark channel credentials. |
| M38.4.1.1 | Every connector built and tested against recorded cassettes, so credentials never block development | reopen partial | Google Drive is tested from documentation because no cassette exists for it. |
| M38.4.1.2 | Contract tests proving the adapter matches the recorded shape | reopen partial | The contract invariant only checks that a connector test file contains the word cassettes; test_google_drive.py passes by saying no recording exists. |
| M38.4.1.3 | Credential slots defined in OpenBao with the scopes each connector needs, empty until go-live | reopen absent | Credential slots in OpenBao need a vault; the install has none (Connectors: connecting needs the vault). Needs: Secrets vault (OpenBao). |

### M39 Agent workspace, the inside-an-agent surface

| Task | What it asks | Verdict | Why |
|---|---|---|---|
| M39.1.1.1 | An agent record composes persona, knowledge scope, connectors, skills, channels, availability, leash, model policy, memory and automations | reopen partial | The served composition (composition_rows over manifest paths) covers five of the ten parts: persona, connectors, skills, model policy and leash; knowledge scope, channels, availability, memory and automations are not composed, and workspace.compose is unwired. No agent can exist on the install to compose anything. Needs: agent creation (nothing on the install installs an agent). |
| M39.1.1.2 | Every attachment stored as a reference with a pinned version, never an inline copy | reopen not wired | Pinned references exist on the one served attach path (skill assignment pins a digest via register_skill and attach_skill), but that path needs an agent and nothing on the install can create one, so no attachment can ever be stored there. Needs: agent creation (nothing on the install installs an agent). |
| M39.1.1.3 | Attachment add and remove written to the ledger with who, when and why | reopen not wired | The ledger rows come from triggers on served writes (skill assignment 0056, automation install 0055, schedule change 0067), but each such write names an agent and none can be created on the install; removal is reachable only as the detach inside a skill replacement or an automation stop. Needs: agent creation (nothing on the install installs an agent). |
| M39.1.1.4 | Divergence flag when an instance edits something its template supplies | reopen not wired | divergent_parts is served in WorkspaceView.divergent and drawn on the Dashboard pane, but it needs an installed agent and nothing on the install produces one. Needs: agent creation (nothing on the install installs an agent). |
| M39.1.1.5 | Composition diff between an instance and its template, rendered side by side | reopen not wired | composition_rows is served and CompositionDiff renders it in the Profile pane, but the rows need an installed agent and nothing on the install produces one. Needs: agent creation (nothing on the install installs an agent). |
| M39.1.2.1 | Agent header: avatar, name, role line, owner, template lineage and version | reopen not wired | The header (initials avatar, name, summary as role line, owner, template id and version) is routed and served, but there is no agent to open and no path on the install creates one. Needs: agent creation (nothing on the install installs an agent). |
| M39.1.2.2 | Tab strip: Conversations, Automations, People, Knowledge, Memory, Artifacts, Settings | reopen partial | Seven tabs are declared but the served strip only ever holds Settings and Automations, and only the Automations panel draws anything; Conversations, People, Knowledge, Memory and Artifacts are never offered. No agent exists to show even those two. Needs: agent creation (nothing on the install installs an agent). |
| M39.1.2.3 | Right pane switches between Dashboard and Profile without losing tab state | reopen not wired | The Dashboard and Profile pane switch is client state in AgentWorkspace and is served, but it renders only inside an agent workspace and no agent can exist on the install. Needs: agent creation (nothing on the install installs an agent). |
| M39.1.2.4 | Deep link to any tab so a console alert can point at the exact place | reopen partial | The address agents/:agentId/:tab is routed, but only two of seven tabs can be landed on, and brain.console.workspace.deep_link has no production caller, so no console alert points at an agent tab. No agent exists either. Needs: agent creation (nothing on the install installs an agent). |
| M39.1.2.5 | Keyboard navigation across tabs and back to the roster | reopen not wired | Arrow keys across tabs and Escape to the roster link are in the served AgentWorkspace and the roster page exists, but the keyboard map only runs inside an agent workspace and no agent can exist on the install. Needs: agent creation (nothing on the install installs an agent). |
| M39.1.3.1 | Spend, runs and messages as the agent's headline figures, not a buried report | reopen partial | The served headline carries spend and runs only: HeadlineView has no messages field and no turns are passed (a0b6b8a: a Turn carries no agent id). Spend rows have no writer on the install (ops.spend_store.record uncalled, ops.spend_actual 0) and no agent exists. Needs: agent creation (nothing on the install installs an agent). |
| M39.1.3.2 | Range selector across seven, thirty and ninety days and month to date | reopen not wired | Range and window exist, but the route fixes HEADLINE_RANGE to thirty days with no range parameter, and the console draws no selector. |
| M39.1.3.3 | Cost attributed per caller so a single heavy user is visible | reopen not wired | Per-caller attribution is workspace.by_caller, which nothing outside its module calls; the workspace route serves no per-caller rows. |
| M39.1.3.4 | Projection to month end against the agent's own budget | reopen not wired | workspace.projection is not called by any route, and nothing on the install writes a per-agent budget (ops.budget_version 0, no route writes it). |
| M39.2.1.1 | Connector row with per-source icons and an overflow count | reopen partial | The connector strip with an overflow sentence is served, but the console draws each source as a code-formatted name with no per-source icon; and no agent can exist to carry a connector. Needs: agent creation (nothing on the install installs an agent). |
| M39.2.1.2 | Attach and detach with the capability check performed at attach time | reopen not wired | agent_tabs.attach, the attach-time capability check, has no production caller, and no route attaches or detaches a connector on an agent. |
| M39.2.1.3 | Each connector shows which fields it projects for this agent | reopen not wired | workspace_capabilities.projected_for is unwired; the served ConnectorView carries only source and presence, no projected fields. |
| M39.2.1.4 | A connector the agent asks for but the department has not granted shown as requested, not attached | reopen not wired | Presence (attached versus requested) is served on each connector row, but it needs an installed agent and nothing on the install produces one. Needs: agent creation (nothing on the install installs an agent). |
| M39.2.1.5 | Health inherited from the connector page so a degraded source is visible here | reopen not wired | No health travels on the served connector row (source and presence only), so a degraded source cannot show on the agent; the install has no connected source either. |
| M39.2.2.1 | Skill chips with version, source and review state | reopen partial | Served skill rows carry name and digest only; source and review state are not sent (agent_tabs.chip_for and chips_for are unwired). No agent exists to carry a skill. Needs: agent creation (nothing on the install installs an agent). |
| M39.2.2.2 | Attach only from the approved library, never directly from a repository | reopen not wired | The served assignment route goes through register_skill and attach_skill, which pin only an approved digest, but it needs an agent (none can be created) and an approval by somebody other than the adder while the install has one person and no skill. Needs: agent creation (nothing on the install installs an agent). |
| M39.2.2.3 | Unapproved skill offered with a route to the review queue rather than an attach button | reopen not wired | offer_for and review_route have no production caller; the agent workspace offers no unapproved skill and no route to the review queue. |
| M39.2.2.4 | Per-skill invocation count so unused attachments can be pruned | reopen not wired | skill_usage has no production caller and nothing records a skill invocation. |
| M39.2.2.5 | Skill description convention enforced: the description is the router prompt | reopen not wired | description_refusals and router_collisions run inside register_skill on the served assignment route, but that route needs an agent and none can be created, so the rule has never run and cannot run on the install. Needs: agent creation (nothing on the install installs an agent). |
| M39.2.3.1 | Knowledge scope expressed as a predicate, not a document list | reopen not wired | KnowledgeScope is used only inside agent_tabs functions no route calls, and knowledge is not a manifest composition path, so no agent knowledge scope is served. |
| M39.2.3.2 | Add by upload, by link extraction, or by widening the predicate | reopen not wired | plan_addition and widen have no production caller; no route adds knowledge to an agent by upload, link or predicate, and documents cannot be brought in on the install. |
| M39.2.3.3 | Preview of how many items the predicate currently matches | reopen not wired | preview_count has no production caller and the workspace serves no knowledge block. |
| M39.2.3.4 | Coverage and staleness for exactly this agent's slice | reopen not wired | slice_health has no production caller; no per-agent coverage or staleness is served. |
| M39.2.3.5 | Items the agent retrieved most and items it never touched | reopen not wired | item_usage has no production caller and nothing records a retrieval per agent. |
| M39.2.4.1 | Channel row with per-channel enable | reopen partial | Channel rows are served (channel and rendering profile), but there is no per-channel enable: channel_rows and enable are unwired and no route writes one. No agent exists either. Needs: agent creation (nothing on the install installs an agent). |
| M39.2.4.2 | Channel capability intersected with the agent ceiling before it is offered | reopen not wired | offered_channels at run_reach(caller, agent ceiling) is served in WorkspaceView.channels, but it needs an agent record and nothing on the install produces one. Needs: agent creation (nothing on the install installs an agent). |
| M39.2.4.3 | Per-channel rendering profile chosen automatically | reopen not wired | rendering_profile is served per offered channel, but only for an agent and nothing on the install produces one. Needs: agent creation (nothing on the install installs an agent). |
| M39.2.4.4 | Group installation where the channel supports it | reopen not wired | install_to_group has no production caller and a48f3a9 states no adapter declares Feature.GROUP_INSTALL; the install has no channel credentials either. Needs: channel credentials (Slack, Teams, Lark). |
| M39.3.1.1 | Availability block: which users and departments can find and invoke this agent | reopen not wired | availability_block has no production caller and no Availability block is served on the agent; no agent exists. Needs: agent creation (nothing on the install installs an agent). |
| M39.3.1.2 | Ceiling block: the widest entitlement any run of this agent may reach | reopen not wired | ceiling_block has no production caller; the roster carries only a scope dump and no agent exists. Needs: agent creation (nothing on the install installs an agent). |
| M39.3.1.3 | Explicit copy stating that availability is discovery and never authority | reopen not wired | The discovery-not-authority copy lives with availability_block and ceiling_block, which nothing served renders. |
| M39.3.1.4 | Worked preview: pick a person, see exactly what their run of this agent would return | reopen not wired | run_preview has no production caller; no screen offers a pick-a-person run preview. |
| M39.3.1.5 | Preview computed by the real gate, never by a separate estimation path | reopen not wired | The real-gate preview is run_preview, which nothing served calls. |
| M39.3.2.1 | Leash matrix per target and scope, read, draft and write on separate rungs | reopen not wired | leash_matrix is reached only via govern_estate.leash_estate, which no route calls. |
| M39.3.2.2 | Promotion requires evidence: clean runs, agreement rate and a named approver | reopen not wired | No route promotes a leash rung; there is no agent, no run and one person on the install. Needs: agent creation (nothing on the install installs an agent). |
| M39.3.2.3 | Demotion by circuit breaker shown with the metric that tripped it | reopen not wired | breaker_trips has no production caller and no agent run exists. |
| M39.3.2.4 | Money and irreversibility boundaries pinned so a rung cannot rise without a second approver | reopen not wired | may_raise is reachable only through scoped_authority helpers no leash route calls; no rung can rise on the install. Needs: agent creation (nothing on the install installs an agent). |
| M39.3.2.5 | History of every promotion and demotion with the evidence attached | reopen not wired | rung_history is reached only via govern_estate.rung_estate, which no route calls. |
| M39.4.1.1 | Memory rendered as readable text, never an opaque store | reopen broken | Memory text is served only on the person Memory screen (404 for the administrator) and My workspace; nothing on the install can form a memory, and no agent Memory tab is served. |
| M39.4.1.2 | Split between curated memory and memory extracted from conversations | reopen broken | split_memory is served via subject_memory on /govern/memory (404 for the administrator) and My workspace, where no memory can ever exist. |
| M39.4.1.3 | Change history with a diff per revision and the trigger that caused it | reopen broken | revisions is served only on /govern/memory, which answers 404 for the administrator, and no memory can form. |
| M39.4.1.4 | Direct edit and direct delete by the owner | reopen not wired | edit_own_memory has no production caller and 5288058 says no route offers edit or delete. |
| M39.4.1.5 | Per-agent and per-person memory shown separately so neither hides the other | reopen not wired | separate_memory has no production caller and the agent Memory tab is never served. |
| M39.4.2.1 | Which tiers are active for this agent | reopen not wired | active_tiers has no production caller; no agent Learning surface is served. |
| M39.4.2.2 | Tier one changes listed with one-click undo, never an approval queue | reopen broken | Tier one undo is served estate-wide on /govern/learning, which answers 404 for the administrator, and no learning can form. |
| M39.4.2.3 | Tier two rules shown with their shadow evidence and a promote control | reopen partial | Tier two rows are listed but the Learning page says Promote is not offered, and the screen answers 404 for the administrator. |
| M39.4.2.4 | Tier three items routed to the department queue with a link back here | reopen partial | Routing of gated changes is shown but decide is not offered, there is no link back to an agent tab and no department; screen 404 for the administrator. |
| M39.4.2.5 | A single control to freeze all learning for this agent during an incident | reopen not wired | freeze has no production caller; no control freezes learning for an agent. |
| M39.5.1.1 | Every produced document, deck, report, export and image recorded as an artifact | reopen not wired | StoredArtifacts.keep has no caller and the install has no object store, so nothing is ever recorded as an artifact. Needs: object store. |
| M39.5.1.2 | Artifact carries producing run, agent version, caller, entitlement hash and timestamp | reopen not wired | agent.artifact has the columns (0058), but nothing on the install can produce an artifact, so no record carrying them can exist. Needs: object store. |
| M39.5.1.4 | Field-level redaction applied at production time, so an artifact never exceeds its caller | reopen not wired | producible_fields has no production caller and no artifact is produced. |
| M39.5.1.5 | Re-download re-checks the requester entitlement rather than trusting the original link | reopen not wired | may_download is imported only by unwired member_library and no download is served. |
| M39.5.2.1 | Artifacts tab listing what this agent produced, newest first | reopen not wired | The agent Artifacts tab is never in the served strip; the estate screen is empty and nothing produces artifacts. |
| M39.5.2.2 | Filter by type, by caller and by date | reopen not wired | No filter by type, caller or date exists: GET /govern/artifacts takes no parameters and no agent tab is served. |
| M39.5.2.3 | Provenance panel: which sources and which knowledge items fed it | reopen not wired | provenance_for has no production caller; the route says sources and knowledge items do not travel. |
| M39.5.2.4 | Supersede and archive rather than delete | reopen not wired | supersede and archive have no production caller; the artifacts route is read only. |
| M39.5.2.5 | Artifact count and storage shown against the retention policy | reopen not wired | storage_summary has no production caller; no count or storage against retention is served. |
| M39.6.1.1 | Automations listed under the agent that owns them, never in a global pile | reopen not wired | GET /agents/{id}/automations is served, but it needs an agent and nothing on the install produces one. Needs: agent creation (nothing on the install installs an agent). |
| M39.6.1.2 | Named as outcomes in the first person, not as trigger and action mechanics | reopen not wired | Outcome names are checked in the served gallery, but the gallery opens only on an agent and none can exist. Needs: agent creation (nothing on the install installs an agent). |
| M39.6.1.3 | Template gallery for common automations with a one-step install | reopen not wired | The gallery and one-step install are served, but only on an agent and none can exist; installed automations never run without a worker. Needs: agent creation (nothing on the install installs an agent). |
| M39.6.1.4 | Per-automation run history with the last result and the next scheduled time | reopen absent | Run history is served, but runs are made only by the worker, which the install does not run, and no agent exists. Needs: worker (brain.ops.worker with the automation_run control); facts: no worker process on the install. |
| M39.6.1.5 | Pause, resume and delete with the scheduler registry updated in the same transaction | reopen partial | Start and stop routes exist but delete does not: agent_automations.remove has no caller and no delete route is mounted; no agent exists. Needs: agent creation (nothing on the install installs an agent). |
| M39.6.2.1 | An automation runs on a named principal, never on the agent itself | reopen absent | Automations run as their owner only in the worker, which the install does not run; no automation exists. Needs: worker (brain.ops.worker with the automation_run control); facts: no worker process on the install. |
| M39.6.2.2 | Schedule changes are a tier three learning event and need a human | reopen not wired | may_change_schedule guards the served start route, but it needs an agent (none can exist) and a second person (one principal). Needs: agent creation (nothing on the install installs an agent). |
| M39.6.2.3 | Failure after a threshold pauses the automation and notifies the owner | reopen absent | failure_pause runs only in the worker run loop and owner notices need a sender; neither runs on the install. Needs: worker (brain.ops.worker with the automation_run control); facts: no worker process on the install. |
| M39.6.2.4 | Every automation appears in the scheduled job registry with what it guards | reopen partial | The registry entry is the automation row itself; automations do not appear on the Scheduled jobs screen, and no automation can exist without an agent. Needs: agent creation (nothing on the install installs an agent). |

### M40 Member self-service application

| Task | What it asks | Verdict | Why |
|---|---|---|---|
| M40.1.1.1 | Single sign-on through Keycloak with the same identity the channels use | reopen absent | Web sign-in through Keycloak is exercised (3 sessions), but the same identity on a channel cannot be shown: the install has no channel credentials. Needs: channel credentials (Lark, Slack, Teams). |
| M40.1.1.2 | Channel identity and web identity resolved to one principal, never two | reopen not wired | one_principal has no production caller and no channel exists on the install. Needs: channel credentials. |
| M40.1.1.3 | Session policy, idle timeout and device list | reopen partial | Keycloak holds the session policy, but no member device list is served (devices and end_device unwired); only the admin Sessions screen lists sessions. |
| M40.1.1.4 | Password reset and recovery handled by the identity provider, not by us | reopen unproven | The app has no password reset of its own and the realm allows reset, but no reset or recovery has happened on the install and the realm export names no SMTP server. To prove it: a user completes a password reset or recovery through Keycloak on the live realm. |
| M40.1.1.5 | Explicit test that a channel-only user can sign in and sees the same entitlement | reopen not wired | The test exists but drives member.shell.sign_in and one_principal over an in-memory store; neither is called by anything served, and no channel exists. |
| M40.1.2.1 | Member navigation distinct from the admin console and much shorter | reopen unproven | The served menu for a non-company reader is the department console plus Use (member.shell.member_navigation is unwired), and no ordinary member has signed in. To prove it: bind the second Keycloak user as an ordinary member, sign in, and see a short Use-only menu. |
| M40.1.2.2 | No administrative surface reachable by URL guessing, enforced server side | reopen unproven | Each served API route checks console.reads.permitted, but member.shell.serve is unwired and no non-administrator has ever requested an admin route. To prove it: as an ordinary member, request an admin route such as /api/v1/govern/people and get the same 404 as a missing path. |
| M40.1.2.3 | Responsive down to a phone since approvals arrive on phones | reopen unproven | Phone-width CSS is in the served bundle (ee744e0) but nothing shows it was checked on the install. To prove it: open the install at 360 pixels wide and see the one-strip navigation and no horizontal scroll. |
| M40.1.2.4 | Entitlement disclosure line rendered from the real entitlement set, never hand-written | reopen unproven | disclosure_line is served on My workspace from the real grants, but the browser pass reports only all-zero figures and does not show the line. To prove it: open My workspace as the administrator and see the line naming the nouns of his 76 grants. |
| M40.2.1.1 | Questions asked this month with corrections separated out | reopen partial | Questions this month are counted, but corrections are not separated: the page says nothing records a correction; nothing has been asked either. |
| M40.2.1.2 | Agents this person can call, split between department-provided and self-built | reopen not wired | my_agents is served, but no agent can exist on the install. Needs: agent creation (nothing on the install installs an agent). |
| M40.2.1.3 | Personal budget consumed against the personal cap | reopen not wired | personal_budget is served, but no route writes a budget and nothing writes spend. |
| M40.2.1.4 | Count of things learned about this person, each reversible | reopen partial | Learned memories are listed but not reversible (learned_undo says undo is not recorded), and nothing on the install forms a memory. |
| M40.2.1.5 | Recent threads continued from any channel, so web and Lark are one history | reopen not wired | recent_threads has no production caller and chat.* is never written. |
| M40.2.2.1 | List with source, where each one runs and how often this person used it | reopen not wired | my_agents is served with provision, channels and uses, but no agent can exist on the install. Needs: agent creation (nothing on the install installs an agent). |
| M40.2.2.2 | Open an agent into the same workspace surface, bounded to what this person may see | reopen not wired | open_workspace has no production caller, My workspace does not link to an agent, and no agent exists. Needs: agent creation (nothing on the install installs an agent). |
| M40.2.2.3 | Build a personal agent from a template within the person's own ceiling | reopen not wired | build_personal_agent has no production caller. |
| M40.2.2.4 | Personal agent visible to nobody else unless explicitly shared | reopen not wired | personal_audience has no production caller and no personal agent can be built or shared. |
| M40.2.2.5 | Request an agent the person cannot yet call, routed to the department admin | reopen not wired | request_agent has no production caller. |
| M40.3.1.1 | Items this person uploaded with visibility scope, verification date and review due | reopen partial | Owned items are listed with visibility, but verification date and review due are not (the page says they are not recorded); know.item is 0 and documents cannot be brought in. |
| M40.3.1.2 | Upload, replace and retire with the review cycle set at upload time | reopen not wired | upload, replace and retire have no production caller. |
| M40.3.1.3 | Usage count so an author can see whether anything reads what they wrote | reopen not wired | authored_usage has no production caller; use is not recorded. |
| M40.3.1.4 | Request promotion to company-wide, routed as a tier three decision | reopen not wired | request_promotion has no production caller. |
| M40.3.1.5 | Never shows an item outside this person's entitlement, including their own department's restricted rows | reopen not wired | reachable_own_items has no production caller; the served My workspace lists owned items through a SECURITY DEFINER function that checks ownership, not current entitlement. |
| M40.3.2.1 | Personal skills with source, version and review state | reopen not wired | my_skills has no production caller. |
| M40.3.2.2 | Author in the studio, import from a repository or paste a URL | reopen not wired | add_skill has no production caller; the admin Skills screen is the only import path. |
| M40.3.2.3 | Submit to the department library for review | reopen not wired | submit_for_review has no production caller. |
| M40.3.2.4 | Personal skills usable only by this person's own agents until approved | reopen not wired | attach_refusals has no production caller. |
| M40.3.3.1 | Everything produced for this person by any agent | reopen not wired | produced_for_me has no production caller and nothing produces artifacts. |
| M40.3.3.2 | Re-download re-checked against current entitlement | reopen not wired | may_redownload has no production caller and no download is served. |
| M40.3.3.3 | Share with a colleague only where that colleague's entitlement already covers the contents | reopen not wired | share has no production caller. |
| M40.4.1.1 | Plain-language list of every preference learned, with the evidence that produced it | reopen partial | My workspace lists learned statements without the evidence behind them (learned_about_me is unwired), and nothing forms a memory. |
| M40.4.1.2 | One-click undo per item and an undo-all control | reopen not wired | undo_one and undo_all have no production caller; My workspace says undo is not recorded. |
| M40.4.1.3 | Weekly digest delivered in the person's own channel with the same controls | reopen not wired | digest_for_channel has no production caller; no worker or channel exists. Needs: worker, channel credentials. |
| M40.4.1.4 | Opt out of personal learning entirely, with the cost stated honestly | reopen not wired | opt_out has no production caller. |
| M40.4.2.1 | What is stored about this person and where, in plain language | reopen not wired | what_is_stored has no production caller; the DSAR export says the per-person read is not built. |
| M40.4.2.2 | My conversation history with export | reopen not wired | export_my_history has no production caller and chat.* is empty. |
| M40.4.2.3 | Deletion request routed to the retention policy rather than performed silently | reopen not wired | request_deletion has no production caller; only the admin erasure route files a request. |
| M40.4.2.4 | Which agents have read my HR record, from the audit ledger | reopen not wired | who_read_my_record has no production caller and no agent reads HR records on the install. |
| M40.5.1.1 | Personal account connections with per-source consent | reopen not wired | connect has no production caller and no personal connection table exists. |
| M40.5.1.2 | Explicit copy stating that connecting an account never widens what the person may see | reopen not wired | The copy is a constant in member.connections, which nothing serves; My workspace says accounts are not read here. |
| M40.5.1.3 | Disconnect with immediate token revocation | reopen not wired | disconnect has no production caller and no token revoker is implemented. |
| M40.5.1.4 | Department-provided connections shown as inherited and not disconnectable here | reopen not wired | inherited has no production caller; no department connection exists. |
| M40.5.2.1 | Which channels this person is reachable on and which is primary | reopen not wired | reachable_on has no production caller and no channel exists. |
| M40.5.2.2 | Per-channel notification preferences | reopen not wired | Channel preferences have no table or route. |
| M40.5.2.3 | Approval routing preference for envelope decisions | reopen not wired | channels_that_may_approve has no production caller. |
| M40.5.2.4 | Quiet hours honoured by automations and digests | reopen not wired | may_deliver has no production caller; no worker or sender runs automations or digests. |
| M40.6.1.1 | Envelope approvals awaiting this person, with what will happen stated before the fact | reopen not wired | The Approvals queue is served, but nothing on the install creates a suspension, and effect_sentence is unwired. Needs: a suspended action (nothing creates one). |
| M40.6.1.2 | Approve, reject and amend, each written to the ledger | reopen not wired | The decision route writes the ledger (af7a7ef, c6cbbcc), but no suspension can exist to decide. Needs: a suspended action (nothing creates one). |
| M40.6.1.3 | Expiry on an unanswered approval with a stated default of doing nothing | reopen not wired | lapsed is unwired in member.approvals and no suspension can exist to expire. Needs: a suspended action (nothing creates one). |
| M40.6.1.4 | Same approval reachable from Lark and from the web without double action | reopen not wired | answer_once has no production caller and the install has no Lark credentials. Needs: Lark channel credentials. |
| M40.6.1.5 | Mobile layout treated as the primary case for approvals | reopen not wired | Phone CSS for approval cards is served, but no approval can exist to show a card. Needs: a suspended action (nothing creates one). |

### M41 Client independence, built in rather than stripped out

| Task | What it asks | Verdict | Why |
|---|---|---|---|
| M41.1.1 | Sweep refusing any client name, domain, work address, company id or storage path literal in src, migrations, console and compose | keep repo | brain.ops.sweeps client_independence runs in the CI sweeps job on every commit. |
| M41.1.2 | One settings module as the only place a client-specific value is read, so nothing reads the environment directly | keep repo | Only brain/settings.py reads os.environ, and the independence sweep refuses a second reader in CI. |
| M41.1.3 | Every client-specific value an environment variable with a declared default, a documented meaning and a refusal when it is missing | keep repo | INSTALLATION declares each client value with meaning, default and whether startup refuses without it, and CI checks it. |
| M41.1.4 | Branding as configuration: company name, logo, colours, product name, sender address | reopen partial | Company name, logo and product name are set by the wizard on the install, but nothing reads INSTALL_SENDER_ADDRESS and the accent colour is read only by build pages. |
| M41.1.5 | Identity provider as configuration: realm, issuer, client id, redirect URIs, brokered directory | reopen partial | Issuer, redirect URIs and brokered directory are configured on the install, but nothing reads INSTALL_OIDC_REALM. |
| M41.1.6 | Model and provider choice as configuration, including a local-only profile with no external provider | reopen absent | The hosted profile and two rungs are set, but no model has ever been called and the local-only profile needs an inference server the install does not run. Needs: Inference server. |
| M41.1.7 | Storage, object store and vector store locations as configuration | reopen absent | Storage shows the default http://seaweedfs:8333 and connects to nothing, and nothing reads INSTALL_VECTOR_STORE. Needs: Object store. |
| M41.1.8 | The sweep runs in CI on every commit, so the final audit verifies a gate that has been green all along rather than cleaning a year of drift | keep repo | The client_independence sweep is a step in the CI sweeps job on every push. |
| M41.1.9 | Test: the application boots and answers a question configured as a fictitious company with no Verz value present | keep repo | The fictitious-company boot test is part of the unit suite CI runs; the install itself has never answered a question. |
| M41.2.1 | Migrations build every schema, table, index, constraint, trigger and row-level security policy from an empty database | keep install | The install schema was built by migrations from empty to 0083 with RLS on every table, and CI install job repeats it on an empty database. |
| M41.2.2 | Generic demo seed: a fictitious company, departments, people and records, deliberately not the Verz test fixture | keep repo | brain.demo is a fictitious company separate from tests/fixtures/company.py, loaded and removed in CI install job. |
| M41.2.3 | No install step depends on a Verz database, dump, backup or export | keep install | The install came up from migrations and the setup wizard with no dump or backup (no backup job exists at all). |
| M41.2.4 | First-run setup creating the first administrator, with no default password anywhere | keep install | The setup wizard ran on the install and appointed one first administrator bound to Keycloak, with no default password. |
| M41.2.5 | CI job installing from empty into a scratch container and asking one question end to end | keep repo | CI install job migrates an empty database, seeds the demo and asks one question with brain.seed --smoke. |
| M41.2.6 | Uninstall and data-return procedure, because a client who leaves must be able to take their data and leave nothing behind | reopen partial | Only demo removal is exercised; on the install the DSAR, knowledge and legal exports are Not available yet, so a leaving client cannot take their data. |
| M41.2.7 | Starter pack loaded at install: the six roles, standard permission sets, the standard agents and sensible defaults, so a new client opens a furnished system rather than a blank one | reopen partial | The starter set is furnished (registry 94, pack starter, company scope, six roles shown), but no standard agent is installed: Agents shows none. |
| M41.2.8 | Starter pack is separate from the demo seed, because a client wants the roles and not the fictitious records | keep install | The install is furnished with the starter set and holds no demo rows. |
| M41.3.1 | The repository is the template and a client install is a release tag plus one environment file, so a fix reaches every client rather than one copy | reopen absent | No release tag has ever been cut; the install runs latest, and Version and updates cannot say which release runs. Needs: Release tag. |
| M41.3.2 | No duplicated repository anywhere, because a copy diverges the first time a bug is fixed in one and not the other and nothing says so | reopen unproven | Nothing in this repository shows whether another copy exists; this v2 was forked by copying the v1 Company Brain repository. To prove it: Confirm the v1 repository is archived and no other copy of this code is developed.. |
| M41.3.3 | Repository structure a reader can navigate: what is application, what is configured per client, what is never edited, where migrations, scripts and documentation live | keep repo | docs/repository-map.md describes application, per-client configuration, migrations, scripts and docs. |
| M41.3.4 | Verz is one deployment: its values live in a per-install file and never in this repository | keep repo | The install values live in its Coolify env and ops.setting rows, and the independence sweep keeps them out of the repository. |

### M42 Client deployment template

| Task | What it asks | Verdict | Why |
|---|---|---|---|
| M42.1.1 | Per-client variables in a separate private repository, one file per install, documented field by field | reopen absent | No per-install variables repository exists; the install values live in the Coolify panel. Needs: private per-install variables repository. |
| M42.1.2 | Secrets minted per install and never copied between clients, enforced by a check that refuses a shared value | reopen not wired | shared_secrets has no caller outside tests and the install secrets were set by hand. |
| M42.1.3 | One published image deployed by tag, never rebuilt per client | reopen absent | The install runs :latest, not a release tag. Needs: published release tag. |
| M42.1.4 | Release pinning per client, so one client can hold a version back without forking | reopen absent | No release exists to pin; Version and updates says this install pins no release. Needs: published release tag. |
| M42.2.1 | Server requirements and a recommended specification per profile, with the memory and connection arithmetic already measured | keep repo | install.md has the per-profile sizing table and requirements.py the arithmetic; cores and disk are stated as ratios. |
| M42.2.2 | Operating system, runtime, package and port requirements | keep repo | install.md Before you start and network.md list OS, runtime, packages and ports, held by tests. |
| M42.2.3 | Step-by-step install from a clean server, written so it needs no reference to the Verz install | reopen absent | The step-by-step install needs a release archive that has never been published and has never been followed. Needs: published release. |
| M42.2.4 | Domain, DNS, reverse proxy, TLS and firewall setup | keep repo | network.md covers domain, DNS, proxy, TLS and firewall, checked against every compose port. |
| M42.2.5 | Configuration guide: every value a new client must set, in one table, with where each comes from | keep repo | configuration.md has one table of every value and where it comes from, held against .env.example. |
| M42.2.6 | Integration guide per connector, including what each one is trusted to read | keep repo | integrations.md has a section per connector with what it is trusted to read. |
| M42.2.7 | Authentication and administrator setup, including the staff source choice | keep repo | authentication.md covers staff source choice and first administrators. |
| M42.2.8 | Backup, monitoring, logging and health-check configuration | reopen partial | operations.md documents a WAL, incremental and full backup schedule that nothing runs, not the nightly brain-backup timer that exists. |
| M42.2.9 | Troubleshooting guide built from the failures actually hit during this build, symptom first | keep repo | troubleshooting.md lists symptom-first entries from failures hit, figures held against code. |
| M42.3.1 | Install script, idempotent and safe to run twice | reopen absent | install.sh has never run on any server and has no release to download. Needs: published release. |
| M42.3.2 | Environment template generator that refuses to emit a value it cannot explain | reopen not wired | variables.template_for has no caller outside tests. |
| M42.3.3 | Database create, migrate and seed scripts | reopen not wired | brain.deployment.database create, migrate and seed are run by nothing; the install migrates with alembic. |
| M42.3.4 | Health-check script a person runs after deploying and can read the output of | reopen unproven | A health-check script exists but nothing on the install shows it has been run. To prove it: Run the health-check script against the install and read its output.. |
| M42.3.6 | Update script pinning a release tag, and a rollback script re-pinning the previous one | reopen absent | No release tag exists and update and rollback have never run on the install. Needs: published release tags. |
| M42.3.7 | Deployment checklist: pre-deployment, server, application, database, environment, integrations, security, testing, go-live and post-deployment | keep repo | docs/install/checklist.md has the ten sections, held by install_docs checks in CI. |
| M42.3.8 | A published release for every fix, with plain-English notes stating what changed, whether it changes the database, and how urgent it is | reopen absent | No release has ever been published, so no release notes exist. Needs: published releases. |
| M42.3.9 | The console shows the running version and whether a newer release exists, because nothing else reminds a client IT team to update | reopen absent | Version and updates says it cannot tell which release is running and whether a newer one exists cannot be answered. Needs: release tag and release feed. |
| M42.4.1 | Audit source, configuration, compose, environment files and git history for any remaining client-specific value | keep repo | The client_independence sweep runs in CI over source, config, compose and env files. |
| M42.5.1 | install.sh: one command on a fresh server that checks the machine, installs what is missing, downloads the release, builds the database and starts everything | reopen absent | install.sh has never run on a server and has no release to download; the install was made by hand. Needs: published release. |
| M42.5.2 | Every password, key and secret generated on the client server during install, never defaulted and never shipped in the template | reopen not wired | The mint step is in install.sh, which never ran; the install secrets were set by hand in Coolify. |
| M42.5.3 | A setup code printed once by the installer and required by the first wizard screen, so whoever loads the page first cannot claim the system | reopen absent | No installer printed the setup code; it was set by hand in Coolify. The wizard half did require it. Needs: installer run. |
| M42.5.4 | Setup wizard in the console: numbered steps with visible progress, step 3 of 10, and a back button on every screen | reopen unproven | The wizard ran once on older code and is now closed; the current wizard with its step count and back buttons has not run on the install. To prove it: On a fresh install, walk every step forward and back.. |
| M42.5.5 | Wizard step: company name, logo upload and web address | reopen partial | The wizard asks for a logo web address, not a logo upload. |
| M42.5.6 | Wizard step: the first administrator, who becomes the Super Admin | keep install | The first administrator was appointed with 76 grants and appears in People and grants. |
| M42.5.7 | Wizard step: staff source, choosing between Google Workspace, Microsoft, Lark or a spreadsheet, signing in and pulling the list | reopen absent | No staff list was pulled (Staff sources says none); signing in needs a directory OAuth app, and bec9a5e says no request has reached any directory. Needs: directory OAuth application. |
| M42.5.8 | Wizard step: AI provider, either their own key or a local-only profile with no external provider | reopen unproven | The step recorded model_profile hosted, but no model has ever been called and the key was set by hand in env. To prove it: Ask a question that reaches the model.. |
| M42.5.9 | Wizard step: optional data source connections, skippable and repeatable later from the console | reopen absent | Connecting a source needs the vault and a worker to read it; Connectors has no source. Needs: vault and worker. |
| M42.5.10 | Wizard step: review everything before any of it is written, because a wizard that commits each screen as it goes cannot be corrected | reopen unproven | The wizard ran once on older code and is closed; nothing shows the review screen was used. To prove it: On a fresh install, reach the review step and correct an answer before writing.. |
| M42.5.11 | The wizard becomes unreachable the moment the first administrator exists, so the install path cannot be replayed by anybody who finds the address | reopen unproven | A first administrator exists, but nothing has shown the setup routes refusing since. To prove it: POST /setup/appointment on the install and see it refused.. |
| M42.5.12 | A resumable install: closing the browser halfway continues where it stopped rather than starting the server again | reopen unproven | Resuming a half-finished wizard has never happened on the install. To prove it: On a fresh install, close the browser mid-wizard and reopen.. |
| M42.5.13 | Every step validates before it advances and says what is wrong in words the person can act on | reopen unproven | No refused step or its message has been shown on the install. To prove it: On a fresh install, enter an invalid value on each step.. |
| M42.5.15 | Installer output legible to a person: what it is doing, how far through it is, and what to do if a step fails | reopen absent | The installer has never run, so no output has been read. Needs: installer run. |
| M42.6.1 | The administrative console is built by the release and served by the install, so an administrator has an address to open rather than a repository to build | keep install | The image builds the console and the install serves it at its root; every screen loaded. |
| M42.6.2 | The setup wizard writes what it collects, so a first install needs no hand-edited environment file for the values it asks for | reopen absent | The install has no vault, so the provider key had to be hand-set in env. Needs: vault. |
| M42.6.3 | A question can be asked and its answer read in the console, so an install proves itself end to end before any channel is connected | reopen unproven | Ask shows Nothing has been asked yet; no question has been answered. To prove it: Ask one question and read its answer.. |
| M42.6.4 | Console screens for skills: add a skill, see what it is trusted to reach, and assign it to an agent | reopen broken | No agent exists and no route creates one, so a skill can never be assigned; approval needs a second person. |
| M42.6.5 | Console screens for connectors: connect one, hold its credential in the vault, and see what it is trusted to read | reopen absent | Connectors says connecting needs the vault, which the install lacks. Needs: vault. |
| M42.6.6 | A helper for a person who does not use a terminal: it asks for the server address and runs the one install command there | reopen absent | The Windows helper runs the installer, which needs a release and has never installed anything. Needs: published release. |
| M42.6.8 | The rule for what belongs to the product and what belongs to one company is written into the repository's own instructions, with worked examples, and a commit that came from a staging problem has to say why its fix is generic | keep repo | CLAUDE.md has the rule with examples and the commit-msg hook refuses Found-on without Generic-because. |

## Possible duplicates, for the owner's decision (nothing removed)

Task ids are positional, so a duplicate is retired by marking it decided in `docs/wbs/acts.js` (merged into the other task), never by deleting it.

| Task | Relation | Other tasks | Recommendation |
|---|---|---|---|
| M0.1.1 | overlaps | M38.1.1.5 | The CODEOWNERS half of M0.1.1 is the same deliverable as M38.1.1.5; keep M0.1.1 for visibility and branch protection and mark its CODEOWNERS clause merged into M38.1.1.5. |
| M0.1.3 | overlaps | M38.1.1.3 | The hook clause is delivered as the pre-push hook of M38.1.1.3; reword M0.1.3 to config only, or mark its hook clause merged into M38.1.1.3. |
| M0.3.4 | overlaps | M31.2.1.3 | M31.2.1.3 (engine against transaction mode) is the application half of this pool; merge into M0.3.4 or reopen together. |
| M0.4.3 | same | M31.3.1.2 | Same deliverable; mark M31.3.1.2 decided (merged into M0.4.3). |
| M0.4.4 | overlaps | M41.2.2 | The seed now loads the M41.2.2 demo rather than the test fixture; reword M0.4.4 to the command only and let M41.2.2 own the demo content. |
| M0.5.3 | same | M38.1.2.2 | Same deliverable; mark M38.1.2.2 decided (merged into M0.5.3). |
| M0.5.6 | overlaps | M12.1.1 | M12.1.1 carries the same closed naming grammar on the tool_definition table; keep both, one grammar. |
| M0.6.5 | overlaps | M38.4.1.1 | M38.4.1.1 is the per-connector use of these cassettes; reopen together, or merge M38.4.1.1 into M0.6.5. |
| M1.1.2 | overlaps | M40.1.1.1 | M40.1.1.1 (single sign-on through Keycloak) describes the same deliverable from the member side; consider marking one decided (merged into M1.1.2). |
| M1.1.7 | overlaps | M27.11.5 | M27.11.5 (service accounts and API keys created, shown once, rotated, revoked; open) is the console half of the same deliverable; keep both but reopen M1.1.7 or mark it merged into M27.11.5. |
| M1.2.2 | overlaps | M3.2.3, M40.1.1.2 | M1.2.2 is the table; M3.2.3 and M40.1.1.2 are the resolution over it. Not a merge, but M3.2.3 and M40.1.1.2 ask for the same outcome and one could be marked decided (merged into M3.2.3). |
| M1.2.3 | overlaps | M26.3.1, M33.1.2.4, M27.11.2 | M26.3.1 (immediate disable with session termination) and M33.1.2.4 (disable a principal) are the same deliverable; mark them decided (merged into M1.2.3), with M27.11.2 (open) as the console control. |
| M1.2.4 | same | M33.7.1.1 | M33.7.1.1 (installing partner, zero standing entitlement) is the same outcome; mark it decided (merged into M1.2.4). |
| M1.2.5 | overlaps | M33.7.1.2, M33.7.1.3, M33.7.1.4, M33.7.1.5 | M33.7.1.2 to M33.7.1.5 split M1.2.5 into its parts (time box, separate audit, notification, expiry); keep the M33.7 parts and mark M1.2.5 decided (merged), or the reverse. |
| M1.3.1 | overlaps | M41.2.7 | M41.2.7 lists 'the six roles' among starter pack contents; no action beyond noting it. |
| M1.3.3 | overlaps | M33.2.2.5 | M33.2.2.5 (appoint a deputy with a thirty-day maximum) is the console half of the same deliverable; mark it decided (merged into M1.3.3). |
| M1.4.3 | overlaps | M26.1.2, M27.11.2 | M26.1.2 (role and starter pack assignment) and open M27.11.2 (pack assignment from People) need the same missing writer; mark M26.1.2 decided (merged into M1.4.3) or reopen both together. |
| M1.5.1 | overlaps | M27.7.4, M27.11.1 | M27.7.4 (departments and teams, who belongs) and open M27.11.1 (create departments and teams) cover the missing writer; M1.5.1 stays as the schema leaf. |
| M1.5.2 | overlaps | M27.7.4 | M27.7.4 (who belongs to each team) is the console half of the membership part; consider merging the membership half there and keeping M1.5.2 for team-referencing predicates. |
| M1.6.5 | overlaps | M42.5.7 | M42.5.7 (wizard step choosing Google, Microsoft, Lark or a spreadsheet and pulling the list) is the wizard half of the same adapters; keep both, noting the dependency. |
| M1.6.11 | same | M27.7.2 | M27.7.2 (open: staff source, choose, configure and try it before it runs) is word for word the same deliverable; reopen M1.6.11 or mark it decided (merged into M27.7.2). |
| M1.6.12 | overlaps | M27.11.4 | M27.11.4 (open: directory sync managed from the console) needs the same sync; keep both, reopen M1.6.12. |
| M2.1.1 | overlaps | M27.3.2 | M27.3.2 is the Scopes and departments screen over this table; no merge, noted for context. |
| M2.1.2 | subset_of | M0.2.2 | M0.2.2 (scope model as jsonb predicate, evaluator, conjunction-only composition) contains M2.1.2, M2.1.3 and M2.1.4; mark the weaker records decided (merged into M0.2.2) or keep M2.1.x as the parts and mark M0.2.2 merged. |
| M2.1.3 | subset_of | M0.2.2 | M0.2.2 (scope model as jsonb predicate, evaluator, conjunction-only composition) contains M2.1.2, M2.1.3 and M2.1.4; mark the weaker records decided (merged into M0.2.2) or keep M2.1.x as the parts and mark M0.2.2 merged. |
| M2.1.4 | subset_of | M0.2.2 | M0.2.2 (scope model as jsonb predicate, evaluator, conjunction-only composition) contains M2.1.2, M2.1.3 and M2.1.4; mark the weaker records decided (merged into M0.2.2) or keep M2.1.x as the parts and mark M0.2.2 merged. |
| M2.2.1 | overlaps | M27.11.1 | M27.11.1 (open: departments, teams and scopes created from the console) is the missing writer; M2.2.1 stays as the schema leaf. |
| M2.2.2 | overlaps | M26.1.3 | M26.1.3 (department assignment with scope) asks for the same scoped appointment; mark one decided (merged into M2.2.2). |
| M2.2.3 | same | M27.11.1 | M27.11.1 (open: departments created from the console, audited) is the working version of this leaf; mark M2.2.3 decided (merged into M27.11.1) or reopen it. |
| M6.2.1 | subset_of | M3.5.1 | mark M6.2.1 decided (merged into M3.5.1) |
| M6.3.6 | same | M3.5.3 | mark M6.3.6 decided (merged into M3.5.3) |
| M6.4.1 | same | M3.7.2 | mark M6.4.1 decided (merged into M3.7.2) |
| M7.1.1 | overlaps | M27.13.4, M40.3.1.2 | Console/member upload surfaces over this endpoint; cross-reference. |
| M7.1.2 | overlaps | M39.2.3.2 | Agent-side link extraction depends on this; reopen together. |
| M7.1.5 | overlaps | M22.2.4 | Same ingestion backpressure from admission control; consider merging. |
| M7.4.4 | overlaps | M40.3.1.4 | Member-side promotion request; reopen together. |
| M7.4.6 | overlaps | M34.2.1.3 | Re-verification nag is the notice half; merge or reopen both. |
| M7.4.7 | same | M34.2.1.2 | Mark M34.2.1.2 decided (merged into M7.4.7). |
| M8.1.3 | overlaps | M3.9.3 | M3.9.3 composes citations with freshness; keep one |
| M8.2.3 | same | M4.3.3 | mark M8.2.3 decided (merged into M4.3.3) |
| M9.1.2 | same | M40.2.1.5 | mark one decided (merged into the other) |
| M9.1.4 | overlaps | M25.3.1, M33.3.1.3, M40.4.2.2 | one conversation export deliverable; merge into M25.3.1 |
| M9.2.2 | same | M4.3.1 | mark M9.2.2 decided (merged into M4.3.1) |
| M10.1.1 | same | M29.1.1 | Mark M29.1.1 decided (merged into M10.1.1). |
| M10.3.1 | overlaps | M27.13.1 | Console surface for binding; reopen together. |
| M10.5.5 | same | M23.1.4 | Closed by the same commit 723341e; mark M23.1.4 decided (merged into M10.5.5). |
| M10.5.7 | overlaps | M27.11.5 | Key management half; merge into M27.11.5 or reopen together. |
| M11.1.6 | overlaps | M33.5.1.1 | Same deliverable from the Connector Admin role. |
| M11.2.2 | overlaps | M31.3.2.3, M31.3.2.4 | Same connector leases; merge. |
| M11.4.9 | overlaps | M8.1.3 | Same answer-side staleness; merge or reopen both. |
| M12.1.2 | subset_of | M0.2.6 | M0.2.6 ToolDefinition already names required capability; mark M12.1.2 decided (merged into M0.2.6) or keep M12.1.2 as the registration-time check only |
| M12.1.4 | overlaps | M0.2.6 | M0.2.6 ToolDefinition names result contract; consider marking M12.1.4 decided (merged into M0.2.6) |
| M12.1.6 | same | M0.5.6 | M0.5.6 'Registry sweep: tool naming grammar' is the same deliverable; mark one decided (merged into the other) |
| M12.2.1 | overlaps | M42.6.4 | M42.6.4 (add a skill screen) covers the same act; keep both but prove them with one act |
| M12.2.2 | overlaps | M40.3.2.2, M27.13.4 | M27.13.4 (open) asks for a skill from a repository from the console; mark M12.2.2 decided (merged into M27.13.4) or reopen it and link the two |
| M12.2.3 | overlaps | M40.3.2.2 | M40.3.2.2 (paste a URL) is the same outcome for personal skills; decide them together |
| M12.2.5 | overlaps | M39.2.2.2, M27.12.1 | M27.12.1 (open) makes an agent run read its skills; the run-time half of M12.2.5 belongs there |
| M12.2.6 | overlaps | M27.3.13 | M27.3.13 (Skills and templates with the review queue) is the screen for this queue; prove both with one act |
| M12.2.8 | overlaps | M27.12.1 | M27.12.1 (open) is where skills start affecting a run; link or merge |
| M13.1.2 | overlaps | M27.11.13 | M27.11.13 (open: who can find and use the agent) is the console half; prove together once agents can be created |
| M13.1.4 | subset_of | M27.11.6 | M27.11.6 (open) includes enabled, disabled, archived; mark M13.1.4 decided (merged into M27.11.6) or reopen and link |
| M13.1.5 | subset_of | M27.11.6, M27.11.2 | M27.11.6 (handed over) and M27.11.2 (leaver with the adoption plan, ownership transfer) cover this; mark M13.1.5 decided (merged into M27.11.6) |
| M13.2.1 | overlaps | M27.9.5 | M27.9.5 (open) furnishes signed built-in templates; the signing half of M13.2.1 lands there |
| M13.2.7 | overlaps | M27.11.6 | M27.11.6 (open: agents created from a template or from scratch) is the reachable version; link |
| M13.3.1 | subset_of | M27.11.6 | M27.11.6 (open: agents created from a template or from scratch) is the reachable deliverable; mark decided (merged into M27.11.6) or reopen and link |
| M13.3.2 | subset_of | M27.11.6 | M27.11.6 (open: agents created from a template or from scratch) is the reachable deliverable; mark decided (merged into M27.11.6) or reopen and link |
| M13.3.3 | subset_of | M27.11.6 | M27.11.6 (open: agents created from a template or from scratch) is the reachable deliverable; mark decided (merged into M27.11.6) or reopen and link |
| M13.3.4 | subset_of | M27.11.6 | M27.11.6 (open) includes the rehearsal and install; mark decided (merged into M27.11.6) or reopen and link |
| M13.3.5 | subset_of | M27.11.6 | M27.11.6 (open) includes the rehearsal and install; mark decided (merged into M27.11.6) or reopen and link |
| M13.3.6 | subset_of | M27.11.6 | M27.11.6 (open) includes the rehearsal and install; mark decided (merged into M27.11.6) or reopen and link |
| M13.3.7 | subset_of | M27.11.6 | merge with the other M13.3 leaves into M27.11.6 |
| M13.5.1 | overlaps | M27.11.7, M38.2.2.4 | M27.11.7 (open: agent templates installed) and M38.2.2.4 (open: agents installed from templates) are where this template becomes usable; keep the catalogue leaf but reopen it until an install from it is proved |
| M13.5.2 | overlaps | M27.11.7, M38.2.2.4 | M27.11.7 (open: agent templates installed) and M38.2.2.4 (open: agents installed from templates) are where this template becomes usable; keep the catalogue leaf but reopen it until an install from it is proved |
| M13.5.3 | overlaps | M27.11.7, M38.2.2.4 | M27.11.7 (open: agent templates installed) and M38.2.2.4 (open: agents installed from templates) are where this template becomes usable; keep the catalogue leaf but reopen it until an install from it is proved |
| M13.5.4 | overlaps | M27.11.7, M38.2.2.4 | M27.11.7 (open: agent templates installed) and M38.2.2.4 (open: agents installed from templates) are where this template becomes usable; keep the catalogue leaf but reopen it until an install from it is proved |
| M13.5.5 | overlaps | M27.11.7, M38.2.2.4 | M27.11.7 (open: agent templates installed) and M38.2.2.4 (open: agents installed from templates) are where this template becomes usable; keep the catalogue leaf but reopen it until an install from it is proved |
| M13.5.6 | overlaps | M27.11.7, M38.2.2.4 | M27.11.7 (open: agent templates installed) and M38.2.2.4 (open: agents installed from templates) are where this template becomes usable; keep the catalogue leaf but reopen it until an install from it is proved |
| M13.5.7 | overlaps | M27.11.7, M38.2.2.4 | M27.11.7 (open: agent templates installed) and M38.2.2.4 (open: agents installed from templates) are where this template becomes usable; keep the catalogue leaf but reopen it until an install from it is proved |
| M13.5.8 | overlaps | M27.11.7, M38.2.2.4 | M27.11.7 (open: agent templates installed) and M38.2.2.4 (open: agents installed from templates) are where this template becomes usable; keep the catalogue leaf but reopen it until an install from it is proved |
| M13.5.9 | overlaps | M27.11.7, M38.2.2.4 | M27.11.7 (open: agent templates installed) and M38.2.2.4 (open: agents installed from templates) are where this template becomes usable; keep the catalogue leaf but reopen it until an install from it is proved |
| M13.5.10 | overlaps | M27.11.7, M38.2.2.4 | M27.11.7 (open: agent templates installed) and M38.2.2.4 (open: agents installed from templates) are where this template becomes usable; keep the catalogue leaf but reopen it until an install from it is proved |
| M13.5.11 | overlaps | M27.11.7, M38.2.2.4 | M27.11.7 (open: agent templates installed) and M38.2.2.4 (open: agents installed from templates) are where this template becomes usable; keep the catalogue leaf but reopen it until an install from it is proved |
| M13.5.12 | overlaps | M27.11.7, M38.2.2.4 | M27.11.7 (open: agent templates installed) and M38.2.2.4 (open: agents installed from templates) are where this template becomes usable; keep the catalogue leaf but reopen it until an install from it is proved |
| M13.5.13 | overlaps | M27.11.7, M38.2.2.4 | M27.11.7 (open: agent templates installed) and M38.2.2.4 (open: agents installed from templates) are where this template becomes usable; keep the catalogue leaf but reopen it until an install from it is proved |
| M13.5.14 | overlaps | M27.11.7, M38.2.2.4 | M27.11.7 (open: agent templates installed) and M38.2.2.4 (open: agents installed from templates) are where this template becomes usable; keep the catalogue leaf but reopen it until an install from it is proved |
| M13.5.15 | overlaps | M27.11.7, M38.2.2.4 | M27.11.7 (open: agent templates installed) and M38.2.2.4 (open: agents installed from templates) are where this template becomes usable; keep the catalogue leaf but reopen it until an install from it is proved |
| M13.5.16 | overlaps | M27.11.7, M38.2.2.4 | M27.11.7 (open: agent templates installed) and M38.2.2.4 (open: agents installed from templates) are where this template becomes usable; keep the catalogue leaf but reopen it until an install from it is proved |
| M13.5.17 | overlaps | M27.11.7, M38.2.2.4 | M27.11.7 (open: agent templates installed) and M38.2.2.4 (open: agents installed from templates) are where this template becomes usable; keep the catalogue leaf but reopen it until an install from it is proved |
| M13.5.18 | overlaps | M27.11.7, M38.2.2.4 | M27.11.7 (open: agent templates installed) and M38.2.2.4 (open: agents installed from templates) are where this template becomes usable; keep the catalogue leaf but reopen it until an install from it is proved |
| M13.5.19 | overlaps | M27.11.7, M38.2.2.4 | M27.11.7 (open: agent templates installed) and M38.2.2.4 (open: agents installed from templates) are where this template becomes usable; keep the catalogue leaf but reopen it until an install from it is proved |
| M13.5.20 | overlaps | M27.11.7, M38.2.2.4 | M27.11.7 (open: agent templates installed) and M38.2.2.4 (open: agents installed from templates) are where this template becomes usable; keep the catalogue leaf but reopen it until an install from it is proved |
| M13.5.21 | overlaps | M27.11.7, M38.2.2.4 | M27.11.7 (open: agent templates installed) and M38.2.2.4 (open: agents installed from templates) are where this template becomes usable; keep the catalogue leaf but reopen it until an install from it is proved |
| M13.5.22 | overlaps | M27.11.7, M38.2.2.4 | M27.11.7 (open: agent templates installed) and M38.2.2.4 (open: agents installed from templates) are where this template becomes usable; keep the catalogue leaf but reopen it until an install from it is proved |
| M13.5.23 | overlaps | M27.11.7, M38.2.2.4 | M27.11.7 (open: agent templates installed) and M38.2.2.4 (open: agents installed from templates) are where this template becomes usable; keep the catalogue leaf but reopen it until an install from it is proved |
| M13.6.1 | subset_of | M27.11.7 | M27.11.7 (open: templates authored from an agent through the leak scan) is the reachable deliverable; mark decided (merged into M27.11.7) or reopen and link |
| M13.6.2 | subset_of | M27.11.7 | M27.11.7 (open: templates authored from an agent through the leak scan) is the reachable deliverable; mark decided (merged into M27.11.7) or reopen and link |
| M13.6.3 | subset_of | M27.11.7 | M27.11.7 (open: templates authored from an agent through the leak scan) is the reachable deliverable; mark decided (merged into M27.11.7) or reopen and link |
| M13.6.4 | subset_of | M27.11.7 | M27.11.7 (open: templates authored from an agent through the leak scan) is the reachable deliverable; mark decided (merged into M27.11.7) or reopen and link |
| M13.6.5 | subset_of | M27.11.7 | M27.11.7 (open: templates authored from an agent through the leak scan) is the reachable deliverable; mark decided (merged into M27.11.7) or reopen and link |
| M13.6.6 | subset_of | M27.11.7 | M27.11.7 (open: templates authored from an agent through the leak scan) is the reachable deliverable; mark decided (merged into M27.11.7) or reopen and link |
| M14.5.4 | subset_of | M24.1.3 | M24.1.3 records merges among other events; cross-reference. |
| M14.5.6 | overlaps | M16.3.5 | Same money-boundary merge rule; cross-reference or merge. |
| M16.1.3 | overlaps | M16.4.1 | The decay half of this leaf is M16.4.1; narrow this leaf to the table or note decay as covered by M16.4.1. |
| M16.1.4 | overlaps | M16.4.4, M26.2.5 | The re-check-at-read half is the same mechanism as M16.4.4 and M26.2.5 (may_recall over the formation's capabilities); mark M26.2.5 decided (merged into M16.4.4). |
| M16.2.3 | overlaps | M9.2.4 | M9.2.4 (Correction handling that feeds the learning signal) is the producer of this signal; cross-reference rather than merge. |
| M16.2.5 | overlaps | M8.3.5 | Same takeover event feeding a different consumer (circuit breaker); keep both, one producer. |
| M16.4.4 | same | M26.2.5 | M26.2.5 asks for the same outcome from the mover side; mark M26.2.5 decided (merged into M16.4.4). |
| M16.5.1 | same | M40.4.1.3 | M40.4.1.3 (Weekly digest delivered in the own channel of the person with the same controls) is the same deliverable; mark it decided (merged into M16.5.1). |
| M16.5.2 | overlaps | M39.4.1.4, M39.4.1.5, M39.1.2.2 | M39.4.1.x (memory viewer with delete, per-agent and per-person shown separately) and the Memory tab of M39.1.2.2 describe this same tab; keep one and mark the others decided. |
| M16.5.4 | overlaps | M39.4.2.4, M27.3.15, M27.7.21 | M39.4.2.4 routes tier three to the department queue this leaf describes; cross-reference and keep the alarm here. |
| M17.1.2 | same | M32.4.1.1 | M32.4.1.1 (Procrastinate on Postgres for the job queue) is the same deliverable; mark it decided (merged into M17.1.2). |
| M17.1.5 | overlaps | M27.13.3 | M27.13.3 (open: dead letters redriven from the console) is the operator-surface half of this leaf; keep M27.13.3 as the owner of the surface. |
| M17.2.1 | same | M32.4.1.2 | M32.4.1.2 (LangGraph checkpointer configuration) is the same deliverable; mark it decided (merged into M17.2.1). |
| M17.2.2 | same | M32.4.1.3 | M32.4.1.3 (Our re-drive driver for crash recovery) is the same deliverable; mark it decided (merged into M17.2.2). |
| M17.3.5 | same | M30.4.8 | M30.4.8 (Kill after every side-effect boundary with exactly-once verification) is the same deliverable; mark one decided (merged into the other). |
| M17.4.2 | overlaps | M39.6.1.2, M39.6.1.3 | M39.6.1.2 (named as outcomes) and M39.6.1.3 (template gallery) describe this same gallery; keep one and mark the others decided. |
| M17.4.3 | overlaps | M39.6.2.1 | M39.6.2.1 (An automation runs on a named principal) is the same property; cross-reference or merge. |
| M17.4.4 | overlaps | M26.3.3 | M26.3.3 (Leaver: Automations suspended) is the leaver case of this leaf; cross-reference. |
| M17.4.6 | overlaps | M39.6.1.4 | M39.6.1.4 (per-automation run history with last result and next time) is the history half; keep cost here. |
| M17.5.3 | same | M27.8.12 | M27.8.12 (Webhooks, managed from the console) is the same deliverable; mark it decided (merged into M17.5.3). |
| M19.1.4 | overlaps | M19.1.3 | M19.1.3 (open: worker owning the CDP session and snapshot serialisation) produces the snapshot this leaf types; close them together. |
| M19.6.1 | subset_of | M22.1.1 | M22.1.1 (global budgets including browser sessions) contains this limit; mark M19.6.1 decided (merged into M22.1.1) or keep it only as the browser proof. |
| M19.6.5 | overlaps | M33.1.1.4, M27.12.4 | M33.1.1.4 (Global kill switch) has identical text and M27.12.4 (open) makes the stop button halt the install; keep one install-wide kill switch leaf and narrow this one to browser runs or mark it decided. |
| M20.1.4 | subset_of | M27.11.6 | M27.11.6 (open: agents created through saved drafts, a rehearsal and a publish a second person approves) is the reachable deliverable; mark decided (merged into M27.11.6) or reopen and link |
| M20.3.1 | subset_of | M27.11.6 | M27.11.6 (open: agents created through saved drafts, a rehearsal and a publish a second person approves) is the reachable deliverable; mark decided (merged into M27.11.6) or reopen and link |
| M20.3.2 | subset_of | M27.11.6 | M27.11.6 (open: agents created through saved drafts, a rehearsal and a publish a second person approves) is the reachable deliverable; mark decided (merged into M27.11.6) or reopen and link |
| M20.3.3 | subset_of | M27.11.6 | M27.11.6 (open: agents created through saved drafts, a rehearsal and a publish a second person approves) is the reachable deliverable; mark decided (merged into M27.11.6) or reopen and link |
| M20.3.4 | subset_of | M27.11.6 | M27.11.6 (open: agents created through saved drafts, a rehearsal and a publish a second person approves) is the reachable deliverable; mark decided (merged into M27.11.6) or reopen and link |
| M20.4.3 | subset_of | M27.11.6 | M27.11.6 (open: a publish a second person approves) is the reachable deliverable; mark decided (merged into M27.11.6) or reopen and link |
| M21.1.1 | overlaps | M33.1.1.3 | M33.1.1.3 (Company budget and consumption) is the Super Admin view of this budget; cross-reference. |
| M21.1.3 | overlaps | M33.3.1.5 | M33.3.1.5 (Their own usage against allowance) is the member view of this allowance; cross-reference. |
| M21.1.5 | overlaps | M27.12.5 | M27.12.5 (open: budgets are versioned from the console) is the writer this leaf lacks; close them together. |
| M21.2.3 | overlaps | M27.12.5 | M27.12.5 (open: model calls are metered into spend) is the writer this leaf lacks; close them together. |
| M21.3.1 | same | M27.7.15, M27.4.3 | M27.7.15 and M27.4.3 are the screen for this report; keep one screen leaf and mark the other decided, and cross-reference this leaf. |
| M21.3.5 | overlaps | M33.2.1.3 | M33.2.1.3 (Department budget against pace) is the department view of this figure; cross-reference. |
| M22.1.1 | overlaps | M11.5.3, M18.3.5 | per-source and parallelism budgets are listed twice; keep M22.1.1 as the umbrella |
| M23.2.3 | overlaps | M21.3.2, M6.1.6 | one machine-traffic exclusion rule; merge into M21.3.2 |
| M27.2.5 | overlaps | M33.2.1.4 | merge department half into this |
| M27.2.6 | overlaps | M27.13.3 | keep one queue screen leaf |
| M27.2.8 | same | M33.1.1.4, M27.12.4 | mark M33.1.1.4 decided (merged into M27.12.4) |
| M27.3.1 | same | M27.7.3 | mark M27.3.1 decided (merged into M27.7.3) |
| M27.3.2 | overlaps | M27.7.4 | merge into M27.7.4 |
| M27.3.3 | same | M27.7.5 | mark M27.3.3 decided (merged into M27.7.5) |
| M27.3.4 | same | M27.7.6 | mark M27.3.4 decided (merged into M27.7.6) |
| M27.3.5 | same | M27.7.2 | mark M27.3.5 decided (merged into open M27.7.2) |
| M27.3.6 | same | M27.7.9 | mark M27.3.6 decided (merged into M27.7.9) |
| M27.3.7 | same | M33.6.1.1 | mark M33.6.1.1 decided (merged into M27.3.7) |
| M27.3.8 | same | M27.7.10 | mark M27.3.8 decided (merged into M27.7.10) |
| M27.3.9 | subset_of | M27.7.24 | mark decided (merged into M27.7.24) |
| M27.3.10 | subset_of | M27.7.24 | mark decided (merged into M27.7.24) |
| M27.3.11 | overlaps | M27.13.6 | merge into M27.13.6 |
| M27.3.14 | same | M27.7.20 | mark M27.3.14 decided (merged into M27.7.20) |
| M27.3.15 | same | M27.7.21 | mark M27.3.15 decided (merged into M27.7.21) |
| M27.3.16 | same | M27.7.23 | mark M27.3.16 decided (merged into M27.7.23) |
| M27.3.17 | same | M27.7.22 | mark M27.3.17 decided (merged into M27.7.22) |
| M27.3.18 | overlaps | M27.7.13 | merge into M27.7.13 |
| M27.4.1 | overlaps | M27.7.18 | merge into M27.7.18 |
| M27.4.2 | same | M27.7.14 | mark M27.4.2 decided (merged into M27.7.14) |
| M27.4.3 | same | M27.7.15 | mark M27.4.3 decided (merged into M27.7.15) |
| M27.4.4 | same | M27.7.19 | mark M27.4.4 decided (merged into open M27.7.19) |
| M27.5.5 | same | M33.1.2.5 | mark M33.1.2.5 decided (merged into M27.5.5) |
| M27.5.10 | overlaps | M27.7.29 | merge into M27.7.29 |
| M27.6.1 | overlaps | M27.7.25 | merge into M27.7.25 |
| M27.6.2 | same | M27.7.26 | mark M27.6.2 decided (merged into M27.7.26) |
| M27.6.3 | subset_of | M27.7.27 | mark decided (merged into M27.7.27) |
| M27.6.4 | subset_of | M27.7.27 | mark decided (merged into M27.7.27) |
| M27.7.13 | overlaps | M33.4.1.1 | merge M33.4.1.1 into M27.7.13 |
| M27.7.15 | overlaps | M33.1.1.3 | merge M33.1.1.3 into M27.7.15 |
| M27.8.2 | overlaps | M27.10.1 | keep both; M27.10.1 regroups |
| M28.1.2 | same | M0.6.4 | mark M28.1.2 decided (merged into M0.6.4) or reword to real questions |
| M28.2.2 | overlaps | M32.1.2.2 | merge into M28.2.2 |
| M29.2.3 | overlaps | M27.8.10 | M27.8.10 (open: feature and module enablement from the console) is where plugin enablement becomes reachable; link |
| M30.1.1 | overlaps | M38.1.3.1 | M38.1.3.1 (Coolify deployment triggered on every push) describes the same Coolify delivery, now pull-based; mark M38.1.3.1 decided (merged into M30.1.1 and M30.2.4). |
| M30.1.4 | same | M42.1.1 | M30.1.4 and M42.1.1 are one deliverable (8cd397c says they share wording and code); mark M30.1.4 decided (merged into M42.1.1). |
| M30.1.4 | overlaps | M34.3.3.2 | Per-client variables reference; the documentation half of the same deliverable. |
| M30.1.5 | same | M42.1.2 | Mark M30.1.5 decided (merged into M42.1.2, which adds the enforcing check). |
| M30.1.5 | overlaps | M42.5.2 | M42.5.2 states the same minting rule from the installer side. |
| M30.2.1 | same | M38.1.2.1 | Same pipeline; mark M30.2.1 decided (merged into M38.1.2.1). M0.5.1, M0.5.2 and M0.5.3 are its parts. |
| M30.2.2 | same | M38.1.2.3, M38.1.2.4 | Image build and image signing are the same deliverable; mark M30.2.2 decided (merged into M38.1.2.4). |
| M30.2.3 | same | M38.1.2.5 | Mark M30.2.3 decided (merged into M38.1.2.5, Push to the registry). |
| M30.2.4 | overlaps | M38.1.3.1 | M38.1.3.1 says Coolify deployment triggered on every push, which 0b0014c replaced with this pull; reword M38.1.3.1 or mark it decided (merged into M30.2.4). |
| M30.2.5 | same | M42.1.4 | Mark M30.2.5 decided (merged into M42.1.4). |
| M30.2.7 | subset_of | M42.3.6 | Mark M30.2.7 decided (merged into M42.3.6, which names the rollback script re-pinning the previous tag). |
| M30.2.7 | overlaps | M34.3.3.3, M42.2.10 | Upgrade and rollback procedure and its rehearsal; keep M42.2.10 open as the proof. |
| M30.2.8 | subset_of | M42.3.7 | Same checklist file; mark M30.2.8 decided (merged into M42.3.7). |
| M30.2.8 | overlaps | M34.3.3.1 | Same checklist (four hidden production links); merge into M42.3.7. |
| M30.3.1 | overlaps | M42.3.5 | M42.3.5 (open) asks for backup and restore scripts; keep this leaf for the WAL and pgBackRest schedule only. |
| M30.3.6 | overlaps | M27.13.5, M42.3.5, M34.3.3.4 | Restore drill procedure, console-started drills and restore scripts describe one capability; keep M30.3.6 as the scheduled run. |
| M30.4.1 | same | M34.3.2.3, M37.4.3.2 | Same register per 8cd397c; mark two decided (merged into M30.4.1). |
| M30.5.4 | same | M37.4.2.2 | Mark M30.5.4 decided (merged into M37.4.2.2). |
| M31.2.1.4 | same | M0.3.5 | Same deliverable as open M0.3.5; mark one merged into the other. |
| M32.5.2.3 | overlaps | M20.2.1, M20.2.2 | Same trace graph and procedure canvas components. |
| M33.2.2.4 | overlaps | M27.12.8 | merge into M27.12.8 |
| M34.1.1.2 | overlaps | M26.1.4, M37.3.2.3 | M26.1.4 (welcome path with three department-scoped starter questions) asks the same deliverable; keep one and mark the other decided (merged). |
| M34.3.2.1 | same | M37.4.3.1 | M37.4.3.1 'Runbook per console screen' is word-for-word the same deliverable; mark M37.4.3.1 decided (merged into M34.3.2.1). |
| M34.3.2.3 | same | M37.4.3.2 | M37.4.3.2 is the same deliverable (the 8cd397c message says so); mark M37.4.3.2 decided (merged into M34.3.2.3). |
| M34.3.2.3 | subset_of | M30.4.1 | The playbook is M30.4.1's failure-mode matrix rendered; keep both only if 'rendered for the client' is the distinct part. |
| M34.3.3.1 | overlaps | M42.3.7, M30.2.8 | All three were closed by 8cd397c on the same checklist page; merge M34.3.3.1 into M42.3.7 (decided) and keep M30.2.8 only for its named contents. |
| M34.3.3.2 | same | M42.2.5 | M42.2.5 'Configuration guide: every value a new client must set, in one table' is the same page; mark M34.3.3.2 decided (merged into M42.2.5). |
| M34.3.3.2 | overlaps | M42.1.1 | M42.1.1 adds the separate variables repository; leave it separate. |
| M34.3.3.3 | overlaps | M42.2.10, M42.3.6 | M42.2.10 (open) asks for the same procedure rehearsed; merge M34.3.3.3 into M42.2.10 (decided). |
| M34.3.3.4 | overlaps | M42.3.5, M37.4.1.3 | M42.3.5 (open, restore verified) and M37.4.1.3 (act) cover running it; keep this as the page only if that split is intended. |
| M35.2.2.1 | overlaps | M39.1.2.5 | M39.1.2.5 (keyboard navigation across agent tabs) is a subset of this; keep it only if the agent workspace is checked separately. |
| M35.3.1.1 | overlaps | M40.1.2.3, M40.6.1.5 | ee744e0 measured the M40 pair on the same console Approvals page; merge into one phone-approvals leaf. |
| M35.3.1.2 | subset_of | M40.6.1.5 | M40.6.1.5 (mobile layout primary for approvals) covers this; mark M35.3.1.2 decided (merged). |
| M36.1.1.1 | subset_of | M36.1.1.2, M36.1.1.3 | M36.1.1.2 and M36.1.1.3 are parts of this leaf; mark them decided (merged into M36.1.1.1). |
| M36.2.4.2 | overlaps | M22.3.4 | Same bottleneck ladder. |
| M37.3.2.4 | overlaps | M21.3.2, M6.1.6 | All three exclude machine traffic from a measure; keep them only if each report is distinct. |
| M37.4.3.4 | overlaps | M37.5.1.1 | Same registry; mark M37.4.3.4 decided (merged into M37.5.1.1). |
| M37.5.3.2 | same | M21.2.4 | Mark M37.5.3.2 decided (merged into M21.2.4). |
| M39.1.2.1 | overlaps | M27.11.11 | M27.11.11 (open) re-specifies the agent header (spend this period, skill count, days since created); mark M39.1.2.1 decided (merged into M27.11.11) |
| M39.1.2.2 | overlaps | M27.11.11 | the owner later chose three views Dashboard, Profile, About (ece82dc, M27.11.11); decide whether the seven-tab strip survives and mark the loser decided |
| M39.1.2.3 | overlaps | M27.11.11 | M27.11.11 (open) replaces the two panes with Dashboard, Profile and About; mark M39.1.2.3 decided (merged into M27.11.11) |
| M39.1.3.1 | overlaps | M27.11.11 | M27.11.11 (open) puts spend this period in the agent header; keep M39.1.3.1 only for runs and messages, or merge |
| M39.2.1.1 | overlaps | M27.11.12 | M27.11.12 (open) asks the Profile for sources, skills and channels with add, remove and an overflow list; mark M39.2.1.1 decided (merged into M27.11.12) |
| M39.2.1.2 | overlaps | M27.11.12 | add and remove on the agent Profile is M27.11.12 (open); mark M39.2.1.2 decided (merged into M27.11.12) |
| M39.2.4.1 | overlaps | M27.11.12 | channels with add and remove on the agent Profile are M27.11.12 (open) |
| M39.3.1.1 | overlaps | M27.11.13 | mark M39.3.1.1 decided (merged into M27.11.13) |
| M39.3.1.5 | subset_of | M39.3.1.4 | mark M39.3.1.5 decided (merged into M39.3.1.4) |
| M39.4.1.3 | overlaps | M27.7.22 | same history at estate level |
| M39.4.2.2 | overlaps | M27.7.21, M16.5.1 | same undoable tier one list at estate level |
| M39.4.2.5 | overlaps | M27.11.14 | learning switch on the agent Profile |
| M39.5.1.5 | same | M40.3.3.2 | mark M40.3.3.2 decided (merged into M39.5.1.5) |
| M39.6.1.2 | overlaps | M17.4.2 | named outcome templates |
| M39.6.1.3 | overlaps | M17.4.2 | named outcome templates |
| M39.6.1.5 | overlaps | M27.12.3 | mark M39.6.1.5 decided (merged into M27.12.3) |
| M39.6.2.4 | overlaps | M37.5.1.1, M37.4.3.4 | scheduled job registry with what each guards |
| M40.1.1.2 | overlaps | M3.2.3 | channel identity to principal resolution |
| M40.4.2.2 | overlaps | M9.1.4 | export of a conversation |
| M40.6.1.1 | overlaps | M33.6.1.1, M33.6.1.2 | pending approvals and the artefact rendered |
| M40.6.1.2 | overlaps | M33.6.1.3 | same verdicts closed in the same commit af7a7ef |
| M40.6.1.3 | overlaps | M33.6.1.4, M17.4.5 | timeout and expiry on approvals |
| M40.6.1.5 | same | M35.3.1.1 | mark M40.6.1.5 decided (merged into M35.3.1.1) |
| M41.1.1 | overlaps | M42.4.1 | M42.4.1 is the one-off audit this sweep keeps green; keep both. |
| M41.1.4 | overlaps | M42.5.5 | Wizard step M42.5.5 is the input half; keep both. |
| M41.1.6 | overlaps | M42.5.8 | Wizard step M42.5.8 is the input half; keep both. |
| M41.2.4 | overlaps | M42.5.6 | Wizard step M42.5.6 is the same appointment; merge one into the other. |
| M41.2.7 | overlaps | M27.9.5 | Open M27.9.5 asks for the same furnishing; merge. |
