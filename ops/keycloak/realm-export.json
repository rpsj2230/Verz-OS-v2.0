{
  "_comment": [
    "Company Brain realm, as configuration in the repository rather than as clicks in an",
    "admin console. Task id M1.1.1.",
    "",
    "WHY THIS FILE EXISTS. A Keycloak realm configured by hand is a permission-adjacent",
    "system whose current state nobody can diff, review or restore. Every value below is a",
    "decision, and several of them are the difference between a working sign-in and a",
    "silent bypass, so they belong where a reviewer sees them change.",
    "",
    "IMPORTED AND VERIFIED on 2026-09-06 against Keycloak 26.0, which is what this file",
    "had been waiting for. The earlier note here said it had never been imported and that",
    "M1.1.1 could not be called done until it was. It has been now, so that note is gone",
    "and this is what happened.",
    "",
    "The import ran on a throwaway server, as the note asked: an ephemeral container bound",
    "to localhost with a memory limit, never registered with Coolify, removed afterwards.",
    "Keycloak reported 'Realm brain imported' and 'Import finished successfully'.",
    "",
    "The first attempt logged three warnings and they were real: 'Referenced client scope",
    "openid doesn't exist. Ignoring', and the same for profile and email. brain-console",
    "listed all three and this file defines none of them, because a full import replaces",
    "the client scope set with the one declared here. They are removed; see the note on",
    "defaultClientScopes. The second import was silent.",
    "",
    "What the server actually stored was then read back rather than assumed: RS256, the",
    "ten-hour session, refresh revocation with no reuse, brute-force protection, no",
    "password grant and no implicit flow on any client, PKCE S256 on the public one, and",
    "the whole group tree including the scoped subgroups under approver and",
    "department-admin. All of it matches what is written here.",
    "",
    "One thing is still untested: ops/keycloak/setup.sh. The import above mounted this file",
    "and started the server with --import-realm; it did not go through kcadm. The script's",
    "own header says it has never been run and that remains true.",
    "",
    "Realm JSON is version-sensitive. This was verified on 26.0; expect a different major",
    "version to reject or ignore fields.",
    "",
    "THE SETTINGS THAT MATTER, AND WHY:",
    "",
    "defaultSignatureAlgorithm RS256, and every client pinned to it. brain.identity.oidc",
    "refuses HS256 outright, because an HMAC-signed token is signed by anything holding the",
    "client secret and the classic confusion attack presents the published RSA public key",
    "as that secret. The two ends agree here so a misconfiguration fails at login rather",
    "than becoming an accepted token.",
    "",
    "ssoSessionMaxLifespan 36000 (10 hours) matches SESSION_ABSOLUTE_MAX in",
    "brain.identity.sessions. If the realm allowed longer, a refresh loop would keep a",
    "March sign-in alive in September on the Keycloak side while our own registry refused",
    "it, and the mismatch would present as random logouts rather than as a policy.",
    "",
    "revokeRefreshToken true with refreshTokenMaxReuse 0 makes a stolen refresh token",
    "detectable: replaying one invalidates the chain. Without it a copied refresh token is",
    "a ten-hour credential with no way to notice it was taken.",
    "",
    "directAccessGrantsEnabled false everywhere. The password grant skips the browser flow,",
    "which means it skips the second factor and every conditional step configured in it. A",
    "flow whose checks can be bypassed by asking a different endpoint is not a flow.",
    "",
    "implicitFlowEnabled false, standardFlowEnabled true, PKCE S256 required on the public",
    "console client. The implicit flow puts the token in a URL fragment, which lands in",
    "browser history and referrer headers.",
    "",
    "GROUPS CARRY ROLES AND NEVER CAPABILITIES. The groups below map to the six platform",
    "roles in brain.identity.roles through GroupRoleRule, and to nothing else. There is",
    "deliberately no group, attribute or client role here that names a capability: putting",
    "one in this file would move the answer to 'who can read the margin on this client'",
    "into a directory that nobody in this company reviews.",
    "",
    "A SECOND FACTOR IS WRITTEN INTO THE TOKEN, AND UNTIL 2026-09-17 IT WAS NOT.",
    "brain.identity.bearer.assurance_from gives Assurance.STRONG, the only level that may",
    "exercise an admin: or approve: capability, to a token whose amr claim names a second",
    "factor. Keycloak 26.0 mints no amr by default: no built-in client scope carries an amr",
    "mapper, and oidc-amr-mapper writes only the reference value configured on each step the",
    "person completed (default.reference.value), and only while that step is younger than",
    "default.reference.maxAge, which counts as 0 when absent. This realm had none of the",
    "three, so no install could mint a strong token, and every admin screen refused everybody,",
    "a person who had set up OTP included. Found on the owner's staging install.",
    "",
    "So brain-identity carries the amr mapper, and the browser flow below is Keycloak's own",
    "built-in browser flow reproduced step for step with a reference on two steps: pwd on the",
    "password form and otp on the OTP form, each valid for the ten-hour session. It is",
    "declared under the built-in alias 'browser' rather than copied under a new name. The",
    "import builds every built-in flow this file does not declare, so a renamed copy would",
    "leave a second, unreferenced browser flow one binding away from being used. And an",
    "install imported before this change is repaired by adding the same two references to the",
    "'browser' flow it already has, so a fresh install and a repaired one end up the same",
    "shape. docs/install/authentication.md has those steps.",
    "",
    "THE ACCOUNT CONSOLE REFUSED EVERYBODY WITH 403, AND UNTIL 2026-09-17 NOTHING HERE SAID WHY.",
    "Keycloak 26.0.0's RealmManager.importRealm builds its own client scopes (roles, profile,",
    "email, web-origins, acr, basic) and attaches them to its own clients only when the file",
    "declares no clientScopes. This file declares brain-identity, so the import built",
    "account-console with no scope at all. Its tokens carry the account audience, from a mapper",
    "Keycloak puts on the client itself, and no resource_access, which only the roles scope",
    "writes. The Account REST API reads the account client's roles out of the token and answers",
    "403 without them, which is why staging saw a 403 and not the 401 a missing audience gets.",
    "So nobody could set up a one-time code there. Found on the owner's staging install.",
    "",
    "So account and account-console are declared below as RealmManager.setupAccountManagement",
    "builds them, and account-console carries the one mapper from the roles scope that the API",
    "needs, narrowed to the account client. The two are declared together because the import",
    "builds both itself unless this file names account: naming account-console alone makes it",
    "twice, and the client table refuses the second. defaultRole is named because, without it,",
    "declaring the default role's composites under its usual name makes the import give the",
    "default role another name, and the composites land on a role nobody holds. Nothing",
    "brain-console, brain-api or brain-sync can reach has changed: the mapper is on",
    "account-console itself, and a client's own mappers run for its tokens alone.",
    "",
    "Rejected: declaring no clientScopes, so that Keycloak builds and attaches its own. It would",
    "work, and it moves brain-identity's mappers onto brain-console, needs every product client",
    "pinned against the realm default scopes that would then exist, and leaves every install",
    "repaired by hand a different shape from a fresh one.",
    "",
    "EVERY REQUIRED ACTION KEYCLOAK REGISTERS IS DECLARED, AND UNTIL 2026-09-17 ONE WAS.",
    "Keycloak 26.0.0's DefaultExportImportManager registers the file's requiredActions plus",
    "delete_account when the file declares any, and Keycloak's own list only when it declares",
    "none. An action nobody registered is skipped at sign-in with a warning. So on every install",
    "a temporary password was never replaced, a password reset never asked for a new password,",
    "the Account Console's Update on a password and Delete on a one-time code came back with",
    "nothing done, and an administrator's request to verify an address or update a profile never",
    "ran. Found on the owner's staging install, which had CONFIGURE_TOTP and delete_account only.",
    "",
    "So requiredActions below is Keycloak's own list for a new realm, with Keycloak's own enabled,",
    "defaultAction and priority, less the two whose features are preview and off by default",
    "(UPDATE_EMAIL, CONFIGURE_RECOVERY_AUTHN_CODES). CONFIGURE_TOTP keeps defaultAction true, the",
    "one deliberate difference, because every admin screen needs a second factor.",
    "",
    "verifyEmail is false, and that is the price of declaring VERIFY_EMAIL at all. With it true,",
    "VERIFY_EMAIL asks every person whose address is not marked verified to click an emailed link",
    "before the sign-in finishes, and this file configures no mail server, so the email fails and",
    "nobody gets in. It was true before and did nothing, because VERIFY_EMAIL was not registered,",
    "so false is what every install has been running. Turn it on in the admin console once that",
    "install can send email. Rejected: VERIFY_EMAIL declared but disabled, which keeps a true",
    "verifyEmail that does nothing and leaves an administrator's own request to verify one",
    "person's address skipped exactly as before."
  ],

  "realm": "brain",
  "displayName": "Company Brain",
  "enabled": true,
  "sslRequired": "all",
  "registrationAllowed": false,
  "resetPasswordAllowed": true,
  "rememberMe": false,
  "verifyEmail": false,
  "loginWithEmailAllowed": true,
  "duplicateEmailsAllowed": false,
  "editUsernameAllowed": false,

  "bruteForceProtected": true,
  "permanentLockout": false,
  "failureFactor": 10,
  "waitIncrementSeconds": 60,
  "maxFailureWaitSeconds": 900,
  "quickLoginCheckMilliSeconds": 1000,
  "minimumQuickLoginWaitSeconds": 60,

  "defaultSignatureAlgorithm": "RS256",
  "revokeRefreshToken": true,
  "refreshTokenMaxReuse": 0,

  "accessTokenLifespan": 300,
  "accessTokenLifespanForImplicitFlow": 300,
  "ssoSessionIdleTimeout": 1800,
  "ssoSessionMaxLifespan": 36000,
  "ssoSessionIdleTimeoutRememberMe": 0,
  "ssoSessionMaxLifespanRememberMe": 0,
  "offlineSessionIdleTimeout": 1800,
  "offlineSessionMaxLifespanEnabled": true,
  "offlineSessionMaxLifespan": 36000,
  "clientSessionIdleTimeout": 1800,
  "clientSessionMaxLifespan": 36000,
  "accessCodeLifespan": 60,
  "accessCodeLifespanUserAction": 300,
  "accessCodeLifespanLogin": 1800,

  "otpPolicyType": "totp",
  "otpPolicyAlgorithm": "HmacSHA1",
  "otpPolicyDigits": 6,
  "otpPolicyPeriod": 30,
  "otpPolicyLookAheadWindow": 1,

  "attributes": {
    "frontendUrl": "",
    "acr.loa.map": "{\"silver\":1,\"gold\":2}"
  },

  "groups": [
    {
      "name": "brain",
      "path": "/brain",
      "_comment": "A container, mapped to nothing. Membership of /brain alone confers no role.",
      "subGroups": [
        { "name": "super-admin", "path": "/brain/super-admin" },
        { "name": "member", "path": "/brain/member" },
        { "name": "auditor", "path": "/brain/auditor" },
        { "name": "connector-admin", "path": "/brain/connector-admin" },
        {
          "name": "department-admin",
          "path": "/brain/department-admin",
          "_comment": [
            "Never mapped itself. department_admin is in SCOPE_REQUIRED, so a rule for it",
            "must carry a scope, and only the per-department children below can supply one.",
            "A rule for the bare parent would be a company-wide admin created by a group."
          ],
          "subGroups": [
            { "name": "web", "path": "/brain/department-admin/web" },
            { "name": "maintenance", "path": "/brain/department-admin/maintenance" }
          ]
        },
        {
          "name": "approver",
          "path": "/brain/approver",
          "_comment": "Also scope-required. Same rule as department-admin: children only.",
          "subGroups": [
            { "name": "web", "path": "/brain/approver/web" },
            { "name": "maintenance", "path": "/brain/approver/maintenance" }
          ]
        }
      ]
    }
  ],

  "defaultRole": {
    "name": "default-roles-brain",
    "description": "${role_default-roles}"
  },

  "roles": {
    "_comment": [
      "Only what the account client needs, because this file now declares that client and so",
      "the import no longer builds it. Every value is RealmManager.setupAccountManagement's in",
      "Keycloak 26.0.0: the seven roles, manage-account containing manage-account-links,",
      "manage-consent containing view-consent, and view-profile and manage-account given to",
      "everybody through the default role. delete-account is not here because the import adds",
      "it to the account client itself (KeycloakModelUtils.setupDeleteAccount). offline_access",
      "and uma_authorization are not here because the import creates both and adds both to the",
      "default role on its own, and a composite naming uma_authorization would stop the import:",
      "the roles are read before that one exists.",
      "",
      "default-roles-brain is named twice, here and in defaultRole above, and both are needed.",
      "With no defaultRole the import calls the default role default-roles-brain-1 if a realm",
      "role named default-roles-brain is declared (RealmManager.determineDefaultRoleName), and",
      "the composites below would land on a role nobody holds.",
      "",
      "None of these is a capability. They decide what a person may do to their own account in",
      "Keycloak's Account Console and nothing in this system reads them."
    ],
    "realm": [
      {
        "name": "default-roles-brain",
        "description": "${role_default-roles}",
        "composite": true,
        "composites": { "client": { "account": ["view-profile", "manage-account"] } }
      }
    ],
    "client": {
      "account": [
        { "name": "view-profile", "description": "${role_view-profile}" },
        {
          "name": "manage-account",
          "description": "${role_manage-account}",
          "composite": true,
          "composites": { "client": { "account": ["manage-account-links"] } }
        },
        { "name": "manage-account-links", "description": "${role_manage-account-links}" },
        { "name": "view-applications", "description": "${role_view-applications}" },
        { "name": "view-consent", "description": "${role_view-consent}" },
        {
          "name": "manage-consent",
          "description": "${role_manage-consent}",
          "composite": true,
          "composites": { "client": { "account": ["view-consent"] } }
        },
        { "name": "view-groups", "description": "${role_view-groups}" }
      ]
    }
  },

  "clientScopes": [
    {
      "name": "brain-identity",
      "description": "The claims brain.identity.oidc.ClaimMapping reads. Nothing else.",
      "protocol": "openid-connect",
      "attributes": {
        "include.in.token.scope": "true",
        "display.on.consent.screen": "false"
      },
      "protocolMappers": [
        {
          "name": "subject",
          "protocol": "openid-connect",
          "protocolMapper": "oidc-sub-mapper",
          "config": {
            "access.token.claim": "true",
            "introspection.token.claim": "true",
            "_comment": "Keycloak 25 moved sub out of the access token into its built-in basic scope. A full import replaces the client scope set with the one this file declares, so naming that built-in on the client would be discarded exactly as openid, profile and email were on 2026-09-06. Minting it from this realm's own scope is the only spelling that survives an import. Without it every token is refused as missing_claim and nobody signs in."
          }
        },
        {
          "name": "groups",
          "protocol": "openid-connect",
          "protocolMapper": "oidc-group-membership-mapper",
          "config": {
            "claim.name": "groups",
            "full.path": "true",
            "id.token.claim": "true",
            "access.token.claim": "true",
            "userinfo.token.claim": "true",
            "_comment": "full.path true because GroupRoleRule compares the whole path exactly; a bare leaf name would make /brain/approver/web and /clientx/approver/web the same string."
          }
        },
        {
          "name": "department",
          "protocol": "openid-connect",
          "protocolMapper": "oidc-usermodel-attribute-mapper",
          "config": {
            "user.attribute": "department",
            "claim.name": "department",
            "jsonType.label": "String",
            "id.token.claim": "true",
            "access.token.claim": "true",
            "userinfo.token.claim": "true"
          }
        },
        {
          "name": "amr",
          "protocol": "openid-connect",
          "protocolMapper": "oidc-amr-mapper",
          "config": {
            "id.token.claim": "false",
            "access.token.claim": "true",
            "introspection.token.claim": "true"
          },
          "_comment": "The authentication methods the sign-in used, which brain.identity.bearer.assurance_from reads to decide between AUTHENTICATED and STRONG. Here on brain-identity for the reason the audience mapper below is: brain-console carries this scope by default, so every token issued to the console gets it. Access token only, because the access token is what the console sends and what the API validates; the console never reads its ID token. access.token.claim is spelled out because OIDCAttributeMapperHelper.includeInAccessToken is \"true\".equals(value), so an absent key is off. The values come from the authenticatorConfig entries the browser flow names, and a step with no reference contributes nothing."
        },
        {
          "name": "brain-api-audience",
          "protocol": "openid-connect",
          "protocolMapper": "oidc-audience-mapper",
          "config": {
            "included.client.audience": "brain-api",
            "id.token.claim": "false",
            "access.token.claim": "true"
          },
          "_comment": "Without an explicit audience mapper, a token minted for the console carries no aud for the API, and validate_token refuses it. That refusal is correct: a token is issued for a party, and accepting one issued for somebody else is the confused deputy. This sits on the brain-identity client scope rather than on brain-api's own protocolMappers, where it was first written. A dedicated mapper applies to tokens issued FOR that client, and brain-api has standardFlow, directAccess, serviceAccounts and implicit all disabled, so no token is ever issued for it and the mapper could never fire. The comment above described precisely the failure its own placement guaranteed: every console sign-in would have completed at Keycloak and then been refused by validate_token for WRONG_AUDIENCE. brain-console carries brain-identity as a default client scope, so here it applies to exactly the tokens that need it."
        }
      ]
    }
  ],

  "clients": [
    {
      "clientId": "brain-console",
      "name": "Company Brain console",
      "description": "The browser app. Public client, authorisation code with PKCE.",
      "enabled": true,
      "publicClient": true,
      "protocol": "openid-connect",
      "standardFlowEnabled": true,
      "implicitFlowEnabled": false,
      "directAccessGrantsEnabled": false,
      "serviceAccountsEnabled": false,
      "frontchannelLogout": false,
      "redirectUris": [
        "https://origin.not-configured.example/auth/callback",
        "http://localhost:5173/auth/callback"
      ],
      "webOrigins": ["https://origin.not-configured.example", "http://localhost:5173"],
      "_comment": [
        "brain-identity alone, and the three that used to sit beside it are gone.",
        "",
        "This file previously listed openid, profile and email here. The first real import,",
        "on 2026-09-06 against Keycloak 26.0, logged three warnings: 'Referenced client scope",
        "openid doesn't exist. Ignoring', and the same for profile and email. They were",
        "ignored because a full realm import replaces the client scope set with the one this",
        "file declares, and this file declares only brain-identity. openid was wrong in a",
        "second way as well: it is a scope value a client asks for in the request, never a",
        "client scope an administrator defines.",
        "",
        "Removing them changes no behaviour, because Keycloak was already discarding them.",
        "What it changes is that the import is now silent, and an import that prints warnings",
        "is one where the next person cannot tell the harmless lines from the real ones.",
        "",
        "Nothing is lost. brain.identity.oidc reads exactly two claims, groups and department,",
        "and brain-identity maps both. profile and email would add name and email claims that",
        "nothing in this system consumes. When something does want them, they are added",
        "deliberately, as a declared scope with its mappers, rather than as a name that",
        "resolves to nothing."
      ],
      "defaultClientScopes": ["brain-identity"],
      "attributes": {
        "pkce.code.challenge.method": "S256",
        "access.token.signed.response.alg": "RS256",
        "id.token.signed.response.alg": "RS256",
        "user.info.response.signature.alg": "RS256",
        "backchannel.logout.session.required": "true",
        "backchannel.logout.revoke.offline.tokens": "true",
        "backchannel.logout.url": "https://origin.not-configured.example/auth/backchannel-logout",
        "post.logout.redirect.uris": "https://origin.not-configured.example/signed-out##http://localhost:5173/signed-out"
      },
      "_comment": [
        "webOrigins is an explicit origin and never '*' or '+'. A wildcard here lets any",
        "site read a token from this realm.",
        "backchannel.logout.session.required true makes Keycloak send the sid, which is",
        "what SessionRegistry.end_session needs; end_all_for covers the case where it does",
        "not arrive or the session was never seen by this replica.",
        "redirectUris name the deployment this realm is imported into, and the risk is real:",
        "a stale redirect URI in a realm import is an open redirect, because Keycloak will",
        "hand a code to any address listed here. So they are exact paths rather than a",
        "wildcard, there are only two, and the second is localhost for development.",
        "",
        "https://origin.not-configured.example is a placeholder and not an address. It",
        "appears four times, here, in webOrigins, in backchannel.logout.url and in",
        "post.logout.redirect.uris, and brain.ops.realm_import.importable_realm replaces",
        "every one of them with this installation's own origin on the way into Keycloak.",
        "That is why they cannot drift apart: they are one value written four times rather",
        "than four values somebody has to remember to change together.",
        "",
        "It named one deployment's host until 2026-09-09, and the consequence was not",
        "cosmetic. Keycloak matches redirect_uri exactly against this list, so a second",
        "client importing this realm got an allowlist naming somebody else's server: their",
        "console's sign-in was refused at Keycloak with 'Invalid parameter: redirect_uri',",
        "before the login form, and nothing near that page said why. The origin is",
        "configuration and it is INSTALL_OIDC_REDIRECT_URIS, which brain.install declares",
        "required with no default for exactly this reason.",
        "",
        "The placeholder is a reserved documentation domain, so it can never resolve and can",
        "never be registered, and importable_realm refuses to emit a realm that still",
        "carries it. A plausible-looking default host here would be the same defect with a",
        "different spelling: it would import, and sign-in would fail somewhere else."
      ]
    },
    {
      "clientId": "brain-api",
      "name": "Company Brain API",
      "description": "The audience every token this system accepts is minted for.",
      "enabled": true,
      "publicClient": false,
      "bearerOnly": false,
      "protocol": "openid-connect",
      "standardFlowEnabled": false,
      "implicitFlowEnabled": false,
      "directAccessGrantsEnabled": false,
      "serviceAccountsEnabled": false,
      "attributes": {
        "access.token.signed.response.alg": "RS256",
        "id.token.signed.response.alg": "RS256"
      },
      "protocolMappers": [],
      "_comment": "No dedicated mappers, deliberately. The brain-api-audience mapper lived here and could never fire: a dedicated mapper applies to tokens issued for its own client, and every flow on this one is disabled because it is a resource server that nobody signs in to. It now sits on the brain-identity client scope, which brain-console carries by default."
    },
    {
      "clientId": "brain-sync",
      "name": "Example service account",
      "description": "A machine caller. Client credentials only; no browser flow at all.",
      "enabled": true,
      "publicClient": false,
      "protocol": "openid-connect",
      "standardFlowEnabled": false,
      "implicitFlowEnabled": false,
      "directAccessGrantsEnabled": false,
      "serviceAccountsEnabled": true,
      "attributes": {
        "access.token.signed.response.alg": "RS256",
        "use.refresh.tokens": "false"
      },
      "_comment": [
        "No secret is stored in this file and none ever should be. kcadm prints one on",
        "creation; it belongs in OpenBao.",
        "use.refresh.tokens false: a client-credentials caller can ask for a new token",
        "whenever it likes, so a refresh token is a second long-lived credential with no",
        "purpose.",
        "This client's tokens carry no sid, which is how brain.identity.sessions keeps the",
        "interactive and service paths disjoint. The owner, ceiling and expiry live in our",
        "own ServiceAccount record, not here: Keycloak knows the robot exists, and this",
        "system decides what it may borrow."
      ]
    },
    {
      "clientId": "account",
      "name": "${client_account}",
      "enabled": true,
      "alwaysDisplayInConsole": false,
      "publicClient": true,
      "protocol": "openid-connect",
      "rootUrl": "${authBaseUrl}",
      "baseUrl": "/realms/brain/account/",
      "redirectUris": ["/realms/brain/account/*"],
      "standardFlowEnabled": true,
      "implicitFlowEnabled": false,
      "directAccessGrantsEnabled": false,
      "serviceAccountsEnabled": false,
      "fullScopeAllowed": false,
      "attributes": {
        "post.logout.redirect.uris": "+",
        "pkce.code.challenge.method": "S256",
        "access.token.signed.response.alg": "RS256"
      },
      "_comment": [
        "Keycloak's own client, declared only because account-console below has to be, and the",
        "import builds the two together or not at all (RealmManager.importRealm calls",
        "setupAccountManagement unless the file names account). The Account REST API is this",
        "client: it refuses a token whose aud does not name account and a token whose",
        "resource_access.account.roles holds neither manage-account nor view-profile.",
        "",
        "Every value is setupAccountManagement's in 26.0.0 except the two attributes this file",
        "pins on every client, PKCE S256 and RS256. The Account Console signs in as",
        "account-console and not as this client, so neither changes what it does. The redirect URI is Keycloak's own wildcard, and it is",
        "relative: it resolves against rootUrl, which is the identity provider's own address,",
        "so a code can only ever be handed back to Keycloak's account pages."
      ]
    },
    {
      "clientId": "account-console",
      "name": "${client_account-console}",
      "enabled": true,
      "alwaysDisplayInConsole": false,
      "publicClient": true,
      "protocol": "openid-connect",
      "rootUrl": "${authBaseUrl}",
      "baseUrl": "/realms/brain/account/",
      "redirectUris": ["/realms/brain/account/*"],
      "standardFlowEnabled": true,
      "implicitFlowEnabled": false,
      "directAccessGrantsEnabled": false,
      "serviceAccountsEnabled": false,
      "fullScopeAllowed": false,
      "attributes": {
        "post.logout.redirect.uris": "+",
        "pkce.code.challenge.method": "S256",
        "access.token.signed.response.alg": "RS256"
      },
      "protocolMappers": [
        {
          "name": "audience resolve",
          "protocol": "openid-connect",
          "protocolMapper": "oidc-audience-resolve-mapper",
          "config": {
            "access.token.claim": "true",
            "introspection.token.claim": "true"
          }
        },
        {
          "name": "account roles",
          "protocol": "openid-connect",
          "protocolMapper": "oidc-usermodel-client-role-mapper",
          "config": {
            "usermodel.clientRoleMapping.clientId": "account",
            "claim.name": "resource_access.${client_id}.roles",
            "jsonType.label": "String",
            "multivalued": "true",
            "access.token.claim": "true",
            "id.token.claim": "false",
            "introspection.token.claim": "true"
          }
        }
      ],
      "_comment": [
        "The client Keycloak's Account Console signs in as (AccountConsole names it), and the",
        "one this file changes. As setupAccountManagement builds it, with RS256 pinned as on every",
        "client here and one mapper more.",
        "",
        "audience resolve is Keycloak's own, on the client itself: it adds account to aud",
        "because the person holds account roles this client may carry. account roles is the",
        "client roles mapper from Keycloak's built-in roles scope, the one thing that scope",
        "gave this client that the Account REST API reads, narrowed to the account client.",
        "multivalued true is load-bearing: a single value is written as one string rather than",
        "as roles, and the API reads no roles from it.",
        "",
        "fullScopeAllowed false with the scope mappings in clientScopeMappings below, as",
        "Keycloak has it, so these tokens carry manage-account, the manage-account-links it",
        "contains and view-groups when the person holds them, and no other role, and aud names",
        "account and nothing else."
      ]
    }
  ],

  "clientScopeMappings": {
    "account": [
      {
        "client": "account-console",
        "roles": ["manage-account", "view-groups"]
      }
    ]
  },

  "identityProviders": [
    {
      "_comment": [
        "Template for M1.1.4, disabled and pointed at .invalid. A client's SAML IdP is added",
        "per deployment; brain.identity.oidc.SamlFederation is the reviewed shape of these",
        "same fields, and it refuses the two settings that are always got wrong:",
        "wantAssertionsSigned false and validateSignature false, either of which turns an",
        "assertion into a form post claiming to be a person.",
        "principalType ATTRIBUTE with a stable attribute, never a transient NameID: a",
        "transient identifier changes every session, so nobody would be recognised twice and",
        "principal_for would return UnmappedSubject to the same person every morning."
      ],
      "alias": "client-saml-template",
      "providerId": "saml",
      "enabled": false,
      "trustEmail": false,
      "storeToken": false,
      "linkOnly": false,
      "firstBrokerLoginFlowAlias": "first broker login",
      "config": {
        "entityId": "https://idp.example.invalid/metadata",
        "singleSignOnServiceUrl": "https://idp.example.invalid/sso",
        "singleLogoutServiceUrl": "https://idp.example.invalid/slo",
        "nameIDPolicyFormat": "urn:oasis:names:tc:SAML:2.0:nameid-format:persistent",
        "principalType": "ATTRIBUTE",
        "principalAttribute": "employeeId",
        "wantAssertionsSigned": "true",
        "wantAssertionsEncrypted": "false",
        "validateSignature": "true",
        "forceAuthn": "false",
        "postBindingResponse": "true",
        "postBindingAuthnRequest": "true",
        "signingCertificate": "REPLACE_WITH_CLIENT_CERTIFICATE"
      }
    }
  ],

  "browserFlow": "browser",

  "authenticatorConfig": [
    {
      "alias": "password-method-reference",
      "config": {
        "default.reference.value": "pwd",
        "default.reference.maxAge": "36000"
      },
      "_comment": [
        "pwd is RFC 8176's value for a password, and brain.identity.bearer does not count it as",
        "a second factor. It is written at all so that a password-only token says so: without",
        "it that token and one from a realm whose amr mapper never ran both carry an empty amr,",
        "and decoding a token, the one diagnostic an administrator has, could not tell a missing",
        "second factor from a missing mapper."
      ]
    },
    {
      "alias": "otp-method-reference",
      "config": {
        "default.reference.value": "otp",
        "default.reference.maxAge": "36000"
      },
      "_comment": [
        "otp is in brain.identity.bearer.SECOND_FACTOR_METHODS, which is what turns a sign-in",
        "that passed this step into Assurance.STRONG.",
        "",
        "maxAge is the reason this was not one line. AmrUtils.isAmrValid keeps a reference only",
        "while authTime + maxAge >= now, and an absent maxAge is read as 0, so a reference with",
        "no maxAge is gone from every token minted a second after the step: the first access",
        "token already lacks it. 36000 seconds is ssoSessionMaxLifespan, so the second factor",
        "presented at sign-in counts for every token that session mints, refreshes included, and",
        "no longer, because the session cannot outlive it. That is the meaning",
        "A_SECOND_FACTOR_IS_A_CLAIM_ABOUT_THIS_SESSION gives the claim. A shorter value would be a",
        "step-up policy, which is a decision somebody should argue for rather than a default."
      ]
    }
  ],

  "authenticationFlows": [
    {
      "alias": "browser",
      "description": "Browser based authentication",
      "providerId": "basic-flow",
      "topLevel": true,
      "builtIn": true,
      "authenticationExecutions": [
        { "authenticator": "auth-cookie", "authenticatorFlow": false, "requirement": "ALTERNATIVE", "priority": 10 },
        { "authenticator": "auth-spnego", "authenticatorFlow": false, "requirement": "DISABLED", "priority": 20 },
        { "authenticator": "identity-provider-redirector", "authenticatorFlow": false, "requirement": "ALTERNATIVE", "priority": 25 },
        { "flowAlias": "Organization", "authenticatorFlow": true, "requirement": "ALTERNATIVE", "priority": 26 },
        { "flowAlias": "forms", "authenticatorFlow": true, "requirement": "ALTERNATIVE", "priority": 30 }
      ],
      "_comment": [
        "Keycloak 26.0.0's DefaultAuthenticationFlows.browserFlow, step for step, as a realm",
        "imported without authenticationFlows was given it: the same aliases, requirements and",
        "priorities, Kerberos disabled because this realm requires no Kerberos credential, and",
        "the Organization step because the organization feature is on by default in 26.0 and the",
        "built-in flow adds it to every realm but master. The only additions are the two",
        "authenticatorConfig references on the password form and the OTP form.",
        "",
        "Declaring it changes nothing else about the import. DefaultExportImportManager imports",
        "the flows this file declares and then calls DefaultAuthenticationFlows.migrateFlows,",
        "which builds every built-in flow still missing by alias: direct grant, registration,",
        "reset credentials, clients, first broker login, saml ecp and docker auth all arrive as",
        "before. No model migration runs on this import, because they run only for a file that",
        "names a keycloakVersion, and this one names none.",
        "",
        "Every authenticatorConfig and flowAlias named here has to be declared in this file. The",
        "import looks each one up by alias before migrateFlows has built anything, and a name it",
        "cannot find stops the import with nothing in the log near the login page."
      ]
    },
    {
      "alias": "Organization",
      "providerId": "basic-flow",
      "topLevel": false,
      "builtIn": true,
      "authenticationExecutions": [
        { "flowAlias": "Browser - Conditional Organization", "authenticatorFlow": true, "requirement": "CONDITIONAL", "priority": 10 }
      ]
    },
    {
      "alias": "Browser - Conditional Organization",
      "description": "Flow to determine if the organization identity-first login is to be used",
      "providerId": "basic-flow",
      "topLevel": false,
      "builtIn": true,
      "authenticationExecutions": [
        { "authenticator": "conditional-user-configured", "authenticatorFlow": false, "requirement": "REQUIRED", "priority": 10 },
        { "authenticator": "organization", "authenticatorFlow": false, "requirement": "ALTERNATIVE", "priority": 20 }
      ]
    },
    {
      "alias": "forms",
      "description": "Username, password, otp and other auth forms.",
      "providerId": "basic-flow",
      "topLevel": false,
      "builtIn": true,
      "authenticationExecutions": [
        { "authenticator": "auth-username-password-form", "authenticatorConfig": "password-method-reference", "authenticatorFlow": false, "requirement": "REQUIRED", "priority": 10 },
        { "flowAlias": "Browser - Conditional OTP", "authenticatorFlow": true, "requirement": "CONDITIONAL", "priority": 20 }
      ]
    },
    {
      "alias": "Browser - Conditional OTP",
      "description": "Flow to determine if the OTP is required for the authentication",
      "providerId": "basic-flow",
      "topLevel": false,
      "builtIn": true,
      "authenticationExecutions": [
        { "authenticator": "conditional-user-configured", "authenticatorFlow": false, "requirement": "REQUIRED", "priority": 10 },
        { "authenticator": "auth-otp-form", "authenticatorConfig": "otp-method-reference", "authenticatorFlow": false, "requirement": "REQUIRED", "priority": 20 }
      ]
    }
  ],

  "requiredActions": [
    {
      "alias": "CONFIGURE_TOTP",
      "providerId": "CONFIGURE_TOTP",
      "name": "Configure OTP",
      "enabled": true,
      "defaultAction": true,
      "priority": 10,
      "_comment": "defaultAction true so a new account is asked to set up a second factor at first login. Assurance.STRONG is what brain.gate.admission requires for approve and admin, so without this nobody can approve anything. Setting one up is not presenting one: this action runs after the flow, so the OTP form was not a completed step of the session it ran in, and that session's tokens carry pwd alone. The person signs out and signs in again, and that sign-in passes the OTP form and carries otp."
    },
    {
      "alias": "TERMS_AND_CONDITIONS",
      "providerId": "TERMS_AND_CONDITIONS",
      "name": "Terms and Conditions",
      "enabled": false,
      "defaultAction": false,
      "priority": 20,
      "_comment": "Disabled, as Keycloak registers it. Made default, it puts a terms page in front of every new account."
    },
    {
      "alias": "UPDATE_PASSWORD",
      "providerId": "UPDATE_PASSWORD",
      "name": "Update Password",
      "enabled": true,
      "defaultAction": false,
      "priority": 30,
      "_comment": "What a temporary password, the last step of a password reset and the Account Console's Update on a password all ask for. Unregistered, each of them is skipped and the old password stays."
    },
    {
      "alias": "UPDATE_PROFILE",
      "providerId": "UPDATE_PROFILE",
      "name": "Update Profile",
      "enabled": true,
      "defaultAction": false,
      "priority": 40
    },
    {
      "alias": "VERIFY_EMAIL",
      "providerId": "VERIFY_EMAIL",
      "name": "Verify Email",
      "enabled": true,
      "defaultAction": false,
      "priority": 50,
      "_comment": "Runs for a person an administrator asks to verify. It runs for everybody unverified only while the realm's verifyEmail is true, which is false here because this file configures no mail server; see the header."
    },
    {
      "alias": "delete_account",
      "providerId": "delete_account",
      "name": "Delete Account",
      "enabled": false,
      "defaultAction": false,
      "priority": 60
    },
    {
      "alias": "webauthn-register",
      "providerId": "webauthn-register",
      "name": "Webauthn Register",
      "enabled": true,
      "defaultAction": false,
      "priority": 70
    },
    {
      "alias": "webauthn-register-passwordless",
      "providerId": "webauthn-register-passwordless",
      "name": "Webauthn Register Passwordless",
      "enabled": true,
      "defaultAction": false,
      "priority": 80
    },
    {
      "alias": "VERIFY_PROFILE",
      "providerId": "VERIFY_PROFILE",
      "name": "Verify Profile",
      "enabled": true,
      "defaultAction": false,
      "priority": 90,
      "_comment": "Asks once for what the user profile requires and an account lacks, first name, last name and email in Keycloak's default profile. A complete account is never asked."
    },
    {
      "alias": "delete_credential",
      "providerId": "delete_credential",
      "name": "Delete Credential",
      "enabled": true,
      "defaultAction": false,
      "priority": 100,
      "_comment": "The Account Console deletes a one-time code through this action. Unregistered, Delete returns to the page with the credential still there."
    },
    {
      "alias": "update_user_locale",
      "providerId": "update_user_locale",
      "name": "Update User Locale",
      "enabled": true,
      "defaultAction": false,
      "priority": 1000
    }
  ]
}
