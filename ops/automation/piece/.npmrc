# The lock records each package's version and integrity hash, and not the registry it came
# from. A company installing this piece from its own npm mirror uses the same lock, and the
# lock carries no host for brain.ops.independence to read as one company's value.
omit-lockfile-registry-resolved=true
