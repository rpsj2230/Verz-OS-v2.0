"""Moving a company off the system it already runs, without losing what it knew.

Every install after the first replaces something: a predecessor assistant, a folder of
documents, a vendor product the company is leaving. This package holds the parts of that
move which are decisions rather than transfers, because the transfers are connectors and
already have modules, and the decisions are what nobody writes down.

Nothing here names a predecessor product or a company. The module that would have to is the
exporter, and an exporter is a plugin under `brain.plugins`.
"""
